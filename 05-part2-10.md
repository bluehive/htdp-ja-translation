## 10 リストのさらなる活用 (More on Lists)

リストは、現在のほぼすべてのプログラミング言語に備わっている多用途なデータ形式です。プログラマは、大規模なアプリケーション、人工知能、分散システムなどを構築するためにリストを使用してきました。本章では、リストを生成する関数、リストの内部に構造体を必要とするデータ表現、テキストファイルをリストとして表現する方法など、この世界からいくつかのアイデアを取り上げて説明します。

### 10.1 リストを生成する関数 (Functions that Produce Lists)

以下は、時給制従業員の賃金を決定するための関数です。

```racket
; Number -> Number
; computes the wage for h hours of work
(define (wage h)
  (* 12 h))
```

この関数は、働いた時間を入力として受け取り、賃金を算出します。しかし、給与計算ソフトウェアを使用したい企業は、この単一の関数には関心がありません。企業が求めているのは、すべての従業員の賃金を計算する関数です。

この新しい関数を wage* と呼びましょう。その役割は、すべての従業員の就労時間を処理し、それぞれに支払われるべき賃金を算出することです。簡単のため、入力は各数値が一人の従業員の就労時間を表す数値のリストであり、期待される結果も各数値が獲得した週給を表す数値のリストであると仮定しましょう。

入力と出力のデータ定義はすでに手元にあるため、直ちに設計レシピの第2段階へ進むことができます。

```racket
; List-of-numbers -> List-of-numbers
; computes the weekly wages for the weekly hours
(define (wage* whrs)
  '())
```

次に、いくつかの入力の例と、それに対応する出力が必要になります。そこで、週の就労時間を表すいくつかの短い数値のリストを作成します。

> given expected'() '()(cons28'()) (cons336'())(cons4(cons2'())) (cons48(cons24'()))

出力を計算するために、与えられた入力リストにある各数値に対して週給を求めます。最初の例では、入力リストに数値が存在しないため、出力は '() となります。2番目と3番目の期待される出力がなぜそのようになるのか、理解できているか確認してください。

wage* は「リスト」の章の他のいくつかの関数と同じ種類のデータを消費すること、そしてテンプレートはデータ定義の形状のみに依存することから、このテンプレートを再利用できます。

```racket
(define (wage* whrs)
  (cond
    [(empty? whrs)...]
    [else (... (first whrs)...
... (wage* (rest whrs))...)]))
```

テンプレート開発の練習をしたい場合は、figure 52 の質問を活用してください。

それでは、最も創造的な設計段階に進みましょう。設計レシピに従い、テンプレートの各 cond 行を個別に検討します。非再帰的なケースでは、(empty? whrs) が真になります。これは入力が '() であることを意味します。上記の例によって望ましい答えは '() と定義されているため、これで完了です。

2番目のケースでは、設計上の質問に従ってテンプレートの各式が何を計算するかを明らかにします。

- (first whrs) は whrs の最初の数値を返します。これは最初に従業員が働いた時間です。
- (rest whrs) は与えられたリストの残りの部分です。
- (wage* (rest whrs)) は、残りのリストがまさに今定義している関数自身によって処理されることを示しています。いつものように、この式の導き出す結果を把握するために、関数のシグネチャと目的文を利用します。シグネチャからはそれが数値のリストであることがわかり、目的文からは、このリストが入力（時間のリストの残り）に対する賃金のリストを表していることが説明されます。

重要なのは、関数がまだ定義されていなくても、このケースにおける結果を計算する式を組み立てる際に、これらの事実に依拠することです。

whrs の最初の要素を除くすべての賃金のリストはすでに手元にあるため、関数は whrs 全体に対する期待される出力を生成するために 2 つの計算を実行する必要があります。(first whrs) に対する週給を計算することと、whrs のすべての週給を表すリストを構築することです。最初の部分には wage を再利用します。2番目の部分には、これら2つの情報を cons によって1つのリストに結合します。

```racket
(cons (wage (first whrs)) (wage* (rest whrs)))
```

これで定義は完了です。figure 64 を参照してください。

```
+-------------------------------------------------------------+
| Figure 64: Computing the wages of all employees             |
|                                                             |
|; List-of-numbers -> List-of-numbers                        |
|; computes the weekly wages for all given weekly hours      |
| (define (wage* whrs)                                        |
|   (cond                                                     |
|     [(empty? whrs) '()]                                     |
|     [else (cons (wage (first whrs)) (wage* (rest whrs)))])) |
|                                                             |
|; Number -> Number                                          |
|; computes the wage for h hours of work                     |
| (define (wage h)                                            |
|   (* 12 h))                                                 |
+-------------------------------------------------------------+
```

**Exercise 161.** 例をテストに変換し、それらがすべて成功することを確認しなさい。次に、figure 64 の関数を変更して、全員の時給が $14 になるようにしなさい。ここで、全員の時給を変更する作業が、プログラム全体の複数箇所の変更ではなく単一の変更で済むように、プログラム全体を改訂しなさい。

**Exercise 162.** 週に 100 時間を超えて働く従業員はおそらく存在しません。不正から会社を守るため、関数は wage* の入力リストのどの要素も 100 を超えていないかチェックする必要があります。もし超えているものがあれば、関数は直ちにエラーを発生させるべきです。この基本的な妥当性チェックを実行したい場合、figure 64 の関数をどのように変更する必要がありますか。

> **Note:** 設計レシピのさまざまな段階の成果物を示しなさい。行き詰まった場合は、設計レシピに従ってどこまで進んだかを誰かに見せてください。レシピは単にあなたが使うための設計ツールであるだけでなく、他の人があなたの自助を支援できるようにするための診断システムでもあります。

**Exercise 163.** convertFC を設計しなさい。この関数は、華氏（Fahrenheit）の測定値のリストを摂氏（Celsius）の測定値のリストに変換します。

**Exercise 164.** 米ドル（US$）の金額のリストをユーロ（€）の金額のリストに変換する関数 convert-euro を設計しなさい。現在の為替レートをウェブで調べなさい。

convert-euro を関数 convert-euro* に一般化しなさい。この関数は為替レートと米ドルの金額のリストを受け取り、後者をユーロの金額のリストに変換します。

**Exercise 165.** おもちゃの説明（単語 1 つの文字列）のリストを受け取り、すべての "robot" の出現を "r2d2" に置換し、他のすべての説明はそのまま残す関数 subst-robot を設計しなさい。

subst-robot を substitute に一般化しなさい。後者は new と old という 2 つの文字列と、文字列のリストを受け取ります。old のすべての出現を new に置換した新しい文字列のリストを生成します。
ます。

### 10.2 リスト内の構造体 (Structures in Lists)

労働時間を単なる数値として表現するのは悪い選択です。なぜなら、給料小切手を印刷するには1週間あたりの労働時間以上の情報が必要だからです。また、すべての従業員が1時間あたり同じ金額を稼ぐわけではありません。幸いなことに、リストにはアトム値以外の項目を含めることができます。実際、リストには私たちが望むあらゆる値を含めることができ、特に構造体を含めることができます。

私たちの実行例では、まさにそのようなデータ表現が求められています。数値の代わりに、従業員と彼らの労働時間および時給を表す構造体を使用します：

```racket
(define-struct work [employee rate hours])
; A (piece of) Work is a structure:
;   (make-work String Number Number)
; interpretation (make-work n r h) combines the name
; with the pay rate r and the number of hours h
```

この表現はまだ単純ですが、構造体を含むリストのデータ定義を公式化することを強いるため、追加の課題としてちょうど十分なものです：

```racket
; Low (short for list of works) is one of:
; – '()
; – (cons Work Low)
; interpretation an instance of Low represents the
; hours worked for a number of employees
```

以下は Low の3つの要素です：

```racket
'()
(cons (make-work "Robby" 11.95 39)
      '())
(cons (make-work "Matthew" 12.95 45)
      (cons (make-work "Robby" 11.95 39)
            '()))
```

データ定義を使用して、なぜこれらのデータが Low に属しているのかを説明しなさい。

止まってください！ また、データ定義を使用して、さらに2つの例を生成しなさい。

> **注:** 実際のプロジェクトで作業する場合、このような接尾辞は使用しません。代わりに、コードの異なるバージョンを管理するためのツールを使用します。

Low の定義が意味をなすことがわかったので、単なる数値のリストではなく Low の要素を受け取るように関数 wage* を再設計するときが来ました：

```racket
; Low -> List-of-numbers
; computes the weekly wages for the given records
(define (wage*.v2 an-low)
  '())
```

関数名の末尾にある接尾辞「.v2」は、これが関数の改訂された第2版であることをコードを読むすべての人に知らせます。この場合、改訂は新しいシグネチャと適合された目的の文（purpose statement）から始まります。ヘッダーは上記と同じです。

デザインレシピの第3ステップは、例を使って考えることです。上記の2番目のリストから始めましょう。それには1つの work レコード、すなわち (make-work "Robby" 11.95 39) が含まれています。その解釈は、"Robby" が39時間働き、時給 $11.95 で支払われるということです。したがって、彼のその週の賃金は $466.05、つまり (* 11.95 39) です。したがって、wage*.v2 の期待される結果は (cons 466.05 '()) です。当然、入力リストに2つの work レコードが含まれている場合、この種の計算を2回実行し、結果は2つの数値のリストになります。止まってください！ 上記の3番目のデータ例に対する期待される結果を決定しなさい。

**数値に関する注意** BSLは——他のほとんどのプログラミング言語とは異なり——あなたと同じように、小数を正確な分数として理解することを覚えておいてください。たとえば Java のような言語は、最初の work レコードの期待される賃金に対して 466.04999999999995 を生成します。小数に対する演算がいつこのような奇妙な振る舞いをするかを予測することはできないため、他のプログラミング言語に備えるためにも、そのような例は次のように記述する方がよいでしょう：

```racket
(check-expect
  (wage*.v2
    (cons (make-work "Robby" 11.95 39) '()))
  (cons (* 11.95 39) '()))
```

また、このスタイルで例を記述することは、賃金の計算方法を本当に理解したことも意味します。**注記終わり**

ここからテンプレートの開発に移ります。テンプレートの質問を使用すると、すぐに次のようになります：

```racket
(define (wage*.v2 an-low)
  (cond
    [(empty? an-low)...]
    [(cons? an-low)
     (... (first an-low)...
... (wage*.v2 (rest an-low))...)]))
```

なぜなら、データ定義は2つの節から構成され、最初の節で '() を導入し、2番目の節で cons された構造体を導入する、といった具合だからです。しかし、このテンプレートが表現している以上に、入力について多くのことを知っていることにも気づくでしょう。たとえば、(first an-low) が与えられたリストから3つのフィールドを持つ構造体を抽出することを知っています。これは、テンプレートにさらに3つの式を追加することを示唆しているようです：

```racket
(define (wage*.v2 an-low)
  (cond
    [(empty? an-low)...]
    [(cons? an-low)
     (... (first an-low)...
...... (work-employee (first an-low))...
...... (work-rate (first an-low))...
...... (work-hours (first an-low))...
      (wage*.v2 (rest an-low))...)]))
```

このテンプレートは、関心を持つ可能性のあるすべてのデータをリストアップしています。

ここでは異なる戦略を使用します。具体的には、他のデータ定義を参照するデータ定義のテンプレートを開発するときはいつでも、個別の関数テンプレートを作成して参照することを提案します：

```racket
(define (wage*.v2 an-low)
  (cond
    [(empty? an-low)...]
    [(cons? an-low)
     (... (for-work (first an-low))
... (wage*.v2 (rest an-low))...)]))

; Work ->???
; a template for processing elements of Work
(define (for-work w)
  (... (work-employee w)...
... (work-rate w)...
... (work-hours w)...))
```

テンプレートを分割することで、作業が自然に関数内および関数間に分割されます。どの関数も大きくなりすぎず、すべてが特定のデータ定義に関連付けられます。

いよいよプログラミングの準備が整いました。いつものように、シンプルに見えるケース、つまりここでの最初の cond 行から始めます。wage*.v2 が '() に適用された場合、'() が返されることを期待し、これで決まりです。次に2行目に進み、これらの式が何を計算するかを自分に思い出させます：

1. (first an-low) はリストから最初の work 構造体を抽出します。
2. (for-work...) は、work 構造体を処理する関数を設計したいということを示しています。
3. (rest an-low) は与えられたリストの残りを抽出します。そして
4. (wage*.v2 (rest an-low)) は、関数の目的文に従って、最初のものを除くすべての work レコードの賃金リストを決定します。

ここで行き詰まった場合は、figure 54 の表の方法を使用してください。

すべてを理解していれば、for-work が最初の work レコードの賃金を計算すると仮定して、2つの式を cons するだけで十分であることがわかります：

```racket
... (cons (for-work (first an-low))
          (wage*.v2 (rest an-low)))...
```

要するに、関数のウィッシュリストに別のエントリを追加することで、関数を完了したことになります。

for-work は単に代理として機能する名前であり、この関数としては悪い名前であるため、この関数を wage.v2 と呼び、その完全なウィッシュリストのエントリを書き留めましょう：

```racket
; Work -> Number
; computes the wage for the given work record w
(define (wage.v2 w)
  0)
```

この種の関数の設計は Fixed-Size Data で広範囲に扱われているため、ここで追加の説明は必要ありません。Figure 65 は wage.v2 と wage*.v2 を開発した最終結果を示しています。

```
+---------------------------------------------------------+
| Figure 65: Computing the wages from work records        |
|                                                         |
|; Low -> List-of-numbers                                |
|; computes the weekly wages for all weekly work records |
|                                                         |
| (check-expect                                           |
|   (wage*.v2 (cons (make-work "Robby" 11.95 39) '()))    |
|   (cons (* 11.95 39) '()))                              |
|                                                         |
| (define (wage*.v2 an-low)                               |
|   (cond                                                 |
|     [(empty? an-low) '()]                               |
|     [(cons? an-low) (cons (wage.v2 (first an-low))      |
|                           (wage*.v2 (rest an-low)))]))  |
|                                                         |
|; Work -> Number                                        |
|; computes the wage for the given work record w         |
| (define (wage.v2 w)                                     |
|   (* (work-rate w) (work-hours w)))                     |
+---------------------------------------------------------+
```

**Exercise 166.** 関数 wage*.v2 は work レコードのリストを受け取り、数値のリストを生成します。もちろん、関数は構造体のリストを生成することもできます。

給料小切手（paycheck）のデータ表現を開発しなさい。給料小切手には、従業員の名前と金額という2つの特徴的な情報が含まれていると仮定します。次に関数 wage*.v3 を設計しなさい。この関数は work レコードのリストを受け取り、それからレコードごとに1つの給料小切手のリストを計算します。

現実には、給料小切手には従業員番号も含まれています。従業員情報のデータ表現を開発し、work レコードのデータ定義を変更して、従業員の名前のための単なる文字列ではなく従業員情報を使用するようにしなさい。また、給料小切手のデータ表現も変更して、従業員の名前と番号も含まれるようにしなさい。最後に、改訂された work レコードのリストを改訂された給料小切手のリストにマッピングする関数 wage*.v4 を設計しなさい。

**反復的洗練に関する注意** この演習はタスクの反復的洗練（iterative refinement）を示しています。関連するすべての情報を含むデータ表現を使用する代わりに、給料小切手の単純化された表現から始めて、徐々にその表現を現実的なものにしていきました。この単純なプログラムの場合、洗練は過剰（overkill）ですが、後で反復的洗練が単なる選択肢ではなく必要不可欠となる状況に遭遇することになります。

**Exercise 167.** 関数 sum を設計しなさい。この関数は Posns のリストを受け取り、そのすべての x 座標の合計を生成します。

**Exercise 168.** 関数 translate を設計しなさい。この関数は Posns のリストを受け取り、Posns のリストを生成します。前者の中の各 (make-posn x y) に対して、後者には (make-posn x (+ y 1)) が含まれます。「translate（平行移動する）」という言葉は幾何学から借りたもので、直線に沿って一定の距離だけ点を移動させることを平行移動（translation）と呼びます。

**Exercise 169.** 関数 legal を設計しなさい。演習 168 の translate と同様に、この関数は Posns のリストを受け取り、Posns のリストを生成します。結果には、x 座標が 0 から 100 の間にあり、y 座標が 0 から 200 の間にあるすべての Posns が含まれます。

**Exercise 170.** 以下は電話番号を表現する1つの方法です：

```racket
(define-struct phone [area switch four])
; A Phone is a structure:
;   (make-phone Three Three Four)
; A Three is a Number between 100 and 999.
; A Four is a Number between 1000 and 9999.
```

関数 replace を設計しなさい。この関数は Phones のリストを受け取り、Phones のリストを生成します。
市外局番（area code）713 のすべての出現を 281 に置き換えます。
y 座標が 0 から 200 の間にあるすべての `Posn` が含まれます。

**Exercise 170.** 以下は電話番号を表現する一つの方法です。

```racket
(define-struct phone [area switch four])
; A Phone is a structure:
;   (make-phone Three Three Four)
; A Three is a Number between 100 and 999.
; A Four is a Number between 1000 and 9999.
```

関数 `replace` を設計してください。この関数は `Phone` のリストを受け取り、`Phone` のリストを生成します。市外局番（area code）713 のすべての出現を 281 に置き換えます。

### 10.3 リストの中のリスト、ファイル (Lists in Lists, Files)

「関数とプログラム」（Functions and Programs）では、テキストファイル全体を1つの文字列として読み込む関数である `read-file` を紹介します。言い換えれば、`read-file` の作成者はテキストファイルを文字列として表現することを選択し、この関数は（名前で指定された）特定のファイルに対するデータ表現を作成します。しかし、テキストファイルは単なる長いテキストや文字列ではありません。それらは行や単語、行（ロー）やセル、その他多くの方法で構成されています。要するに、ファイルの内容を単なる文字列として表現することは、稀な機会には機能するかもしれませんが、通常は悪い選択です。

> **注:** 定義領域に `( require 2htdp/batch-io )` を追加してください。

```
+------------------------------------e +
| Figure
|6: Things   ke time          |
|                                      |
| +----------------------------------+ |
| | `TTT`                            | |
| +----------------------------------+ |
| | `Put up in a place`              | |
| | `where it's easy to see`         | |
| | `the cryptic admonishment`                | |
| | `When you feel how depressingly` | |
| | `slowly you climb,`              | |
| | `it's well to remember that`     | |
| | `Things Take Time.`              | |
| | `Piet Hein`                      | |
| +----------------------------------+ |
+--------------------------------------+
```

```
+--------------------------------------+
| Figure 66: Things take time          |
|                                      |
| +----------------------------------+ |
| | `TTT`                            | |
| +----------------------------------+ |
| | `Put up in a place`              | |
| | `where it's easy to see`         | |
| | `the cryptic admonishment`       | |
| | `T.T.T.`                         | |
| | `When you feel how depressingly` | |
| | `slowly you climb,`              | |
| | `it's well to remember that`     | |
| | `Things Take Time.`              | |
| | `Piet Hein`                      | |
| +----------------------------------+ |
+--------------------------------------+
```

具体的に説明するために、figure 66 のサンプルファイルを見てみましょう。ここには Piet Hein による詩が含まれており、多くの行と単語で構成されています。プログラム

```racket
(read-file "ttt.txt")
```

を使ってこのファイルを BSL 文字列に変換すると、次のようになります：

> **Note:** ご想像の通り、ドットは実際の結果の一部ではありません。

```racket
"TTT\n \nPut up in a place\nwhere...."
```

ここで文字列内の `"\n"` は改行を示しています。

文字列に対する基本操作（例えば `explode`）でこの文字列を分割することは確かに可能ですが、BSL を含むほとんどのプログラミング言語は、ファイルの多様な表現や、既存のファイルからそうした表現を作成する関数をサポートしています：

- このファイルを表現する1つの方法は行のリストであり、各行は1つの文字列として表現されます：
(cons "TTT" (cons "" (cons "Put up in a place" (cons... '()))))
ここでは、ファイルに空行が含まれているため、リストの2番目の項目は空文字列になっています。
- 別の方法は単語のリストを使用することであり、ここでも各単語は文字列として表現されます：
(cons "TTT" (cons "Put" (cons "up" (cons "in" (cons... '())))))
この表現では空の2行目が消失していることに注目してください。結局のところ、空行には単語が存在しないからです。
- そして3番目の表現は、単語のリストのリストに基づいています：
(cons (cons "TTT" '()) (cons '() (cons (cons "Put" (cons "up" (cons... '()))) (cons... '()))))
この表現は2番目の表現に対して、2行目が空行であることも含めてファイルの構成を保存できるという利点があります。その代償として、リストが突然リストを含むことになります。

リストを含むリストというアイデアは最初は恐ろしく思えるかもしれませんが、心配する必要はありません。デザインレシピはこのような複雑な場合でも役立ちます。

```
+--------------------------------------------------------------+
| Figure 67: Reading files                                     |
|                                                              |
|; String -> String                                           |
|; produces the content of file f as a string                 |
| (define (read-file f)...)                                   |
|                                                              |
|; String -> List-of-string                                   |
|; produces the content of file f as a list of strings,       |
|; one per line                                               |
| (define (read-lines f)...)                                  |
|                                                              |
|; String -> List-of-string                                   |
|; produces the content of file f as a list of strings,       |
|; one per word                                               |
| (define (read-words f)...)                                  |
|                                                              |
|; String -> List-of-list-of-string                           |
|; produces the content of file f as a list of list of        |
|; strings, one list per line and one string per word         |
| (define (read-words/line f)...)                             |
|                                                              |
|; The above functions consume the name of a file as a String |
|; argument. If the specified file does not exist in the      |
|; same folder as the program, they signal an error.          |
+--------------------------------------------------------------+
```

使い始める前に、figure 67 を見てみましょう。ここには、ファイルを読み込むための便利な関数がいくつか紹介されています。これらは網羅的なものではありません。ファイルからのテキストを扱う方法は他にもたくさんあり、あらゆるテキストファイルを扱うにははるかに多くのことを知る必要があります。ここでの私たちの目的（体系的なプログラムデザインの原則を教え、学ぶこと）においては、これらで十分であり、十分に興味深いプログラムをデザインできるようになります。

figure 67 では、リストを含むリストに関わるものも含め、まだ存在しない2つのデータ定義の名前が使用されています。いつものようにデータ定義から始めますが、今回はこの作業をあなたにお任せします。したがって、先を読み進める前に、以下の練習問題を解いてください。図の意味を完全に理解するには解答が必要であり、解答を自力で導き出さなければ、このセクションの残りの部分を本当に理解することはできません。

**Exercise 171.** `List-of-strings` のデータ定義がどのようなものか、あなたは知っています。それを明確に書き出してください。Piet Hein の詩を、各行が1つの文字列として表現される定義のインスタンスとして表現できること、および各単語が文字列となる別のインスタンスとして表現できることを確認してください。`read-lines` と `read-words` を使用して、表現の選択を確認してください。

次に、`List-of-list-of-strings` のデータ定義を開発してください。再び Piet Hein の詩を、各行が単語ごとに1つの文字列のリストとして表現され、詩全体がそのような行表現のリストとなる定義のインスタンスとして表現してください。選択を確認するために `read-words/line` を使用してもかまいません。

ご存知かもしれませんが、オペレーティングシステムにはファイルを測定するプログラムが付属しています。あるものは行数を数え、別のものは行ごとの単語数を特定します。複雑な関数のデザインにおいてデザインレシピがどのように役立つかを説明するために、後者から始めましょう。

最初のステップは、必要なすべてのデータ定義が揃っているか確認することです。上記の練習問題を解いたのであれば、目的の関数のすべての可能な入力に対するデータ定義があり、前節ではすべての可能な出力を記述する `List-of-numbers` を定義しました。簡潔にするため、文字列のリストのリストのクラスを指すのに `LN` を使用し、それを使って目的の関数のヘッダー素材を書き出します：

```racket
; LN -> List-of-numbers
; determines the number of words on each line
(define (words-on-line lls) '())
```

目的の文を1つのフレーズで捉えており適切であるため、この関数を `words-on-line` と命名します。

しかし、本当に必要なのはデータ例のセットです：

```racket
(define line0 (cons "hello" (cons "world" '())))
(define line1 '())

(define lls0 '())
(define lls1 (cons line0 (cons line1 '())))
```

最初の2つの定義は行の2つの例を導入しています。1つは2つの単語を含み、もう1つは何も含みません。最後の2つの定義は、これらの行の例から `LN` のインスタンスを構築する方法を示しています。関数にこれら2つの例が与えられたときに期待される結果を特定してください。

データ例を用意できれば、機能的な例を定式化するのは簡単です。データ例のそれぞれに関数を適用することを想像するだけです。`words-on-line` を `lls0` に適用すると、行が存在しないため空のリストが返されるはずです。`words-on-line` を `lls1` に適用すると、2つの行があるため、2つの数値のリストが返されるはずです。`lls1` の2つの行にはそれぞれ2つと0の単語が含まれているため、2つの数値はそれぞれ 2 と 0 です。

これをすべてテストケースに変換する方法は次のとおりです：

```racket
(check-expect (words-on-line lls0) '())
(check-expect (words-on-line lls1)
              (cons 2 (cons 0 '())))
```

第2ステップの最後にこれを行うことで、実行するとテストケースの一部が失敗するものの、完全なプログラムが得られます。

このサンプル問題において、テンプレートの開発は興味深いステップです。figure 52 のテンプレートの質問に答えることで、通常のリスト処理テンプレートがすぐに得られます：

```racket
(define (words-on-line lls)
  (cond
    [(empty? lls)...]
    [else
     (... (first lls); a list of strings
... (words-on-line (rest lls))...)]))
```

前節と同様に、式 `(first lls)` が `List-of-strings` を抽出すること、そしてそれもまた複雑な構成を持っていることを私たちは知っています。この知識を表現するためにネストされたテンプレートを挿入したくなりますが、思い出してほしいように、より良いアイデアは2番目の補助テンプレートを開発し、2番目の条件の最初の行を変更してこの補助テンプレートを参照するようにすることです。

この補助テンプレートはリストを消費する関数のためのものなので、テンプレートは直前のものとほぼ同じに見えます：

```racket
(define (line-processor ln)
  (cond
    [(empty? ln)...]
    [else
     (... (first ln); a string
... (line-processor (rest ln))...)]))
```

重要な違いは、`(first ln)` がリストから文字列を抽出し、文字列をアトミックな値として扱うことです。このテンプレートを手に入れると、`words-on-line` の2番目のケースの最初の行を次のように変更できます：

```racket
... (line-processor (first lls))...
```

これは第5ステップにおいて、`words-on-line` の定義が補助関数のデザインを要求する可能性があることを思い出させてくれます。

さあ、プログラミングの時です。いつものように、このステップをガイドするために figure 53 の質問を使用します。行の空リストに関する最初のケースは簡単なケースです。私たちの例によると、この場合の答えは `'()`、つまり数値の空リストです。`cons` に関する2番目のケースにはいくつかの式が含まれており、それらが何を計算するかの確認から始めます：

- `(first ln)` は、（表現された）行の空でないリストから最初の行を抽出します。
- `(line-processor (first ln))` は、この行を処理するための補助関数をデザインしたいかもしれないことを示唆しています。
- `(rest ln)` は、行のリストの残りです。
- `(words-on-line (rest ln))` は、リストの残りの行ごとの単語のリストを計算します。なぜこれがわかるのでしょうか？ `words-on-line` のシグネチャと目的の文でまさにそれを約束したからです。

1行を消費してその1行の単語数をカウントする補助関数（`words#` と呼びましょう）をデザインできると仮定すると、2番目の条件を完了するのは簡単です：

```racket
(cons (words# (first ln))
      (words-on-line (rest ln)))
```

この式は、`ln` の最初の行の単語数を、`ln` の残りの行の単語数を表す数値のリストに `cons` します。

残るは `words#` 関数のデザインです。そのテンプレートは `line-processor` と呼ばれ、その目的は1行（これは単なる文字列のリストです）の単語数をカウントすることです。したがって、ウィッシュリストのエントリは次のようになります：

```racket
; List-of-strings -> Number
; counts the number of words on los
(define (words# los) 0)
```

この時点で、Designing with Self-Referential Data Definitions で自己参照データのデザインレシピを説明するために使用された例を思い出すかもしれません。その関数は `how-many` と呼ばれ、これも文字列のリストにある文字列の数をカウントします。`how-many` の入力が名前のリストを表すことになっていたとしても、この違いはまったく問題になりません。文字列のリストにある文字列の数を正確にカウントする限り、`how-many` は私たちの問題を解決します。

既存の関数を再利用することは良いことなので、`words#` を次のように定義できます：

```racket
(define (words# los)
  (how-many los))
```

しかし実際には、プログラミング言語にはそのような問題をすでに解決する関数が付属しています。BSL はこの関数を `length` と呼び、値が何であるかに関わらず、あらゆる値のリストにある値の数をカウントします。

+-------------------------------------------------------------+
| Figure 68: Counting the words on a line                     |
|                                                             |
|; An LN is one of:                                          |
|; – '()                                                     |
|; – (cons Los LN)                                           |
|; interpretation a list of lines, each is a list of Strings |
|                                                             |
| (define line0 (cons "hello" (cons "world" '())))            |
| (define line1 '())                                          |
|                                                             |
| (define ln0 '())                                            |
| (define ln1 (cons line0 (cons line1 '())))                  |
|                                                             |
|; LN -> List-of-numbers                                     |
|; determines the number of words on each line               |
|                                                             |
| (check-expect (words-on-line ln0) '())                      |
| (check-expect (words-on-line ln1) (cons 2 (cons 0 '())))    |
|                                                             |
| (define (words-on-line ln)                                  |
|   (cond                                                     |
|     [(empty? ln) '()]                                       |
|     [else (cons (length (first ln))                         |
|                 (words-on-line (rest ln)))]))               |
+-------------------------------------------------------------+
```

```
+-------------------------------------------------------------+
| Figure 68: Counting the words on a line                     |
|                                                             |
|; An LN is one of:                                          |
|; – '()                                                     |
|; – (cons Los LN)                                           |
|; interpretation a list of lines, each is a list of Strings |
|                                                             |
| (define line0 (cons "hello" (cons "world" '())))            |
| (define line1 '())                                          |
|                                                             |
| (define ln0 '())                                            |
| (define ln1 (cons line0 (cons line1 '())))                  |
|                                                             |
|; LN -> List-of-numbers                                     |
|; determines the number of words on each line               |
|                                                             |
| (check-expect (words-on-line ln0) '())                      |
| (check-expect (words-on-line ln1) (cons 2 (cons 0 '())))    |
|                                                             |
| (define (words-on-line ln)                                  |
|   (cond                                                     |
|     [(empty? ln) '()]                                       |
|     [else (cons (length (first ln))                         |
|                 (words-on-line (rest ln)))]))               |
+-------------------------------------------------------------+
```

```
+-------------------------------------------------------------+
| Figure 68: Counting the words on a line                     |
|                                                             |
|; An LN is one of:                                          |
|; – '()                                                     |
|; – (cons Los LN)                                           |
|; interpretation a list of lines, each is a list of Strings |
|                                                             |
| (define line0 (cons "hello" (cons "world" '())))            |
| (define line1 '())                                          |
|                                                             |
| (define ln0 '())                                            |
| (define ln1 (cons line0 (cons line1 '())))                  |
|                                                             |
|; LN -> List-of-numbers                                     |
|; determines the number of words on each line               |
|                                                             |
| (check-expect (words-on-line ln0) '())                      |
| (check-expect (words-on-line ln1) (cons 2 (cons 0 '())))    |
|                                                             |
| (define (words-on-line ln)                                  |
|   (cond                                                     |
|     [(empty? ln) '()]                                       |
|     [else (cons (length (first ln))                         |
|                 (words-on-line (rest ln)))]))               |
+-------------------------------------------------------------+
```

```
+-------------------------------------------------------------+
| Figure 68: Counting the words on a line                     |
|                                                             |
|; An LN is one of:                                          |
|; – '()                                                     |
|; – (cons Los LN)                                           |
|; interpretation a list of lines, each is a list of Strings |
|                                                             |
| (define line0 (cons "hello" (cons "world" '())))            |
| (define line1 '())                                          |
|                                                             |
| (define ln0 '())                                            |
| (define ln1 (cons line0 (cons line1 '())))                  |
|                                                             |
|; LN -> List-of-numbers                                     |
|; determines the number of words on each line               |
|                                                             |
| (check-expect (words-on-line ln0) '())                      |
| (check-expect (words-on-line ln1) (cons 2 (cons 0 '())))    |
|                                                             |
| (define (words-on-line ln)                                  |
|   (cond                                                     |
|     [(empty? ln) '()]                                       |
|     [else (cons (length (first ln))                         |
|                 (words-on-line (rest ln)))]))               |
+-------------------------------------------------------------+
```

> **Note:** BSL に付属している関数の一覧を見直しておくとよいでしょう。中には馴染みの薄いものに見えるかもしれませんが、今後の問題のどれかで役立つ可能性があります。そうした関数を使用することは、私たちの時間ではなく、あなた自身の時間を節約することになります。

Figure 68 は、サンプル問題に対する完全な設計をまとめたものです。この図には 2 つのテストケースが含まれています。また、独立した関数 `words#` を使う代わりに、`words-on-line` の定義では BSL に付属している `length` 関数を単に呼び出しています。DrRacket でこの定義を実験し、これら 2 つのテストケースが関数定義全体をカバーしていることを確認してください。

小さな一歩を踏み出すことで、最初のファイルユーティリティを設計できるようになります：

```racket
; String -> List-of-numbers
; counts the words on each line in the given file
(define (file-statistic file-name)
  (words-on-line
    (read-words/line file-name)))
```

```racket
; String -> List-of-numbers
; counts the words on each line in the given file
(define (file-statistic file-name)
  (words-on-line
    (read-words/line file-name)))
```

これは単に、ティーチパックの関数と `words-on-line` を合成しているだけです。前者はファイルを `List-of-list-of-strings` として読み込み、その値を後者に渡します。

組み込み関数と新しく設計した関数を合成するというこの考え方は一般的なものです。当然ながら、無作為に関数を設計した上で、その設計を補完するものが選択したプログラミング言語の中に見つかることなど期待しません。代わりに、プログラムの設計者はあらかじめ計画を立て、利用可能な関数が提供する出力に合わせて関数を設計します。さらに一般的に言えば、前述のように、解決策を 2 つの計算の合成として捉え、一方の計算の結果をもう一方の計算に伝えるための適切なデータコレクションを開発することはよくあることです。ここで、それぞれの計算は関数として実装されます。

```
+-----------------------------------------------------------+
| Figure 69: Encoding strings                               |
|                                                           |
|; 1String -> String                                       |
|; converts the given 1String to a 3-letter numeric String |
|                                                           |
| (check-expect (encode-letter "z") (code1 "z"))            |
| (check-expect (encode-letter "\t")                        |
|               (string-append "00" (code1 "\t")))          |
| (check-expect (encode-letter "a")                         |
|               (string-append "0" (code1 "a")))            |
|                                                           |
| (define (encode-letter s)                                 |
|   (cond                                                   |
|     [(>= (string->int s) 100) (code1 s)]                  |
|     [(< (string->int s) 10)                               |
|      (string-append "00" (code1 s))]                      |
|     [(< (string->int s) 100)                              |
|      (string-append "0" (code1 s))]))                     |
|                                                           |
|; 1String -> String                                       |
|; converts the given 1String into a String                |
|                                                           |
| (check-expect (code1 "z") "122")                          |
|                                                           |
| (define (code1 c)                                         |
|   (number->string (string->int c)))                       |
+-----------------------------------------------------------+
```

**Exercise 172.** 行のリスト（list of lines）を 1 つの文字列に変換する関数 `collapse` を設計しなさい。文字列どうしは半角スペース（`" "`）で区切られる必要があります。行どうしは改行（`"\n"`）で区切られる必要があります。

チャレンジ：完成したら、次のようにプログラムを使用してください：

```racket
(write-file "ttt.dat"
            (collapse (read-words/line "ttt.txt")))
```

2 つのファイル `"ttt.dat"` と `"ttt.txt"` が同一であることを確認するために、あなたが作成した `T.T.T.` の詩のバージョンから余分な空白をすべて削除してください。

**Exercise 173.** テキストファイルからすべての冠詞（articles）を削除するプログラムを設計しなさい。このプログラムはファイル名 `n` を受け取り、ファイルを読み込み、冠詞を削除して、その結果を `"no-articles-"` に `n` を連結した名前のファイルに出力します。この演習では、冠詞とは `"a"`、`"an"`、`"the"` の 3 つの単語のいずれかを指します。

元のテキストの行と単語への構造が変換後も維持されるように `read-words/line` を使用しなさい。プログラムが設計できたら、Piet Hein の詩に対して実行しなさい。

**Exercise 174.** テキストファイルを数値的に符号化（encode）するプログラムを設計しなさい。単語内の各文字は、0 から 256 の間の値を持つ 3 文字の数値文字列として符号化されるべきです。Figure 69 は 1 文字に対する私たちの符号化関数を示しています。始める前に、これらの関数について説明しなさい。

ヒント：(1) ファイルの行と単語への構造を保持するために `read-words/line` を使用すること。(2) `explode` についてもう一度よく読んでおくこと。

**Exercise 175.** Unix コマンドの `wc` をシミュレートする BSL プログラムを設計しなさい。このコマンドの目的は、与えられたファイル内の `1String`、単語、および行の数を数えることです。すなわち、このコマンドはファイル名を受け取り、3 つの数値からなる値を生成します。（なぜあなたのコンピュータの `wc` は、あなたが作成したコードとわずかに異なる結果を返す可能性があるのでしょうか？）

> **Note:** Unix は古いオペレーティングシステムであり、Linux、macOS、そしてある程度現代の Windows によってエミュレートされています。

```
+----------------------------------------------------------+
| Figure 70: Transpose a matrix                            |
|                                                          |
|; Matrix -> Matrix                                       |
|; transposes the given matrix along the diagonal         |
|                                                          |
| (define wor1 (cons 11 (cons 21 '())))                    |
| (define wor2 (cons 12 (cons 22 '())))                    |
| (define tam1 (cons wor1 (cons wor2 '())))                |
|                                                          |
| (check-expect (transpose mat1) tam1)                     |
|                                                          |
| (define (transpose lln)                                  |
|   (cond                                                  |
|     [(empty? (first lln)) '()]                           |
|     [else (cons (first* lln) (transpose (rest* lln)))])) |
+----------------------------------------------------------+
```

**Exercise 176.** 数学の先生がこれまでに行列（matrix）の計算を教えてくれたかもしれません。原則として、行列とは単に数値の長方形を意味します。以下は行列に対する可能なデータ表現の 1 つです：

```racket
; A Matrix is one of:
;  – (cons Row '())
;  – (cons Row Matrix)
; constraint all rows in matrix are of the same length

; A Row is one of:
;  – '()
;  – (cons Number Row)
```

行列に関する制約に注意してください。データ定義を熟読し、数 11, 12, 21, 22 からなる 2×2 の行列をこのデータ表現に変換しなさい。立ち止まって、このデータの例を自力で考え出すまでは読み進めないでください。

5 秒で解くパズルの解答は以下のとおりです：

```racket
(define row1 (cons 11 (cons 12 '())))
(define row2 (cons 21 (cons 22 '())))
(define mat1 (cons row1 (cons row2 '())))
```

もし自力で作成できなかった場合は、今すぐじっくりと確認してください。

Figure 70 の関数は、行列の要素を転置（transpose）するという重要な数学的操作を実装しています。転置するとは、対角線（すなわち左上から右下への直線）に沿って要素を反転させることを意味します。

立ち止まって！手作業で `mat1` を転置してから、Figure 70 を読みなさい。なぜ `transpose` は `(empty? (first lln))` を尋ねるのでしょうか？

この定義は 2 つの補助関数を前提としています：

- `first*`: 行列を受け取り、最初の列を数値のリストとして生成します。
- `rest*`: 行列を受け取り、最初の列を取り除きます。結果は行列になります。

これらの関数の定義がまだ手元になくても、`transpose` がどのように動作するのかは理解できるはずです。また、これまでに学んだ設計レシピではこの関数を設計できないことも理解できるはずです。その理由を説明しなさい。

ウィッシュリストにあるこれら 2 つの関数を設計しなさい。その後、いくつかのテストケースを追加して `transpose` の設計を完成させなさい。

### 10.4 グラフィカルエディタ再訪 (A Graphical Editor, Revisited)

「グラフィカルエディタ（A Graphical Editor）」では、対話的なグラフィカル1行エディタの設計を扱いました。そこではエディタの状態を表現する2通りの異なる方法が提案され、その両方を探索することが促されました。すなわち、ペアの文字列を含む構造体、または文字列と現在の位置を指すインデックスを組み合わせた構造体です（演習問題 87 を参照）。

3つ目の代替案は、2つの 1String のリストを組み合わせた構造体を使用することです。

```racket
(define-struct editor [pre post])
; An Editor is a structure:
;   (make-editor Lo1S Lo1S)
; An Lo1S is one of:
; – '()
; – (cons 1String Lo1S)
```

なぜそうするのか疑問に思う前に、2つのデータ例を作成してみましょう。

```racket
(define good
  (cons "g" (cons "o" (cons "o" (cons "d" '())))))
(define all
  (cons "a" (cons "l" (cons "l" '()))))
(define lla
  (cons "l" (cons "l" (cons "a" '()))))

; data example 1:
(make-editor all good)

; data example 2:
(make-editor lla good)
```

これら2つの例は、解釈を書き下すことがどれほど重要であるかを示しています。editor の2つのフィールドがカーソルの左側と右側の文字を表していることは明確ですが、これら2つの例は構造体型の解釈方法に少なくとも2通りのやり方があることを示しています。

1. `(make-editor pre post)` は、`pre` の文字がカーソルの前にあり、`post` の文字がカーソルの後にあって、結合されたテキストが `(string-append (implode pre) (implode post))` であることを意味する可能性があります。`implode` は 1String のリストを String に変換することを思い出してください。
2. `(make-editor pre post)` は、`pre` の文字が逆順でカーソルの前にある、という意味である可能性も同様にあります。その場合、表示されるエディタ内のテキストは次のように得られます： `(string-append (implode (rev pre)) (implode post))`。関数 `rev` は 1String のリストを受け取り、それを逆順にしなければなりません。

`rev` の完全な定義がなくても、それがどのように機能するかは想像できるでしょう。この理解を利用して、最初のデータ例を最初の解釈に従って情報に変換した場合と、2番目のデータ例を2番目の解釈に従って処理した場合に、同じエディタ表示が得られることを確認してください。

> [image: pict_101.png]

どちらの解釈も優れた選択肢ですが、2番目の解釈を使用するとプログラムの設計が大幅に単純化されることが分かります。本節の残りの部分ではこの点を示し、同時に構造体内部でのリストの使用方法を説明します。このレッスンを適切に理解するには、「グラフィカルエディタ（A Graphical Editor）」の演習問題を解いておく必要があります。

データ定義の意味を理解するには明らかにこの関数が必要ですので、まずは `rev` から始めましょう。そのヘッダー情報はわかりやすいものです。

```racket
; Lo1s -> Lo1s
; produces a reverse version of the given list

(check-expect
  (rev (cons "a" (cons "b" (cons "c" '()))))
  (cons "c" (cons "b" (cons "a" '()))))

(define (rev l) l)
```

念のため、テストケースとして「明白な」例を1つ追加しました。何が必要かを確実に理解するために、追加の例をいくつか足してみてもよいでしょう。

`rev` のテンプレートは、通常のリスト用テンプレートです。

```racket
(define (rev l)
  (cond
    [(empty? l)...]
    [else (... (first l)...
... (rev (rest l))...)]))
```

ケースは2つあり、2番目のケースにはいくつかのセレクタ式と1つの自己参照式が含まれています。

テンプレートの最初の節を埋めるのは簡単です。空リストの逆順バージョンは空リストです。2番目の節については、再びコーディングの質問を使用します。

- `(first l)` は 1String のリストの最初の要素です。
- `(rest l)` はリストの残りの部分です。そして
- `(rev (rest l))` はリストの残りの部分の逆順です。

ここで手を止めましょう！これらのヒントをもとに `rev` の設計を完成させてみてください。

```
+--------------------------------------------------------------------------------+
| Figure 71: Tabulating for rev                                                  |
|                                                                                |
| +--------------------------------------+--+-------------+--+------------------ |
| | l | | (first ⏎ l) | | (rest ⏎ l) | | (rev ⏎ (rest l)) | | (rev l) |          |
| +--------------------------------------+--+-------------+--+------------------ |
| | (first | | | | | | | | |                                                     |
| | l) | | | | | | | | |                                                         |
| | (rest | | | | | | | | |                                                      |
| | l) | | | | | | | | |                                                         |
| | (rev | | | | | | | | |                                                       |
| | (rest l)) | | | | | | | | |                                                  |
| | (cons "a" ⏎ '()) | | "a" | | '() | | '() | | (cons "a" ⏎ '()) |              |
| | (cons "a" | | | | | | | | |                                                  |
| | '()) | | | | | | | | |                                                       |
| | (cons "a" | | | | | | | | |                                                  |
| | '()) | | | | | | | | |                                                       |
| | (cons "a" ⏎ (cons "b" ⏎ (cons "c" ⏎… | | "a" | | (cons "b" ⏎ (cons "c" ⏎     |
| '())) | | (cons "c" ⏎ (cons "b" ⏎ '())) | | (cons "c" ⏎ (cons "b" ⏎ (cons "a"  |
| ⏎… |                                                                           |
| | (cons "a" | | | | | | | | |                                                  |
| | (cons "b" | | | | | | | | |                                                  |
| | (cons "c" | | | | | | | | |                                                  |
| | '()))) | | | | | | | | |                                                     |
| | (cons "b" | | | | | | | | |                                                  |
| | (cons "c" | | | | | | | | |                                                  |
| | '())) | | | | | | | | |                                                      |
| | (cons "c" | | | | | | | | |                                                  |
| | (cons "b" | | | | | | | | |                                                  |
| | '())) | | | | | | | | |                                                      |
| | (cons "c" | | | | | | | | |                                                  |
| | (cons "b" | | | | | | | | |                                                  |
| | (cons "a" | | | | | | | | |                                                  |
| | '()))) | | | | | | | | |                                                     |
| +--------------------------------------+--+-------------+--+------------------ |
+--------------------------------------------------------------------------------+
```

これらのヒントで行き詰まった場合は、例から表を作成することを思い出してください。Figure 71 は、2つの例 `(cons "a" '())` および `(cons "a" (cons "b" (cons "c" '())))` に対する表を示しています。2番目の例は特にわかりやすいものです。後ろから2番目の列を見ると、`(rev (rest l))` が `(cons "c" (cons "b" '()))` を生成することで、仕事の大部分を果たしていることがわかります。希望する結果は `(cons "c" (cons "b" (cons "a" '())))` なので、`rev` は再帰の結果の末尾に何らかの方法で `"a"` を追加しなければなりません。実際、`(rev (rest l))` は常にリストの残りの部分の逆順であるため、その末尾に `(first l)` を追加するだけで十分であることは明らかです。リストの末尾に要素を追加する関数はまだありませんが、それを「おねだり（wish）」して、関数定義を完成させるために使用することができます。

```racket
(define (rev l)
  (cond
    [(empty? l) '()]
    [else (add-at-end (rev (rest l)) (first l))]))
```

以下は、`add-at-end` に対する拡張されたウィッシュリスト・エントリです。

```racket
; Lo1s 1String -> Lo1s
; creates a new list by adding s to the end of l

(check-expect
  (add-at-end (cons "c" (cons "b" '())) "a")
  (cons "c" (cons "b" (cons "a" '()))))

(define (add-at-end l s)
  l)
```

これが「拡張」されているのは、テストケースとして定式化された例が付属しているためです。この例は `rev` の例から導き出されたものであり、まさにこのウィッシュリスト・エントリの動機となった例そのものです。読み進める前に、`add-at-end` が空リストを受け取る例をご自身で作成してみてください。

`add-at-end` もリスト処理関数であるため、テンプレートは皆様が熟知しているものを名前変更しただけです。

```racket
(define (add-at-end l s)
  (cond
    [(empty? l)...]
    [else (... (first l)...
... (add-at-end (rest l) s)...)]))
```

これを関数定義として完成させるために、ステップ 5 のレシピの質問に従って進めます。最初の質問は、「基本（basic）」ケース、すなわちここでの最初のケースに対する回答を定式化することです。提案された演習に取り組んだのであれば、

```racket
(add-at-end '() s)
```

の結果が常に `(cons s '())` になることがわかるでしょう。結局のところ、結果はリストでなければならず、そのリストには与えられた 1String が含まれていなければならないからです。

次の2つの質問は、「複雑な」ケースまたは「自己参照」ケースに関するものです。2番目の `cond` 行の式が何を計算するかはわかっています。最初の式は与えられたリストから最初の 1String を取り出し、2番目の式は「`(rest l)` の末尾に `s` を追加して新しいリストを作成」します。つまり、目的記述（purpose statement）が、関数がここで何を生成すべきかを指示しています。ここから、関数は再帰の結果に `(first l)` を再び追加しなければならないことが明確になります。

```racket
(define (add-at-end l s)
  (cond
    [(empty? l) (cons s '())]
    [else
     (cons (first l) (add-at-end (rest l) s))]))
```

例としてのテストを実行して、この関数が動作すること、したがって `rev` も動作することを確信してください。もちろん、BSLが 1String のリストを含む任意の与えられたリストを反転する関数をすでに提供していることを知っても驚くべきではありません。そして当然ながら、それは `reverse` と呼ばれています。

**Exercise 177.** 関数 `create-editor` を設計しなさい。この関数は2つの文字列を受け取り、`Editor` を生成します。最初の文字列はカーソルの左側のテキストであり、2番目の文字列はカーソルの右側のテキストです。本節の残りの部分は、この関数に依存しています。

この時点で、グラフィカルな1行エディタに対するデータの表現方法を完全に理解しているはずです。「世界プログラムの設計（Designing World Programs）」での対話型プログラムの設計戦略に従って、物理定数（例えばエディタの幅や高さなど）およびグラフィック定数（例えばカーソルなど）を定義する必要があります。以下は私たちの定義例です。

```racket
(define HEIGHT 20); the height of the editor
(define WIDTH 200); its width
(define FONT-SIZE 16); the font size
(define FONT-COLOR "black"); the font color

(define MT (empty-scene WIDTH HEIGHT))
(define CURSOR (rectangle 1 HEIGHT "solid" "red"))
```

しかし重要な点は、イベントハンドラおよびエディタの状態を描画する関数に対するウィッシュリストを書き出すことです。`2htdp/universe` ティーチパックがこれらの関数のヘッダー情報を定めていることを思い出してください。

```racket
; Editor -> Image
; renders an editor as an image of the two texts
; separated by the cursor
(define (editor-render e) MT)

```racket
; Editor -> Image
(define (editor-render e)
  (place-image/align
    (beside (editor-text (editor-pre e))
            CURSOR
            (editor-text (editor-post e)))
    1 1
    "left" "top"
    MT))
```

```racket
; Editor -> Image
; renders an editor as an image of the two texts
; separated by the cursor
(define (editor-render e) MT)

; Editor KeyEvent -> Editor
; deals with a key event, given some editor
(define (editor-kh ed ke) ed)
```

```racket
; Editor -> Image
(define (editor-render e)
  (place-image/align
    (beside (editor-text (editor-pre e))
            CURSOR
            (editor-text (editor-post e)))
    1 1
    "left" "top"
    MT))
```

; Editor KeyEvent -> Editor
; deals with a key event, given some editor
(define (editor-kh ed ke) ed)
```

さらに、「世界プログラムの設計（Designing World Programs）」では、プログラムの `main` 関数を書き出すことが求められます。

```racket
; main: String -> Editor
; launches the editor given some initial string
(define (main s)
   (big-bang (create-editor s "")
     [on-key editor-kh]
     [to-draw editor-render]))
```

```racket
; main: String -> Editor
; launches the editor given some initial string
(define (main s)
   (big-bang (create-editor s "")
     [on-key editor-kh]
     [to-draw editor-render]))
```

このプログラムの初期エディタを決定するために、演習問題 177（exercise 177）を読み返してください。

次にどのウィッシュに取り組むかはどちらでも構いませんが、ここでは最初に `editor-kh` を設計し、次に `editor-render` を設計することにします。ヘッダー情報はすでにあるので、キーイベントハンドラの機能を2つの例で説明しましょう。

```racket
(check-expect (editor-kh (create-editor "" "") "e")
              (create-editor "e" ""))
(check-expect
  (editor-kh (create-editor "cd" "fgh") "e")
  (create-editor "cde" "fgh"))
```

これら両方の例は、キーボードで文字「e」を押したときに何が起こるかを示しています。コンピュータは、エディタの現在の状態と `"e"` に対して関数 `editor-kh` を実行します。最初の例ではエディタは空であり、これは結果が文字 `"e"` とそれに続くカーソルだけを持つエディタになることを意味します。2番目の例では、カーソルは文字列 `"cd"` と `"fgh"` の間にあり、したがって結果は `"cde"` と `"fgh"` の間にカーソルがあるエディタになります。要するに、この関数は常にカーソル位置に通常の文字を挿入します。

読み進める前に、文字を削除するためにバックスペース（`"\b"`）キーを押したとき、カーソルを移動するために `"left"` や `"right"` の矢印キーを押したとき、あるいはその他の矢印キーを押したときに `editor-kh` がどのように機能するかを示す例を作成してみてください。どのような場合でも、エディタが空のとき、カーソルがエディタ内の空でない文字列の左端または右端にあるとき、そして中央にあるときに何が起こるべきかを考慮してください。ここで区間（interval）を扱っているわけではありませんが、「極端な」ケースに対する例を作成しておくことは、依然として良い考えです。

テストケースを用意したら、テンプレートを開発する段階です。`editor-kh` の場合、2つの複雑な形態のデータを消費する関数を扱うことになります。1つはリストを含む構造体であり、もう1つは文字列の大きな列挙体（enumeration）です。一般的に言って、このような設計ケースでは改善された設計レシピが要求されます。しかしこのようなケースでは、入力の1つ、すなわちキーストローク（キー入力）を最初に処理すべきであることも明白です。

そうは言っても、テンプレートは関数がどの `KeyEvent` を受け取ったかをチェックするための大きな `cond` 式にすぎません。

```racket
(define (editor-kh ed k)
  (cond
    [(key=? k "left")...]
    [(key=? k "right")...]
    [(key=? k "\b")...]
    [(key=? k "\t")...]
    [(key=? k "\r")...]
    [(= (string-length k) 1)...]
    [else...]))
```

cond 式は KeyEvent t`データ定義に完完全には一致していません。なぜなら、一部の `KeyEvent` は特別な注意を必要とし（`"left"` や `"\b"` など）、一部は特殊であるため無視される必要があり（`"\t"` や `"\r"`）、また一部は一つの大きなグループ（通常のキー）に分類されるべきだからです。

**Exercise 178.** `editor-kh` のテンプレートが、長さ1の文字列であるかをチェックする前に `"\t"` と `"\r"` を処理している理由を説明せよ。

第5ステップ——関数の定義——では、条件式の各節に個別に立ち向かいます。最初の節は、カーソルを移動させ、エディタの文字列コンテンツには手を加えない結果を要求します。2番目の節も同様です。しかし3番目の節は、エディタのコンテンツから文字を（もし文字が存在すれば）削除することを要求します。最後に、6番目の `cond` 節はカーソル位置への文字の追加に関係しています。最初の基本的なガイドラインに従い、私たちはウィッシュリストを広範に活用し、タスクごとに1つの関数を想像します。

```racket
(define (editor-kh ed k)
  (cond
    [(key=? k "left") (editor-lft ed)]
    [(key=? k "right") (editor-rgt ed)]
    [(key=? k "\b") (editor-del ed)]
    [(key=? k "\t") ed]
    [(key=? k "\r") ed]
    [(= (string-length k) 1) (editor-ins ed k)]
    [else ed]))
```

`editor-kh` の定義から分かるように、ウィッシュリストの4つの関数のうち3つは同じシグネチャを持っています。

```racket
; Editor -> Editor
```

最後の関数は1つではなく2つの引数を取ります。

```racket
; Editor 1String -> Editor
```

最初の3つの関数に対する正しい願いの定式化はあなたに委ね、ここでは4番目の関数に焦点を当てます。

目的文と関数ヘッダーから始めましょう。

```racket
; insert the 1String k between pre and post
(define (editor-ins ed k)
  ed)
```

目的は問題文からそのまま取ってきたものです。関数ヘッダーの構築には、`Editor` のインスタンスが必要です。`pre` と `post` は現在のインスタンスの構成要素であるため、単にそれらを再び組み立てるだけです。

次に、`editor-kh` の例から `editor-ins` の例を導き出します。

```racket
(check-expect
  (editor-ins (make-editor '() '()) "e")
  (make-editor (cons "e" '()) '()))

(check-expect
  (editor-ins
    (make-editor (cons "d" '())
                 (cons "f" (cons "g" '())))
    "e")
  (make-editor (cons "e" (cons "d" '()))
               (cons "f" (cons "g" '()))))
```

`Editor` の解釈を用いて、これらの例を検討してください。すなわち、与えられたエディタが情報の観点から何を意味しているのか、そして関数呼び出しがその観点から何を達成することになっているのかを理解しているか確認してください。このケースにおいては、エディタの視覚表現を描くことが情報を適切に表現するためベストです。

第4ステップでは、テンプレートの開発が求められます。第1引数は構造体であることが保証されており、第2引数はアトミックなデータである文字列です。言い換えれば、テンプレートは与えられたエディタの表現からパーツを取り出しているだけです。

```racket
(define (editor-ins ed k)
  (... ed... k...
... (editor-pre ed)...
... (editor-post ed)...))
```

テンプレートにはパラメータも利用可能であるため列挙されていることを思い出してください。

テンプレートと例から、`editor-ins` は与えられたエディタの `pre` フィールドと `post` フィールドからエディタを作成し、前者の先頭に `k` を追加すればよいと結論付けるのは比較的容易です。

```racket
(define (editor-ins ed k)
  (make-editor (cons k (editor-pre ed))
               (editor-post ed)))
```

`(editor-pre ed)` と `(editor-post ed)` の両方が `1String` のリストであっても、補助関数を設計する必要はありません。望む結果を得るには、リストを作成する `cons` を使えば十分です。

この時点で、あなたは2つのことをすべきです。第一に、この関数のテストを実行すること。第二に、`Editor` の解釈を用いて、なぜこの関数が挿入を行えるのかを抽象的に説明することです。そして、これだけでは物足りないなら、このシンプルな定義を exercise 84 の定義と比較し、あちらの定義では補助関数が必要であったのに対し、ここでの私たちの定義ではなぜ必要ないのかを考えてみるとよいでしょう。

**Exercise 179.** 次の関数を設計せよ。

```racket
; Editor -> Editor
; moves the cursor position one 1String left,
; if possible
(define (editor-lft ed) ed)

; Editor -> Editor
; moves the cursor position one 1String right,
; if possible
(define (editor-rgt ed) ed)

; Editor -> Editor
; deletes a 1String to the left of the cursor,
; if possible
(define (editor-del ed) ed)
```

繰り返しになりますが、十分な数の例を使って詳しく確認することが非常に重要です。

`Editor` の描画（rendering）関数を設計することには、いくつか新しいものの小さな課題があります。第1の課題は、十分な数のテストケースを開発することです。一方では、考えられる組み合わせのカバレッジが求められます。カーソルの左側の空文字列、右側の空文字列、そして両方の文字列が空である場合です。他方では、`2htdp/image` ティーチパックが提供する関数をいくつか実験してみることも必要です。具体的には、テキスト画像として描画された2つの文字列のピースを合成する方法が必要であり、テキスト画像を空の画像フレーム（`MT`）に配置する方法が必要です。以下は、`(create-editor "pre" "post")` の結果に対する画像を作成するために行うことです。

```racket
(place-image/align
  (beside (text "pre" FONT-SIZE FONT-COLOR)
          CURSOR
          (text "post" FONT-SIZE FONT-COLOR))
  1 1
  "left" "top"
  MT)
```

これを上のエディタの画像と比較すると、いくつかの違いに気づくでしょう。しかし正確なレイアウトはこの演習の目的において本質的ではなく、また改訂されたレイアウトによって問題が極端に簡単になるわけでもないため、問題ありません。いずれにせよ、DrRacket の対話領域で実験して、お気に入りのエディタ表示を見つけてください。

これでテンプレートを開発する準備が整いました。少なくともここまでは作成できるはずです。

```racket
(define (editor-render e)
  (... (editor-pre e)... (editor-post e)))
```

与えられた引数は2つのフィールドを持つ構造体型にすぎません。しかしそれらの値は `1String` のリストであり、テンプレートをさらに洗練させたくなるかもしれません。そうしてはいけません！ その代わりに、あるデータ定義が別の複雑なデータ定義を参照している場合、ウィッシュリストを使用する方がよいということを心に留めておいてください。

十分な数の例を消化していれば、ウィッシュリストに何を載せたいかも分かっているはずです。それは、文字列を適切なサイズと色のテキスト画像に変換する1つの関数です。この関数を `editor-text` と呼びましょう。そうすると、`editor-render` の定義は単に `editor-text` を2回使用し、その結果を `beside` と `place-image` で合成するだけになります。

```racket
; Editor -> Image
(define (editor-render e)
  (place-image/align
    (beside (editor-text (editor-pre e))
            CURSOR
            (editor-text (editor-post ed)))
    1 1
    "left" "top"
    MT))
```

この定義は式を3段階の深さにネストしていますが、仮想の `editor-text` 関数を使用することで、非常に読みやすくなっています。

残るは `editor-text` の設計です。`editor-render` の設計から、`editor-text` は `1String` のリストを受け取り、テキスト画像を生成することが分かっています。

```racket
; Lo1s -> Image
; renders a list of 1Strings as a text image
(define (editor-text s)
  (text "" FONT-SIZE FONT-COLOR))
```

このダミー定義は、空のテキスト画像を生成します。

`editor-text` が何を計算することになっているかを実証するために、例を詳しく見ていきます。例となる入力は

```racket
(create-editor "pre" "post")
```

であり、これは `editor-render` の説明にも使用されたもので、以下と等価です。

```racket
(make-editor
  (cons "e" (cons "r" (cons "p" '())))
  (cons "p" (cons "o" (cons "s" (cons "t" '())))))
```

私たちは2番目のリストを `editor-text` のサンプル入力として選びます。そして期待される結果は `editor-render` の例から分かっています。

```racket
(check-expect
  (editor-text
   (cons "p" (cons "o" (cons "s" (cons "t" '())))))
  (text "post" FONT-SIZE FONT-COLOR))
```

読み進める前に、2つ目の例を作成してみるとよいでしょう。

`editor-text` が `1String` のリストを受け取ることを踏まえれば、手際よくテンプレートを書き出すことができます。

```racket
(define (editor-text s)
  (cond
    [(empty? s)...]
    [else (... (first s)
... (editor-text (rest s))...)]))
```

結局のところ、テンプレートは関数の入力を記述するデータ定義によって規定されます。しかし、`Editor` の解釈を理解し心に留めていれば、テンプレートは必要ありません。そこでは文字列を `1String` のリストに変換するために `explode` が使用されています。当然ながら、その逆の計算を行う `implode` という関数が存在します。すなわち、

```racket
> (implode   (cons "p" (cons "o" (cons "s" (cons "t" '())))))
"post"
```

この関数を使用すると、`editor-text` の定義は例から関数本体へとほんのわずかなステップで到達できます。

```racket
(define (editor-text s)
  (text (implode s) FONT-SIZE FONT-COLOR))
```

**Exercise 180.** `implode` を使わずに `editor-text` を設計せよ。

本当の驚きは、これら2つの関数をテストするときに訪れます。`editor-text` のテストは成功するものの、`editor-render` のテストは失敗します。失敗箇所を調べると、カーソルの左側の文字列—— `"pre"` ——が逆順に組版されていることが分かります。エディタのこの部分の状態が逆順で表現されていることを忘れていたのです。幸いなことに、2つの関数の単体テストはどちらの関数が誤っているかを正確に突き止め、その関数のどこが間違っているのかを伝え、問題を解決する方法すら提案してくれます。

```racket
(define (editor-render ed)
  (place-image/align
    (beside (editor-text (reverse (editor-pre ed)))
            CURSOR
            (editor-text (editor-post ed)))
    1 1
    "left" "top"
    MT))
```

この定義は、`ed` の `pre` フィールドに対して `reverse` 関数を使用しています。

**Note** 現代のアプリケーションでは、ユーザーがマウス（またはその他のジェスチャーベースのデバイス）を使ってカーソルを配置できるようになっています。原理的にはこの機能をエディタに追加することは可能ですが、A Graphical Editor, with Mouse のセクションまでその実施を保留します。

```
+-------------------------------------------------------------+
| Figure 68: Counting the words on a line                     |
|                                                             |
|; An LN is one of:                                          |
|; – '()                                                     |
|; – (cons Los LN)                                           |
|; interpretation a list of lines, each is a list of Strings |
|                                                             |
| (define line0 (cons "hello" (cons "world" '())))            |
| (define line1 '())                                          |
|                                                             |
| (define ln0 '())                                            |
| (define ln1 (cons line0 (cons line1 '())))                  |
|                                                             |
|; LN -> List-of-numbers                                     |
|; determines the number of words on each line               |
|                                                             |
| (check-expect (words-on-line ln0) '())                      |
| (check-expect (words-on-line ln1) (cons 2 (cons 0 '())))    |
|                                                             |
| (define (words-on-line ln)                                  |
|   (cond                                                     |
|     [(empty? ln) '()]                                       |
|     [else (cons (length (first ln))                         |
|                 (words-on-line (rest ln)))]))               |
+-------------------------------------------------------------+
```

