## 12 プロジェクト：リスト (Projects: Lists)

本章ではいくつかの発展的な演習問題を取り上げます。これらはいずれも、設計の構成要素についての理解を確実なものにすることを目的としています。すなわち、バッチプログラムおよび対話型プログラムの設計、構成による設計（design by composition）、設計ウィッシュリスト（design wish lists）、そして関数のための設計レシピ（design recipe）です。最初の節では、英語の辞書や iTunes ライブラリといった現実世界のデータを扱う問題を扱います。単語パズルの問題には2つの節を費やします。1つは構成による設計を示すため、もう1つは問題の中核に取り組むためです。残りの節では、ゲームや有限状態機械（finite-state machines）を扱います。

> **注:** 本章は `2htdp/batch-io` ティーチパックに依存しています。

### 12.1 現実世界のデータ：辞書 (Real-World Data: Dictionaries)

現実世界の情報は大量に存在する傾向があり、だからこそプログラムを使ってそれを処理することには大きな意味があります。例えば、辞書にはわずか十数個の単語だけでなく、数十万個もの単語が含まれています。このような大量の情報を処理したい場合は、小さな例を用いてプログラムを慎重に設計しなければなりません。プログラムが正しく動作することを十分に確認できたら、現実世界のデータで実行して実際の結果を得ます。もしプログラムが遅すぎてこの大量のデータを処理できない場合は、各関数とその仕組みについて振り返ってみてください。冗長な計算を排除できないか検討しましょう。

> **注:** 性能に関する問題については、「生成基底の再帰（Generative Recursion）」を参照してください。そこに至るまでは、性能上の問題を適切に探求できるように、プログラムを体系的に設計することに焦点を当てます。

```
+-----------------------------------------------------------+
| Figure 74: Reading a dictionary                           |
|                                                           |
|; On OS X:                                                |
| (define LOCATION "/usr/share/dict/words")                 |
|; On LINUrds  |
|; On WINDOWS: borrow the word file from your Linux friend |
|                                                           |
|; A Dictionary is a List-of-strings.                      |
| (define AS-LIST (read-lines LOCATION))                    |
+-----------------------------------------------------------+
```

図74（Figure 74）は、英語の辞書全体を読み込むために必要な1行のコードを示しています。このような辞書がどれほど大きいかを実感するために、図のコードをお使いのコンピュータに合わせて調整し、`length` を使用してご自身の辞書に何単語あるかを測定してみてください。私たちが測定した本日（2017年7月25日）の辞書には235,886語が含まれていました。

```
+-----------------------------------------------------------+
| Figure 74: Reading a dictionary                           |
|                                                           |
|; On OS X:                                                |
| (define LOCATION "/usr/share/dict/words")                 |
|; On LINUX: /usr/share/dict/words or /var/lib/dict/words  |
|; On WINDOWS: borrow the word file from your Linux friend |
|                                                           |
|; A Dictionary is a List-of-strings.                      |
| (define AS-LIST (read-lines LOCATION))                    |
+-----------------------------------------------------------+
```

以下の演習問題では、アルファベットの文字（letter）が重要な役割を果たします。図74のコードを調整したものに加えて、プログラムの先頭に以下を追加するとよいでしょう。

```racket
; A Letter is one of the following 1Strings:
; – "a"
; –...
; – "z"
; or, equivalently, a member? of this list:
(define LETTERS
  (explode "abcdefghijklmnopqrstuvwxyz"))
```

ヒント: 演習問題の例やテストを作成するには `list` を使用してください。

**Exercise 195.** `Letter` と `Dictionary` を受け取り、与えられた `Dictionary` 内の何個の単語が与えられた `Letter` で始まるかを数える関数 `starts-with#` をデザインしなさい。関数が正しく動作することを確認したら、お使いのコンピュータの辞書で `"e"` で始まる単語が何個あるか、また `"z"` で始まる単語が何個あるかを求めなさい。

**Exercise 196.** `count-by-letter` をデザインしなさい。この関数は `Dictionary` を受け取り、与えられた辞書の中で各文字が単語の先頭文字として使われている回数を数えます。その結果は、文字とカウントを組み合わせたデータである `Letter-Count` のリスト（list of Letter-Counts）になります。

関数のデザインが完了したら、お使いのコンピュータの辞書内のすべての文字に対して何個の単語が存在するかを求めなさい。

**Exercise 197.** `most-frequent` をデザインしなさい。この関数は `Dictionary` を受け取ります。与えられた `Dictionary` 内で単語の先頭文字として最も頻繁に現れる文字に対する `Letter-Count` を生成します。

お使いのコンピュータの辞書で最も頻繁に使用される先頭文字は何ですか？また、それは何回使用されていますか？

デザイン選択に関する注記 この演習問題では、前問の解法と、`Letter-Count` のリストから正しいペアを選ぶ関数との合成が求められます。後者の関数をデザインするには、次の2つの方法があります。

- カウントが最大のペアを選ぶ関数をデザインする。
- ソートされたペアのリストから最初の要素を選択する関数をデザインする。

両方のデザインを検討しなさい。あなたはどちらを好みますか？その理由はなぜですか？

**Exercise 198.** `words-by-first-letter` をデザインしなさい。この関数は `Dictionary` を受け取り、`Letter` ごとに1つの `Dictionary` を含む `Dictionary` のリスト（list of Dictionarys）を生成します。

この新しい関数を使用して、演習197の `most-frequent` を再デザインしなさい。新しい関数を `most-frequent.v2` と命名しなさい。デザインが完了したら、お使いのコンピュータの辞書に対して、これら2つの関数が同じ結果を計算することを確認しなさい。

```racket
(check-expect
  (most-frequent AS-LIST)
  (most-frequent.v2 AS-LIST))
```

デザイン選択に関する注記 `words-by-first-letter` においては、与えられた辞書に特定の文字で始まる単語が含まれていない場合の状況に対処するための選択肢があります。

- 1つの選択肢は、結果として生じる空の辞書を全体 Result から除外することです。これを行うことで、関数のテストと `most-frequent.v2` のデザインの両方がシンプルになりますが、補助関数のデザインが必要になります。
- もう1つの選択肢は、ある文字の単語を探した結果として、単語が存在しない場合であっても `'()` を結果に含めることです。この選択肢は、最初の選択肢で必要となる補助関数を回避できますが、`most-frequent.v2` のデザインに複雑さを加えます。

中間データと脱森林化に関する注記 単語をカウントする関数のこの第2バージョンは、その構成部分がカウントされること以外には実質的な目的を持たない巨大な中間データ構造を作成することによって、目的の結果を計算します。場合によっては、プログラミング言語が2つの関数を1つに融合させることで、これらを自動的に排除します。これはプログラムの変換であり、「脱森林化 (deforestation)」とも呼ばれます。プログラミング言語がプログラムを脱森林化しないとわかっており、プログラムが十分な速度でデータを処理できない場合は、このようなデータ構造を排除することを検討してください。

### 12.2 実世界データ: iTunes (Real-World Data: iTunes)

AppleのiTunesソフトウェアは、音楽、ビデオ、TV番組などを収集するために広く利用されています。自身のiTunesアプリケーションが収集した情報を分析したいと思うかもしれません。そのデータベースを抽出することは、実のところ非常に簡単です。アプリケーションの`File`メニューを選択し、`Library`を選んでから`Export`を選択します。すると、iTunes情報のいわゆるXML表現をエクスポートできます。
XMLの処理は「Project: The Commerce of XML」で詳しく扱われています。ここでは、情報を取得するために`2htdp/itunes`ティーチパックに依存します。具体的には、このティーチパックを使用することで、iTunesライブラリに含まれる音楽トラックを取得できます。

> **Note:** このような独自の形式は定期的に変更されるため、本書には2つのサンプルファイル`itunes.xml`と`itunes2.xml`が付属しています。ご自身のファイルの代わりに、これらを使用しても構いません。

詳細には違いがありますが、iTunesライブラリは各音楽トラックに対して以下のような種類の情報を保持しています（場合によってはこれより少し少ないこともあります）。

- Track ID: ライブラリ内でのトラックの一意の識別子。例: `442`
- Name: トラックのタイトル。`Wild Child`
- Artist: 制作アーティスト。`Enya`
- Album: トラックが所属するアルバムのタイトル。`A Day Without`
- Genre: トラックに割り当てられた音楽ジャンル。`New Age`
- Kind: 音楽のエンコーディング。MPEG audio file
- Size: ファイルのサイズ。`4562044`
- Total Time: トラックの長さ（ミリ秒単位）。`227996`
- Track Number: アルバム内でのトラックの順序。`2`
- Track Count: アルバムに含まれるトラックの数。`11`
- Year: リリース年。`2000`
- Date Added: トラックが追加された日時。`2002-7-17 3:55:14`
- Play Count: 再生回数。`20`
- Play Date: 最後に再生された日時。`3388484113` Unix秒
- Play Date UTC: 最後に再生されたUTC日時。`2011-5-17 17:35:13`

いつものように、最初のタスクはこの情報に対するBSLデータ表現を選択することです。本節では、音楽トラックに対して2つの表現を使用します。1つは構造体に基づく表現、もう1つはリストに基づく表現です。前者は、すべての情報が利用可能な場合にのみトラックごとに固定数の属性を記録するのに対し、後者は利用可能なあらゆる情報をデータとして表現します。それぞれが特定の用途に適しており、用途によっては両方の表現が役立ちます。

> **Note:** `2htdp/batch-io`ティーチパックに加えて、本節は`2htdp/itunes`ティーチパックに依存します。

```
+----------------------------------------------------------------------+
| Figure 75: Representing iTunes tracks as structures (the structures) |
|                                                                      |
|; the 2htdp/itunes teachpack documentation, part 1:                  |
|                                                                      |
|; An LTracks is one of:                                              |
|; – '()                                                              |
|; – (cons Track LTracks)                                             |
|                                                                      |
| (define-struct track                                                     |
| (define-struct track                                                 |
|   [name artist album time track# added play# played])                |
|; A Track is a structure:                                            |
|; (make-track String String String N N Date N Date)                  |
|; interpretation An instance records in order: the track's           |
|; title, its producing artist, to which album it belongs,            |
|; its playing time in milliseconds, its position within the          |
|; album, the date it was added, how often it has been                |
|; played, and the date when it was last played                       |
|                                                                      |
| (define-struct date [year month day hour minute second])             |
|; A Date is a structure:                                             |
|; (make-date N N N N N N)                                            |
|; interpretation An instance records six pieces of information:      |
|; the date's year, month (between 1 and 12 inclusive),               |
|; day (between 1 and 31), hour (between 0                            |
|; and 23), minute (between 0 and 59), and                            |
|; second (also between 0 and 59).                                    |
+----------------------------------------------------------------------+
```

```
+---------------------------------------------------------------------+
| Figure 76: Representing iTunes tracks as structures (the functions) |
|                                                                     |
|; Any Any Any Any Any Any Any Any -> Track or #false                |
|; creates an instance of Track for legitimate inputs                |
|; otherwise it produces #false                                      |
| (define (create-track name artist album time                        |
|                       track# added play# played)                    |
|...)                                                              |
|                                                                     |
|; Any Any Any Any Any Any -> Date or #false                         |
|; creates an instance of Date for legitimate inputs                 |
|; otherwise it produces #false                                      |
| (define (create-date y mo day h m s)                                |
|...)                                                              |
|                                                                     |
|; String -> LTracks                                                 |
|; creates a list-of-tracks representation from the                  |
|; text in file-name (an XML export from iTunes)                     |
| (define (read-itunes-as-tracks file-name)                           |
|...)                                                              |
+---------------------------------------------------------------------+
```

図75と図76は、`2htdp/itunes` ティーチパックによって実装されているトラックの構造体に基づく表現を導入しています。`track` 構造体型には8つのフィールドがあり、それぞれがトラックの特定のプロパティを表しています。ほとんどのフィールドには `String` や `N` などのアトミックな種類のデータが含まれ、他のフィールドには6つのフィールドを持つ構造体型である `Date` が含まれています。`2htdp/itunes` ティーチパックは、`track` と `date` の構造体型のすべての述語とセレクタをエクスポートしますが、コンストラクタの代わりにチェック機能付きコンストラクタを提供します。

```
+----------------------------------------------------------------------+
| Figure 75: Representing iTunes tracks as structures (the structures) |
|                                                                      |
|; the 2htdp/itunes teachpack documentation, part 1:                  |
|                                                                      |
|; An LTracks is one of:                                              |
|; – '()                                                              |
|; – (cons Track LTracks)                                             |
|                                                                      |
| (define-struct track                                                 |
|   [name artist album time track# added play# played])                |
|; A Track is a structure:                                            |
|; (make-track String String String N N Date N Date)                  |
|; interpretation An instance records in order: the track's           |
|; title, its producing artist, to which album it belongs,            |
|; its playing time in milliseconds, its position within the          |
|; album, the date it was added, how often it has been                |
|; played, and the date when it was last played                       |
|                                                                      |
| (define-struct date [year month day hour minute second])             |
|; A Date is a structure:                                             |
|; (make-date N N N N N N)                                            |
|; interpretation An instance records six pieces of information:      |
|; the date's year, month (between 1 and 12 inclusive),               |
|; day (between 1 and 31), hour (between 0                            |
|; and 23), minute (between 0 and 59), and                            |
|; second (also between 0 and 59).                                    |
+----------------------------------------------------------------------+
```

`2htdp/itunes` ティーチパックの説明の最後の要素は、iTunesのXMLライブラリ記述を読み込んでトラックのリストである `LTracks` を返す関数です。iTunesアプリからXMLライブラリをエクスポートしたら、以下のコードスニペットを実行してすべてのレコードを取得できます。

```racket
; modify the following to use your chosen name
(define ITUNES-LOCATION "itunes.xml")

; LTracks
(define itunes-tracks
  (read-itunes-as-tracks ITUNES-LOCATION))
```

このスニペットを、iTunesのXMLエクスポートと同じフォルダに保存してください。例として `itunes-tracks` を使用しないように注意してください。例として使うには大きすぎます。実際、非常に大きいため、DrRacketでBSLプログラムを実行するたびにファイルを読み込むと多大な時間がかかる可能性があります。そのため、関数を設計している間は、この2行目をコメントアウトしておくとよいでしょう。iTunesコレクションに関する情報を計算したいときだけ、コメントアウトを解除してください。

**Exercise 199.** 重要なデータ定義はすでに提供されていますが、設計レシピの最初のステップはまだ不完全です。`Date`、`Track`、および `LTracks` の例を作成してください。これらの例は、以降の演習問題で入力として重宝します。

**Exercise 200.** `LTracks` の要素を受け取り、総再生時間を返す関数 `total-time` を設計してください。プログラムが完成したら、お使いのiTunesコレクションの総再生時間を計算してください。

**Exercise 201.** `select-all-album-titles` を設計してください。この関数は `LTracks` を受け取り、アルバムタイトルのリストを `List-of-strings` として返します。

また、関数 `create-set` を設計してください。この関数は `List-of-strings` を受け取り、与えられたリスト内の各 `String` を正確に1度だけ含むリストを構築します。ヒント: `String` である `s` が与えられたリストの先頭にあり、かつリストの残りの部分にも存在する場合、`create-set` は `s` を残しません。

最後に、`LTracks` を受け取り、重複のないアルバムタイトルのリストを返す `select-album-titles/unique` を設計してください。この関数を使用して、お使いのiTunesコレクション内のすべてのアルバムタイトルを特定し、そこに含まれる異なるアルバムの数も求めてください。

**Exercise 202.** `select-album` を設計してください。この関数はアルバムのタイトルと `LTracks` を受け取ります。後者から、指定されたアルバムに属するトラックのリストを抽出します。

**Exercise 203.** `select-album-date` を設計してください。この関数はアルバムのタイトル、日付（`Date`）、および `LTracks` を受け取ります。後者から、指定されたアルバムに属し、かつ指定された日付より後に再生されたトラックのリストを抽出します。ヒント: 2つの `Date` を受け取り、1つ目が2つ目よりも前であるかどうかを判定する関数を設計する必要があります。

**Exercise 204.** `select-albums` を設計してください。この関数は `LTracks` の要素を受け取ります。アルバムごとに1つの `LTracks` を含む、`LTracks` のリストを返します。各アルバムはそのタイトルによって一意に識別され、結果には1回だけ登場します。ヒント: (1) 前の演習問題の解法のいくつかを利用するとよいでしょう。(2) グループ化を行う関数は2つのリスト（アルバムタイトルのリストとトラックのリスト）を受け取り、後者が補助関数に渡されるまではアトミックとして扱います。演習問題196を参照してください。

用語: `select-` で始まる名前の関数は、いわゆるデータベースクエリ（database queries）です。詳細は「Project: Database」を参照してください。

```
+------------------------------------------------------------+
| Figure 77: Representing iTunes tracks as lists             |
|                                                            |
|; the 2htdp/itunes teachpack documentation, part 2:        |
|                                                            |
|; An LLists is one of:                                     |
|; – '()                                                    |
|; – (cons LAssoc LLists)                                   |
|                                                            |
|; An LAssoc is one of:                                     |
|; – '()                                                    |
|; – (cons Association LAssoc)                              |
|;                                                          |
|; An Association is a list of two items:                   |
|; (cons String (cons BSDN '()))                            |
|                                                            |
|; A BSDN is one of:                                        |
|; – Boolean                                                |
|; – Number                                                 |
|; – String                                                 |
|; – Date                                                   |
|                                                            |
|; String -> LLists                                         |
|; creates a list of lists representation for all tracks in |
|; file-name, which must be an XML export from iTunes       |
| (define (read-itunes-as-lists file-name)                   |
|...)                                                     |
+------------------------------------------------------------+
```

図77は、`2htdp/itunes` ティーチパックがリストを使ってどのようにトラックを表現しているかを示しています。`LLists` はトラック表現のリストであり、そのそれぞれが `String` と4種類の値のいずれかをペアにしたリストのリストです。`read-itunes-as-lists` 関数はiTunesのXMLライブラリを読み込み、`LLists` の要素を返します。したがって、以下の定義をプログラムに追加すれば、すべてのトラック情報にアクセスできるようになります。

```racket
; modify the following to use your chosen name
(define ITUNES-LOCATION "itunes.xml")

; LLists
(define list-tracks
  (read-itunes-as-lists ITUNES-LOCATION))
```

そして、iTunesライブラリが保存されているのと同じフォルダに保存します。

**Exercise 205.** `LAssoc` と `LLists` の例、つまりトラックのリスト表現およびそのようなトラックのリストの例を作成してください。

**Exercise 206.** 関数 `find-association` を設計してください。この関数は、`key` という名前の `String`、`LAssoc`、および `default` という名前の `Any` の要素という3つの引数を受け取ります。最初の要素が `key` と等しい最初の `Association` を返すか、そのような `Association` が存在しない場合は `default` を返します。

注: この関数を設計した後に、`assoc` について調べてみてください。

**Exercise 207.** `LLists` を受け取り、総再生時間を返す `total-time/list` を設計してください。ヒント: まず演習問題206を解いてください。

設計が完了したら、お使いのiTunesコレクションの総再生時間を計算してください。この結果を演習問題200の `total-time` 関数が計算した時間と比較してください。なぜ違いがあるのでしょうか？

**Exercise 208.** `boolean-attributes` を設計してください。この関数は `LLists` を受け取り、`Boolean` 属性に関連付けられている `String` のリストを返します。ヒント: 演習問題201の `create-set` を使用してください。

完了したら、お使いのiTunesライブラリがトラックに採用しているブール値属性の数を特定してください。それらは意味をなしていますか？

注: リストに基づく表現は、構造体に基づく表現ほど整理されていません。この文脈では「半構造化（semi-structured）」という言葉が使われることがあります。このようなリスト表現は、めったに現れず構造体型に適合しないプロパティにも対応できます。人々は未知の情報を探索するためにこのような表現をよく使用し、フォーマットが周知のものとなった後に構造体を導入します。可能である場合に `LAssoc` を `Track` に変換する関数 `track-as-struct` を設計してください。

### 12.3 単語ゲーム、合成の例証 (Word Games, Composition Illustrated)

新聞や雑誌の単語パズルを解いたことがある人もいるでしょう。次の問題を考えてみてください：

> サンプル問題: 与えられた単語と同じ文字から構成されるすべての単語を見つけよ。例えば “cat” は “act” と綴ることもできる。

例を通して考えてみましょう。あなたに “dear” という単語が与えられたとします。この4つの文字の並べ替え（配置）は、全部で24通りあります：

> ader aedr aerd adre arde areddaer eadr eard dare rade raeddear edar erad drae rdae readdera edra erda drea rdea reda

このリストの中には、正しい単語が3つあります：“read”、“dear”、“dare” です。

**注:** 単語に同じ文字が2回含まれている場合、すべての並べ替えのコレクションには同じ文字列の複製が複数含まれることがあります。私たちの目的においては、これで問題ありません。現実的なプログラムでは、リストの代わりに集合（set）を使用して重複するエントリを回避したくなるかもしれません。「リストと集合に関する注意」を参照してください。**注終わり**

あらゆる可能な並べ替えを系統的に列挙することは、英語辞書における検索と同様に、明らかにプログラムの役目です。本節では検索関数の設計を取り扱い、もう一方の課題の解決は次節に委ねます。この2つを分離することで、最初の節では系統的なプログラム設計のハイレベルな考え方に集中することができます。

> **注:** 現実世界の辞書を扱う方法については、「現実世界のデータ: 辞書」を参照してください。

手作業でこの問題をどのように解決するか、少し想像してみましょう。もし十分な時間があれば、与えられた単語に含まれるすべての文字の可能な並べ替えをすべて列挙し、その中から辞書にも存在する異形（バリアント）だけを選び出すことでしょう。明らかにプログラムもこのように進めることができ、これは合成（composition）による自然な設計を示唆しています。しかし、いつものように私たちは系統的に進め、まずは入力と出力のデータ表現を選択することから始めます。

少なくとも一見したところでは、単語を `String` として表現し、結果を単語のリスト、すなわち `List-of-strings` として表現するのが自然です。この選択に基づいて、シグネチャと目的の記述を策定できます：

```racket
; String -> List-of-strings
; finds all words that use the same letters as s
(define (alternative-words s)
...)
```

次に、いくつか例が必要です。与えられた単語が “cat” の場合、*c*, *a*, *t* という3つの文字を扱います。いろいろ試してみると、これらの文字の並べ替えは *cat*, *cta*, *tca*, *tac*, *act*, *atc* の6通りあることがわかります。このうち2つが実際の単語です：“cat” と “act” です。`alternative-words` は `String` のリストを生成するため、結果を表現する方法は `(list "act" "cat")` と `(list "cat" "act")` の2通りあります。幸いにも、BSLには関数が2つの可能な結果のうちいずれか1つを返すことを表現する手段が備わっています：

```racket
(check-member-of (alternative-words "cat")
                 (list "act" "cat")
                 (list "cat" "act"))
```

手を止めましょう（Stop!）！ドキュメントで `check-member-of` について調べて読んでください。

この例を詳しく調べることで、2つの問題が浮き彫りになります：

- 1つ目はテストに関する問題です。例えば「rat」という単語を使ったとします。これには「rat」、「tar」、「art」の3つの選択肢があります。この場合、関数の結果となり得る6つのリストを策定しなければなりません。4つの可能な選択肢がある「dear」のような単語では、テストの策定はさらに困難になります。
- 2つ目の問題は単語の表現の選択に関するものです。最初は `String` が自然に思えますが、例から明らかなように、私たちの関数の一部は単語を文字の列として捉え、自由自在に並べ替えることができる必要があります。`String` 内の文字を並べ替えることも可能ですが、文字のリスト（lists of letters）の方が明らかにこの目的に適しています。

まずはテストの問題から、順を追って対処していきましょう。

`alternative-words` と `"rat"` に対するテストを策定したいとします。上記より、結果には `"rat"`、`"tar"`、`"art"` が含まれていなければならないことはわかっていますが、これらの単語がどのような順序で結果に現れるかはわかりません。

このような状況では、`check-satisfied` が役に立ちます。これを用いて、`String` のリストに私たちの3つの `String` が含まれているかどうかをチェックする関数と一緒に使用できます：

> **注:** 「合間 1: 初級学生言語」を参照してください。

```racket
; List-of-strings -> Boolean
(define (all-words-from-rat? w)
  (and (member? "rat" w)
       (member? "art" w)
       (member? "tar" w)))
```

この関数を使えば、`alternative-words` のテストを策定するのは簡単です：

```racket
(check-satisfied (alternative-words "rat")
                 all-words-from-rat?)
```

**データと設計に関する注意:** この議論が示唆しているのは、`alternative-words` 関数が構築するのはリストではなく集合（set）であるということです。差異の詳細な説明については、「リストと集合に関する注意」を参照してください。ここでは、集合とは値の順序やそれらの値が何回出現するかを考慮しない値のコレクションを表すということを知っておけば十分です。プログラミング言語が集合のデータ表現をサポートしていない場合、プログラマーはここでの `List-of-strings` 表現のように、近い代替手段に頼る傾向があります。プログラムが大きくなるにつれて、この選択はプログラマーを悩ませるかもしれませんが、この種の問題への対処は2冊目の本のテーマです。**注終わり**

```
+-------------------------------------------------------------+
| Figure 78: Finding alternative words                        |
|                                                             |
|; List-of-strings -> Boolean                                |
| (define (all-words-from-rat? w)                             |
|   (and                                                      |
|     (member? "rat" w) (member? "art" w) (member? "tar" w))) |
|                                                             |
|; String -> List-of-strings                                 |
|; finds all words that the letters of some given word spell |
|                                                             |
| (check-member-of (alternative-words "cat")                  |
|                  (list "act" "cat")                         |
|                  (list "cat" "act"))                        |
|                                                             |
| (check-satisfied (alternative-words "rat")                  |
|                  all-words-from-rat?)                       |
|                                                             |
| (define (alternative-words s)                               |
|   (in-dictionary                                            |
|     (words->strings (arrangements (string->word s)))))      |
|                                                             |
|; List-of-words -> List-of-strings                          |
|; turns all Words in low into Strings                       |
| (define (words->strings low) '())                           |
|                                                             |
|; List-of-strings -> List-of-strings                        |
|; picks out all those Strings that occur in the dictionary  |
| (define (in-dictionary los) '())                            |
+-------------------------------------------------------------+
```

単語の表現に関する問題については、次の節に先送りします。具体的には、次の節で (1) 文字の並べ替えに適した `Word` のデータ表現、(2) `List-of-words` のデータ定義、(3) `Word` を `List-of-words`（すなわちすべての可能な並べ替えのリスト）にマッピングする関数を導入することにします：

```racket
; A Word is...

; A List-of-words is...

; Word -> List-of-words
; finds all rearrangements of word
(define (arrangements word)
  (list word))
```

**Exercise 209.** 上記により、2つの追加の願いが残されます：`String` を受け取り、対応する `Word` を生成する関数、および逆方向の関数です。ウィッシュリストのエントリは以下のとおりです：

```racket
; String -> Word
; converts s to the chosen word representation
(define (string->word s)...)

; Word -> String
; converts w to a string
(define (word->string w)...)
```

次節で `Word` のデータ定義を参照し、`string->word` と `word->string` の定義を完成させなさい。ヒント: BSLが提供する関数の一覧を参照するとよいでしょう。

これら2つの小さな問題が解決したところで、`alternative-words` の設計に戻りましょう。これで手元には、(1) シグネチャ、(2) 目的の記述、(3) 例とテスト、(4) データ表現の選択に関する洞察、(5) 問題を2つの大きなステップに分解する方法の考えが揃いました。

したがって、テンプレートを作成する代わりに、私たちが考えている合成を書き出します：

```racket
(in-dictionary (arrangements s))
```

この式は、与えられた単語 `s` に対して、`arrangements` を使用して文字のすべての可能な並べ替えのリストを作成し、`in-dictionary` を使用して辞書にも存在する並べ替えを選択することを意味しています。

手を止めましょう（Stop!）！合成がうまくいくことを確認するために、2つの関数のシグネチャを調べてみてください。正確には何をチェックする必要がありますか？

この式が捉えきれていないのは4番目の点、すなわち文字を並べ替えるためにプレーンな文字列を使わないという決定です。`s` を `arrangements` に渡す前に、それを単語に変換する必要があります。幸いなことに、演習問題 209 はまさにそのような関数を求めています：

```racket
(in-dictionary
  (... (arrangements (string->word s))))
```

同様に、得られた単語のリストを文字列のリストに変換する必要があります。演習問題 209 は単一の単語を変換する関数を求めていましたが、ここではそれらのリストを扱う関数が必要です。別の願いをする時間です：

```racket
(in-dictionary
  (words->strings
    (arrangements (string->word s))))
```

手を止めましょう（Stop!）！`words->strings` のシグネチャは何であり、その目的は何ですか？

図 78 にすべてのピースがまとめられています。以下の演習問題では、残りの関数を設計することが求められます。

**Exercise 210.** 図 78 で指定されている `words->strings` 関数の設計を完成させなさい。ヒント: 演習問題 209 の解法を使用しなさい。

**Exercise 211.** 図 78 で指定されている `in-dictionary` の設計を完成させなさい。ヒント: 辞書の読み込み方については「現実世界のデータ: 辞書」を参照しなさい。
実世界のデータ：辞書（Real-World Data: Dictionaries）」を参照しなさい。

### 12.4 単語ゲーム、問題の中核 (Word Games, the Heart of the Problem)

目標は、`Word` を受け取り、その単語の文字ごとの並べ替えのリストを生成する関数 `arrangements` を設計することです。この発展的な演習は、深いウィッシュリスト（wish list）、すなわち1つの関数を完成させるたびに増えていくように思える希望する関数のリストの必要性を強化します。

> **注記:** 数学的な用語では順列（permutations）です。

前述のように、`String`（文字列）は単語の表現として機能させることもできますが、`String` はアトミック（不可分）であり、`arrangements` がその文字を並べ替える必要があるという事実そのものが、異なる表現を求めています。したがって、私たちが選択した単語のデータ表現は、入力の各要素が文字を表す `1String` のリストです。

```racket
; A Word is one of:
; – '() or
; – (cons 1String Word)
; interpretation a Word is a list of 1Strings (letters)
```

**Exercise 212.** `List-of-words` のデータ定義を書き下してください。`Word` と `List-of-words` の例を作成してください。最後に、上記の機能例を `check-expect` で定式化してください。完全な例の代わりに、たとえば `"d"` と `"e"` のようなわずか2文字の単語で作業することを検討してください。

`arrangements` のテンプレートは、リスト処理関数のテンプレートです。

```racket
; Word -> List-of-words
; creates all rearrangements of the letters in w
(define (arrangements w)
  (cond
    [(empty? w)...]
    [else (... (first w)...
... (arrangements (rest w))...)]))
```

第5ステップの準備として、テンプレートの `cond` 行を見てみましょう。

1. 入力が `'()` の場合、入力の可能な並べ替えは1つしかありません。それは `'()` という単語です。したがって結果は `(list '())` となります。これは空リストを唯一の要素として含むリストです。
2. それ以外の場合、単語には最初の文字が存在し、`(first w)` がその文字となります。また、再帰呼び出しによって、単語の残りの部分に対するすべての可能な並べ替えのリストが生成されます。たとえば、リストが `(list "d" "e" "r")` の場合、再帰呼び出しは `(arrangements (list "e" "r"))` です。これは `(cons (list "e" "r") (cons (list "r" "e") '()))` という結果を生成します。リスト全体に対して可能なすべての並べ替えを得るには、これらのすべての単語の先頭、末尾、および可能なすべての文字の間に、最初の要素（この例では `"d"`）を挿入しなければなりません。

私たちの分析によると、多くの異なる単語のすべての位置に1つの文字をなんとか挿入できれば、`arrangements` を完成させることができると示唆されます。このタスク記述の最後の側面は、暗黙のうちにリストについて言及しており、本章のアドバイスに従って補助関数を求めています。この関数を `insert-everywhere/in-all-words` と呼び、それを使って `arrangements` の定義を完成させましょう。

```racket
(define (arrangements w)
  (cond
    [(empty? w) (list '())]
    [else (insert-everywhere/in-all-words (first w)
            (arrangements (rest w)))]))
```

**Exercise 213.** `insert-everywhere/in-all-words` を設計してください。この関数は `1String` と単語のリストを消費します。結果は第2引数のような単語のリストになりますが、与えられたリストのすべての単語の先頭、すべての文字の間、および末尾に第1引数が挿入されます。

完全なウィッシュリストのエントリから始めてください。空リストのテスト、1文字の単語を持つリストのテスト、2文字の単語を持つリストのテストなどを補足してください。次に進む前に、以下の3つのヒントを注意深く学習してください。

ヒント (1) 上記の例を再考してください。それは、`"d"` を単語 `(list "e" "r")` および `(list "r" "e")` に挿入する必要があることを示しています。したがって、次の適用例は自然な例の候補の1つです。

```racket
(insert-everywhere/in-all-words "d"
  (cons (list "e" "r")
    (cons (list "r" "e")
      '())))
```

(2) 2つのリストを消費してその2つのリストの連結を生成するBSL+の操作 `append` を使用したくなるでしょう。

```racket
> (append (list "a" "b" "c") (list "d" "e"))
(list "a" "b" "c" "d" "e")
```

`append` のような関数の開発は「Simultaneous Processing」（同時処理）のテーマです。

(3) この演習問題の解法は一連の関数です。忍耐強くデザインレシピに従い、体系的にウィッシュリストに取り組んでください。

**Exercise 214.** `arrangements` を「Word Games, Composition Illustrated」（単語ゲーム、プログラム合成の例示）の不完全なプログラムと統合してください。全テストスイートがパスすることを確認した後、お気に入りのいくつかの例で実行してください。

### 12.5 ワームに餌を与える (Feeding Worms)

Worm（*Snake* としても知られています）は、最も古いコンピューターゲームの1つです。ゲームが始まると、1匹のワームと1つの餌が現れます。ワームは壁に向かって移動しています。壁に到達させてはいけません。さもないとゲームオーバーになります。代わりに、矢印キーを使ってワームの動きをコントロールします。

ゲームの目標は、ワームにできるだけ多くの餌を食べさせることです。ワームが餌を食べるにつれて、体長が長くなり、より多くのセグメントが現れます。1つの餌が消化されると、別の餌が現れます。しかし、ワームの成長はワーム自身を危険にさらします。十分に長く成長すると、自分自身に衝突する可能性があり、もし衝突すれば、その場合もゲームオーバーとなります。

```
+-------------------------------------------------------------------+
| Figure 79: Playing Worm                                           |
|                                                                   |
| [image: pict_105.png]                                             |
| [image: pict_106.png]                                             |
| [image: pict_107.png]                                             |
| [image: pict_105.png] [image: pict_106.png] [image: pict_107.png] |
+-------------------------------------------------------------------+
```

Figure 79 は、ゲームが実際にどのように動作するかを示す一連のスクリーンショットを表示しています。左側には初期設定が表示されています。ワームは1つの赤いセグメント、つまりその頭部のみで構成されています。ワームは緑色のディスクとして表示されている餌に向かって移動しています。中央のスクリーンショットは、ワームがまさに餌を食べようとしている状況を示しています。一番右のスクリーンショットでは、ワームが右の壁に衝突しています。ゲームは終了し、プレイヤーは11点を獲得しました。

以下の演習問題では、Worm ゲームの設計と実装をガイドします。 Structures in Lists と同様に、これらの演習問題は反復的洗練（iterative refinement）を通じて難度の高い問題に取り組む方法を示しています。つまり、対話型プログラム全体を一度に設計するのではなく、イテレーション（iteration）と呼ばれるいくつかの段階に分けて設計します。各イテレーションでは、詳細を追加してプログラムを洗練させ、あなたや顧客が満足するまで繰り返します。演習問題の結果に満足がいかない場合は、自由にバリエーションを作成してください。

**Exercise 215.** 1セグメントのワームを継続的に移動させ、プレイヤーが4方向の矢印キーでワームの移動をコントロールできるようにするワールドプログラムを設計しなさい。プログラムは、ワームの唯一のセグメントを描画するために赤いディスクを使用する必要があります。クロックのティックごとに、ワームはその直径分だけ移動する必要があります。

ヒント (1) ワールドプログラムの設計方法を思い出すために Designing World Programs を再読してください。 `worm-main` 関数を定義するときは、クロックが刻むレートを引数として使用します。レートの記述方法については `on-tick` のドキュメントを参照してください。(2) ワームのデータ表現を開発するときは、物理的表現と論理的表現という2種類の異なる表現の使用を検討してください。物理的表現はキャンバス上のワームの実際の物理的位置を追跡し、論理的表現はワームが左および上から何セグメント（の幅）離れているかをカウントします。「ゲーム」の物理的外観（ワームのセグメントのサイズ、ゲームボックスのサイズ）を変更しやすいのは、この2つのうちどちらでしょうか？

**Exercise 216.** ワームがワールドの壁に到達した場合に停止するように、exercise 215 のプログラムを修正しなさい。この条件によってプログラムが停止したとき、ワールドシーンの左下に `"worm hit border"` というテキストを含む最終シーンを描画する必要があります。ヒント 最後のワールドを特別な方法で描画するために、`big-bang` の `stop-when` 句を使用できます。

**Exercise 217.** 尻尾を持つワームのデータ表現を開発しなさい。ワームの尻尾は、「接続された」セグメントの（空である可能性もある）シーケンスです。ここでの「接続された」とは、あるセグメントの座標がその前のセグメントの座標と高々1つの方向にしか違わないことを意味します。話を単純に保つため、すべてのセグメント（頭部セグメントと尻尾セグメント）を同一に扱ってください。

ここで、複数セグメントのワームに対応するように exercise 215 のプログラムを修正しなさい。シンプルに保ちましょう：(1) プログラムはすべてのワームセグメントを赤いディスクとして描画してよく、(2) ワームが壁や自分自身に衝突する可能性は無視してください。ヒント ワームの移動を実現する1つの方法は、移動している方向にセグメントを1つ追加し、最後のセグメントを削除することです。

```
+-----------------------------------------------------------+
| Figure 80: Random placement of food                       |
|                                                           |
|; Posn -> Posn                                            |
|;???                                                     |
| (check-satisfied (food-create (make-posn 1 1)) not=-1-1?) |
| (define (food-create p)                                   |
|   (food-check-create                                      |
|      p (make-posn (random MAX) (random MAX))))            |
|                                                           |
|; Posn Posn -> Posn                                       |
|; generative recursion                                    |
|;???                                                     |
| (define (food-check-create p candidate)                   |
|   (if (equal? p candidate) (food-create p) candidate))    |
|                                                           |
|; Posn -> Boolean                                         |
|; use for testing only                                    |
| (define (not=-1-1? p)                                     |
|   (not (and (= (posn-x p) 1) (= (posn-y p) 1))))          |
+-----------------------------------------------------------+
```

**Exercise 218.** ワームがワールドの壁に衝突した場合、または自分自身に衝突した場合に停止するように、exercise 217 のプログラムを再設計しなさい。ワームが壁に衝突したためか、それとも自分自身に衝突したためかにプログラムが停止した理由を説明するために、exercise 216 と同様のメッセージを表示してください。

ヒント (1) ワームが自分自身に衝突するかどうかを判定するには、移動した場合に頭部の位置が古い尻尾のセグメントのいずれかと一致するかどうかを確認します。(2) `member?` 関数について調べてみてください。

**Exercise 219.** exercise 218 のプログラムに餌を装備させなさい。いかなる時点でも、ボックスには1つの餌が含まれている必要があります。シンプルにするため、1つの餌はワームのセグメントと同じサイズとします。ワームの頭部が餌と同じ位置にあるとき、ワームは餌を食べます。これは、ワームの尻尾が1セグメント拡張されることを意味します。餌が食べられると、別の餌が異なる場所に現れます。

ゲームに餌を追加するには、ワールド状態のデータ表現を変更する必要があります。状態にはワームに加えて、餌の表現、特にその現在の位置が含まれるようになります。ゲーム表現の変更は、イベントを処理するための新しい関数を示唆しますが、これらの関数は（exercise 218 からの）ワーム用の関数とそのテストケースを再利用できます。また、ティックハンドラはワームを移動させるだけでなく、食べるプロセスと新しい餌の作成を管理しなければならないことも意味します。

プログラムはボックス内にランダムに餌を配置する必要があります。これを適切に行うには、まだ見たことのない設計技法である、いわゆる生成再帰（generative recursion。Generative Recursion で紹介されています）が必要となるため、figure 80 にこれらの関数を提供しています。ただし、これらを使用する前に、`MAX` が 1 より大きいと仮定してこれらの関数がどのように動作するかを説明し、その後に目的文（purpose statement）を公式化しなさい。

> **Note:** `random` の動作については、マニュアルまたは exercise 99 をお読みください。

ヒント (1) 「食べる」ことの1つの解釈方法は、頭部が以前餌があった場所に移動し、頭部が以前あった場所に挿入された1つのセグメントによって尻尾が伸びる、と言うことです。なぜこの解釈は関数として設計しやすいのでしょうか？ (2) この最後のステップにおいて、`worm-main` 関数に2番目のパラメータとして、`big-bang` がワールドの現在の状態を別ウィンドウで表示するかどうかを決定する Boolean 値を追加することが役立つとわかりました。この情報を要求する方法については、`state` のドキュメントを参照してください。

この最後の演習問題を終えると、完全なワームゲームが完成します。ここで `worm-main` 関数を修正して、最終的なワームの長さを返すようにしてください。次に、DrRacket の（`Racket` メニューにある）`Create Executable` を使用して、プログラムを BSL+ について知っている人だけでなく、誰でも起動できるものに変換してください。

ゲームを真に自分自身のゲームにするために、特別な工夫を追加したくなるかもしれません。私たちは、面白いゲーム終了メッセージ、周りに複数の異なる餌を用意すること、部屋に追加の障害物を配置すること、その他いくつかのアイデアを試してみました。あなたならどのようなアイデアを思いつきますか？
の他いくつかのアイデアを試してみました。あなたはどんなことを思いつきますか？

### 12.6 簡易テトリス (Simple Tetris)

テトリス（Tetris）は、ソフトウェア黎明期のもう1つのゲームです。本格的なテトリスゲームの設計はわずかな見返りに対して膨大な労力を要するため、本節では簡略化されたバージョンに焦点を当てます。意欲的であれば、実際のテトリスの仕組みを調べて本格的なバージョンを設計してみてください。

```
+-------------------------------------------------------------------+
| Figure 81: Simple Tetris                                          |
|                                                                   |
| [image: pict_108.png]                                             |
| [image: pict_109.png]                                             |
| [image: pict_110.png]                                             |
| [image: pict_108.png] [image: pict_109.png] [image: pict_110.png] |
+-------------------------------------------------------------------+
```

私たちの簡略化されたバージョンでは、ゲームはシーンの上部から個々のブロックが落下することから始まります。ブロックの1つが地面に着地すると静止し、ランダムな場所から別のブロックが落下し始めます。プレイヤーは「左」および「右」の矢印キーで落下中のブロックを操作できます。ブロックがキャンバスの床、あるいはすでに静止しているブロックの上に到達すると、静止して動かせなくなります。短時間でブロックは積み上がっていきます。もしブロックの積み上がりがキャンバスの天井に達すると、ゲームオーバーです。当然ながら、このゲームの目的はできるだけ多くのブロックを着地させることです。アイデアのイラストについては figure 81 を参照してください。

この説明を踏まえて、「Designing World Programs（ワールドプログラムの設計）」にある対話型プログラムの設計ガイドラインに取り組むことができます。ガイドラインでは、定数プロパティと変数プロパティを分離することが求められます。前とができ、後者は簡易テトリスゲームの考え得るすべての状態を構成するデータを表します。以下にいくつかの例を挙げます：

- ゲームの幅と高さは、ブロックと同様に固定されています。BSL+ の観点からは、次のような定義が望ましいでしょう：
(define WIDTH 10);# of blocks, horizontally(define SIZE 10);blocks are squares(define SCENE-SIZE (* WIDTH SIZE)) (define BLOCK;red squares with black rims (overlay (square (- SIZE 1) "solid" "red") (square SIZE "outline" "black")))読み進める前に、これらの定義を説明してください。
- ブロックの「景観（landscapes）」は、ゲームごと、またクロックティックごとに異なります。これをもっと正確にしましょう。ブロックの見た目は同じままであり、その位置が異なります。

これで、落下中のブロックと地面のブロックの景観のためのデータ表現を設計するという中心的な問題が残ります。落下中のブロックに関しては、ここでも2つの可能性が考えられます。1つは「物理的」表現を選択することであり、もう1つは「論理的」表現です。物理的表現はキャンバス上のブロックの実際の物理的位置を追跡し、*論理的*表現はブロックが左および上からブロック幅何個分離れているかを数えます。静止しているブロックに関しては、個々のブロックよりもさらに多くの選択肢があります。物理的位置のリスト、論理的位置のリスト、積み上がった高さのリストなどです。

> **Note:** 関連する設計上の決定については exercise 215 を参照してください。

この節では、データ表現をあらかじめ選択しておきます：

```racket
(define-struct tetris [block landscape])
(define-struct block [x y])

; A Tetris is a structure:
;   (make-tetris Block Landscape)
; A Landscape is one of:
; – '()
; – (cons Block Landscape)
; A Block is a structure:
;   (make-block N N)

; interpretations
; (make-block x y) depicts a block whose left
; corner is (* x SIZE) pixels from the left and
; (* y SIZE) pixels from the top;
; (make-tetris b0 (list b1 b2...)) means b0 is the
; dropping block, while b1, b2, and... are resting
```

これが論理的表現と呼ばれるものです。なぜなら、座標はブロックの物理的な位置を反映しておらず、原点からのブロックサイズの個数を表しているだけだからです。この選択は、x が常に 0 と WIDTH の間（WIDTH は含まない）であり、y が 0 と HEIGHT の間（HEIGHT は含まない）であることを意味しますが、私たちはこの知識を無視します。

**Exercise 220.** テトリスゲームの状態に対する定義のように、複雑なデータ定義が提示された場合は、まず多様なデータコレクションのインスタンスを作成することから始めます。以下は、後で関数の例として使用できる、示唆に富んだ例の名称です：

```racket
(define landscape0...)
(define block-dropping...)
(define tetris0...)
(define tetris0-drop...)
...
(define block-landed (make-block 0 (- HEIGHT 1)))
...
(define block-on-block (make-block 0 (- HEIGHT 2)))
```

与えられた `Tetris` のインスタンスを `Image` に変換するプログラム `tetris-render` を設計しなさい。DrRacket の対話領域（interactions area）を使用して、（きわめて）シンプルなデータ例のいくつかを描画する式を作成しなさい。次に、それらの関数例をユニットテストおよび関数自体として定式化しなさい。

**Exercise 221.** キャンバスの上部から直線状に落下し、床または既に静止しているブロックの上に着地するブロックを表示する対話型プログラム `tetris-main` を設計しなさい。`tetris-main` への入力により、クロックが刻むレートを決定するようにしなさい。レートの指定方法については `on-tick` のドキュメントを参照しなさい。

ブロックが着地したかどうかを発見するために、ブロックを落下させて床の上にあるか、または静止しているブロックのリストにあるいずれかのブロックと重なっているかをチェックすることを推奨します。ヒント：組込み関数 `member?` についておさらいしてください。

ブロックが着地したとき、プログラムは直ちに現在の列の右隣の列から降下する別のブロックを生成する必要があります。現在のブロックが既に最も右の列にある場合、次のブロックは最も左の列を使用する必要があります。あるいは、現在の列とは異なる列をランダムに選択する関数 `block-generate` を定義しなさい。インスピレーションを得るには exercise 219 を参照してください。

**Exercise 222.** プレイヤーが落下中のブロックの水平方向の動きを操作できるように、exercise 221 のプログラムを修正しなさい。プレイヤーが「left」矢印キーを押すたびに、列 0 にあるか、またはその左側に既に静止ブロックの山がある場合を除き、落下中のブロックは左に 1 列シフトする必要があります。同様に、プレイヤーが「right」を押すたびに、可能であれば落下中のブロックは右に 1 列移動する必要があります。

**Exercise 223.** exercise 222 のプログラムに `stop-when` 句を備え付けなさい。いずれかの列にキャンバスの天井に「触れる」のに十分なブロックが含まれると、ゲームは終了します。

exercise 223 を解くと、最小限のテトリスゲームが完成します。友達に見せる前に、少し磨きをかけたくなるかもしれません。例えば、最終キャンバスにプレイヤーがいくつブロックを積み上げることができたかを示すテキストを表示することができます。あるいは、すべてのキャンバスにそのようなテキストを含めることもできます。選択はあなた次第です。

### 12.7 本格的なスペースウォーズ (Full Space War)

「要素の列挙と構造体（Itemizations and Structures）」では、アクションがほとんどないスペースインベーダーゲームをほのめかしていました。プレイヤーは地上部隊を前後に動かすことしかできませんでした。「リストとワールド（Lists and World）」では、プレイヤーが希望するだけ多くの弾を発射できるようになります。この節では、このゲームを完成させるのに役立つ練習問題を出題します。

いつものように、UFO が地球に着陸しようとしています。プレイヤーの任務は UFO の着陸を防ぐことです。この目的のために、ゲームには任意の数の弾を発射できる戦車が用意されています。これらの弾の1つが UFO の重心に十分に近づくと、ゲームは終了し、プレイヤーの勝ちとなります。UFO が地面に十分に近づくと、プレイヤーの負けとなります。

**Exercise 224.** 前の2つの節から学んだ教訓を活用し、ゲームの機能を1つずつ追加しながら、ゲームの拡張をゆっくりと設計しなさい。常に設計レシピを使用し、補助関数のガイドラインを頼りにしなさい。このゲームが気に入ったら、他の機能を追加してみてください。ランニングテキストを表示する、戦車を破壊できる電荷を UFO に装備する、攻撃する UFO の艦隊全体を作成する、そして何よりも想像力を働かせてください。

UFO と戦車が撃ち合うのが好きではない場合は、同じアイディアを使って、似たような文明的なゲームを作成してください。

**Exercise 225.** 消火ゲームを設計しなさい。

このゲームは、広大な森林で火災が猛威を振るう西部諸州を舞台にしています。これは空中消火活動をシミュレートしたものです。具体的には、プレイヤーは地面の火災に水を投下する飛行機のパイロットとして行動します。プレイヤーは飛行機の水平方向の移動と水の投下を制御します。

作成するゲームソフトウェアは、地面のランダムな場所に火災を発生させます。火災の数を制限し、現在燃えている火災の数やその他の要因の関数にしてもよいでしょう。ゲームの目的は、制限時間内にすべての火災を消火することです。ヒント：このゲームを作成するには、この章で説明されている反復的な設計アプローチを使用してください。

### 12.8 有限状態機械 (Finite State Machines)

有限状態機械（FSM: Finite state machine）と正規表現は、プログラミングにおいてどこにでも存在する要素です。「有限状態の世界（Finite State Worlds）」で説明したように、状態機械はワールドプログラムについて考える1つの方法です。逆に、演習問題 109 は、FSM を実装し、プレイヤーが特定の一連のキープレスを行ったかどうかをチェックするワールドプログラムの設計方法を示しています。

思い出されるかもしれませんが、有限状態機械は正規表現と等価です。したがって、コンピュータ科学者は、演習問題 109 の

> `a (b|c)* d`

のような、特定の正規表現に一致するキー入力の列を FSM が受容する（accepts）と言いがちです。もし別のパターン、例えば

> `a (b|c)* a`

を認識するプログラムが欲しい場合、既存のプログラムを適切に修正するだけで済みます。これら2つのプログラムは互いに似たものになり、いくつかの異なる正規表現に対してこの演習を繰り返すと、最終的には似たような見た目のプログラムが大量に出来上がることになります。

自然な考え方としては、一般解、つまり FSM のデータ表現を受け取り、プレイヤーがマッチするキーのシーケンスを押したかどうかを認識するワールドプログラムを探すことです。本節では、まさにそのようなワールドプログラムの設計を提示しますが、これは大幅に単純化されたものです。具体的には、この FSM には初期状態や終了状態がなく、マッチングでは実際のキー入力を無視します。代わりに、いずれかのキーが押されるたびに、ある状態から別の状態への遷移が起こります。さらに、状態を色の文字列とすることを要求します。そうすれば、FSM を解釈するプログラムは、単に現在の状態を色として表示できます。

設計上の選択に関する注記 ここに一般化のもう1つの試みがあります。

> サンプル問題 与えられた FSM を特定の KeyEvents のリスト上で解釈するプログラムを設計しなさい。つまり、このプログラムは FSM のデータ表現と文字列を受け取ります。文字列が FSM に対応する正規表現に一致する場合、その結果は #true になり、それ以外の場合は #false になります。

しかしながら、判明することですが、最初の2つのパートの原則だけでこのプログラムを設計することはできません。実際、この問題を解決するには「バックトラックするアルゴリズム（Algorithms that Backtrack）」まで待つ必要があります。演習問題 476 を参照してください。```
+---------------------------------------------------------------------------+
| Figure 82: Representing and interpreting finite state machines in general |
|                                                                           |
|; An FSM is one of:                                                       |
|; – '()                                                                   |
|; – (cons Transition FSM)                                                 |
|                                                                           |
| (define-struct transition [current next])                                 |
|; A Transition is a structure:                                            |
|; (make-transition FSM-State FSM-State)                                   |
|                                                                           |
|; FSM-State is a Color.                                                   |
|                                                                           |
|; interpretation An FSM represents the transitions that a                 |
|; finite state machine can take from one state to another                 |
|; in reaction to keystrokes                                               |
+---------------------------------------------------------------------------+
```

単純化された問題の記述は、FSM の表現のためのデータ定義の必要性、その状態の性質、および画像としての見た目を含む、いくつかの点を要求しています。図 82（Figure 82）はこの情報をまとめたものです。まず FSM のデータ定義から始まります。お分かりのように、FSM は単なる Transition のリストです。ワールドプログラムが任意の FSM に対して動作するようにしたいので、リストを使用する必要があります。それは、有限であるものの、任意に大きい数の状態を意味します。各 Transition は、構造体の中で2つの状態を組み合わせます。現在の状態（current state）と次の状態（next state）、つまりプレイヤーがキーを押したときにマシンが遷移する状態です。データ定義の最後の部分では、状態は単なる色の名前であると述べています。

```
+---------------------------------------------------------------------------+
| Figure 82: Representing and interpreting finite state machines in general |
|                                                                           |
|; An FSM is one of:                                                       |
|; – '()                                                                   |
|; – (cons Transition FSM)                                                 |
|                                                                           |
| (define-struct transition [current next])                                 |
|; A Transition is a structure:                                            |
|; (make-transition FSM-State FSM-State)                                   |
|                                                                           |
|; FSM-State is a Color.                                                   |
|                                                                           |
|; interpretation An FSM represents the transitions that a                 |
|; finite state machine can take from one state to another                 |
|; in reaction to keystrokes                                               |
+---------------------------------------------------------------------------+
```

**Exercise 226.** 状態のための同等性述語 `state=?` を設計しなさい。

この定義は複雑であるため、設計レシピに従って例を作成します。

```racket
(define fsm-traffic
  (list (make-transition "red" "green")
        (make-transition "green" "yellow")
        (make-transition "yellow" "red")))
```

```racket
(define fsm-traffic
  (list (make-transition "red" "green")
        (make-transition "green" "yellow")
        (make-transition "yellow" "red")))
```

この遷移表が信号機を記述していることに、おそらくお気づきでしょう。その最初の遷移は、信号機が "red" から "green" へ跳ぶことを示し、2番目の遷移は "green" から "yellow" への遷移を表し、最後の遷移は "yellow" から "red" への遷移です。

**Exercise 227.** BW Machine は、すべてのキーイベントに対して黒から白へ、そして白から黒へと反転する FSM です。BW Machine のデータ表現を定式化しなさい。

明らかに、私たちの問題に対する解決策はワールドプログラムです。

```racket
; FSM ->???
; match the keys pressed with the given FSM
(define (simulate an-fsm)
  (big-bang...
    [to-draw...]
    [on-key...]))
```

このプログラムは FSM を受け取ることになっていますが、プログラムが何を生成すべきかについては手がかりがありません。プレイヤーのキープレスに応じて与えられた FSM のように振る舞うため、このプログラムを simulate と呼びます。

とにかくワールドプログラムの設計レシピに従って、どこまで進められるかを見てみましょう。設計レシピは、「現実世界」において変化するものと変化しないものを区別するよう教えてくれます。simulate 関数は FSM のインスタンスを受け取りますが、この FSM が変化しないこともわかっています。変化するのはマシンの現在の状態です。

この分析は以下のデータ定義を示唆しています。

```racket
; A SimulationState.v1 is an FSM-State.
```

ワールドプログラムの設計レシピによれば、このデータ定義によりメイン関数が完成します。

> **注記:** empty-image 定数は「不可視の」画像を表します。これは、描画関数のヘッダーを書き出すための優れたデフォルト値です。

```racket
(define (simulate.v1 fsm0)
  (big-bang initial-state
    [to-draw render-state.v1]
    [on-key find-next-state.v1]))
```

そして、2つのエントリを持つウィッシュリストを意味します。

```racket
; SimulationState.v1 -> Image
; renders a world state as an image
(define (render-state.v1 s)
  empty-image)

; SimulationState.v1 KeyEvent -> SimulationState.v1
; finds the next state from ke and cs
(define (find-next-state.v1 cs ke)
   cs)
```

このスケッチは2つの疑問を提起します。第一に、まさに最初の SimulationState.v1 がどのように決定されるかという問題です。現在、選択されている状態 initial-state は、この問題について警告するために灰色でマーキングされています。第二に、ウィッシュリストの2番目のエントリは、いくらかの狼狽を引き起こすに違いありません。

> 与えられているのが現在の状態とキー入力だけである場合、find-next-state は一体どのようにして次の状態を見つけることができるのでしょうか？

単純化された問題の記述によれば、キー入力の正確な性質が無関係であるため、この疑問は特に当てはまります。どのキーが押されたかに関係なく、FSM は次の状態へと遷移するからです。

この2番目の問題が露呈しているのは、BSL+ の根本的な限界です。この限界を理解するために、まずは回避策（work-around）から始めます。基本的に、この分析は find-next-state 関数が現在の状態だけでなく FSM も受け取り、遷移のリストを検索して次の状態を選択できるようにすることを要求します。言い換えると、ワールドの状態には FSM の現在の状態と FSM 自体の両方が含まれていなければなりません。

> **注記:** 最初の2人のコンピュータ科学者であるアロンゾ・チャーチとアラン・チューリングは、1930年代にすべてのプログラミング言語が数値上の特定関数を計算できることを証明しました。したがって、彼らはすべてのプログラミング言語が同等であると主張しました。本書の第一著者はこれに同意しません。彼は、プログラマーが解法を表現することを言語がどのように許可しているかに基づいて、言語を区別します。

```racket
(define-struct fs [fsm current])
; A SimulationState.v2 is a structure:
;   (make-fs FSM FSM-State)
```

ワールド設計レシピによれば、この変更はキーイベントハンドラもこの組み合わせを返さなければならないことを意味します。

```racket
; SimulationState.v2 -> Image
; renders a world state as an image
(define (render-state.v2 s)
  empty-image)

; SimulationState.v2 KeyEvent -> SimulationState.v2
; finds the next state from ke and cs
(define (find-next-state.v2 cs ke)
   cs)
```

最後に、メイン関数は2つの引数、すなわち FSM とその最初の状態を受け取らなければならなくなりました。結局のところ、simulate が受け取るさまざまな FSM にはあらゆる種類の状態が備わっており、それらすべてが同じ初期状態を持っていると仮定することはできません。改訂された関数ヘッダーは以下の通りです。

```racket
; FSM FSM-State -> SimulationState.v2
; match the keys pressed with the given FSM
(define (simulate.v2 an-fsm s0)
  (big-bang (make-fs an-fsm s0)
    [to-draw state-as-colored-square]
    [on-key find-next-state]))
```

信号機の FSM の例に戻りましょう。このマシンについては、マシンと "red" に simulate を適用するのが最善でしょう。

```racket
(simulate.v2 fsm-traffic "red")
```

止まって考えよう！ なぜ信号機にとって "red" が良いのだと思いますか？

> **注記:** エンジニアは "red" を安全な状態（safe state）と呼びます。

表現力に関する注記 回避策を踏まえると、これで BSL の限界を説明できます。与えられた FSM はシミュレーションの過程で変化しないにもかかわらず、その記述はワールドの状態の一部にならなければなりません。理想的には、プログラムは FSM の記述が一定のままであることを表現すべきですが、代わりにプログラムは FSM を常に変化する状態の一部として扱わなければなりません。プログラムの読者は、big-bang の最初の部分だけからこの事実を推測することはできません。

本書の次のパートでは、新しいプログラミング言語と特定の言語構造である ISL とローカル定義（local definitions）の導入によって、この難題を解決します。詳細については、「ローカル定義は表現力を追加する（Local Definitions Add Expressive Power）」を参照してください。現時点で、ウィッシュリストに戻り、そのエントリを1つずつ処理していくことができます。最初のもの、state-as-colored-square の設計は非常に単純であるため、完全な定義をそのまま提示します。

```racket
; SimulationState.v2 -> Image
; renders current world state as a colored square

(check-expect (state-as-colored-square
                (make-fs fsm-traffic "red"))
              (square 100 "solid" "red"))

(define (state-as-colored-square an-fsm)
  (square 100 "solid" (fs-current an-fsm)))
```

対照的に、キーイベントハンドラの設計はいくらかの議論に値します。ヘッダーの内容を思い出してください。

```racket
; SimulationState.v2 KeyEvent -> SimulationState.v2
; finds the next state from an-fsm and ke
(define (find-next-state an-fsm ke)
  an-fsm)
```

設計レシピによれば、ハンドラはワールドの状態と KeyEvent を受け取り、ワールドの次の状態を生成しなければなりません。シグネチャを平易な言葉で明確に表現することは、例の設計のガイドにもなります。最初の2つの例は以下の通りです。

```racket
(check-expect
  (find-next-state (make-fs fsm-traffic "red") "n")
  (make-fs fsm-traffic "green"))
(check-expect
  (find-next-state (make-fs fsm-traffic "red") "a")
  (make-fs fsm-traffic "green"))
```

これらの例が言っているのは、現在の状態が fsm-traffic マシンとその "red" 状態を組み合わせている場合、プレイヤーがキーボードで "n" または "a" のどちらを叩いたかに関係なく、結果は同じ FSM と "green" を組み合わせたものになるということです。もう1つの例を以下に示します。

```racket
(check-expect
  (find-next-state (make-fs fsm-traffic "green") "q")
  (make-fs fsm-traffic "yellow"))
```

先を読む前に、この例を解釈してください。他の例を思いつくことができますか？

この関数は構造体を受け取るため、構造体処理のためのテンプレートを書き出します。

```racket
(define (find-next-state an-fsm ke)
  (... (fs-fsm an-fsm).. (fs-current an-fsm)...))
```

さらに、望ましい結果が SimulationState.v2 であるため、適切なコンストラクタを追加してテンプレートを洗練させることができます。

```racket
(define (find-next-state an-fsm ke)
  (make-fs
... (fs-fsm an-fsm)... (fs-current an-fsm)...))
```

例が示唆しているのは、抽出された FSM が新しい SimulationState.v2 の最初のコンポーネントになり、この関数は実際には現在の状態と与えられた FSM を構成する Transitions のリストから次の状態を計算するだけでよいということです。後者は任意に長いため、私たちは願い（wish）を作成します。それは、リストをトラバースして current 状態が (fs-current an-fsm) である Transition を探す find 関数です。そして、定義を完成させます。

```racket
(define (find-next-state an-fsm ke)
  (make-fs
    (fs-fsm an-fsm)
    (find (fs-fsm an-fsm) (fs-current an-fsm))))
```

新しい願いの定式化は以下の通りです。

```racket
; FSM FSM-State -> FSM-State
; finds the state representing current in transitions
; and retrieves the next field
(check-expect (find fsm-traffic "red") "green")
(check-expect (find fsm-traffic "green") "yellow")
(check-error (find fsm-traffic "black")
             "not found: black")
(define (find transitions current)
  current)
```

これらの例は find-next-state の例から導き出されています。

止まって考えよう！ いくつか追加の例を作成し、それから演習問題に取り組みなさい。

**Exercise 228.** find の設計を完成させなさい。

補助関数がテストされたら、simulate を使用して fsm-traffic や演習問題 227 の BW Machine を動かして遊びなさい。

私たちのシミュレーションプログラムは意図的にかなり制限されています。特に、プレイヤーがどのキーを押すかに応じてある状態から別の状態に遷移する FSM を表現するために使用することはできません。しかし、体系的な設計を踏まえれば、プログラムを拡張してそのような機能を持たせることができます。

**Exercise 229.** 以下は Transition の改訂されたデータ定義です。

```racket
(define-struct ktransition [current key next])
; A Transition.v2 is a structure:
;   (make-ktransition FSM-State KeyEvent FSM-State)
```

Transition.v2 のリストを使用して演習問題 109 の FSM を表現しなさい。エラーと最終状態は無視してかまいません。

キー入力を適切な方法で処理するように simulate の設計を修正しなさい。データ例の適応から始めて、設計レシピに従いなさい。

改訂されたプログラムを使用して、以下のキー入力シーケンスにおける演習問題 109 の FSM の実行をシミュレートしなさい："a"、"b"、"b"、"c"、"d"。

有限状態機械には通常、初期状態と最終状態が伴います。FSM を「実行する」プログラムが最終状態に達したとき、プログラムは停止すべきです。最後の演習問題では、これらの概念を導入するためにもう一度 FSM のデータ表現を改訂します。

**Exercise 230.** FSMs に関する以下のデータ表現を考えなさい。

```racket
(define-struct fsm [initial transitions final])
(define-struct transition [current key next])
; An FSM.v2 is a structure:
;   (make-fsm FSM-State LOT FSM-State)
; A LOT is one of:
; – '()
; – (cons Transition.v3 LOT)
; A Transition.v3 is a structure:
;   (make-transition FSM-State KeyEvent FSM-State)
```

このコンテキストにおいて演習問題 109 の FSM を表現しなさい。

FSM.v2 を受け取り、プレイヤーのキー入力上でそれを実行する関数 fsm-simulate を設計しなさい。キー入力のシーケンスによって FSM.v2 が最終状態に達した場合、fsm-simulate は停止します。
ヒント: この関数は、与えられた fsm 構造体の initial フィールドを使用して現在の状態を追跡します。

反復的洗練に関する注記（Note on Iterative Refinement） これら最後の2つのプロジェクトは、「反復的洗練による設計（design by iterative refinement）」の概念を導入しています。基本的な考え方は、最初のプログラムは望ましい動作のほんの一部しか実装せず、次のプログラムはもう少し実装し、これを繰り返していくというものです。最終的に、望ましい動作のすべて、あるいは少なくとも顧客を満足させるのに十分な動作を示すプログラムが得られます。詳細については、「反復的洗練（Iterative Refinement）」を参照してください。

```
+---------------------------------------------------------------------------+
| Figure 82: Representing and interpreting finite state machines in general |
|                                                                           |
|; An FSM is one of:                                                       |
|; – '()                                                                   |
|; – (cons Transition FSM)                                                 |
|                                                                           |
| (define-struct transition [current next])                                 |
|; A Transition is a structure:                                            |
|; (make-transition FSM-State FSM-State)                                   |
|                                                                           |
|; FSM-State is a Color.                                                   |
|                                                                           |
|; interpretation An FSM represents the transitions that a                 |
|; finite state machine can take from one state to another                 |
|; in reaction to keystrokes                                               |
+---------------------------------------------------------------------------+
```

