# I 固定サイズのデータ (Fixed-Size Data)

本書の第I部では、基本的な原子データ（数値、文字列、画像、真偽値）を操作する「算術」と、それらを組み合わせた最も単純なプログラムである「関数」について学び、体系的なプログラム設計の最初の一歩を踏み出します。

## 1 算術 (Arithmetic)

BSL（Beginning Student Language）における算術は、前置表記（Prefix Notation）で記述されます。
基本規則：
1. 開き括弧 `(` を書く
2. 基本演算の名前（`+` など）を書く
3. 引数をスペースで区切って書く
4. 閉じ括弧 `)` を書く

例：
```racket
(+ 1 2)
```

ネスト（入れ子）された例：
```racket
(+ 1 (+ 1 (+ 1 1) 2) 3 4 5)
```

評価は、まず引数となる式をすべて評価し、その結果のデータ片を演算に与えて計算します。

```racket
(+ 1 2)
==
3
```

### 1.1 数の算術 (The Arithmetic of Numbers)
BSLは自然数、整数、有理数、実数、複素数をサポートし、正確数（Exact）と不正確数（Inexact）を区別します。

演算例：
`+`, `-`, `*`, `/`, `abs`, `add1`, `ceiling`, `denominator`, `exact->inexact`, `expt`, `floor`, `gcd`, `log`, `max`, `numerator`, `quotient`, `random`, `remainder`, `sqr`, `tan`

```racket
> (sin 0)
0
```

また、円周率 `pi` やオイラーの定数 `e` も事前定義されています。不正確数（近似値）には `#i` プレフィックスが付きます（例: `#i1.4142135623730951`）。

**Exercise 1.** 定義領域に以下を追加し、デカルト平面上の点 (x, y) から原点 (0, 0) までの距離を計算する式を記述せよ。
```racket
(define x 3)
(define y 4)
```

### 1.2 文字列の算術 (The Arithmetic of Strings)
文字列はダブルクォーテーション `"` で囲まれた文字列です。

演算例：
`string-append`, `string-length`, `substring`, `string-ith`

```racket
(string-append "hello" " " "world")
; "hello world"
```

**Exercise 2.** `prefix` と `suffix` という名前の文字列定数を定義し、それらを結合する式を記述せよ。

### 1.3 混ぜ合わせ (Mixing It Up)
文字列と数値を相互に変換したり、型を操作する演算です。

演算例：
`number->string`, `string->number`, `string-length`

```racket
(string-length "42")
==
2
```

**Exercise 3.** 文字列 `str` と数値 `i` を定義し、`str` の `i` 番目の位置に `_` を挿入する式を記述せよ。
```racket
(define str "helloworld")
(define i 5)
```

**Exercise 4.** 文字列 `str` と数値 `i` が与えられたとき、`i` 番目の文字を削除する式を記述せよ。

### 1.4 画像の算術 (The Arithmetic of Images)
`2htdp/image` ティーチパックを使用すると、画像をデータとして操作できます。

演算例：
`circle`, `rectangle`, `empty-scene`, `place-image`, `overlay`, `image-width`, `image-height`

**Exercise 5.** 1枚の画像のピクセル数を計算する式を作成せよ。
```racket
(define cat <画像>)
```

**Exercise 6.** 任意の画像 `cat` が与えられたとき、それが縦長か、横長か、正方形かを判定する式を作成せよ。

**Exercise 7.** 以下の画像作成を試せ。
```racket
(circle 10 "solid" "red")
(rectangle 10 20 "outline" "blue")
```

**Exercise 8.** 青いバナーの上に赤い文字でテキストを表示する画像を生成する式を記述せよ。

### 1.5 ブール値の算術 (The Arithmetic of Booleans)
真偽値（`#true`, `#false`）と、論理演算（`and`, `or`, `not`）、および比較述語（`<`, `>`, `=`, `string=?`）です。

```racket
(or #false #true)
==
#true
```

**Exercise 9.** 画像 `in` に対して、それが縦長であるか、または横長であるかを判定するブール値の式を作成せよ。

### 1.6 ブール値と混ぜ合わせ (Mixing It Up with Booleans)
条件に応じて処理を分岐させるための `if` 式です。

```racket
(if (string=? "red" "green") "stop" "go")
==
"go"
```

**Exercise 10.** 与えられた画像が縦長なら `"tall"`、横長なら `"wide"`、正方形なら `"square"` を返す `if` 式を記述せよ。

### 1.7 述語: 自分のデータを知れ (Predicates: Know Thy Data)
データ型を検査するための述語です。
`number?`, `string?`, `image?`, `boolean?`, `any?`

**Exercise 11.** `number?` や `string?` の動作を確認する式を記述せよ。

**Exercise 12.** 値 `v` が与えられたとき、それが画像ならその面積、文字列ならその長さ、数値ならその値自体を返す条件分岐式を記述せよ。

---

## 2 関数とプログラム (Functions and Programs)

プログラムの半分は算術であり、もう半分はそれらを再利用可能にする「関数」です。

### 2.1 関数 (Functions)
`define` を使って新しい関数を定義します。

```racket
(define (f x) (+ x 10))
```

**Exercise 13.** 与えられた文字列の最初の文字を返す関数 `string-first` を定義せよ。

**Exercise 14.** 与えられた文字列の最後の文字を返す関数 `string-last` を定義せよ。

**Exercise 15.** 2つのブール値 `sunny` と `friday` を消費し、金曜日で晴れの日であるかを判定する関数 `play` を定義せよ。

**Exercise 16.** 画像を消費し、そのピクセル面積を計算する関数 `image-area` を定義せよ。

**Exercise 17.** 画像を消費し、それが縦長であるかを判定する関数 `image-classify` を定義せよ。

**Exercise 18.** 2つの文字列を消費し、それらを `"_"` で結合する関数 `string-join` を定義せよ。

**Exercise 19.** 文字列 `str` と位置 `i` を消費し、`i` 番目に `"_"` を挿入する関数 `string-insert` を定義せよ。

**Exercise 20.** 文字列 `str` と位置 `i` を消費し、`i` 番目の文字を削除する関数 `string-delete` を定義せよ。

### 2.2 計算 (Computing)
関数の評価は、実引数（Argument）を仮引数（Parameter）に置き換える「代入モデル」に従って行われます。

```racket
(f 2)
==
(+ 2 10)
==
12
```

**Exercise 21.** `(ff 2)` がどのように評価されるか代入ステップを記述せよ。
```racket
(define (ff x) (* 10 x))
```

**Exercise 22.** `(distance 3 4)` の評価ステップを記述せよ。

**Exercise 23.** `(string-first "hello")` の評価ステップを記述せよ。

**Exercise 24.** `(implies #true #false)` の評価ステップを記述せよ。

**Exercise 25.** 画像分類関数の評価ステップを記述せよ。

**Exercise 26.** `(string-insert "hello" 2)` の評価ステップを記述せよ。

### 2.3 関数の合成 (Composing Functions)
複雑なタスクは、単純な関数を組み合わせることで解決します。

```racket
(define (profit price)
  (- (revenue price) (cost price)))
```

**Exercise 27.** 映画館のチケット価格と利益の関係をモデル化するプログラムを構築せよ。

**Exercise 28.** チケット価格ごとの利益をシミュレーションし、最適なチケット価格を見つけよ。

**Exercise 29.** コスト構造が変化した場合の最適なチケット価格を再計算せよ。

### 2.4 グローバル定数 (Global Constants)
変更に強いプログラムを作るために、マジックナンバーをグローバル定数として定義します。

```racket
(define BASE-PRICE 5)
```

**Exercise 30.** 映画館の利益計算プログラムにおける定数（基本価格、固定費、変動費など）を定数定義として分離せよ。

### 2.5 プログラム (Programs)
BSLでは `2htdp/batch-io` を使ったバッチ処理プログラムや、`2htdp/universe` を使った対話型（イベント駆動）プログラム（`big-bang`）を設計できます。

```racket
(big-bang 0
  [on-tick sub1]
  [to-draw render]
  [on-key handle-key])
```

**Exercise 31.** ファイルからテキストを読み込んで処理するバッチプログラムを作成せよ。

**Exercise 32.** `big-bang` を使って、画面を動くUFOやロケットのアニメーションプログラムを作成せよ。

---

## 3 プログラムの設計方法

本書の最初の数章では、プログラミングを学ぶには多くの概念をある程度習得する必要があることを示してきました。一方では、プログラミングには言語が必要です。つまり、計算したいことを伝えるための表記法です。プログラムを定式化するための言語は人工的な構成物ですが、プログラミング言語の習得は自然言語の習得といくつかの要素を共有しています。どちらも語彙、文法、そして「句」が何を意味するかの理解を伴います。

他方では、問題文からプログラムに至る方法を学ぶことが重要です。問題文の中で何が関連していて、何を無視できるかを判断する必要があります。プログラムが何を消費し、何を生成し、入力と出力をどのように関連付けるかを明らかにする必要があります。選択した言語とそのティーチパックが、プログラムが処理するデータに対する特定の基本演算を提供しているかどうかを知る（または調べる）必要があります。提供していない場合は、それらの演算を実装する補助関数を開発しなければならないかもしれません。最後に、プログラムができたら、それが実際に意図した計算を行っているかどうかを確認しなければなりません。そして、これはあらゆる種類のエラーを明らかにする可能性があり、それらを理解して修正できなければなりません。

これらすべてはかなり複雑に聞こえ、なぜただ実験しながら適当にやって、結果がまあまあならそのままにしておけばいいのかと思うかもしれません。このプログラミングへのアプローチはしばしば「ガレージプログラミング」と呼ばれ、多くの場合に成功します。時にはスタートアップ企業の立ち上げの足がかりになることもあります。しかし、スタートアップはその「ガレージ努力」の結果を販売することはできません。なぜなら、元のプログラマとその友人しか使えないからです。

良いプログラムには、それが何をするか、どのような入力を期待するか、何を生成するかを説明する短い書き込みが付属しています。理想的には、それが実際に動作することをある程度保証するものも付きます。最良の場合、プログラムの問題文へのつながりが明白なので、問題文への小さな変更をプログラムへの小さな変更に簡単に翻訳できます。ソフトウェアエンジニアはこれを「プログラミング製品」と呼びます。

このすべての追加作業が必要なのは、プログラマが自分自身のためにプログラムを作成するわけではないからです。プログラマは、他のプログラマが読むためにプログラムを書きます。そして時折、人々は仕事をするためにこれらのプログラムを実行します。

「他の」という言葉には、通常、若い頃の自分がプログラムの制作に込めた思考のすべてを思い出すことができない年上のバージョンのプログラマも含まれます。

ほとんどのプログラムは、協力し合う関数の大規模で複雑な集合であり、誰もが1日でこれらすべての関数を書くことはできません。プログラマはプロジェクトに参加し、コードを書き、プロジェクトを去ります。他の人がそのプログラムを引き継いで作業します。もう一つの困難は、プログラマのクライアントが、本当に解決してほしい問題について考えを変えがちだということです。彼らは通常、ほぼ正しく理解していますが、多くの場合、いくつかの詳細を間違えます。さらに悪いことに、プログラムのような複雑な論理的構成物は、ほとんど常に人間のエラーに悩まされます。要するに、プログラマは間違いを犯します。最終的に誰かがこれらのエラーを発見し、プログラマはそれらを修正しなければなりません。彼らは1ヶ月前、1年前、または20年前のプログラムを読み直して変更する必要があります。

**Exercise 33.** 「year 2000」問題を調べよ。

ここでは、ステップバイステップのプロセスと、問題データを中心にプログラムを組織する方法を統合した**設計レシピ**を提示します。長い間空白の画面を見つめ続けるのが嫌いな読者にとって、この設計レシピは体系的な方法で進捗を達成する方法を提供します。他の人をプログラム設計に教える人にとって、レシピは初心者の困難を診断するための道具です。他の人々にとっては、このレシピは医学、ジャーナリズム、工学など他の分野にも適用できるものかもしれません。本物のプログラマになりたい人にとって、設計レシピは既存のプログラムを理解し作業するための方法も提供します——ただし、すべてのプログラマがこのような設計レシピのような方法を使ってプログラムを考え出すわけではありません。この章の残りは、設計レシピの世界への最初の一歩を捧げます。以降の章とパートは、レシピを何らかの形で洗練・拡張していきます。

## 3.1 関数の設計

### 情報とデータ

プログラムの目的は、ある情報を消費し、新しい情報を生成する計算プロセスを記述することです。この意味で、プログラムは数学の教師が生徒に与える指示のようなものです。しかし生徒とは異なり、プログラムは数値以上のものを扱います。ナビゲーション情報を計算し、人の住所を調べ、スイッチを入れ、ビデオゲームの状態を検査します。これらすべての情報は現実世界の一部——しばしばプログラムの**ドメイン**と呼ばれる——から来ており、プログラムの計算結果はこのドメイン内のさらなる情報を表します。

**情報**は私たちの説明で中心的な役割を果たします。情報をプログラムのドメインに関する事実として考えてください。家具カタログを扱うプログラムにとって、「5本脚のテーブル」や「2メートル四方の正方形テーブル」は情報の断片です。ゲームプログラムは異なる種類のドメインを扱い、「5」はあるオブジェクトがキャンバスのある部分から別の部分へ移動する際の、クロックティックあたりのピクセル数を指すかもしれません。また、給与計算プログラムは「5つの控除」を扱う可能性が高いでしょう。

プログラムが情報を処理するためには、それをプログラミング言語の何らかの形の**データ**に変換しなければなりません。そしてデータを処理し、完了したら結果のデータを再び情報に変換します。対話型プログラムはこれらのステップをさらに混ぜ合わせ、必要に応じて世界からより多くの情報を取得し、その間に情報を配信するかもしれません。

私たちはBSLとDrRacketを使用するので、情報からデータへの変換について心配する必要はありません。DrRacketのBSLでは、関数をデータに直接適用して何を生成するかを観察できます。その結果、情報をデータに変換したりその逆を行う関数を書くという深刻なニワトリと卵の問題を避けられます。単純な種類の情報の場合、そのようなプログラム部品の設計は些細です。単純な情報以外のものについては、例えばパージングを知る必要があり、それは直ちにプログラム設計における多くの専門知識を必要とします。

ソフトウェアエンジニアは、BSLとDrRacketがデータ処理を情報→データのパージングやデータ→情報への変換から分離する方法を**model-view-controller (MVC)** というスローガンで表現します。実際、十分に設計されたソフトウェアシステムはこの分離を強制することが今や受け入れられた知恵となっています。入門書の多くは依然としてそれらを混同していますが。したがって、BSLとDrRacketで作業することで、プログラムの核心の設計に集中でき、そこに十分な経験を積んだ後、情報-データ変換部分の設計を学ぶことができます。

ここでは、データと情報の分離を示すために、2つのプリインストール済みティーチパックを使用します：`2htdp/batch-io` と `2htdp/universe` です。この章から、バッチプログラムと対話型プログラムのための設計レシピを開発し、完全なプログラムがどのように設計されるかのアイデアを提供します。完全なプログラミング言語のティーチパックは完全なプログラムのためのより多くの文脈を提供しており、設計レシピを適切に適応させる必要があることを心に留めておいてください。

**図15: 情報からデータへ、そして戻る**

情報とデータの中心的な役割を考えると、プログラム設計はそれらの間のつながりから始めなければなりません。具体的には、私たちプログラマは、選択したプログラミング言語を使って関連する情報の断片をデータとしてどのように**表現**し、データをどのように**解釈**して情報とするかを決定しなければなりません。図15はこの考えを抽象的な図で説明しています。

この考えを具体的にするために、いくつかの例を見てみましょう。数値の形で情報を消費・生成するプログラムを設計しているとします。表現の選択は簡単ですが、解釈には42のような数値がドメインで何を表すかを説明する必要があります：

- 42 は画像のドメインでは上端からのピクセル数を表すかもしれない
- 42 はシミュレーションやゲームオブジェクトが移動するクロックティックあたりのピクセル数を表すかもしれない
- 42 は物理学のドメインでは華氏、摂氏、または絶対温度目盛りでの温度を意味するかもしれない
- 42 はプログラムのドメインが家具カタログの場合、あるテーブルのサイズを指定するかもしれない
- 42 は単に文字列内の文字数を数えるかもしれない

鍵は、数値を情報として数値をデータとして（そしてその逆へ）どのように移行するかを知ることです。

この知識はプログラムを読むすべての者にとって非常に重要なので、私たちはしばしばそれをコメントの形で書き留め、**データ定義**と呼びます。データ定義には2つの目的があります。第一に、意味のある単語を使ってデータのあるコレクション（クラス）に名前を付けます。第二に、そのクラスの要素をどのように作成し、任意のデータ片がそのクラスに属するかどうかをどのように判断するかを読者に伝えます。

例：

```
; Temperature は Number
; 摂氏での温度を表す
```

このデータ定義は、任意の数値を温度として解釈できることを述べています。-400 も Temperature です。読者はデータ定義から、-400 が有効な Temperature であることを理解します。

あるデータ定義が与えられたとき、読者は「このクラスに属するデータ片の例を挙げよ」と尋ねられるべきです。Temperature の場合、-273、0、100 は良い例です。読者はまた「この特定のデータはどのクラスに属するか？」と尋ねられるべきです。42 は Number、String、Image、Boolean のいずれでもありません。なぜなら文字列は Temperature に属さないからです。そして、Temperature のサンプルを作れと言われたら、-400 のようなものを思いつくかもしれません。

可能な最低温度が約-273℃であることを知っている場合、この知識をデータ定義で表現できるかどうかを疑問に思うかもしれません。データ定義は実際にはクラスの英語による記述に過ぎないので、確かにここに示したよりもはるかに正確な方法で温度のクラスを定義できます。本書では、このようなデータ定義に様式化された英語を使用し、次の章で「-274より大きい」などの制約を課すためのスタイルを導入します。

これまで、4つのデータクラスの名前に出会いました：`Number`、`String`、`Image`、`Boolean` です。この知識があれば、新しいデータ定義を定式化することは、既存のデータ形式に新しい名前を導入することに他なりません。例えば「temperature」を数値に与えることです。この限られた知識でも、私たちの設計プロセスの概要を説明するのに十分です。

### 設計プロセス

入力情報をデータとして表現する方法と、出力データを情報として解釈する方法を理解したら、個々の関数の設計は簡単なプロセスに従って進められます。

1. 情報をデータとしてどのように表現したいかを表現せよ。1行のコメントで十分です：
   ```
   ; センチメートルを表すために数値を使います。
   ```

2. プログラムの成功する設計に関連すると考えるデータクラスに対して、Temperature のようなデータ定義を定式化せよ。

3. 署名、目的文、関数ヘッダを書け。

   **関数署名** は、設計の読者にあなたの関数がいくつの入力を消費し、それらがどのクラスから来ているか、何種類のデータを生成するかを伝えるコメントです。以下は3つの例です：

   ```
   ; String -> String
   ; String -> Number
   ; Number String Image -> Image
   ```

   **目的文** は、関数が何をするかを1文で述べるコメントです。良い目的文は、関数を読まずに何をするかを誰でも理解できるようにします。

   **ヘッダ**（スタブとも呼ばれる）は単純化された関数定義です。署名内の各入力クラスに対して1つの変数名を選び、関数の本体は出力クラスからの任意のデータ片にできます。

   ```
   (define (f a-string) 0)
   (define (g n) "a")
   (define (h num str img) (empty-scene 100 100))
   ```

   パラメータ名は、パラメータが表すデータの種類を反映すべきです。時には、パラメータの目的を示唆する名前を使いたいと思うかもしれません。

   目的文を定式化するとき、パラメータ名を使って何が計算されるかを明確にすると役立つことがよくあります。

4. 関数の例を作成せよ。

   各関数について、少なくとも2〜3個の具体例を挙げ、入力と期待される出力を書きましょう。これを「指の運動」と呼びます。

   ```
   ; given: "hello", expect: "h"
   ; given: "world", expect: "w"
   ```

5. テンプレートを書け。

   データ定義から関数のアウトラインを導き出します。条件分岐が必要な場合は cond の骨組みを、構造体が必要なら選択子を使った骨組みを書きます。

6. 関数定義を完成させよ。

   テンプレートを埋め、目的文と例を使って実際の計算ロジックを記述します。

7. テストを行え。

   例を `check-expect` に変換し、すべてが通ることを確認します。

**図1: 関数設計レシピの基本ステップ**（前述の6ステップの要約図）

このプロセスは初心者が進捗を確実にし、エラーを早期に発見するのを助けます。

### 関数の例から設計へ

ここでは、文字列の最初の文字を抽出する簡単な関数 `string-first` を設計する例を示します。

（コード例は原文のまま）

```racket
; String -> String
; 文字列 s の最初の文字を抽出する
(define (string-first s) "a")

; 例:
; given: "hello", expect: "h"
; given: "world", expect: "w"
; given: " ", expect: " "

(check-expect (string-first "hello") "h")
(check-expect (string-first "world") "w")
```

（以降の章の例も同様に設計レシピを適用して進めます。）

## 3.2 指の運動: 関数

この節の最初のいくつかの練習問題は、前の「関数」節のものとほぼコピーです。ただし、後者で「define」という単語を使っているところを、ここでは「design」という単語を使っています。この違いが意味するのは、設計レシピに従ってこれらの関数を作成し、解答には関連するすべての部品を含めるべきだということです。

節のタイトルが示唆するように、これらはプロセスを身体化するための練習問題です。ステップが第二の天性になるまで、1つも飛ばさないでください。飛ばすと簡単に避けられるエラーにつながります。プログラミングには複雑なエラーの余地がたくさん残されています。ばかばかしいエラーに時間を無駄にする必要はありません。

**Exercise 34.** 関数 `string-first` を設計せよ。文字列 s から最初の文字を抽出する。

**Exercise 35.** 関数 `string-last` を設計せよ。文字列 s から最後の文字を抽出する。

**Exercise 36.** 関数 `image-area` を設計せよ。画像 img の面積を計算する。

**Exercise 37.** 関数 `string-rest` を設計せよ。文字列 s から最初の文字を除いた残りを返す。

**Exercise 38.** 関数 `string-remove-last` を設計せよ。文字列 s から最後の文字を除いた残りを返す。

（本書ではさらに多くの練習問題が続き、すべて設計レシピの適用を練習するものです。）

## 3.3 ドメイン知識

関数の本体をコード化するのにどのような知識が必要かを不思議に思うのは自然です。少し反省すると、このステップにはプログラムのドメインに対する適切な把握が必要であることがわかります。実際、そのような**ドメイン知識**には2つの形態があります：

1. 外部ドメインからの知識——数学、音楽、生物学、土木工学、芸術など。プログラマはコンピューティングのすべての応用ドメインを知ることはできないため、ドメイン専門家と問題を議論できるように、さまざまな応用分野の言語を理解する準備ができていなければなりません。数学は多くの（すべてではない）ドメインの交差点にあります。したがって、プログラマはドメイン専門家と問題を処理する際に新しい言語を習得しなければならないことがよくあります。

2. ティーチング言語とそのライブラリに関する知識。プログラマは、与えられた問題に対して言語が何を提供しているかを知る必要があります。

設計プロセスの中で、ドメイン知識は主にステップ5（関数定義を完成させる）で必要になりますが、良い例を作成するためにも役立ちます。

## 3.4 関数からプログラムへ

すべてのプログラムが単一の関数定義から成るわけではありません。いくつかの関数を必要とするものもあります。多くのものは定数定義も使用します。いずれにせよ、すべての関数を体系的に設計することは常に重要ですが、グローバル定数や補助関数は設計プロセスを少し変えます。

グローバル定数を定義した場合、関数は結果を計算するためにそれらを使用するかもしれません。それらの存在を自分に思い出させるために、テンプレートにこれらの定数を追加するとよいでしょう。結局のところ、それらは関数定義に寄与し得るものの目録に属します。

複数関数のプログラムは、対話型プログラムが自動的にキーイベントやマウスイベントを処理する関数、状態を画像として描画する関数などを必要とするために生じます。各補助関数も設計レシピに従って設計すべきです。

プログラム全体にはトップレベルの目的文も書くべきです。

## 3.5 テストについて

テストはすぐに労働集約的な作業になります。小さなプログラムを対話領域で実行するのは簡単ですが、多くの機械的な作業と複雑な検査が必要です。プログラマがシステムを成長させるにつれ、多くのテストを実施したいと思うようになります。やがてこの作業は圧倒的になり、プログラマはそれを怠るようになります。同時に、テストは基本的な欠陥を発見・防止するための最初の道具です。ずさんなテストはすぐにバグのある関数——隠れた問題を抱えた関数——につながり、バグのある関数はプロジェクトをしばしば複数の方法で遅らせます。

したがって、手動でテストを行うのではなく、テストを機械化することが重要です。多くのプログラミング言語と同様に、BSLにはテスト機能が含まれており、DrRacketはこの機能を認識しています。

`check-expect` を使って例を自動テストに変換します：

```racket
(check-expect (string-first "hello") "h")
```

DrRacket は RUN 時にすべてのテストを実行し、結果を表示します。失敗したテストは赤で表示され、デバッグに役立ちます。

良いテストには以下が含まれます：
- 典型的なケース
- 境界値（空文字列、長さ1の文字列など）
- 特殊ケース

テストはまた、プログラムのドキュメントとしても機能します。

## 3.6 World プログラムの設計

前の章では `2htdp/universe` ティーチパックをアドホックな方法で紹介しましたが、この節では**設計レシピ**がどのようにして world プログラムを体系的に作成するのを助けるかを示します。まず、データ定義と関数署名に基づく `2htdp/universe` ティーチパックの簡単な要約から始めます。次に、world プログラムのための設計レシピを詳述します。

ティーチパックは、プログラマが世界の状態を表すデータ定義と、すべての可能な世界状態に対して画像を作成する方法を知っている関数 `render` を開発することを期待します。プログラムの必要性に応じて、プログラマは次にクロックティック、キー入力、マウスイベントに応答する関数を設計しなければなりません。最後に、対話型プログラムは `big-bang` を使って状態、描画関数、イベントハンドラを結び付ける必要があります。

**world プログラム設計レシピの概要**：

1. 世界の状態を表すデータ定義を書く。
2. `render` 関数の署名と目的文を書く：`WorldState -> Image`
3. 必要に応じて `on-tick`、`on-key`、`on-mouse` などのハンドラの署名を書く。
4. 各関数について設計レシピを適用する。
5. `big-bang` でプログラムを組み立てる。

例として、シンプルなカウンターやアニメーションを設計できます。

## 3.7 バーチャルペットの世界

この練習問題の節は、バーチャルペットゲームの最初の2つの要素を紹介します。キャンバスを横切って歩き続ける猫の表示から始まります。もちろん、歩き続けると猫は不幸になり、その不幸が現れます。すべてのペットと同様に、撫でて少し助けるか、餌を与えてたくさん助けることができます。

まず、私たちのお気に入りの猫の画像から始めましょう：

```racket
(define cat1 <画像>)
```

猫の画像をコピーして DrRacket に貼り付け、`define` で画像に名前を付けましょう。

**Exercise 45.** 「バーチャルキャット」の world プログラムを設計せよ。猫を左から右に継続的に動かす。`cat-prog` と呼び、猫の開始位置を消費すると仮定せよ。さらに、猫をクロックティックあたり3ピクセル動かせ。猫が右端で消えたら左端に再出現させる。`modulo` 関数について読むとよい。

**Exercise 46.** 少し異なる画像で猫のアニメーションを改善せよ：

```racket
(define cat2 <画像>)
```

Exercise 45 の描画関数を調整し、x座標が奇数かどうかで2つの猫画像のどちらかを使うようにせよ。HelpDesk で `odd?` を調べ、`cond` 式を使って猫画像を選択せよ。

**Exercise 47.** 「幸福ゲージ」を維持・表示する world プログラムを設計せよ。`gauge-prog` と呼び、幸福度は0から100（両端を含む）の数値であると合意せよ。

各クロックティックで幸福レベルは0.1減少する。ただし0を下回らない。下矢印キーが押されるたびに幸福度は1/5減少、上矢印が押されると1/3ジャンプする。

幸福レベルを表示するために、黒い枠のある塗りつぶしの赤い長方形を持つシーンを使え。幸福レベル0では赤いバーは消え、最大幸福レベルHではバーがシーン全体にわたるようにせよ。

**Note.** 十分な知識を得たら、`gauge-prog` を Exercise 45 の解と組み合わせる方法を説明する。その後、猫を助けることができるようになる。無視し続けると猫はますます不幸になる。撫でるとより幸せに、餌をやるとずっとずっと幸せになる。だからこそ、これらの最初の3章が教えてくれるよりもずっと多くの world プログラムの設計について知りたいと思う理由がわかるだろう。

---

**注意**: この章のすべての Racket コード、関数定義、`check-expect`、画像リテラル、`big-bang` 式などは**原文のまま**保持しています。翻訳は説明文、演習の記述、注記、設計プロセスの解説のみです。

*原典: How to Design Programs, Second Edition — Chapter 3 "How to Design Programs"*
*URL: https://htdp.org/2026-5-28/Book/part_one.html*
## 4 区間、列挙、項目化 (Intervals, Enumerations, and Itemizations)

現時点では、情報をデータとして表現するための選択肢は、数値、文字列、画像、真偽値の4つしかありません。多くの問題に対してはこれで十分ですが、これら4つの基本データの集合だけでは対応しきれない複雑な問題も多く存在します。優れたソフトウェア設計者には、情報を表現するためのさらなる仕組みが必要です。

少なくとも、プログラムを設計する際には、これらの組み込みデータの集合に対して「制約」を課す方法を学ばねばなりません。制約を課す一つの方法は、データ集合からいくつかの具体的なデータだけをリストアップし、この問題においてはそれらしか使用しないと合意することです。このように要素をリストアップする方法を**列挙 (Enumeration)** と呼びますが、これは要素数が有限の場合にのみ有効です。一方で、「無限に」多くの要素を含む集合を扱うために、特定の条件を満たす要素の範囲を指定する**区間 (Interval)** を導入します。

> [!NOTE]
> ここでいう「無限」とは、「要素数が多すぎて、一つひとつリストアップするのが完全に不可能である」という現実的な意味も含まれます。

列挙や区間を定義することは、異なる種類の要素を区別することを意味します。プログラムコード内でこれらを区別するには、**条件分岐関数 (Conditional Functions)**、すなわち引数の値に応じて異なる計算方法を選択する関数が必要です。「プロローグ：プログラムの仕方」や「ブール値と混ぜ合わせ」では、こうした条件分岐関数の書き方をいくつかの例で示しましたが、いずれも体系的な設計手法（設計レシピ）は用いていませんでした。本章では、列挙、区間、そしてこれらを組み合わせた新しいデータ表現方法である**項目化 (Itemization)** の一般的な設計手法を解説します。

まず、`cond` 式の使い方を詳細に見直し、その後に列挙、区間、項目化の3つのデータ定義方法について説明します。列挙はそのクラスに属するすべてのデータを一つずつリストアップするものであり、区間はデータの範囲を指定するものです。最後の項目化は、定義の一部で範囲を指定し、別の部分で特定のデータを指定するという、両者を混ぜ合わせたものです。最後に、これらのデータ型に対する一般的な設計レシピ（設計戦略）を整理します。

### 4.1 条件式を使ったプログラミング (Programming with Conditionals)

「プロローグ：プログラムの仕方」で学んだ条件式を思い出してください。`cond` 式は本書で最も複雑な式の形であるため、その一般的な形式を詳しく見てみましょう。

```racket
(cond
  [ConditionExpression1 ResultExpression1]
  [ConditionExpression2 ResultExpression2]
  ...
  [ConditionExpressionN ResultExpressionN])
```

`cond` 式はキーワードである `(cond` で始まり、対応する `)` で終わります。キーワードに続いて、必要なだけの `cond` 行（これを **cond 節** と呼びます）を記述します。各 `cond` 節は、角括弧 `[` と `]` で囲まれた2つの式で構成されます（※丸括弧 `( )` を使っても文法上問題ありませんが、視認性を高めるために角括弧を使うのが一般的です）。

条件式を使用した関数定義の例を見てみましょう：

```racket
(define (next traffic-light-state)
  (cond
    [(string=? "red" traffic-light-state) "green"]
    [(string=? "green" traffic-light-state) "yellow"]
    [(string=? "yellow" traffic-light-state) "red"]))
```

この例は、`cond` 式を使用することの便利さを示しています。多くの問題において、関数は複数の異なる状況を区別しなければなりません。`cond` 式を使用すれば、発生しうる可能性ごとに1つの `cond` 節を割り当てることができ、問題文で示された異なる状況とプログラムコードとの対応関係を読者に対して明確に示すことができます。

`if` 式（「ブール値と混ぜ合わせ」を参照）との使い分けについての注意：
`if` 式は「ある1つの状況と、それ以外のすべての状況」を区別するために使われます。そのため、3つ以上の状況を区別する必要がある文脈には適していません。`if` 式は「AかBか」の二者択一を簡潔に表現する場合にのみ使用すべきです。したがって、データ定義から導き出される複数の明確に区別された状況をコードで表現する場合には、常に `cond` 式を使用します。

`cond` 式の条件が複雑になり、「その他のすべての場合」と表現したい場合があります。このような場合、`cond` 式の最後の節に限って `else` キーワードを使用することが許可されています。

```racket
(cond
  [ConditionExpression1 ResultExpression1]
  [ConditionExpression2 ResultExpression2]
  ...
  [else DefaultResultExpression])
```

もし最後以外の `cond` 節で `else` を使用すると、DrRacket の BSL はエラーを返します：

```racket
> (cond
    [(> x 0) 10]
    [else 20]
    [(< x 10) 30])
cond:found an else clause that isn't the last clause in its cond expression
```

文法的に正しくないプログラムコードは意味を持たないため、BSLは実行前にこれを拒否します。

ゲームプログラムの一部として、ゲーム終了時のスコアに応じて賞（アワード）を計算する関数 `reward` を設計するとします。
関数のヘッダとデータ定義は以下の通りです：

```racket
; A PositiveNumber is a Number greater than/equal to 0.
; PositiveNumber -> String
; computes the reward level from the given score s
```

この関数を実装する2つのバリエーションを比較してみましょう：

**左側の実装（3つの明確な条件を使用）：**
```racket
(define (reward s)
  (cond
    [(<= 0 s 10) "bronze"]
    [(and (< 10 s) (<= s 20)) "silver"]
    [(< 20 s) "gold"]))
```

**右側の実装（else 節を使用）：**
```racket
(define (reward s)
  (cond
    [(<= 0 s 10) "bronze"]
    [(and (< 10 s) (<= s 20)) "silver"]
    [else "gold"]))
```

左側の実装では、3番目の条件として `(< 20 s)` を正しく記述するために、以下の計算が成り立つことをプログラマが頭の中で保証する必要があります：
*   `s` は非負の数値であること。
*   `(<= 0 s 10)` が `#false` であること。
*   `(and (< 10 s) (<= s 20))` もまた `#false` であること。

この例では計算は簡単に見えますが、複雑な条件分岐では境界値の計算ミスによってプログラムにバグ（不具合）が混入しやすくなります。したがって、最後のケースが他の条件の完全な「補集合（それ以外）」であることが明らかな場合は、右側のように `else` 節を使用する方が安全で優れています。

### 4.2 条件付きの計算 (Computing Conditionally)

DrRacket が `cond` 式をどのように評価するかをより正確に見ていきましょう。先ほどの `reward` 関数の定義を考えます：

```racket
(define (reward s)
  (cond
    [(<= 0 s 10) "bronze"]
    [(and (< 10 s) (<= s 20)) "silver"]
    [else "gold"]))
```

関数を呼び出す際、`cond` 式単体を見ただけではどの節が実行されるかを予測することはできません。これが関数の目的そのものです。関数は多様な入力（例えば `3`, `12`, `21` など）を受け取り、それぞれの入力に対して異なる振る舞いをします。入力データのバリエーションを区別して処理することが `cond` 式の役割です。

具体的な評価ステップを見てみましょう。まず、`(reward 3)` を呼び出した場合です：

```racket
(reward 3)
==
(cond
  [(<= 0 3 10) "bronze"]
  [(and (< 10 3) (<= 3 20)) "silver"]
  [else "gold"])
==
(cond
  [#true "bronze"]
  [(and (< 10 3) (<= 3 20)) "silver"]
  [else "gold"])
==
"bronze"
```

ここでは、最初の条件 `(<= 0 3 10)` が `#true` に評価されたため、対応する結果式である `"bronze"` が `cond` 式全体の値となります。

次に、`(reward 21)` を呼び出した場合を見てみます：

```racket
(reward 21)
==
(cond
  [(<= 0 21 10) "bronze"]
  [(and (< 10 21) (<= 21 20)) "silver"]
  [else "gold"])
==
(cond
  [#false "bronze"]
  [(and (< 10 21) (<= 21 20)) "silver"]
  [else "gold"])
==
(cond
  [(and (< 10 21) (<= 21 20)) "silver"]
  [else "gold"])
```

最初の条件が `#false` と評価されたため、その `cond` 節全体が評価対象から取り除かれ、次の節へと計算が進みます：

```racket
(cond
  [(and (< 10 21) (<= 21 20)) "silver"]
  [else "gold"])
==
(cond
  [(and #true (<= 21 20)) "silver"]
  [else "gold"])
==
(cond
  [(and #true #false) "silver"]
  [else "gold"])
==
(cond
  [#false "silver"]
  [else "gold"])
==
(cond
  [else "gold"])
==
"gold"
```

このように、1番目と2番目の条件が共に `#false` であったため、最後の `else` 節へと処理が移行し、結果として `"gold"` が得られます。

**Exercise 48.** DrRacket の定義領域に `reward` 関数の定義と `(reward 18)` を入力し、ステップ実行（Stepper）を使用して評価のプロセスを確認せよ。

**Exercise 49.** `cond` 式は式の一種であるため、他の式の中にネスト（入れ子に）して記述することができます。
```racket
(- 200 (cond [(> y 200) 0] [else y]))
```
`y` の値が `100` の場合と `210` の場合について、Stepper を使ってこの式がどのように評価されるかを確認せよ。

また、図20に示すUFO/ロケットのアニメーションシーン描画関数 `create-rocket-scene.v5` をリファクタリングし、`cond` 式をネストさせることで、`place-image` の記述が一度だけで済むように実装を書き換えよ。

```racket
(define WIDTH 100)
(define HEIGHT 60)
(define MTSCN (empty-scene WIDTH HEIGHT))
(define ROCKET <画像>)
(define ROCKET-CENTER-TO-TOP (- HEIGHT (/ (image-height ROCKET) 2)))

; 元の実装
(define (create-rocket-scene.v5 h)
  (cond
    [(<= h ROCKET-CENTER-TO-TOP) (place-image ROCKET 50 h MTSCN)]
    [(> h ROCKET-CENTER-TO-TOP) (place-image ROCKET 50 ROCKET-CENTER-TO-TOP MTSCN)]))
```

### 4.3 列挙 (Enumerations)

すべての文字列が任意に使えるわけではありません。例えば、マウスイベントをプログラムに通知する文字列は以下の6つに制限されています：

```racket
; A MouseEvt is one of these Strings:
; – "button-down"
; – "button-up"
; – "drag"
; – "move"
; – "enter"
; – "leave"
```

このように、取りうるすべての具体的なデータを網羅して定義するデータ型を**列挙 (Enumeration)** と呼びます。日常のプログラム設計において、列挙型は非常によく現れます。例えば、信号機の状態を以下のように定義できます：

```racket
; A TrafficLight is one of the following Strings:
; – "red"
; – "green"
; – "yellow"
; interpretation: the three strings represent the three possible states of a traffic light
```

列挙データ型を消費する関数の設計は非常にシンプルです。入力データがケースバイケースで列挙されている場合、関数側でもそれぞれのケースを `cond` 節で区別して処理します。信号機の状態を受け取って、次の信号機の状態を返す関数 `traffic-light-next` は以下のように設計できます：

```racket
; TrafficLight -> TrafficLight
; yields the next state given current state s
(check-expect (traffic-light-next "red") "green")

(define (traffic-light-next s)
  (cond
    [(string=? "red" s) "green"]
    [(string=? "green" s) "yellow"]
    [(string=? "yellow" s) "red"]))
```

`TrafficLight` の定義が3つのデータ要素で構成されているため、`traffic-light-next` 関数もまた自然に3つの `cond` 節で分岐を行います。

**Exercise 50.** 上記の `traffic-light-next` 関数の定義を DrRacket にコピー＆ペーストして RUN すると、テストが不十分なため、いくつかの `cond` 節がハイライト表示（テスト未通過）されます。すべての節がカバーされるようにテストケースを追加せよ。

**Exercise 51.** 信号機のアニメーションをシミュレートする `big-bang` プログラムを設計せよ。信号機の状態を適切な色の円で描画し、クロックティックごとに状態（色）が遷移するようにせよ。最も適切な初期状態について検討せよ。

列挙型の本質は、データ集合を「有限個の具体的なデータ片」として定義することです。
もう一つの重要な例を紹介します：

```racket
; A 1String is a String of length 1, including:
; – "\" (the backslash),
; – " " (the space bar),
; – "	" (tab),
; – "
" (return), and
; – "" (backspace).
; interpretation: represents keys on the keyboard
```

BSL では、キーボードの入力イベント（`KeyEvent`）を以下のように表現します：

```racket
; A KeyEvent is one of:
; – 1String
; – "left"
; – "right"
; – "up"
; – "down"
; – ...
```

この定義に従うと、キーイベントを処理する関数（キーイベントハンドラ）の設計テンプレートは自然と以下のようになります：

```racket
; WorldState KeyEvent -> WorldState
(define (handle-key-events w ke)
  (cond
    [(= (string-length ke) 1) ...]
    [(string=? "left" ke) ...]
    [(string=? "right" ke) ...]
    [(string=? "up" ke) ...]
    [(string=? "down" ke) ...]
    [else ...]))
```

**Exercise 52.** キーボードの左右の矢印キーが押されたときに、赤いドットを左右に移動させるキーイベントハンドラ `keh` を設計せよ。不要なキーイベントは `else` 節で無視すること。図21に示す以下のコードを参考にせよ。

```racket
; A Position is a Number.
; interpretation: distance between the left margin and the ball
; Position KeyEvent -> Position
; computes the next location of the ball

(check-expect (keh 13 "left") 8)
(check-expect (keh 13 "right") 18)
(check-expect (keh 13 "a") 13)

(define (keh p k)
  (cond
    [(string=? "left" k) (- p 5)]
    [(string=? "right" k) (+ p 5)]
    [else p]))
```

### 4.4 区間 (Intervals)

UFOが降下する様子を描画するプログラムを考えます（図22を参照）。

```racket
; A WorldState is a Number.
; interpretation: number of pixels between the top and the UFO
(define WIDTH 300)
(define HEIGHT 100)
(define CLOSE (/ HEIGHT 3))
(define MTSCN (empty-scene WIDTH HEIGHT))
(define UFO (overlay (circle 10 "solid" "green") (rectangle 30 4 "solid" "green")))

; WorldState -> WorldState
(define (main y0)
  (big-bang y0
    [on-tick nxt]
    [to-draw render]))

; WorldState -> WorldState
(define (nxt y) (+ y 3))

; WorldState -> Image
(define (render y)
  (place-image UFO (/ WIDTH 2) y MTSCN))
```

このUFOシミュレーションにおいて、UFOの高さ（位置）に応じて以下のようなステータス画面を表示させたいとします：
*   UFOの高さが画面上部の3分 wea の1（`CLOSE`）より上にあるときは `"descending"`（降下中）。
*   それを超えて下部（`HEIGHT`）までの間にあるときは `"closing in"`（接近中）。
*   画面の底（`HEIGHT`）に達したときは `"landed"`（着陸）。

この場合、状態を表す数値は無限に存在するため、単純に個々の値を列挙することは不可能です。代わりに、境界値を用いて数値の範囲を切り分ける**区間 (Intervals)** を使用します。

区間は、境界値（左端と右端）によって定義されます。境界値自体が範囲に含まれる場合を「閉じた (closed)」、含まれない場合を「開いた (open)」と呼びます。
数学の表記に倣い、閉じた境界にはブラケット `[` や `]`、開いた境界にはペアレントシス `(` や `)` を用いて表現します：
*   `[3, 5]` : 閉区間（3以上5以下）
*   `(3, 5]` : 左開・右閉区間（3より大きく5以下）
*   `[3, 5)` : 左閉・右開区間（3以上5未満）
*   `(3, 5)` : 開区間（3より大きく5未満）

これを用いると、世界の状態 `WorldState` は以下の3つの区間に分類できます：
1.  `0` から `CLOSE` の間（降下中）
2.  `CLOSE` から `HEIGHT` の間（接近中）
3.  `HEIGHT` 以上（着陸）

これをプログラムで表現する場合、境界値がどの区間に属するかという「重複」を避ける必要があります。BSLの `cond` 条件式は順番に評価されるため、この性質を利用して以下のように記述できます：

```racket
; WorldState -> Image
(define (render/status y)
  (cond
    [(<= 0 y CLOSE)
     (place-image (text "descending" 11 "green") 10 10 (render y))]
    [(and (< CLOSE y) (<= y HEIGHT))
     (place-image (text "closing in" 11 "orange") 10 10 (render y))]
    [(> y HEIGHT)
     (place-image (text "landed" 11 "red") 10 10 (render y))]))
```

UFOのアニメーションプログラムをこのステータス表示付きのバージョンにするには、`main` 関数で `[to-draw render/status]` を呼び出すように変更します。

UFOの位置情報や表示位置を一箇所で管理するために、UFOの座標やテキスト表示位置の「コピー」を避ける実装を設計するのが賢明です。図24のように、`cond` 式自体を `place-image` の引数として入れ子にすることができます：

```racket
(define (render/status y)
  (place-image
    (cond
      [(<= 0 y CLOSE) (text "descending" 11 "green")]
      [(and (< CLOSE y) (<= y HEIGHT)) (text "closing in" 11 "orange")]
      [else (text "landed" 11 "red")])
    20 20
    (render y)))
```

### 4.5 項目化 (Itemizations)

列挙型は具体的な有限のデータ片をリストアップし、区間は無限に存在する数値の範囲を指定します。
これら両方を混在させて定義するデータ型を**項目化 (Itemizations)** と呼びます。

例えば、`string->number` 関数の出力結果を考えてみましょう。この関数は、文字列が数値に変換可能な場合は数値を返し、不可能な場合は `#false` を返します。この結果を表すデータ型 `NorF` は以下のように定義できます：

```racket
; An NorF is one of:
; – #false
; – a Number
```

これは、具体的なデータ片である `#false` と、無限のデータクラスである `Number` を組み合わせた項目化の典型例です。
この `NorF` を消費する関数の設計は、やはりそれぞれのケース（項目）に対応する `cond` 節を持つ形になります：

```racket
; NorF -> Number
; adds 3 to the given number; 3 otherwise
(check-expect (add3 #false) 3)
(check-expect (add3 0.12) 3.12)

(define (add3 x)
  (cond
    [(false? x) 3]
    [else (+ x 3)]))
```

もう少し実用的な例として、ロケットの打ち上げカウントダウンをシミュレートするプログラムを設計します。

```racket
; An LRCD (for launching rocket countdown) is one of:
; – "resting"
; – a Number between -3 and -1
; – a NonnegativeNumber
; interpretation:
;   "resting" represents a grounded rocket.
;   a number in [-3, -1] denotes the countdown phase.
;   a nonnegative number denotes the height of the rocket in flight.
```

このデータ型に対するシミュレーションプログラムを以下のように構成します：
*   **定数定義**:
```racket
(define HEIGHT 300)
(define WIDTH 100)
(define YDELTA 3)
(define BACKG (empty-scene WIDTH HEIGHT))
(define ROCKET (rectangle 5 30 "solid" "red"))
(define CENTER (/ (image-height ROCKET) 2))
```

*   **描画関数 `show`**:
```racket
; LRCD -> Image
(define (show x)
  (cond
    [(string? x)
     (place-image ROCKET 10 (- HEIGHT CENTER) BACKG)]
    [(<= -3 x -1)
     (place-image (text (number->string x) 20 "red")
                  10 (* 3/4 WIDTH)
                  (place-image ROCKET 10 (- HEIGHT CENTER) BACKG))]
    [(>= x 0)
     (place-image ROCKET 10 (- x CENTER) BACKG)]))
```

*   **キーイベントハンドラ `launch`**:
```racket
; LRCD KeyEvent -> LRCD
(define (launch x ke)
  (cond
    [(string? x) (if (string=? " " ke) -3 x)]
    [(<= -3 x -1) x]
    [(>= x 0) x]))
```

*   **時間経過ハンドラ `fly`**（カウントダウンおよび飛行のシミュレーション）:
```racket
; LRCD -> LRCD
(define (fly x)
  (cond
    [(string? x) x]
    [(<= -3 x -1) (if (= x -1) HEIGHT (+ x 1))]
    [(>= x 0) (- x YDELTA)]))
```

*   **メインプログラム実行**:
```racket
(define (main2 s)
  (big-bang s
    [to-draw show]
    [on-key launch]
    [on-tick fly 1])) ; クロックティックは1秒ごと
```

**Exercise 53〜57.** 以上のカウントダウン付きロケットプログラムを実際に DrRacket 上で構築・実行し、終了処理（`stop-when`）の追加などの拡張を試みよ。

### 4.6 項目化を用いた設計 (Designing with Itemizations)

これまでに学んだ内容を基に、「From Functions to Programs」で紹介した設計レシピを拡張し、列挙、区間、項目化を扱う関数を体系的に設計するためのステップを定義します。

以下の消費税計算を例に、ステップを確認します：
> 安価な商品（1,000ドル未満）には税金がかかりません。
> 高級品（10,000ドル超）には8%の税率が適用されます。
> それらの中間の商品には5%の税金がかかります。

1.  **データ定義 (Data Definition)**
    問題文で区別されている状況をカバーする、重複のない明確なデータ定義を記述します。
    ```racket
    ; A Price falls into one of three intervals:
    ; — 0 through 1000 (exclusive)
    ; — 1000 through 10000 (exclusive)
    ; — 10000 and above.
    ```
2.  **署名、目的文、ヘッダ (Signature, Purpose, Header)**
    ```racket
    ; Price -> Number
    ; computes the amount of tax charged for p
    (define (sales-tax p) 0)
    ```
3.  **具体的な例 (Examples)**
    それぞれの区間、および境界値からテストデータを抽出します（境界値の扱いについて専門家や仕様書に確認することが重要です）。
    ```racket
    (check-expect (sales-tax 537) 0)
    (check-expect (sales-tax 1000) (* 0.05 1000))
    (check-expect (sales-tax 12017) (* 0.08 12017))
    ```
4.  **テンプレート (Template)**
    データ定義の項目（サブクラス）の数と同じ数の `cond` 節を持つ条件分岐のアウトラインを作成します。
    ```racket
    (define (sales-tax p)
      (cond
        [(and (<= 0 p) (< p 1000)) ...]
        [(and (<= 1000 p) (< p 10000)) ...]
        [(>= p 10000) ...]))
    ```
5.  **定義の完成 (Definition)**
    各節の空欄 `...` を、パラメータを用いた具体的な計算式で埋めます。
    ```racket
    (define (sales-tax p)
      (cond
        [(and (<= 0 p) (< p 1000)) 0]
        [(and (<= 1000 p) (< p 10000)) (* 0.05 p)]
        [(>= p 10000) (* 0.08 p)]))
    ```
6.  **テスト (Testing)**
    テストを実行し、すべての `cond` 節がカバーされ、期待通りの値が返ることを確認します。

### 4.7 有限状態世界 (Finite State Worlds)

信号機のように、「有限個の状態」と「状態間の遷移ルール」によって定義されるシステムを**有限状態機械 (Finite State Machine: FSM)** または**有限状態オートマトン (Finite State Automaton: FSA)** と呼びます。

信号機のシミュレーションプログラムは、以下の要素で構成できます：
*   **状態の定義**: `"red"`, `"green"`, `"yellow"`
*   **時間経過による遷移関数 `tl-next`**:
    ```racket
    ; TrafficLight -> TrafficLight
    (define (tl-next cs)
      (cond
        [(string=? "red" cs) "green"]
        [(string=? "green" cs) "yellow"]
        [(string=? "yellow" cs) "red"]))
    ```
*   **描画関数 `tl-render`**: 状態に応じて適切な色のライトを表示する画像を生成する。

**Exercise 59〜61.** 信号機の有限状態シミュレーションプログラムを完成させよ。

#### 自動ドアクローザーのシミュレーション

自動ドアは以下の状態と遷移ルールを持ちます：
*   **状態 (`DoorState`)**:
    *   `LOCKED`（施錠中）
    *   `CLOSED`（閉扉中・未施錠）
    *   `OPEN`（開扉中）
*   **イベントルール**:
    *   `LOCKED` 状態で `"u"` キーを押すと `CLOSED` になる（解錠）。
    *   `CLOSED` 状態で `"l"` キーを押すと `LOCKED` になる（施錠）。
    *   `CLOSED` 状態でスペースキー `" "` を押すと `OPEN` になる（開扉）。
    *   `OPEN` 状態からは、クロックティック（時間経過）により自動的に `CLOSED` に戻る。

このシミュレーションプログラムは以下のように設計できます：

```racket
(define LOCKED "locked")
(define CLOSED "closed")
(define OPEN "open")

; DoorState -> DoorState
; closes an open door over the period of one tick
(define (door-closer state-of-door)
  (cond
    [(string=? LOCKED state-of-door) LOCKED]
    [(string=? CLOSED state-of-door) CLOSED]
    [(string=? OPEN state-of-door) CLOSED]))

; DoorState KeyEvent -> DoorState
; turns key event k into an action on state s
(define (door-action s k)
  (cond
    [(and (string=? LOCKED s) (string=? "u" k)) CLOSED]
    [(and (string=? CLOSED s) (string=? "l" k)) LOCKED]
    [(and (string=? CLOSED s) (string=? " " k)) OPEN]
    [else s]))

; DoorState -> Image
; translates the state s into a large text image
(define (door-render s)
  (text s 40 "red"))

; DoorState -> DoorState
(define (door-simulation initial-state)
  (big-bang initial-state
    [on-tick door-closer 3] ; 3秒ごとにドアが閉まる
    [on-key door-action]
    [to-draw door-render]))
```

**Exercise 62.** ドアシミュレータを DrRacket で実行し、動作を確認せよ。

## 5 構造の追加 (Adding Structure)

ある架空の完璧な部屋の床と天井の間で、直線の垂直線上を往復して跳ね返るボールをシミュレートするワールドプログラムを設計したいとしましょう。ボールは 1 クロックティックごとに常に 2 ピクセル移動すると仮定します。デザインレシピに従うなら、最初の目標は時間の経過に伴って変化するもののデータ表現を開発することです。ここで、ボールの位置と方向は時間の経過とともに変化しますが、これは 2 つの値であるのに対し、big-bang が追跡するのは 1 つの値だけです。したがって、1 つのデータがどのようにして 2 つの変化する情報量を表現できるのかという疑問が生じます。

> **Note:** 数学者は、元の数値を復元できるように 2 つの数値を 1 つの数値に「マージ（統合）」するトリックを知っています。プログラマーは、このようなトリックがプログラムの真の意図を曖昧にするため、悪質であると考えています。

同じ疑問を提起するもう 1 つのシナリオを紹介します。あなたの携帯電話は、要するにプラスチックで包まれた数百万行のコードです。とりわけ、携帯電話は連絡先を管理しています。各連絡先には、名前、電話番号、メールアドレス、そしておそらくその他のいくつかの情報が含まれています。多数の連絡先がある場合、単一の連絡先は 1 つのデータとして表現するのが最適です。そうでなければ、さまざまな情報が誤って混ざってしまう可能性があるからです。

このようなプログラミング上の問題があるため、すべてのプログラミング言語は、複数のデータを 1 つの複合データ（compound data）に組み合わせるメカニズムと、必要に応じて構成値を抽出する方法を提供しています。本章では、BSL のメカニズムであるいわゆる構造体型定義（structure type definitions）と、複合データを扱うプログラムの設計方法について紹介します。

### 5.1 位置から posn 構造体へ (From Positions to posn Structures)

ワールドキャンバス上の位置は、左端からの距離と上端からの距離という 2 つのデータによって一義的に特定されます。前者は x 座標（x-coordinate）と呼ばれ、後者は y 座標（y-coordinate）と呼ばれます。

基本的に BSL プログラムである DrRacket は、そのような位置を posn 構造体（posn structures）で表現します。posn 構造体は 2 つの数を 1 つの値に組み合わせます。2 つの数を消費して posn を作成する操作 make-posn を使用して、posn 構造体を作成できます。例えば、

```racket
(make-posn 3 4)
```

は、x 座標が 3 で y 座標が 4 である posn 構造体を作成する式です。

posn 構造体は、数やブーリアン、文字列と同じステータスを持ちます。特に、基本操作と関数の両方が構造体を消費し、生成することができます。また、プログラムは posn 構造体に名前を付けることができます。

```racket
(define one-posn (make-posn 8 6))
```

Stop! 座標の観点から one-posn を説明してください。

他のことを行う前に、posn 構造体の計算規則（laws of computation）を見てみましょう。そうすることで、posn 構造体を処理する関数を作成し、それが何を計算するかを予測できるようになります。

### 5.2 posn を用いた計算 (Computing with posns)

関数や関数の規則は初等代数から完全に馴染み深いものですが、posn 構造体は新しい考え方のように見えます。とはいえ、posn の概念は、以前に出会ったことがあるかもしれない直交座標系の点や平面上の位置に似ているはずです。

```
+------------------------------+
| Figure 29: A Cartesian point |
|                              |
| [image: pict_65.png]         |
| [image: pict_65.png]         |
+------------------------------+
```

直交座標の要素を選択することも馴染みのあるプロセスです。例えば、先生が「figure 29 のグラフを見て、px と py が何であるか教えてください」と言った場合、あなたはそれぞれ 31 と 26 と答えるでしょう。なぜなら、p から放射状に伸びる垂直線と水平線が軸と交わる値を読み取る必要があることを知っているからです。

> **Note:** plot ライブラリを提供してくれた Neil Toronto に感謝します。

この考え方は BSL で表現できます。次を定義領域（definitions area）に追加すると仮定します。

```racket
(define p (make-posn 31 26))
```

定義領域に追加し、*RUN* をクリックして、次の操作（interactions）を実行します。

```racket
> (posn-x p)
31
> (posn-y p)
26
```

p を定義することは、直交座標平面上に点を打つようなものです。posn-x と posn-y を使用することは、p にインデックスで下付き文字を付けるようなものです（px と py）。

計算の観点から言えば、posn 構造体には 2 つの方程式が付属しています。

> (posn-x(make-posnx0y0)) == x0(posn-y(make-posnx0y0)) == y0

DrRacket は計算の際、これらの方程式を使用します。posn 構造体を含む計算の例を次に示します。

```racket
(posn-x p)
==; DrRacket replaces p with (make-posn 31 26)
(posn-x (make-posn 31 26))
==; DrRacket uses the law for posn-x
31
```

Stop! 上記の 2 番目の相互作用をご自身の計算で確認してください。また、DrRacket のステッパー（stepper）を使用してダブルチェックしてください。

### 5.3 posn を用いたプログラミング (Programming with posn)

次に、キャンバスの原点からある位置までの距離を計算する関数の設計について考えてみましょう。

> [image: pict_66.png]

画像は、「距離」が指定された点からキャンバスの左上隅までの最も直接的な経路（「直線距離」）の長さであることを明確にしています。

目的記述（purpose statement）とヘッダーを以下に示します。

```racket
; computes the distance of ap to the origin
(define (distance-to-0 ap)
  0)
```

鍵となるのは、distance-to-0 が単一の値、すなわち何らかの posn を消費することです。これは単一の値、すなわちその位置から原点までの距離を生成します。

例を作成するには、この距離の計算方法を知る必要があります。座標のいずれか一方が 0 である点の場合、結果はもう一方の座標になります。

```racket
(check-expect (distance-to-0 (make-posn 0 5)) 5)
(check-expect (distance-to-0 (make-posn 7 0)) 7)
```

一般の場合については、自力で公式を考え出すこともできますし、幾何学の授業で習った公式を思い出すこともできます。ご存知のように、これはあなたが持っているかもしれないドメイン知識ですが、持っていない場合に備えて提供します。結局のところ、このドメイン知識はコンピューターサイエンスではありません。それでは、(x,y) に対する距離の公式を改めて示します。

> [image: pict_67.png]

この公式を踏まえると、さらにいくつかの機能的な例を簡単に作成できます。

```racket
(check-expect (distance-to-0 (make-posn 3 4)) 5)
(check-expect (distance-to-0 (make-posn 8 6)) 10)
(check-expect (distance-to-0 (make-posn 5 12)) 13)
```

念のためにお伝えすると、結果が簡単に計算できるように例を調整しています。すべての posn 構造体でこうなるわけではありません。

Stop! 例の x 座標と y 座標を公式に代入してください。5 つの例すべてについて期待される結果を確認してください。

次に、関数の定義に目を向けることができます。これらの例は、distance-to-0 の設計が異なる状況を区別する必要がないことを示唆しています。常に与えられた posn 構造体の内部にある x 座標と y 座標から距離を計算するだけです。しかし、関数は与えられた posn 構造体からこれらの座標を選択しなければなりません。そして、そのために posn-x と posn-y という基本操作を使用します。具体的には、ap は与えられた未知の posn 構造体の名前であるため、関数は (posn-xap) と (posn-yap) を計算する必要があります。

```racket
(define (distance-to-0 ap)
  (... (posn-x ap)...
... (posn-y ap)...))
```

このテンプレートと例を使用すれば、残りは簡単です。

```racket
(define (distance-to-0 ap)
  (sqrt
    (+ (sqr (posn-x ap))
       (sqr (posn-y ap)))))
```

この関数は、x 座標と y 座標を表す (posn-xap) と (posn-yap) を 2 乗し、その結果を足し合わせ、平方根をとります。DrRacket を使用すると、新しい関数が例に対して適切な結果を生成することを迅速に確認できます。

**Exercise 63.** 以下の式を:

- (distance-to-0(make-posn34))
- (distance-to-0(make-posn6(*24)))
- (+(distance-to-0(make-posn125))10)

手計算で評価してください。すべてのステップを示してください。sqr はその計算を単一のステップで実行すると仮定します。DrRacket のステッパー（stepper）で結果を確認してください。

**Exercise 64.** ある点から原点へのマンハッタン距離（Manhattan distance）は、マンハッタンに見られる格子状の道路網に沿った経路を考慮します。以下に 2 つの例を示します。

> [image: pict_68.png] [image: pict_69.png]

左側の例は「直接（direct）」戦略を示しており、必要な分だけ左に進み、続いて必要な分だけ上に進みます。これに対し、右側の例は「ランダムウォーク（random walk）」戦略を示しており、目的地（ここでは原点）に到達するまで、数ブロック左に進み、数ブロック上に進む、といった動作を繰り返します。

Stop! どちらの戦略に従うかは重要でしょうか？

与えられた posn から原点までのマンハッタン距離を計測する関数 manhattan-distance を設計してください。


### 5.4 構造体型の定義 (Defining Structure Types)

数値やブール値とは異なり、`posn` のような構造体は通常、プログラミング言語に最初から備わっているわけではありません。構造型を定義するためのメカニズムのみが提供されており、残りはプログラマーに委ねられています。これは BSL においても同様です。

構造型定義は、定数定義や関数定義とは異なる、定義のもう1つの形式です。DrRacket の制作者が BSL で `posn` 構造型をどのように定義したかを以下に示します：

```racket
(define-struct posn [x y])
```

一般に、構造型定義は次のような形式を持ちます：

> **Note:** 構造型定義で角括弧（ブラケット）を使用するのは慣習であり、必須ではありません。角括弧を使うことでフィールド名が目立つようになります。角括弧を丸括弧（ペアレンセシス）に置き換えてもまったく問題ありません。

```racket
(define-struct StructureName [FieldName...])
```

キーワード `define-struct` は、新しい構造型の導入を示します。その後に構造体の名前が続きます。構造型定義の3番目の部分は、角括弧で囲まれた一連の名前であり、これらがフィールドです。

構造型定義は、実際には関数を定義します。しかし、通常の関数定義とは異なり、構造型定義は同時に複数の関数を定義します。具体的には、以下の3種類の関数を定義します：

- 1つのコンストラクター：構造体インスタンスを作成する関数です。フィールドの数と同じだけの値を受け取ります。前述したように、構造体は構造体インスタンスの略称です。「構造型」というフレーズは、考えられるすべてのインスタンスの集まりを指す総称です。
- フィールドごとに1つのセレクター：構造体インスタンスからそのフィールドの値を抽出します。
- 1つの構造体述語：通常の述語と同様に、そのインスタンスを他のすべての種類の値から区別します。

プログラムは、これらがあたかも通常の関数や組み込みプリミティブであるかのように使用できます。

面白いことに、構造型定義は、新しく作成する多様な操作のために名前を自動的に生成します。コンストラクターの名前として、構造体名の前に “make-” という接頭辞を付けます。セレクターの名前としては、構造体名の後に各フィールド名を接尾辞として付けます。最後に、述語は構造体名に “?” を付けただけであり、音読するときは「ハ（huh）」と発音されます。

この命名規約は複雑で、混乱を招くように思えるかもしれません。しかし、少し練習すればコツを掴むことができます。これは、`posn` 構造体に付属している関数についても説明しています。`make-posn` はコンストラクターであり、`posn-x` と `posn-y` はセレクターです。まだ `posn?` には出くわしていませんが、それが存在することが分かりました。次の章では、これらの述語の役割について詳しく説明します。

**Exercise 65.** 次の構造型定義を見てみましょう：

- (define-structmovie[titleproduceryear])
- (define-structperson[namehaireyesphone])
- (define-structpet[namenumber])
- (define-structCD[artisttitleprice])
- (define-structsweater[materialsizeproducer])

それぞれが導入する関数（コンストラクター、セレクター、述語）の名前を書き出してください。

`posn` 構造体の話はひとまずこれくらいにしましょう。携帯電話の連絡先などを管理するために使用できるような構造型定義を見てみましょう：

```racket
(define-struct entry [name phone email])
```

この定義が導入する関数の名前は以下のとおりです：

- `make-entry`：3つの値を受け取り、`entry` のインスタンスを構築します。
- `entry-name`、`entry-phone`、`entry-email`：`entry` の1つのインスタンスを受け取り、3つのフィールド値のいずれか1つを抽出します。
- `entry?`：述語です。

それぞれの `entry` は3つの値を組み合わせるため、次の式：

```racket
(make-entry "Al Abe" "666-7771" "lee@x.me")
```

は、`name` フィールドに "Al Abe"、`phone` フィールドに "666-7771"、`email` フィールドに "lee@x.me" を持つ `entry` 構造体を作成します。

**Exercise 66.** 練習問題 65 の構造型定義を再確認してください。どのフィールドにどんな種類の値が入るか妥当な推測を行ってください。その上で、構造型定義ごとに少なくとも1つのインスタンスを作成してください。

あらゆる構造型定義は、他のすべての構造体とは区別される、新しい種類の構造体を導入します。プログラマーがこのような表現力を求めるのは、構造体名によって意図を伝えたいからです。構造体が作成、選択、またはテストされるすべての場所で、プログラムのコードは読み手にその意図を明示的に思い出させます。もしコードの将来の読み手が存在しないのであれば、プログラマーは1つのフィールドを持つ構造体用に1つの構造体定義を、2つのフィールドを持つ構造体用に別の定義を、3つのフィールドを持つ構造体用に3つ目の定義を…といった使い分けで済ませることもできるでしょう。

この文脈において、別のプログラミング問題を検討してみましょう：

> **サンプル問題** 本章の冒頭で簡単に触れた「跳ねるボール」を扱うプログラムのための構造型定義を開発しなさい。ボールの位置は単一の数値、すなわち上端からのピクセル距離です。ボールの一定の速さは、クロックの1目盛りごとに移動するピクセル数です。ボールの速度は、速さに移動方向を加えたものです。

ボールは直線的な垂直線に沿って移動するため、その速度をデータ表現するには数値が完全に適しています：

- 正の数値はボールが下に移動することを意味します。
- 負の数値はボールが上に移動することを意味します。

この領域知識を利用して、構造型定義を定式化することができます：

```racket
(define-struct ball [location velocity])
```

両方のフィールドに数値が入るため、`(make-ball10-3)` は良いデータ例となります。これは、上端から10ピクセルの位置にあり、クロックの1目盛りあたり3ピクセルの速さで上に移動するボールを表しています。

原理的には、`ball` 構造体が `posn` 構造体と同じように、単に2つの数値を組み合わせているだけである点に注目してください。プログラムに `(ball-velocitya-ball)` という式が含まれている場合、そのプログラムがボールとその速度の表現を扱っていることが即座に伝わります。対照的に、もしプログラムが代わりに `posn` 構造体を使用していた場合、`(posn-ya-ball)` はコードの読み手に、その式が y 座標に関するものであるという誤解を与える可能性があります。

**Exercise 67.** 跳ねるボールを表現する別の方法を以下に示します：

```racket
(define SPEED 3)
(define-struct balld [location direction])
(make-balld 10 "up")
```

このコード断片を解釈し、`balld` の他のインスタンスを作成してください。

構造体は数値やブール値や文字列と同様に値であるため、ある構造体のインスタンスが別のインスタンスの内部に現れるのは理にかなっています。ゲームオブジェクトを考えてみましょう。跳ねるボールとは異なり、そのようなオブジェクトは常に垂直線に沿って移動するとは限りません。代わりに、キャンバス上を「斜め」に移動します。2次元のワールドキャンバス上を移動するボールの位置と速度の両方を記述するには、各方向に1つずつ、計2つの数値が必要になります。位置のパートについては、2つの数値が x 座標と y 座標を表します。速度は水平方向と垂直方向の変化を記述します。言い換えれば、これらの「変化の数値」をそれぞれの座標に加算することで、オブジェクトが次にどこに位置するのかを突き止めることができます。

> **Note:** オブジェクトの次の位置を得るために位置に速度を加算するというのは、物理学の教えるところです。開発者は、どの領域について誰に尋ねるべきかを学ぶ必要があります。

明らかに、`posn` 構造体は位置を表すことができます。速度については、`vel` 構造型を定義します：

```racket
(define-struct vel [deltax deltay])
```

これには `deltax` と `deltay` という2つのフィールドがあります。「デルタ」という単語は、物理的活動のシミュレーションにおいて変化について語る際に一般的に使用され、x と y の部分はどの軸が関係しているかを示します。

これで、`ball` のインスタンスを使用して `posn` 構造体と `vel` 構造体を組み合わせることで、直線的に移動するものの、必ずしも垂直（または水平）線に沿ってのみ移動するとは限らないボールを表現できます：

```racket
(define ball1
  (make-ball (make-posn 30 40) (make-vel -10 5)))
```

このインスタンスを解釈する1つの方法は、左端から30ピクセル、上端から40ピクセルの位置にあるボールを考えることです。このボールは、クロックの1目盛りごとに左に向かって10ピクセル移動します。なぜなら、x 座標から10ピクセルを引くと左端に近づくからです。垂直方向に関しては、ボールはクロックの1目盛りあたり5ピクセルの速さで落下します。なぜなら、y 座標に正の数を加えると上端からの距離が増加するからです。

**Exercise 68.** ボールのネストされたデータ表現に代わる方法として、4つのプロパティを追跡するために4つのフィールドを使用するものがあります：

> **Note:** さらなる代替案として、複素数を使用する方法もあります。複素数について知っている方は、位置と速度の両方に複素数を使用するデータ表現について考えてみてください。例えば BSL では、`4-3i` は複素数であり、位置や速度 ( 4,- 3 ) を表現するために使用できます。

```racket
(define-struct ballf [x y deltax deltay])
```

プログラマーはこれをフラット表現と呼びます。`ball1` と同じ解釈を持つ `ballf` のインスタンスを作成してください。

ネストされた構造体の2つ目の例として、連絡先リストの例を簡単に見てみましょう。多くの携帯電話の連絡先リストは、1つの名前に対して複数の電話番号（自宅用、職場用、携帯電話用など）を保持できるようになっています。電話番号については、市外局番と加入者番号の両方を含めたいと考えています。これは情報をネストするため、ネストされたデータ表現を作成するのが最善です：

```racket
(define-struct centry [name home office cell])

(define-struct phone [area number])

(make-centry "Shriram Fisler"
             (make-phone 207 "363-2421")
             (make-phone 101 "776-1099")
             (make-phone 208 "112-9981"))
```

ここでの意図は、連絡先リストの1つのエントリが4つのフィールド（1つの名前と3つの電話レコード）を持つことです。後者は市外局番と加入者番号を分離する `phone` のインスタンスで表現されます。

要約すると、情報をネストさせるのは自然なことです。このような情報をデータで表現する最善の方法は、ネストされた構造体インスタンスを用いてそのネスト構造を反映させることです。そうすることで、プログラムのアプリケーション領域においてデータを解釈することが容易になり、情報の例からデータへの移行も明快になります。もちろん、情報とデータの間をどのように行き来するかを規定するのは、本来データ定義の役割です。しかし、構造型定義に対するデータ定義を学ぶ前に、まず構造体を用いた計算や思考について体系的に見ていきましょう。
どのように指定するかは、本来データ定義の役割です。しかし、構造型定義のためのデータ定義を学ぶ前に、まず構造体を用いた計算とそれに対する考え方について体系的に見ていきましょう。


### 5.5 構造体を使った計算 (Computing with Structures)

構造体型はデカルト座標（Cartesian points）を2つの方法で一般化します。第一に、構造体型は任意の数のフィールドを指定できます。ゼロ、1つ、2つ、3つ、その他いくらでも可能です。第二に、構造体型はフィールドに名前を付けるのであり、番号を振るわけではありません。これはプログラマーがコードを読むのに役立ちます。なぜなら、姓（family name）が7番目のフィールドにあると思い出すよりも、`last-name` というフィールドにあると思い出す方がはるかに簡単だからです。

> **注:** ほとんどのプログラミング言語は、数値のフィールド名を使用する構造体風のデータもサポートしています。

同じ趣旨で、構造体インスタンスを使った計算は、デカルト座標の操作を一般化したものです。この考え方をよく理解するために、まず構造体インスタンスをフィールドの数だけ区画（compartment）を持つ鍵付きの箱（lockbox）として考える図的な捉え方を見てみましょう。以下は、

```racket
(define pl (make-entry "Al Abe" "666-7771" "lee@x.me"))
```

のような図による表現です。

> [image: pict_70.png]

箱の斜体ラベルは、それが特定の構造体型のインスタンスであることを識別します。各区画にもラベルが付けられています。以下は別のインスタンスです。

```racket
(make-entry "Tara Harp" "666-7770" "th@smlu.edu")
```

これは内容が異なりますが、同様の箱の図に対応します。

> [image: pict_71.png]

驚くことではありませんが、入れ子になった構造体インスタンスは、箱の中に箱が入れ子になった図になります。したがって、前述の `ball1` は次の図と等価です。

> [image: pict_72.png]

この場合、外側の箱には2つの箱が含まれており、各フィールドに1つずつ割り当てられています。

**Exercise 69.** 演習65の解答に対する箱表現を描きなさい。

このイメージの文脈では、セレクタ（selector）は鍵のようなものです。特定の種類の箱の特定の区画を開け、それにより保持者はその中身を取り出すことができます。したがって、前述の `pl` に `entry-name` を適用すると文字列が得られます。

```racket
> (entry-name pl)
"Al Abe"
```

しかし、`posn` 構造体に `entry-name` を適用するとエラーが送出されます。

```racket
> (entry-name (make-posn 42 5))
entry-name:expects an entry, given (make-posn 42 5)
```

区画に箱が含まれている場合、目的の数値を得るために2つのセレクタを続けて使用する必要があるかもしれません。

```racket
> (ball-velocity ball1)
(make-vel -10 5)
```

`ball1` に `ball-velocity` を適用すると、`vel` のインスタンスである `velocity` フィールドの値が取り出されます。x軸に沿った速度を得るには、最初の選択結果に対してセレクタを適用します。

```racket
> (vel-deltax (ball-velocity ball1))
-10
```

内側の式が `ball1` から速度を取り出すので、外側の式は `deltax` フィールドの値を取り出します。この場合は -10 です。

この対話（interaction）結果は、構造体インスタンスが値であることを示してもいます。DrRacket は数値のような単純な値と同様に、入力されたとおりにそのまま印字します。

```racket
> (make-vel -10 5)
(make-vel -10 5)
> (make-entry "Tara Harp" "666-7770" "th@smlu.edu")
(make-entry "Tara Harp" "666-7770" "th@smlu.edu")
> (make-centry    "Shriram Fisler"    (make-phone 207 "363-2421")    (make-phone 101 "776-1099")    (make-phone 208 "112-9981"))
(make-centry...)
```

Stop! 自分でこの最後の対話を試してみて、正しい結果を確認してください。

一般的に言えば、構造体型の定義は新しい関数や値を生成する新しい方法を作成するだけでなく、DrRacket の知識に新しい計算規則（laws of computation）を追加します。これらの規則は「posn による計算（Computing with posns）」における `posn` 構造体の規則を一般化したものであり、例を通して理解するのが最も適切です。

DrRacket が2つのフィールドを持つ構造体型定義に遭遇すると、

```racket
(define-struct ball [location velocity])
```

セレクタごとに1つずつ、2つの規則を導入します。

> (ball-location(make-balll0v0)) == l0(ball-velocity(make-balll0v0)) == v0

異なる構造体型定義に対しては、同様の規則が導入されます。したがって、

```racket
(define-struct vel [deltax deltay])
```

DrRacket はこれら2つの規則を知識に追加します。

> (vel-deltax(make-veldx0dy0)) == dx0(vel-deltay(make-veldx0dy0)) == dy0

これらの規則を使用すると、前述の対話を説明できるようになります。

```racket
(vel-deltax (ball-velocity ball1))
==; DrRacket replaces ball1 with its value
(vel-deltax
  (ball-velocity
    (make-ball (make-posn 30 40) (make-vel -10 5))))
==; DrRacket uses the law for ball-velocity
(vel-deltax (make-vel -10 5))
==; DrRacket uses the law for vel-deltax
-10
```

**Exercise 70.** 以下の構造体型定義に対する計算規則を詳細に書き下しなさい：

```racket
(define-struct centry [name home office cell])
(define-struct phone [area number])
```

DrRacket のステッパーを使用して、次の式の値が 101 であることを確認しなさい：

```racket
(phone-area
 (centry-office
  (make-centry "Shriram Fisler"
    (make-phone 207 "363-2421")
    (make-phone 101 "776-1099")
    (make-phone 208 "112-9981"))))
```

述語（predicate）は、構造体型定義を理解するために説明しなければならない最後の概念です。前述のように、すべての構造体型定義は1つの新しい述語を導入します。DrRacket はこれらの述語を使用して、セレクタが適切な種類の値に適用されているかどうかを検出します。次の章では、この考え方を詳しく説明します。ここでは、これらの述語が「算術（arithmetic）」の述語とまったく同じであることをお伝えしたいだけです。`number?` が数値かを識別し、`string?` が文字列かを識別するように、`posn?` や `entry?` といった述語は `posn` 構造体や `entry` 構造体を識別します。これらがどのように機能するかについての考えは、対話領域での実験で確認できます。定義領域に以下の定義が含まれていると仮定します。

```racket
(define ap (make-posn 7 0))

(define pl (make-entry "Al Abe" "666-7771" "lee@x.me"))
```

もし `posn?` が `posn` を他のすべての値から区別する述語であるなら、数値に対しては `#false` を返し、`ap` に対しては `#true` を返すことが期待されます。

```racket
> (posn? ap)
#true
> (posn? 42)
#false
> (posn? #true)
#false
> (posn? (make-posn 3 4))
#true
```

同様に、`entry?` は `entry` 構造体を他のすべての値から区別します。

```racket
> (entry? pl)
#true
> (entry? 42)
#false
> (entry? #true)
#false
```

一般に、述語は同名のコンストラクタで構築された値だけを正確に識別します。「合間 1: 初級学生言語（Intermezzo 1: Beginning Student Language）」ではこの規則を詳しく説明しており、BSL の計算規則も1箇所にまとめています。

**Exercise 71.** DrRacket の定義領域に以下を配置しなさい：

```racket
; distances in terms of pixels:
(define HEIGHT 200)
(define MIDDLE (quotient HEIGHT 2))
(define WIDTH  400)
(define CENTER (quotient WIDTH 2))

(define-struct game [left-player right-player ball])

(define game0
  (make-game MIDDLE MIDDLE (make-posn CENTER CENTER)))
```

*RUN*（実行）をクリックし、以下の式を評価しなさい：

```racket
(game-ball game0)
(posn? (game-ball game0))
(game-left-player game0)
```

ステップごとの計算で結果を説明しなさい。DrRacket のステッパーを使用して、自分の計算を再確認しなさい。


### 5.6 構造体を用いたプログラミング (Programming with Structures)

適切なプログラミングにはデータ定義が必要です。構造体型定義の導入によって、データ定義は興味深いものになります。データ定義とは、情報をデータへと表現する方法と、そのデータを情報として解釈する方法を提供するものであることを思い出してください。構造体型の場合、これはどのフィールドにどのような種類のデータが入るのかを記述することを意味します。いくつかの構造体型定義においては、そうした記述を策定することは簡単で明白です。

```racket
(define-struct posn [x y])
; A Posn is a structure:
;   (make-posn Number Number)
; interpretation a point x pixels from left, y from top
```

posn を作成するために他の種類のデータを使用することは何の意味もなしません。同様に、連絡先リストのエントリのための構造体型定義である entry のすべてのフィールドも、前の節での使い方に従えば、明らかに文字列であることが想定されています。

```racket
(define-struct entry [name phone email])
; An Entry is a structure:
;   (make-entry String String String)
; interpretation a contact's name, phone#, and email
```

posn と entry のどちらについても、読者はアプリケーションドメインにおけるこれらの構造体のインスタンスを簡単に解釈できます。

この単純さを、明らかに少なくとも2通りの異なる解釈が可能な ball の構造体型定義と対比してみましょう。

```racket
(define-struct ball [location velocity])
; A Ball-1d is a structure:
;   (make-ball Number Number)
; interpretation 1 distance to top and velocity
; interpretation 2 distance to left and velocity
```

プログラムでそのうちのどちらを使用するにしても、一貫してそれを使い続けなければなりません。しかし、「構造体型の定義 (Defining Structure Types)」で示されているように、ball 構造体をまったく異なる方法で使用することも可能です。

```racket
; A Ball-2d is a structure:
;   (make-ball Posn Vel)
; interpretation a 2-dimensional position and velocity

(define-struct vel [deltax deltay])
; A Vel is a structure:
;   (make-vel Number Number)
; interpretation (make-vel dx dy) means a velocity of
; dx pixels [per tick] along the horizontal and
; dy pixels [per tick] along the vertical direction
```

ここでは、ワールドキャンバス上を直線状に移動するボールのデータ表現を記述するために、Ball-1d とは区別される2つ目のデータコレクションを Ball-2d と命名しています。要するに、まったく同じ構造体型を2つの異なる方法で使用することが可能です。もちろん、1つのプログラム内では、唯一の用途だけに絞るのが最善です。さもなければ、自ら問題を招くことになります。

また、Ball-2d は私たちが定義した別のデータ定義、すなわち Vel の定義を参照しています。これまで他のすべてのデータ定義は組み込みのデータコレクション（Number、Boolean、String）を参照していましたが、自分のデータ定義の1つが別のデータ定義を参照することは完全に許容されますし、実際によくあることです。
とです。


**Exercise 72.** 上記の phone 構造型定義に対し、与えられた例に適合するデータ定義を作成しなさい。

次に、以下の構造型定義を用いて電話番号のデータ定義を作成しなさい：

```racket
(define-struct phone# [area switch num])
```

歴史的には、最初の3桁が市外局番（area code）、次の3桁があなたの地域の電話交換局（phone switch (exchange)）のコード、最後の4桁はその地域内での個人の電話番号を表します。これら3つのフィールドの内容を、区間（interval）を用いて可能な限り正確に記述しなさい。

この時点で、データ定義が実際に何を意味するのか疑問に思っているかもしれません。この疑問とその答えは次節のトピックです。今のところは、プログラム設計においてデータ定義をどのように使うかを示します。

文脈を設定するための問題文を以下に示します：

> Sample Problem あなたのチームは、赤い点を [image: pict_73.png] キャンバス上で移動させ、プレイヤーがマウスを使ってその点をリセットできるようにするインタラクティブなゲームプログラムを設計しています。チームでここまでに共同で進めた内容は以下の通りです：
> (define MTS (empty-scene 100 100))(define DOT (circle 3 "solid" "red"));APosn represents the state of the world.;Posn ->Posn(define (main p0) (big-bang p0 [on-tick x+] [on-mouse reset-dot] [to-draw scene+dot]))あなたのタスクは、指定された位置にある空のキャンバスに赤い点を追加する関数 scene+dot を設計することです。

問題の文脈によって、関数のシグネチャが決定されます：

```racket
; Posn -> Image
; adds a red spot to MTS at p
(define (scene+dot p) MTS)
```

目的記述（purpose statement）の追加は簡単です。「関数の設計（Designing Functions）」で述べるように、目的記述は関数のパラメータを使用して関数が何を計算するかを表現します。

ここで、いくつかの例を作成し、それらをテストとして定式化します：

```racket
(check-expect (scene+dot (make-posn 10 20))
              (place-image DOT 10 20 MTS))
(check-expect (scene+dot (make-posn 88 73))
              (place-image DOT 88 73 MTS))
```

この関数が Posn を受け取る（consume）ことから、関数は x フィールドと y フィールドの値を取り出すことができるとわかります：

```racket
(define (scene+dot p)
  (... (posn-x p)... (posn-y p)...))
```

関数の本体の中にこれらの追加の構成要素が揃えば、残りの定義は簡単です。place-image を使用して、この関数は p に含まれる座標の位置で MTS に DOT を配置します：

```racket
(define (scene+dot p)
  (place-image DOT (posn-x p) (posn-y p) MTS))
```

関数は構造体を生成する（produce）こともあります。まさにそのようなタスクが含まれているため、上記の例題を再開しましょう：

> Sample Problem 同僚が Posn を受け取り、x 座標を 3 増加させる関数 x+ を定義するよう頼まれました。

x+ 関数がクロックティック（clock tick）を処理することを思い出してください。

scene+dot の設計の最初のいくつかのステップを適応させることができます：

```racket
; Posn -> Posn
; increases the x-coordinate of p by 3
(check-expect (x+ (make-posn 10 0)) (make-posn 13 0))
(define (x+ p)
  (... (posn-x p)... (posn-y p)...))
```

シグネチャ、目的、および例はすべて問題文から導き出されます。ヘッダー（デフォルトの結果を返す関数）の代わりに、私たちのスケッチには Posn に対する2つのセレクタ式が含まれています。結局のところ、結果のための情報は入力から来なければならず、入力は2つの値を含む構造体だからです。

これで定義を完成させるのは簡単です。望まれる結果は Posn であるため、関数は make-posn を使用して各要素を組み合わせます：

```racket
(define (x+ p)
  (make-posn (+ (posn-x p) 3) (posn-y p)))
```

**Exercise 73.** Posn である p と Number である n を受け取る関数 posn-up-x を設計しなさい。この関数は、x フィールドが n に置き換わった p のような Posn を生成します。

興味深い観察として、posn-up-x を使って x+ を定義できることが挙げられます：

```racket
(define (x+ p)
  (posn-up-x p (+ (posn-x p) 3)))
```

> **Note:** posn-up-x のような関数は、しばしばアップデータ（updater）や関数型セッター（functional setter）と呼ばれます。これらは大きなプログラムを書く際に非常に便利です。

関数は、アトミックなデータ（atomic data）からインスタンスを生成することもあります。make-posn はそれを行う組み込みのプリミティブですが、進行中の例題がもう一つの好例を提供しています：

> Sample Problem 別の同僚が、マウスがクリックされたときに点をリセットする関数 reset-dot を設計するタスクを担当しています。

この問題に取り組むには、「ワールドプログラムの設計（Designing World Programs）」から、マウスイベントハンドラが4つの値（ワールドの現在の状態、マウスクリックの x 座標と y 座標、および MouseEvt）を受け取ることを思い出す必要があります。

例題からの知識をプログラム設計レシピに追加することで、シグネチャ、目的記述、およびヘッダーが得られます：

```racket
; Posn Number Number MouseEvt -> Posn
; for mouse clicks, (make-posn x y); otherwise p
(define (reset-dot p x y me) p)
```

マウスイベントハンドラの例には、Posn、2つの数値、および String の特別な種類に過ぎない MouseEvt が必要です。例えばマウスクリックは、"button-down" と "button-up" という2つの文字列のいずれかで表されます。前者はユーザーがマウスボタンをクリックしたことを示し、後者はボタンが離されたことを示します。これを念頭に置いて、以下に示す2つの例を熟考し解釈してみてください：

```racket
(check-expect
  (reset-dot (make-posn 10 20) 29 31 "button-down")
  (make-posn 29 31))
(check-expect
  (reset-dot (make-posn 10 20) 29 31 "button-up")
  (make-posn 10 20))
```

この関数はアトミックな形式のデータのみを受け取りますが、その目的記述と例は、2種類の MouseEvt（"button-down" とそれ以外のすべて）を区別することを示唆しています。このような場合の分岐は cond 式を示唆します：

```racket
(define (reset-dot p x y me)
  (cond
    [(mouse=? "button-down" me) (... p... x y...)]
    [else (... p... x y...)]))
```

設計レシピに従い、このスケルトンは利用可能なデータを思い出させるためにパラメータを記述しています。

目的記述自体が2つのケースのそれぞれで関数が何を計算するかを指示しているため、残りの部分は再び簡単です：

```racket
(define (reset-dot p x y me)
  (cond
    [(mouse=? me "button-down") (make-posn x y)]
    [else p]))
```

上記と同様に、make-posn が Posn のインスタンスを作成することに言及することもできましたが、皆さんはすでにこのことを知っており、常に思い起こさせる必要はないでしょう。

**Exercise 74.** 関連するすべての定数定義および関数定義を DrRacket の定義領域（definitions area）にコピーしなさい。テストを追加して、それらがパスすることを確認しなさい。その後、プログラムを実行し、マウスを使って赤い点を配置しなさい。

多くのプログラムは入れ子になった構造体（nested structures）を扱います。ワールドプログラムからのもう一つの短い抜粋を使ってこの点を示します：

> Sample Problem あなたのチームは、変化する速度でキャンバス上を移動するオブジェクトを追跡するゲームプログラムを設計しています。選択されたデータ表現には2つのデータ定義が必要です：
> (define-struct ufo [loc vel]);AUFO is a structure:; (make-ufoPosnVel);interpretation(make-ufopv) is at location;p moving at velocityv与えられた UFO が1クロックティック経過した後の位置を計算する関数 ufo-move-1 を開発することがあなたのタスクです。

> **Note:** 物理学に関することであることを思い出してください。

データ定義を少し探求するいくつかの例から始めましょう：

```racket
(define v1 (make-vel 8 -3))
(define v2 (make-vel -5 -3))

The order of these definitions matters. See Intermezzo 1: Beginning Student Language.
(define p1 (make-posn 22 80))
(define p2 (make-posn 30 77))

(define u1 (make-ufo p1 v1))
(define u2 (make-ufo p1 v2))
(define u3 (make-ufo p2 v1))
(define u4 (make-ufo p2 v2))
```

> **Note:** これらの定義の順序は重要です。「合間1: 初等学生言語（Intermezzo 1: Beginning Student Language）」を参照してください。

最初の4つは Vel と Posn の要素です。最後の4つは、最初の4つを考えられるあらゆる組み合わせで組み合わせたものです。

次に、シグネチャ、目的、いくつかの例、および関数ヘッダーを書き下します：

```racket
; UFO -> UFO
; determines where u moves in one clock tick;
; leaves the velocity as is

(check-expect (ufo-move-1 u1) u3)
(check-expect (ufo-move-1 u2)
              (make-ufo (make-posn 17 77) v2))

(define (ufo-move-1 u) u)
```

関数の例については、データの例と、位置と速度に関するドメイン知識を使用します。具体的には、時速60マイルで北へ、時速10マイルで西へ移動している乗り物は、1時間の走行後にスタート地点から北へ60マイル、西へ10マイル離れた場所に到達することがわかっています。2時間後には、スタート地点から北へ120マイル、西へ20マイルの場所になります。

いつものように、構造体のインスタンスを受け取る関数は、その結果を計算するために構造体から情報を抽出することができます（そしておそらくそうしなければなりません）。したがって、再び関数の定義にセレクタ式を追加します：

```racket
(define (ufo-move-1 u)
  (... (ufo-loc u)... (ufo-vel u)...))
```

> **Note:** セレクタ式は、このスケッチをさらに洗練させる必要があるかという疑問を生じさせます。結局のところ、2つの式はそれぞれ Posn と Vel のインスタンスを抽出します。これら2つも構造体インスタンスであり、今度はそれらから順番に値を抽出することもできます。その結果得られるスケルトンは以下のようになります：

```racket
; UFO -> UFO
(define (ufo-move-1 u)
  (... (posn-x (ufo-loc u))...
... (posn-y (ufo-loc u))...
... (vel-deltax (ufo-vel u))...
... (vel-deltay (ufo-vel u))...))
```

しかし、そうすることでスケッチは明らかにかなり複雑に見えるようになります。真に現実的なプログラムにおいて、この考え方を論理的な極限まで進めると、信じられないほど複雑なプログラムのアウトラインが作成されてしまいます。より一般的には、

> 関数が入れ子になった構造体を扱う場合は、入れ子のレベルごとに1つの関数を開発しなさい。

本書の第2部では、このガイドラインはさらに重要になり、少し洗練させていきます。（終わり）

ここでは、与えられた Posn と与えられた Vel を組み合わせて UFO の次の位置を取得する方法に焦点を当てます。なぜなら、それが私たちの物理学の知識が教えてくれることだからです。具体的には、この2つを「加算する（add）」ことを意味しますが、ここでの「加算」は私たちが通常数値に適用する操作を意味することはできません。そこで、Vel を Posn に加算するための関数が存在すると仮定してみましょう：

```racket
; Posn Vel -> Posn
; adds v to p
(define (posn+ p v) p)
```

このようにシグネチャ、目的、およびヘッダーを書き下すことは、プログラミングの正当な方法です。これは「願い事をする（making a wish）」と呼ばれ、「関数からプログラムへ（From Functions to Programs）」で説明されている「ウィッシュリスト（願い事リスト）を作る（making a wish list）」ことの一部です。

重要なのは、現在取り組んでいる関数を完成させることができるように願い事をすることです。このようにして、難解なプログラミングタスクを異なるタスクに分割することができ、これは合理的に小さなステップで問題を解決するのに役立つテクニックです。この例題では、ufo-move-1 の完全な定義が得られます：

```racket
(define (ufo-move-1 u)
  (make-ufo (posn+ (ufo-loc u) (ufo-vel u))
            (ufo-vel u)))
```

ufo-move-1 と posn+ は完全な定義であるため、*RUN* をクリックすることさえでき、これによって DrRacket がこれまでの作業における文法上の問題について文句を言わないことを確認できます。当然ながら、posn+ は単なる願い事であり、必要な関数そのものではないため、テストは失敗します。

今こそ posn+ に集中するときです。設計の最初の2つのステップ（データ定義、シグネチャ/目的/ヘッダー）を完了したため、例を作成しなければなりません。「願い事」に対する機能的な例を作成する簡単な方法の1つは、元の関数の例を使用して、それらを新しい関数の例に変換することです：

> **Note:** 幾何学では、posn+ に対応する操作は平行移動（translation）と呼ばれます。

```racket
(check-expect (posn+ p1 v1) p2)
(check-expect (posn+ p1 v2) (make-posn 17 77))
```

この問題において、(ufo-move-1(make-ufop1v1)) が p2 を生成することを知っています。同時に、ufo-move-1 が posn+ を p1 と v1 に適用することを知っており、これは posn+ がこれらの入力に対して p2 を生成しなければならないことを意味します。手を止めてください！（Stop!）私たちが何をしているかを理解できているか、手計算を確認してください。

これで、設計スケッチにセレクタ式を追加できるようになりました：

```racket
(define (posn+ p v)
  (... (posn-x p)... (posn-y p)...
... (vel-deltax v)... (vel-deltay v)...))
```

posn+ は Posn と Vel のインスタンスを受け取り、各データは2つのフィールドを持つ構造体のインスタンスであるため、4つの式が得られます。上記の入れ子になったセレクタ式とは対照的に、これらはパラメータに対するセレクタの単純な適用です。

これら4つの式が何を表しているのかを思い出すか、または2つの構造体から望ましい結果をどのように計算したかを思い出せば、posn+ の定義の完成は簡単です：

```racket
(define (posn+ p v)
  (make-posn (+ (posn-x p) (vel-deltax v))
             (+ (posn-y p) (vel-deltay v))))
```

最初のステップは、水平方向の速度を x 座標に加算し、垂直方向の速度を y 座標に加算することです。これにより、新しい座標ごとに1つずつ、計2つの式が得られます。make-posn を使用することで、これらを再び単一の Posn に結合できます。

**Exercise 75.** これらの定義とそのテストケースを DrRacket の定義領域に入力し、正常に動作することを確認しなさい。これは皆さんが「願い事（wish）」を扱った初めての機会であり、2つの関数がどのように連携して動作するかを確実に理解する必要があります。


### 5.7 データの宇宙 (The Universe of Data)

すべての言語にはデータの宇宙が存在します。このデータは外部世界からの、そして外部世界についての情報を表現しており、プログラムが操作する対象です。このデータの宇宙とは、組み込みのデータをすべて含むだけでなく、あらゆるプログラムが作成し得るあらゆるデータをも含むコレクション（集まり）です。

> **注:** 数学者はデータのコレクションやデータクラスのことを集合（sets）と呼ぶことを思い出してください。

figure 30 の左側は、BSL の宇宙を想像するための一つの方法を示しています。数値や文字列は無限に存在するため、すべてのデータのコレクションは無限です。この図では「無限」を「...」で表していますが、実際の定義ではこのような曖昧さを回避しなければなりません。

プログラムもプログラム内の個々の関数も、データの宇宙全体を扱うわけではありません。データの定義の目的は、この宇宙の一部を説明し、それらに名前を付けることで、簡潔に参照できるようにすることです。言い換えれば、名前付きのデータ定義とはデータのコレクションの記述であり、その名前は他のデータ定義や関数のシグネチャで使用できます。関数のシグネチャにおいて、その名前は関数がどのデータを扱うかを規定し、暗黙のうちにデータの宇宙のどの部分を扱わないかも規定します。

```
+-------------------------------------------+
| Figure 30: The universe of data           |
|                                           |
| [image: pict_74.png]                      |
| [image: pict_75.png]                      |
| [image: pict_74.png] [image: pict_75.png] |
+-------------------------------------------+
```

実際問題として、最初の4章におけるデータ定義は、組み込みのデータのコレクションを制限するものでした。それらは、含まれるすべての値を明示的または暗黙的に箇条書き（itemization）にすることによって制限を行います。例えば、figure 30 の右側に灰色で陰影が付けられた領域は、以下のデータ定義を描写しています。

```racket
; A BS is one of:
; — "hello",
; — "world", or
; — pi.
```

この特定のデータ定義は一見おかしなものに見えるかもしれませんが、英語と BSL が様式化されて混ざり合って使用されていることに注目してください。その意味は正確かつ一義的であり、どの要素が BS に属し、どの要素が属さないかを正確に明確化しています。

構造体型（structure type）の定義は、この構図を完全に一新しました。プログラマーが構造体型を定義すると、宇宙は可能なすべての構造体インスタンスとともに拡張されます。例えば、posn の追加は、2つのフィールドに可能なあらゆる値を持つ posn のインスタンスが登場することを意味します。figure 31 の中央のバブルは、(make-posn"hello"0) や (make-posn(make-posn01)2) のような一見無意味に見えるものを含め、これらの値が追加された様子を描写しています。そして確かに、これらの posn のインスタンスのいくつかは私たちが理解できる意味を持ちません。しかし、BSL プログラムはそれらのどれでも構築することができるのです。

```
+-------------------------------------------+
| Figure 31: Adding structure to a universe |
|                                           |
| [image: pict_76.png]                      |
| [image: pict_76.png]                      |
+-------------------------------------------+
```

さらにもう一つの構造体型定義を追加すると、すべてが再び組み合わされます。例えば、同じく2つのフィールドを持つ ball の定義を追加したとしましょう。figure 31 の3つ目のバブルが示すように、この追加により、数値や posn 構造体などを含む ball のインスタンスが生み出されるだけでなく、ball のインスタンスを含む posn のインスタンスも生み出されます。DrRacket で試してみてください！

```racket
(define-struct ball [location velocity])
```

を定義エリア（definitions area）に追加し、*RUN* を押して、いくつかの構造体インスタンスを作成してみましょう。

データ定義の運用面に関して言えば、構造体型のデータ定義は、既存のデータ定義とインスタンスの組み合わせを通じて、大きなデータのコレクションを記述します。私たちが

```racket
; Posn is (make-posn Number Number)
```

と書くとき、posn の可能なインスタンスの無限の数を記述していることになります。上記と同様に、データ定義は自然言語、他で定義されたデータのコレクション、およびデータコンストラクタの組み合わせを使用します。現時点では、データ定義にそれ以外のものが現れるべきではありません。

構造体のデータ定義は、私たちの関数によって使用されるインスタンスで構成された、新しいデータのコレクションを指定します。例えば、Posns のデータ定義は、figure 31 の宇宙の中央のバブルにおいて灰色で陰影が付けられた領域を特定します。ここには、2つのフィールドに数値を含むすべての posn 構造体が含まれます。同時に、両方のフィールドに数値が含まれるという要件を満たさない posn のインスタンスを構築することも完全に可能です。

```racket
(make-posn (make-posn 1 1) "hello")
```

この構造体は、x フィールドに posn を、y フィールドに文字列を含んでいます。

**Exercise 76.** 以下の構造体型定義に対するデータ定義を作成しなさい。

- (define-structmovie[titleproduceryear])
- (define-structperson[namehaireyesphone])
- (define-structpet[namenumber])
- (define-structCD[artisttitleprice])
- (define-structsweater[materialsizeproducer])

各フィールドにどのような種類の値が入るかについて、妥当な仮定を立てなさい。

**Exercise 77.** 深夜（午前0時）からの経過時刻を表す構造体型定義およびデータ定義を提供しなさい。時刻は、時間（hours）、分（minutes）、秒（seconds）の3つの数値で構成されます。

**Exercise 78.** 3文字の単語を表す構造体型およびデータ定義を提供しなさい。単語は、"a" から "z" までの 1Strings および #false で表される小文字で構成されます。注: この問題はハングマン（hangman）ゲームの設計の一部です。exercise 396 を参照してください。

プログラマーはデータ定義を書くだけでなく、プログラムを理解するため、扱うことのできるデータの種類を拡張するため、エラーを排除するためなどにデータ定義を読みます。私たちは、指定されたコレクションに属するデータをどのように作成するかを理解し、あるデータが指定されたクラスに属するかどうかを判断するためにデータ定義を読みます。

データ定義は設計プロセスにおいて非常に中心的かつ重要な役割を果たすため、関数の挙動を例で説明するのと同様に、データ定義も例を用いて説明するのが最善であることがよくあります。そして実際、データ定義からデータの例を作成することは簡単です。

- 組み込みのデータコレクション（数値、文字列、ブール値、画像）については、お気に入りの例を選びます。注: 時として、`NegativeNumber` や `OneLetterString` のように、組み込みデータコレクションを修飾するために説明的な名前が使われることがあります。これらは適切に書かれたデータ定義の代わりにはなりません。注終わり
- 列挙型（enumeration）については、列挙型の項目をいくつか使用します。
- 区間（interval）については、端点（含まれている場合）および少なくとも一つの内部の点を使用します。
- 箇条書き（itemization）については、各部分を個別に扱います。そして
- 構造体のデータ定義については、自然言語による記述に従います。つまり、コンストラクタを使用し、各フィールドに指定されたデータコレクションから例を選びます。

本書の大部分においてデータ定義から例を構築するために必要なことはこれですべてですが、データ定義はこれまで見てきたものよりもずっと複雑になっていきます。

**Exercise 79.** 以下のデータ定義の例を作成しなさい。

- ;AColor is one of:;—"white";—"yellow";—"orange";—"green";—"red";—"blue";—"black" 注: DrRacket はさらに多くの文字列を色として認識します。注終わり
- ;H is aNumber between0 and100.;interpretation represents a happiness value
- (define-struct person [fstname lstname male?]);APerson is a structure:; (make-personStringStringBoolean) 述語（predicate）の名前のように見えるフィールド名を使用するのは良い考えでしょうか？
- (define-struct dog [owner name age happiness]);ADog is a structure:; (make-dogPersonStringPositiveIntegerH) このデータ定義にも解釈（interpretation）を追加しなさい。
- ;AWeapon is one of:;—#false;—Posn;interpretation#false means the missile hasn't;been fired yet; aPosn means it is in flight

最後の定義は変則的な箇条書き（itemization）であり、組み込みのデータと構造体型を組み合わせています。次の章では、このような定義について詳しく扱います。

### 5.8 構造体を用いた設計 (Designing with Structures)

構造体型の導入は、設計レシピ（design recipe）の6つのステップすべてを行う必要性を補強します。情報を表現するのに組み込みのデータコレクションに頼るだけではもはや不十分であり、プログラマーは最も単純な問題を除くすべての問題についてデータ定義を作成しなければならないことが明確になります。

本節では、設計レシピを追加し、それを以下の問題を用いて説明します。

> **問題例** 3次元空間におけるオブジェクトの原点からの距離を計算する関数を設計しなさい。

それでは始めましょう。

1. 問題が、互いに関連し合う情報や自然なまとまり（全体）を表現することを求めている場合、構造体型の定義が必要です。それには、関連する属性の数だけのフィールドが必要となります。この構造体型のインスタンスは全体に対応し、フィールド内の値はその属性に対応します。構造体型のデータ定義は、正当なインスタンスのコレクションに対する名前を導入します。さらに、どの種類のデータがどのフィールドに対応するかを記述しなければなりません。組み込みデータコレクションの名前、または以前に定義されたデータ定義の名前のみを使用してください。最終的に、私たちが（そして他の人も）そのデータ定義を使用してサンプルとなる構造体インスタンスを作成できなければなりません。そうでなければ、私たちのデータ定義のどこかが間違っています。インスタンスを作成できることを確実にするため、データ定義にはデータの例を添付する必要があります。この考え方を問題例に適用する方法は以下のとおりです。
(define-struct r3 [x y z]);AnR3 is a structure:; (make-r3NumberNumberNumber) (define ex1 (make-r3 1 2 13))(define ex2 (make-r3 -1 0 3))構造体型定義は新しい種類の構造体 r3 を導入し、データ定義は数値のみを含む r3 のすべてのインスタンスに対する名前として R3 を導入します。
2. シグネチャ、目的文、関数のヘッダーが引き続き必要ですが、これらは従来と同じです。手を止めて、問題例に対してこれを行ってください。
3. 最初のステップの例を使用して、関数の例を作成します。区間や列挙型に関連付けられた各フィールドについて、端点や中間点を選んで関数の例を作成するようにしてください。あなたが問題例への取り組みを続けることを期待します。
4. 構造体を消費する関数は、通常（常にではありませんが）、構造体内の様々なフィールドから値を抽出します。この可能性を自分自身に思い起こさせるために、そのような関数のテンプレートに各フィールドのセレクタを追加します。問題例に対して作成したテンプレートは以下のとおりです。
;R3 ->Number;determines the distance ofp to the origin(define (r3-distance-to-0 p) (... (r3-x p)... (r3-y p)... (r3-z p)...))各セレクタ式の隣に、それが与えられた構造体からどのような種類のデータを抽出するかを書き留めておくとよいでしょう。この情報はデータ定義の中で見つけることができます。手を止めて、今すぐやってみてください！
5. 関数を定義する際には、テンプレートのセレクタ式を使用します。それらのいくつかは必要ないかもしれないことに注意してください。
6. テスト。関数のヘッダーが書かれたらすぐにテストします。すべての式がカバーされるまでテストします。変更を加えたときには再びテストします。

問題例を完成させなさい。3次元の点の原点からの距離が思い出せない場合は、幾何学の本で調べてみてください。

> **注:** そこには  のような公式が見つかるでしょう。

**Exercise 80.** 以下の構造体型のインスタンスを消費する関数用のテンプレートを作成しなさい。

- (define-structmovie[titledirectoryear])
- (define-structpet[namenumber])
- (define-structCD[artisttitleprice])
- (define-structsweater[materialsizecolor])

いいえ、このタスクにデータ定義は必要ありません。

**Exercise 81.** time 構造体のインスタンス（exercise 77 を参照）を消費し、深夜からの経過秒数を生成する関数 time->seconds を設計しなさい。例えば、12時間30分2秒をこれらの構造体の一つで表現し、そのインスタンスに time->seconds を適用した場合、正しい結果は 45002 となります。

**Exercise 82.** 関数 compare-word を設計しなさい。この関数は 2 つの 3 文字の単語（exercise 78 を参照）を消費します。そして、与えられた単語がどこで一致し、どこで一致しないかを示す単語を生成します。2 つの単語が一致する場合、関数は構造体フィールドの内容を維持し、そうでない場合は結果の単語のフィールドに #false を配置します。ヒント: この問題では「単語の比較」と「文字の比較」という 2 つのタスクに言及しています。
容を保持します。そうでない場合、結果となる単語のフィールドに #false を配置します。ヒント: この演習問題では、単語の比較と「文字」の比較という2つのタスクについて言及されています。


### 5.9 ワールド内の構造 (Structure in the World)

ワールドプログラムが独立した2つの情報を追跡しなければならない場合、ワールドの状態データを表現するために構造体のコレクションを使用する必要があります。一方のフィールドが1つの情報を追跡し、もう一方のフィールドが2つ目の情報を追跡します。当然のことながら、対象領域（ドメイン）のワールドに2つより多くの独立した情報が含まれている場合、構造体型の定義には異なる情報の数と同じだけのフィールドを指定しなければなりません。

UFOと戦車（tank）で構成されるインベーダーゲームを考えてみましょう。UFOは真っ直ぐな垂直線に沿って降下し、戦車はシーンの底部で水平に移動します。両方のオブジェクトが既知の一定速度で移動する場合、これら2つのオブジェクトを記述するために必要なのは、オブジェクトごとに1つの情報だけです。すなわち、UFOのy座標と戦車のx座標です。これらをまとめるには、2つのフィールドを持つ構造体が必要です。

```racket
(define-struct space-game [ufo tank])
```

解釈を含め、この構造体型定義に対する適切なデータ定義を定式化することは皆さんにお任せします。構造体の名前にあるハイフンについて考えてみてください。BSLでは、変数、関数、構造体、およびフィールドの名前にあらゆる種類の文字を使用することが実際に許可されています。この構造体のセレクタ名は何でしょうか？ 述語の名前は何でしょうか？

「1つの情報」と言うとき、それが必ずしも単一の数値や単一の単語を意味するとは限りません。1つの情報自体が、複数の情報を組み合わせたものである場合もあります。そのような種類の情報のデータ表現を作成すると、自然と入れ子構造（ネストされた構造体）に至ります。

架空のインベーダーゲームに少しのスパイスを加えてみましょう。垂直線に沿って降下するだけのUFOは退屈です。このアイデアを、戦車が何らかの武器でUFOを攻撃する面白いゲームに変えるには、UFOが単純ではない線に沿って降下できるようにする必要があり、おそらくランダムにジャンプするかもしれません。このアイデアの実装は、UFOの位置を記述するために2つの座標が必要であることを意味し、その結果、スペースゲームのための改訂されたデータ定義は以下のようになります。

```racket
; A SpaceGame is a structure:
;   (make-space-game Posn Number).
; interpretation (make-space-game (make-posn ux uy) tx)
; describes a configuration where the UFO is
; at (ux,uy) and the tank's x-coordinate is tx
```

ワールドプログラムにどのようなデータ表現が必要であるかを理解するには練習が必要です。続く2つの節では、ある程度複雑な問題文をいくつか紹介します。ご自身で設計したくなるような種類のゲームに進む前に、これらを解決してください。

### 5.10 グラフィカルエディタ (A Graphical Editor)

BSLでプログラミングをするには、DrRacketを開き、キーボードを打ち、テキストが表示されるのを見ます。キーボードの左矢印キーを押すとカーソルが左に移動し、バックスペース（または delete）キーを押すと、カーソルの左側にある1文字が消去されます（そもそも文字が存在する場合）。

このプロセスは「編集（editing）」と呼ばれますが、キーボードで入力するよりも要求の厳しいタスクのために「編集」という言葉を使用するため、その正確な名称は「プログラムのテキスト編集」とするべきでしょう。英語の課題など、他の種類のドキュメントを執筆・改訂するときは、ワードプロセッサと呼ばれる他のソフトウェアアプリケーションを使用することが多いでしょうが、コンピュータ科学者はそれらすべてをエディタ、あるいはグラフィカルエディタと呼びます。

皆さんは今や、プレーンテキスト用の1行エディタとして機能するワールドプログラムを設計できる立場にあります。ここでの編集には、文字の入力や、文字の削除・挿入を含む既存テキストの何らかの変更が含まれます。これは、テキスト内における位置という概念を意味します。人々はこの位置をカーソル（cursor）と呼び、ほとんどのグラフィカルエディタは簡単に目につくようにそれを表示します。

以下のエディタ構成を見てみてください。

> [image: pict_78.png]

誰かが「helloworld」というテキストを入力し、左矢印キーを5回押したとします。これにより、カーソルはテキストの末尾から「o」と「w」の間の位置に移動します。ここでスペースキーを押すと、エディタは表示を次のように変更します。

> [image: pict_79.png]

要約すると、この操作は「 」を挿入し、カーソルをそれと「w」の間に配置します。

以上のことから、エディタは2つの情報を追跡しなければなりません。

1. これまでに入力されたテキスト
2. カーソルの現在の位置

そして、これは2つのフィールドを持つ構造体型を示唆しています。

情報からデータへ、そしてその逆へ変換するさまざまな方法を想像することができます。たとえば、構造体の一方のフィールドに入力されたテキスト全体が含まれ、他方のフィールドに最初の文字（左から数えて）とカーソルの間の文字数が含まれるかもしれません。もう1つのデータ表現は、2つのフィールドで2つの文字列を使用することです。すなわち、カーソルの左側のテキスト部分と、その右側のテキスト部分です。以下は、エディタの状態を表現するための私たちの推奨するアプローチです。

```racket
(define-struct editor [pre post])
; An Editor is a structure:
;   (make-editor String String)
; interpretation (make-editor s t) describes an editor
; whose visible text is (string-append s t) with
; the cursor displayed between s and t
```

このデータ表現に基づいて、続くいくつかの練習問題を解いてください。

**Exercise 83.** 関数 `render` を設計してください。この関数は Editor を受け取り、イメージを生成します。

この関数の目的は、[image: pict_80.png] ピクセルの空のシーン（empty scene）内にテキストを描画することです。カーソルには [image: pict_81.png] の赤い長方形を使用し、文字列にはサイズ 16 の黒いテキストを使用してください。

DrRacket の交互作用エリア（interactions area）でサンプル文字列のイメージを作成してください。私たちは次の式から始めました。

```racket
(overlay/align "left" "center"
               (text "hello world" 11 "black")
               (empty-scene 200 20))
```

`beside` や `above` などの関数について調べてみるとよいでしょう。イメージの見た目に満足したら、その式をテストとして、また `render` の設計のガイドとして使用してください。

**Exercise 84.** `edit` を設計してください。この関数は2つの入力、エディタ `ed` と KeyEvent `ke` を受け取り、別のエディタを生成します。そのタスクは、`ke` がバックスペース（`"\b"`）キーを表していない限り、1文字の KeyEvent `ke` を `ed` の `pre` フィールドの末尾に追加することです。バックスペースキーを表している場合、カーソルの直前（左側）にある文字を消去します（文字が存在する場合）。この関数はタブキー（`"\t"`）とリターンキー（`"\r"`）を無視します。

この関数は、1文字より長い KeyEvents のうち、`"left"` と `"right"` の2つだけに注意を払います。左矢印はカーソルを左に1文字移動させ（文字が存在する場合）、右矢印は右に1文字移動させます（文字が存在する場合）。これら以外のそのような KeyEvents はすべて無視されます。

特殊なケースに注意を払いながら、`edit` の例をかなりの数作成してください。私たちがこの練習問題を解いたときは、20個の例を作成し、それらすべてをテストに変換しました。

ヒント: この関数を、列挙型（enumeration）として指定されたコレクションである KeyEvents を受け取るものとして考えてください。Editor 構造体を扱うために補助関数を使用します。ウィッシュリストを手元に用意しておきましょう。`string-first`、`string-rest`、`string-last`、`string-remove-last` など、これらの補助関数の大部分に対して追加の関数を設計する必要があります。まだ行っていない場合は、『Functions（関数）』の練習問題を解いてください。

**Exercise 85.** 関数 `run` を定義してください。エディタの `pre` フィールドが与えられると、前述の2つの練習問題の `render` と `edit` をそれぞれ `to-draw` 句と `on-key` 句に使用して、対話型エディタを起動します。

**Exercise 86.** たくさん入力すると、エディタプログラムがテキストのすべてを表示しないことに注意してください。代わりに、テキストは右マージンで切り取られます。Exercise 84 の関数 `edit` を修正し、`pre` フィールドの末尾にキー入力を追加すると描画されたテキストがキャンバスに対して幅広になりすぎる場合に、そのキー入力を無視するようにしてください。

**Exercise 87.** 文字列とインデックスを使用する私たちの最初のアイデアに基づいて、エディタのデータ表現を開発してください。その後、前の練習問題をもう一度解いてください。設計レシピ（design recipe）をたどり直してください。ヒント: まだ行っていない場合は、『Functions（関数）』の練習問題を解いてください。

**設計上の選択に関する注記**（Note on Design Choices） この練習問題は、設計上の選択を行うための最初の調査です。ごく最初の設計上の選択がデータ表現に関するものであることを示しています。正しい選択をするには、事前計画を立て、それぞれの複雑さを秤にかける必要があります。もちろん、これを得意になることは経験を積むことの問題です。

### 5.11 さらなるバーチャルペット (More Virtual Pets)

この節では、『Virtual Pet Worlds（バーチャルペットワールド）』からのバーチャル動物園プロジェクトを継続します。具体的には、この練習問題の目標は、猫のワールドプログラムと、その幸福度ゲージ（happiness gauge）を管理するプログラムを組み合わせることです。組み合わされたプログラムが実行されると、猫がキャンバスを横切って歩くのが見られ、一歩進むごとにその幸福度が下がります。猫を幸せにする唯一の方法は、餌を与えるか、撫でる（上矢印キー）ことです。最後に、この節の最後の練習問題の目標は、別の幸せなバーチャルペットを作成することです。

**Exercise 88.** 猫の x 座標とその幸福度を追跡する構造体型を定義してください。次に、解釈を含めて `VCat` と呼ばれる猫のデータ定義を定式化してください。

**Exercise 89.** 歩く猫とその幸福度レベルを管理する `happy-cat` ワールドプログラムを設計してください。猫は完璧な幸福度から始めると仮定しましょう。

ヒント: (1) 『Virtual Pet Worlds（バーチャルペットワールド）』のワールドプログラムの関数を再利用してください。(2) ワールドの状態を表現するために前の練習問題の構造体型を使用してください。

**Exercise 90.** 猫の幸福度が 0 に落ちたときはいつでも停止するように、前の練習問題の `happy-cat` プログラムを修正してください。

**Exercise 91.** Exercise 88 の構造体型定義とデータ定義を拡張して、`direction` フィールドを含めてください。指定された方向に猫が移動するように `happy-cat` プログラムを調整してください。プログラムは猫を現在の方向に移動させ、シーンのどちらかの端に達したときに猫を反転させる必要があります。

```racket
(define cham [image:chameleon.png])
```

上記のカメレオンの図は透明なイメージです。これを DrRacket に挿入するには、「Insert Image（イメージの挿入）」メニュー項目を使用して挿入します。この手順を使用すると、図のピクセルの透明度が保持されます。

部分的に透明なイメージが、たとえば長方形のような色のついた図形と組み合わされると、イメージは下にある色を帯びます。カメレオンの図では、実際に透明なのは動物の内側であり、外側の領域は純白です。お使いの DrRacket で次の式を試してみてください。

```racket
(define background
  (rectangle (image-width cham)
             (image-height cham)
             "solid"
             "red"))

(overlay cham background)
```

**Exercise 92.** カメレオンがキャンバスを左から右へと連続的に歩く `cham` プログラムを設計してください。キャンバスの右端に達すると、消えてすぐに左側に再出現します。猫と同様に、カメレオンは歩き回ることで腹が減り、時間が経過するにつれて、この空腹は不幸（不機嫌）として現れます。

カメレオンの幸福度ゲージを管理するには、バーチャル猫の幸福度ゲージを再利用できます。カメレオンを幸せにするには、餌を与えます（下矢印キー、2ポイントのみ）。撫でることは許可されていません。もちろん、すべてのカメレオンと同様に、私たちのカメレオンも色を変えることができます。`"r"` は赤色に、`"b"` は青色に、`"g"` は緑色に変えます。カメレオンのワールドプログラムをバーチャル猫ゲームに追加し、可能であれば後者（バーチャル猫ゲーム）の関数を再利用してください。

カメレオンを表現するためのデータ定義 `VCham` から始めてください。

**Exercise 93.** Exercise 92 の解法をコピーし、カメレオンが3色の背景を横切って歩くようにそのコピーを修正してください。私たちの解法では以下の色を使用しています。

```racket
(define BACKGROUND
  (beside (empty-scene WIDTH HEIGHT "green") Have some Italian pizza when you’re done.
          (empty-scene WIDTH HEIGHT "white")
          (empty-scene WIDTH HEIGHT "red")))
```

> **Note:** 終わったらイタリアンピザでも食べてみてください。

ただし、どんな色を使用してもかまいません。カメレオンが2つの色の境界を越えるときに、どのように色を変えて背景に溶け込むかを観察してください。

**注記**（Note） アニメーションを注意深く観察すると、カメレオンが白い長方形の上に乗っているのが見えます。画像編集ソフトウェアの使用方法を知っている場合は、白い長方形が見えなくなるように画像を修正してください。そうすれば、カメレオンは本当に溶け込むようになります。

## 6 項目化と構造 (Itemizations and Structures)

前章までの2つの章では、データ定義を定式化するための2つの方法を紹介しました。項目化（列挙と区間）を用いるものは、大きなコレクションから小さなコレクションを作成するために使用されます。構造体を使用するものは、複数のコレクションを結合します。データ表現の開発が適切なプログラム設計の出発点であるため、プログラマが構造体を含むデータ定義を項目化したいと考えたり、項目化されたデータを結合するために構造体を使いたいと頻繁に考えるのは驚くことではありません。

前章の『Structure in the World』で扱った架空のインベーダーゲームを思い出してください。これまでのところ、ゲームには宇宙から下降してくる1つのUFOと、地上で水平移動する1つの戦車（タンク）が登場します。私たちのデータ表現では、2つのフィールドを持つ構造体を使用しています。1つはUFOのデータ表現のため、もう1つは戦車のデータ表現のためです。当然ながら、プレイヤーはミサイルを発射できる戦車を望むでしょう。突然ですが、私たちは3つの独立して移動するオブジェクト（UFO、戦車、ミサイル）を含む2つ目の種類の状態を考えることができます。したがって、2つの独立して移動するオブジェクトを表現するためのものと、3つ目のオブジェクトを含むものを表現するための、2つの異なる構造体を持つことになります。ワールド状態（world state）はこれら2つの構造体のいずれかになり得るため、起こり得るすべての状態を記述するために項目化を使用するのが自然です：

1. ワールドの状態は2つのフィールドを持つ構造体である、または
2. ワールドの状態は3つのフィールドを持つ構造体である。

私たちのドメイン（実際のゲーム）に関する限り、1番目の種類の状態は戦車が唯一のミサイルを発射する前の時間を表し、2番目の状態はミサイルが発射された後の時間を表します。

> **Note:** ご安心ください。本書の次のパートでは、リロードなしで好きなだけミサイルを発射する方法について扱います。

本章では、構造体を含むデータ定義を項目化するという基本的な考え方を紹介します。必要な他の要素はすべて揃っているため、構造体の項目化からすぐに始めます。その後、この新しい能力を活用するワールドプログラムを含め、いくつかの例について議論します。最後のセクションはプログラミングにおけるエラーについて扱います。

### 6.1 項目化を用いた設計、再び (Designing with Itemizations, Again)

『Programming with Structures』で扱ったインベーダーゲームの洗練された問題記述から始めましょう。

> **例題 (Sample Problem)** `2htdp/universe` 教材パック（teachpack）を使用して、簡単なインベーダーゲームをプレイするためのゲームプログラムを設計しなさい。プレイヤーは戦車（小さな長方形）を操作し、キャンバスの上部から下部へ下降してくるUFO（ひとつの可能性については『Intervals』を参照）から、私たちの惑星（キャンバスの最下部）を守らなければなりません。UFOの着陸を阻止するために、プレイヤーはスペースキーを押すことで単一のミサイル（戦車より小さな三角形）を発射することができます。キー操作に応答して、ミサイルが戦車から現れます。UFOがミサイルと衝突した場合、プレイヤーの勝利となります。そうでない場合、UFOが着陸してプレイヤーの敗北となります。ここでは、3つのゲームオブジェクトとその移動に関するいくつかの詳細を述べます。第一に、戦車はキャンバスの底面に沿って一定の速度で移動しますが、プレイヤーは左矢印キーと右矢印キーを使って方向を変更できます。第二に、UFOは一定の速度で下降しますが、左右に小さなランダムなジャンプを行います。第三に、一度発射されると、ミサイルはUFOが下降する速度の少なくとも2倍以上の一定速度で、垂直な直線に沿って上昇します。最後に、UFOとミサイルの参照点が十分に接近している場合、それらは衝突したとみなされます（「十分に接近している」の定義は自由に決定してかまいません）。

続く2つのサブセクションでは、この例題を継続的な例として使用します。そのため、先へ進む前にこれをよく学び、次の演習問題を解いてください。そうすることで、問題を十分な深さで理解するのに役立ちます。

**Exercise 94.** 様々な段階におけるゲーム風景がどのように見えるか、いくつかのスケッチを描きなさい。そのスケッチを使って、ゲームの定数部分と変数部分を特定しなさい。前者については、ワールド（キャンバス）とそのオブジェクトの寸法を表す物理定数および画像定数（graphical constants）を作成しなさい。また、いくつかの背景風景も作成しなさい。最後に、戦車、UFO、背景の定数から初期シーン（initial scene）を作成しなさい。

**項目化の定義 (Defining Itemizations)** 設計レシピの最初のステップでは、データ定義の定式化が求められます。データ定義の1つの目的は、ワールドの状態を表すデータの構築方法を記述することであり、もう1つの目的は、ワールドプログラムのイベントハンドリング関数が消費する可能性のあるすべてのデータを記述することです。構造体を含む項目化はまだ見ていないため、この最初のサブセクションではこの考え方を紹介します。これにはおそらく驚かないでしょうが、細心の注意を払ってください。

> **Note:** このインベーダーゲームでは、3つのフィールドを持つ1つの構造体型定義だけで済ませることもできます。その場合、3番目のフィールドにはミサイルが発射されるまで `#false` が格納され、発射後はミサイルの座標を表す `Posn` が格納されます。下記を参照してください。

本章の導入部で論じたように、ミサイルを発射する戦車を伴うインベーダーゲームには、2種類の異なるゲーム状態を表すデータ表現が必要です。私たちは2つの構造体型定義を選択します：

```racket
(define-struct aim [ufo tank])
(define-struct fired [ufo tank missile])
```

```racket
; A UFO is a Posn.
; interpretation (make-posn x y) is the UFO's location
; (using the top-down, left-to-right convention)

(define-struct tank [loc vel])
; A Tank is a structure:
;   (make-tank Number Number).
; interpretation (make-tank x dx) specifies the position:
; (x, HEIGHT) and the tank's speed: dx pixels/tick

; A Missile is a Posn.
; interpretation (make-posn x y) is the missile's place
```

最初の構造体は、プレイヤーが射撃のために戦車の位置を合わせようとしている期間のためのものであり、2番目の構造体はミサイルが発射された後の状態を表現するためのものです。しかし、ゲーム全体の状態に対するデータ定義を立案する前に、戦車、UFO、ミサイルのためのデータ表現が必要です。

演習問題 94の対象である `WIDTH` や `HEIGHT` といった物理定数の定数定義が存在すると仮定すると、データ定義を次のように立案できます：

```racket
; A UFO is a Posn.
; interpretation (make-posn x y) is the UFO's location
; (using the top-down, left-to-right convention)

(define-struct tank [loc vel])
; A Tank is a structure:
;   (make-tank Number Number).
; interpretation (make-tank x dx) specifies the position:
; (x, HEIGHT) and the tank's speed: dx pixels/tick

; A Missile is a Pに関して、`Posn` が世界の2つの異なる側面を表すために使用されていることに少し驚くかもしれません。しかし、私たちは実世界の多くの異なる種類の情報を表すために数値（および文字列やブール値）を使用してきたので、`Posn` のような構造体のコレクションを再利用することは大きな問題ではありません。

これで、インベーダーゲームの状態に関するデータ定義を公式化する準備が整いました。

```racket
; A SIGS is one of:
; – (make-aim UFO Tank)
; – (make-fired UFO Tank Missile)
; interpretation represents the complete state of a
; space invader game
```

このデータ定義の形は項目化の形をしています。しかし、各条項（clause）はこれまで見てきた構造体型のデータ定義と同じように、構造体型の内容を記述しています。それでも、このデータ定義は、すべてのデータ定義がちょうど1つの構造体型定義を伴うわけではないことを示しています。ここでは、1つのデータ定義に2つの異なる構造体型定義が関わっています。

そのようなデータ定義の意味も明快です。これは、定義に従って作成できるすべての構造体インスタンスのコレクションに対して `SIGS` という名前を導入します。それでは、いくつか作成してみましょう。

- ミサイルを発射するために戦車が位置につこうと操縦している様子を記述するインスタンスです。
`(make-aim (make-posn 20 10) (make-tank 28 -3))`
- これは前述のものとほぼ同じですが、ミサイルが発射された後の状態です。
`(make-fired (make-posn 20 10) (make-tank 28 -3) (make-posn 28 (- HEIGHT TANK-HEIGHT)))`
もちろん、大文字の名前はあなたが定義した物理定数を参照しています。
- 最後に、ミサイルがUFOに今にも衝突しそうな状態です。
`(make-fired (make-posn 20 100) (make-tank 100 3) (make-posn 22 103))`
この例は、キャンバスの高さが100ピクセル以上であることを前提としています。

`SIGS` の最初のインスタンスはデータ定義の第1条項に従って生成され、2番目と3番目は第2条項に従っていることに注目してください。当然ながら、各フィールドの数値はグローバルなゲーム定数の選択に依存します。

**Exercise 95.** なぜ3つのインスタンスがデータ定義の第1条項または第2条項に従って生成されるのかを説明しなさい。

**Exercise 96.** [image: pict_82.png] のキャンバスを仮定して、3つのゲーム状態のそれぞれがどのように描写されるかをスケッチしなさい。

**設計レシピ** データ定義を公式化する新しい方法に伴い、設計レシピの再点検が行われます。本章ではデータを記述する2つ以上の手段を組み合わせる方法を紹介し、改訂された設計レシピ、特に最初のステップがこれを反映しています。

1. **いつこの新しいデータ定義方法が必要になるか？** 項目化の必要性が問題文における異なるクラスの情報区別に由来することは既に知っています。同様に、構造体ベースのデータ定義の必要性は、いくつかの異なる情報をグループ化するという要求に由来します。異なる形式のデータ（構造体のコレクションを含む）の項目化が必要となるのは、問題文が異なる種類の情報を区別しており、かつ、これらの情報のうち少なくともいくつかが複数の異なる要素で構成されている場合です。1つ心に留めておくべきことは、データ定義が他のデータ定義を参照できるということです。したがって、データ定義内の特定の条項が複雑すぎるように思われる場合は、その条項のために別のデータ定義を記述し、その補助的な定義を参照することが認められます。そして、いつものように、データ定義を使用してデータの例を公式化します。
2. **2番目のステップは同じままです。** 定義済みまたは組み込みのデータコレクションの名前のみに言及する関数のシグネチャを公式化し、目的のステートメントを追加し、関数ヘッダーを作成します。
3. **3番目のステップでも何も変わりません。** 2番目のステップからの目的のステートメントを例示する機能的な例を公式化する必要があり、項目化の項目ごとに1つの例が依然として必要です。
4. **テンプレートの開発は、項目化自体と、その条項における構造体の使用という2つの異なる次元を活用するようになります。** 第一に、テンプレートの本体は、項目化が持つ項目の数と同じ数の `cond` 条項を持つ `cond` 式で構成されます。さらに、対応する項目のデータのサブクラスを特定する条件を各 `cond` 条項に追加しなければなりません。第二に、ある項目が構造体を扱う場合、テンプレートには、その項目で記述されたデータのサブクラスを扱う `cond` 条項内にセレクタ式が含まれます。しかし、別個のデータ定義でデータを記述することを選択した場合は、セレクタ式を追加しません。代わりに、手元のタスクに合わせて別個のデータ定義用のテンプレートを作成し、関数呼び出しによってそのテンプレートを参照します。後者は、このデータのサブクラスが別個に処理されていることを示します。テンプレートの開発作業に入る前に、関数の性質について簡単に熟考してください。問題文が実行すべきいくつかのタスクがあることを示唆している場合、テンプレートの代わりに、個別設計された複数の関数の合成が必要である可能性が高いです。その場合、テンプレートのステップをスキップしてください。
5. **テンプレートの隙間を埋めます。** データ定義を複雑にすればするほど、このステップは複雑になります。良い知らせは、この設計レシピが多くの状況で役立つということです。行き詰まった場合は、まず簡単なケースを埋め、他のケースにはデフォルト値を使用します。これにより一部のテストケースは失敗しますが、前進しており、その進捗を視覚化できます。項目化のいくつかのケースで行き詰まった場合は、それらのケースに対応する例を分析します。テンプレートの各パーツが与えられた入力から何を取得（計算）するかを決定します。次に、これらのパーツ（およびいくつかの定数）をどのように組み合わせて目的の出力を計算するかを考えます。補助関数が必要になる可能性があることを心に留めておいてください。また、データ定義が互いに参照し合っているためにテンプレートが別のテンプレートを「呼び出す」場合は、その他方の関数の定義がまだ完了していなくても、その他方の関数が目的のステートメントと例で約束されたものを提供すると仮定します。
6. **テスト。** テストが失敗した場合は、何が間違っているか（関数、テスト、またはその両方）を特定します。適切なステップに戻ります。

「関数の設計 (Designing Functions)」に戻り、シンプルな設計レシピの説明を読み返して、この改訂版と比較してみてください。

本節の冒頭にある例題に対する描写（レンダリング）関数の設計で、この設計レシピを解説しましょう。`big-bang` 式は、時計の各刻み（クロックティック）、マウスクリック、またはキー入力の後に、世界の状態を画像に変換するために、このような描写関数を必要とすることを思い出してください。

この描写関数のシグネチャは、世界の状態クラスのエレメントを `Image` のクラスへとマッピングすることを示しています。

```racket
; SIGS -> Image
; adds TANK, UFO, and possibly MISSILE to
; the BACKGROUND scene
(define (si-render s) BACKGROUND)
```

ここで `TANK`、`UFO`、`MISSILE`、および `BACKGROUND` は、演習問題 94で求められた画像定数です。このシグネチャは、常に世界の状態のコレクションを消費して何らかの画像を生成する、描写関数の一般的なシグネチャの1つのインスタンスにすぎないことを思い出してください。

```
+--------------------------------------------------------------------+
| Figure 32: Rendering space invader game states, by example         |
|                                                                    |
| [image: pict_83.png]                                               |
| [image: pict_84.png]                                               |
| [image: pict_85.png]                                               |
|                                                                    |
| +--------------------------------------+--+----------------------+ |
| | s                                    |  | (si-render s)        | |
| +--------------------------------------+--+----------------------+ |
| | (make-aim ⏎ (make-posn 10 20) ⏎ (ma… |  | [image: pict_83.png] | |
| | (make-aim                            |  |                      | |
| |   (make-posn 10 20)                  |  |                      | |
| |   (make-tank 28 -3))                 |  |                      | |
| | (make-fired ⏎ (make-posn 20 100) ⏎ … |  | [image: pict_84.png] | |
| | (make-fired                          |  |                      | |
| |   (make-posn 20 100)                 |  |                      | |
| |   (make-tank 100 3)                  |  |                      | |
| |   (make-posn 22 103))                |  |                      | |
| | (make-fired ⏎ (make-posn 10 20) ⏎ (… |  | [image: pict_85.png] | |
| | (make-fired                          |  |                      | |
| |   (make-posn 10 20)                  |  |                      | |
| |   (make-tank 28 -3)                  |  |                      | |
| |   (make-posn 32 (- HEIGHT TANK-HEIG… |  |                      | |
| +--------------------------------------+--+----------------------+ |
+--------------------------------------------------------------------+
```

データ定義の項目化は2つの項目で構成されているため、上記のデータ例を使用して3つの例を作成しましょう。figure 32 を参照してください。数学の本に見られる関数表とは異なり、この表は垂直にレンダリングされます。左の列には目的の関数のサンプル入力が含まれ、右の列には対応する期待される結果が一覧表示されます。おわかりのように、設計レシピの最初のステップからのデータ例を使用しており、これらは項目化の双方の項目を網羅しています。

次に、設計プロセスの最も重要なステップであるテンプレートの開発に移ります。まず、`si-render` の本体は2つの `cond` 条項を持つ `cond` 式でなければならないことがわかります。設計レシピに従うと、2つの条件は `(aim? s)` と `(fired? s)` であり、これらは `si-render` が消費する可能性のある2種類のデータを区別します。

```racket
(define (si-render s)
  (cond
    [(aim? s)...]
    [(fired? s)...]))
```

第二に、構造体を扱うすべての `cond` 条項にセレクタ式を追加します。この場合、両方の条項が構造体の処理に関わっています（`aim` と `fired`）。前者は2つのフィールドを持つため、最初の `cond` 条項に対して2つのセレクタ式を必要とし、後者の種類の構造体は3つの値で構成されるため、3つのセレクタ式を要求します。

```racket
(define (si-render s)
  (cond
    [(aim? s) (... (aim-tank s)... (aim-ufo s)...)]
    [(fired? s) (... (fired-tank s)... (fired-ufo s)
... (fired-missile s)...)]))
```

テンプレートには、タスクを完了するために必要なほぼすべてのものが含まれています。定義を完成させるために、各 `cond` 行について、期待される結果を計算するために手中にある値をどのように組み合わせるかを考えます。入力の要素以外にも、グローバルに定義された定数（例えば、ここでは明らかに役立つ `BACKGROUND`）、プリミティブまたは組み込みの操作、そして万策尽きた場合には「ウィッシュリスト（望むもの一覧）関数」（つまり、あればいいのにと望む関数を記述すること）を使用できます。

戦車のデータ表現（すなわち `(aim-tank s)`）とUFOのデータ表現（すなわち `(aim-ufo s)`）を持っている最初の `cond` 条項について考えます。figure 32 の最初の例から、背景風景にこれら2つのオブジェクトを追加する必要があることがわかります。さらに、設計レシピは、これらのデータが独自のデータ定義を伴う場合、ヘルパー（補助）関数を定義することを検討し、それらを使用して結果を計算することを提案しています。

```racket
... (tank-render (aim-tank s)
                 (ufo-render (aim-ufo s) BACKGROUND))
```

ここで `tank-render` と `ufo-render` はウィッシュリスト関数です。

```racket
; Tank Image -> Image
; adds t to the given image im
(define (tank-render t im) im)

; UFO Image -> Image
; adds u to the given image im
(define (ufo-render u im) im)
```

```
+---------------------------------------------------------+
| Figure 33: The complete rendering function              |
|                                                         |
|; SIGS -> Image                                         |
|; renders the given game state on top of BACKGROUND     |
|; for examples see figure 32                            |
| (define (si-render s)                                   |
|   (cond                                                 |
|     [(aim? s)                                           |
|      (tank-render (aim-tank s)                          |
|                   (ufo-render (aim-ufo s) BACKGROUND))] |
|     [(fired? s)                                         |
|      (tank-render                                       |
|        (fired-tank s)                                   |
|        (ufo-render (fired-ufo s)                        |
|                    (missile-render (fired-missile s)    |
|                                    BACKGROUND)))]))     |
+---------------------------------------------------------+
```

少々の類推を用いれば、2番目の `cond` 条項も同様に扱うことができます。figure 33 に完全な定義を示します。何よりも素晴らしいことに、ウィッシュリスト関数である `tank-render` と `ufo-render` をすぐに再利用でき、追加する必要があるのはシーンにミサイルを含めるための関数だけです。適切なウィッシュリストの項目は以下の通りです。

```racket
; Missile Image -> Image
; adds m to the given image im
(define (missile-render m im) im)
```

上記と同様に、コメントは私たちが望むものを十分な詳細さで記述しています。
定義を示しています。何よりも素晴らしいことに、ウィッシュリスト関数である `tank-render` と `ufo-render` をすぐに再利用でき、追加する必要があるのはシーンにミサイルを含めるための関数だけです。適切なウィッシュリストの項目は以下の通りです：

上記と同様に、コメントは私たちが望むものを十分な詳細さで説明しています。


**Exercise 97.** 関数 `tank-render`、`ufo-render`、`missile-render` を設計せよ。以下の式：

```racket
(tank-render
  (fired-tank s)
  (ufo-render (fired-ufo s)
              (missile-render (fired-missile s)
                              BACKGROUND)))
```

と、こちらの式を比較せよ：

```racket
(ufo-render
  (fired-ufo s)
  (tank-render (fired-tank s)
               (missile-render (fired-missile s)
                               BACKGROUND)))
```

これら2つの式は、どのような場合に同じ結果を生成するか？

**Exercise 98.** `stop-when` ハンドラとして使用する関数 `si-game-over?` を設計せよ。UFOが着陸した場合、またはミサイルがUFOに命中した場合にゲームは停止する。どちらの条件についても、あるオブジェクトと別のオブジェクトとの接近度を判定することを推奨する。

`stop-when` 句には、オプションで2番目の部分式、すなわちゲームの最終状態を描画する関数を指定できる。`si-render-final` を設計し、演習問題100のメイン関数における `stop-when` 句の2番目のパーツとして使用せよ。

**Exercise 99.** `si-move` を設計せよ。この関数は、オブジェクトが現在どの位置に移動するかを決定するために、クロックティックごとに呼び出される。したがって、この関数は `SIGS` の要素を1つ受け取り、別の `SIGS` 要素を生成する。

戦車とミサイル（存在する場合）の移動は比較的分かりやすい。これらは一定の速度で直線上に移動する。一方、UFOの移動には左右への小さなランダムなジャンプが求められる。あなたはこれまで乱数を生成する関数を扱ったことがないため、この演習問題の残りの部分は、この問題に対処する方法についての少し長めのヒントとなっている。

BSLには乱数を生成する関数が付属している。この関数を導入することは、なぜシグネチャと目的の記述が設計においてこれほど重要な役割を果たすのかを示している。必要な関数の関連資料は以下のとおりである：

```racket
; Number -> Number
; produces a number in the interval [0,n),
; possibly a different one each time it is called
(define (random n)...)
```

シグネチャと目的の記述は関数が何を計算するかを正確に説明しているため、DrRacket の対話領域で `random` を試してみることができる。手を止めて、実際に試してみよう！

`random` が呼び出されるたびに（ほぼ）異なる数値を生成する場合、`random` を使用する関数のテストは困難になる。まずは、`si-move` とその本来の機能を2つの部分に分割する：

> **注:** `random` を使用しなければならないという考えは BSL の知識であり、習得すべき設計スキルのパートではないため、このヒントを提供している。また、`random` は数学的関数ではない BSL の最初にして唯一のプリミティブである。プログラミングにおける関数は数学的関数からインスピレーションを得ているが、同一の概念ではない。

```racket
(define (si-move w)
  (si-move-proper w (random...)))

; SIGS Number -> SIGS
; moves the space-invader objects predictably by delta
(define (si-move-proper w delta)
  w)
```

この定義により、乱数の生成とゲームオブジェクトの移動という行為を分離する。`random` は呼び出されるたびに異なる結果を生成するかもしれないが、`si-move-proper` は特定の数値入力でテストできるため、同じ入力が与えられた場合には確実に同じ結果を返すことが保証される。要するに、コードの大部分がテスト可能なまま残るのである。

`random` を直接呼び出す代わりに、UFOのランダムな x 座標を生成する関数を設計したいと思うかもしれない。そのような関数をテストするために、BSL のテストフレームワークに含まれる `check-random` の使用を検討せよ。

**Exercise 100.** キーイベント（`KeyEvent`）ハンドラの役割を果たす関数 `si-control` を設計せよ。この関数はゲーム状態と `KeyEvent` を受け取り、新しいゲーム状態を生成する。この関数は3つの異なるキーに反応する：

- 左矢印キーを押すと、戦車が左に移動する。
- 右矢印キーを押すと、戦車が右に移動する。
- スペースキーを押すと、ミサイルがまだ発射されていない場合にミサイルを発射する。

この関数ができあがったら、`big-bang` を使用してゲームプレイウィンドウを起動する `si-main` 関数を定義できる。楽しんでほしい！

```
+-----------------------------------------------------+
| Figure 34: Rendering game states again              |
|                                                     |
|; SIGS.v2 -> Image                                  |
|; renders the given game state on top of BACKGROUND |
| (define (si-render.v2 s)                            |
|   (tank-render                                      |
|     (sigs-tank s)                                   |
|     (ufo-render (sigs-ufo s)                        |
|                 (missile-render.v2 (sigs-missile s) |
|                                    BACKGROUND))))   |
+-----------------------------------------------------+
```

データ表現が唯一であることはめったにない。例えば、インベーダーゲームの状態を表すために単一の構造体型を使用することもできる：

```racket
(define-struct sigs [ufo tank missile])
; A SIGS.v2 (short for SIGS version 2) is a structure:
;   (make-sigs UFO Tank MissileOrNot)
; interpretation represents the complete state of a
; space invader game

; A MissileOrNot is one of:
; – #false
; – Posn
; interpretation#false means the missile is in the tank;
; Posn says the missile is at that location
```

ゲーム状態の最初のデータ表現とは異なり、この2番目のバージョンではミサイルの発射前と発射後を区別しない。代わりに、各状態にはミサイルに関するデータが含まれる。ただし、このデータはミサイルがまだ発射されていないことを示す `#false` である場合もある。

その結果、この2番目の状態のデータ表現に対する関数は、最初のための関数とは異なる。特に、`SIGS.v2` の要素を受け取る関数は `cond` 式を使用しない。なぜなら、そのコレクションには1種類の要素しか存在しないからである。設計アプローチの観点からは、「Designing with Structures」の構造体のための設計レシピで十分である。Figure 34 は、このデータ表現に対する描画関数を設計した結果を示している。

対照的に、`MissileOrNot` を使用する関数の設計には、本節のレシピが必要となる。画像にミサイルを追加する役割を持つ `missile-render.v2` の設計を見てみよう。以下はヘッダの素材である：

```racket
; MissileOrNot Image -> Image
; adds an image of missile m to scene s
(define (missile-render.v2 m s)
  s)
```

例に関しては、少なくとも2つのケースを考慮しなければならない。1つは `m` が `#false` の場合、もう1つは `m` が `Posn` の場合である。最初のケースでは、ミサイルは発射されていないため、与えられたシーンにミサイルの画像を追加する必要はない。2番目のケースでは、ミサイルの位置が指定されており、そこにミサイルの画像が表示されなければならない。Figure 35 は、2つの異なるシナリオにおけるこの関数の動作を示している。

```
+-----------------------------------------------------------------------+
| Figure 35: Rendering the space invader games, with tanks              |
|                                                                       |
| [image: pict_86.png]                                                  |
| [image: pict_87.png]                                                  |
|                                                                       |
| +--------------------------------------+--+-------------------------+ |
| | m                                    |  | (missile-render.v2 m s) | |
| +--------------------------------------+--+-------------------------+ |
| | #false                               |  | [image: pict_86.png]    | |
| | (make-posn ⏎ 32 ⏎ (- HEIGHT ⏎ TANK-… |  | [image: pict_87.png]    | |
| | (make-posn                           |  |                         | |
| | 32                                   |  |                         | |
| |   (- HEIGHT                          |  |                         | |
| | TANK-HEIGHT                          |  |                         | |
| |      10))                            |  |                         | |
| +--------------------------------------+--+-------------------------+ |
+-----------------------------------------------------------------------+
```

**Exercise 101.** Figure 35 の例をテストケースに変換せよ。

これでテンプレートを作成する準備が整った。主要な引数（`m`）のデータ定義は2つの要素を持つ箇条書きであるため、関数本体は2つの句を持つ `cond` 式で構成される可能性が高い：

```racket
(define (missile-render.v2 m s)
  (cond
    [(boolean? m)...]
    [(posn? m)...]))
```

再びデータ定義に従うと、最初の `cond` 句は `m` が `Boolean` 値であるかどうかをチェックし、2番目の句はそれが `Posn` の要素であるかどうかをチェックする。そして、仮に誰かが誤って `missile-render.v2` を `#true` と何らかの画像に適用した場合、関数は最初の `cond` 句を使用して結果を計算することになる。このようなエラーについては、以下でさらに詳しく述べる。

テンプレートの第2ステップでは、構造体を扱うすべての `cond` 句にセレクタ式を含めることを要求している。今回の例では、これは2番目の句に当てはまり、セレクタ式は与えられた `Posn` から x 座標と y 座標を抽出する：

```racket
(define (missile-render.v2 m s)
  (cond
    [(boolean? m)...]
    [(posn? m) (... (posn-x m)... (posn-y m)...)]))
```

このテンプレートを上記の `si-render` のテンプレートと比較してみよう。後者のデータ定義は2つの異なる構造体型を扱っているため、`si-render` の関数テンプレートには両方の `cond` 句にセレクタ式が含まれている。しかし、`MissileOrNot` のデータ定義は、単なる値である要素と構造体を記述する要素を混在させている。どちらの種類の定義もまったく問題ない。重要なのは、レシピに従い、データ定義に一致するコード構造を見つけることである。

以下が完全な関数定義である：

```racket
(define (missile-render.v2 m s)
  (cond
    [(boolean? m) s]
    [(posn? m)
     (place-image MISSILE (posn-x m) (posn-y m) s)]))
```

これをステップバイステップで行うには、まず簡単な句から取り組む。この関数では、それが最初の句である。ミサイルが発射されていないことを示しているため、関数は与えられた `s` をそのまま返す。2番目の句については、`(posn-x m)` と `(posn-y m)` がミサイル画像の座標を選択することを思い出す必要がある。この関数は `s` に `MISSILE` を追加しなければならないため、4つの値を組み合わせるのに最適なプリミティブ操作と独自の関数の組み合わせを見つける必要がある。この結合操作の選択こそが、プログラマーとしてのあなたの創造的洞察力が活かされる場所である。

**Exercise 102.** この2番目のデータ定義に対してゲームを完成させるために必要な、他のすべての関数を設計せよ。
` と `(posn-y m)` がミサイルの画像の座標を選択することを思い出す必要があります。この関数は `s` に `MISSILE` を追加しなければならないため、4つの値を組み合わせるためにプリミティブ操作と独自の関数の最適な組み合わせを見つける必要があります。この結合操作の選択こそが、プログラマーとしてのあなたの創造的な洞察力が発揮される場所です。

**Exercise 102.** この2番目のデータ定義において、ゲームを完成させるために必要な他のすべての関数を設計しなさい。


**Exercise 103.** 次の4種類の動物園の動物に関するデータ表現を作成しなさい：

- クモ（spiders）：関連する属性は残りの足の数（クモは事故で足を失う可能性があると仮定します）と、輸送時に必要なスペースです。
- ゾウ（elephants）：唯一の属性は輸送時に必要なスペースです。
- ボアコンストリクター（boa constrictors）：属性には長さと胴回りが含まれます。
- アルマジロ（armadillos）：輸送に必要なスペースを決定する属性を含め、適切な属性をあなたが決定しなければなりません。

動物園の動物を受け取る関数のためのテンプレートを作成しなさい。

動物園の動物とケージの説明を受け取る `fits?` 関数をデザインしなさい。この関数は、ケージの容積がその動物にとって十分な大きさであるかどうかを判定します。

**Exercise 104.** あなたの地元の町では、自動車（automobiles）、バン（vans）、バス（buses）、SUV という車両群（fleet of vehicles）を管理しています。乗り物のためのデータ表現を作成しなさい。各乗り物の表現には、乗車可能な乗客数、ナン、および燃料消費量（1ガロンあたりのマイル数、miles per gallon）を記述しなければなりません。乗り物を受け取る関数のためのテンプレートを作成しなさい。

**Exercise 105.** あるプログラムに次のデータ定義が含まれています。

```racket
; A Coordinate is one of:
; – a NegativeNumber
; interpretation on the y axis, distance from top
; – a PositiveNumber
; interpretation on the x axis, distance from left
; – a Posn
; interpretation an ordinary Cartesian point
```

データ定義の各節（clause）について、少なくとも2つのデータ例を作成しなさい。
それぞれの例について、キャンバスのスケッチを用いてその意味を説明しなさい。


### 6.2 世界を混ぜ合わせる (Mixing Up Worlds)

このセクションでは、バーチャルペットに関する簡単な拡張問題をはじめとして、ワールドプログラムについてのいくつかの設計問題を提案します。

**Exercise 106.** 「さらにいくつかのバーチャルペット（More Virtual Pets）」では、幸福度ゲージが付いたバーチャルペットの作成について議論しました。バーチャルペットの一方はネコで、もう一方はカメレオンです。しかし、それぞれのプログラムは単一のペット専用になっていました。

位置と動物の両方を受け取り、与えられた位置から出発して後者の動物をキャンバスを横切って歩かせる `cat-cham` ワールドプログラムを設計しなさい。動物として選択されたデータ表現は以下の通りです。

```racket
; A VAnimal is either
; – a VCat
; – a VCham
```

ここで `VCat` と `VCham` は、演習問題 88 と 92 で作成したデータ定義です。

`VAnimal` がワールド状態の集合であるとして、以下を設計する必要があります。

- `VAnimal` から `Image` への描画関数
- `VAnimal` から `VAnimal` へのクロックティックを処理する関数
- 該当する場合に応じて、動物に餌を与えたり、撫でたり、色を変えたりできるように、キーイベントを扱う関数

ネコの色を変えたり、カメレオンを撫でたりすることは依然として不可能です。

**Exercise 107.** バーチャルネコとバーチャルカメレオンの両方を扱う `cham-and-cat` プログラムを設計しなさい。両方の動物を含む「zoo（動物園）」のデータ定義と、それを扱う関数が必要となります。

問題文では、キーが2匹の動物をどのように操作するかについては開かれたままになっています。以下に2つの可能な解釈を示します。

1. 各キーイベントが両方の動物に届く。
2. 各キーイベントが2匹の動物のうち一方のみに適用される。この選択肢では、フォーカスされている動物、つまり現在操作可能な動物を指定するデータ表現が必要です。フォーカスを切り替えるには、キー処理関数に「kitty（子猫）」を表す `"k"` と「lizard（トカゲ）」を表す `"l"` を解釈イヤてくださいすとイレイ `"k"が "押す押す以降、それ以降のキーストロークは猫にのみ適用されます。

選択肢のいずれか1つを選び、適切なプログラムを設計しなさい。

**Exercise 108.** デフォルト状態では、歩行者用信号機は黒の背景の上にオレンジ色の立っている人の姿を表示します。歩行者が道路を渡る時間になると、信号機はシグナルを受信し、緑色の歩いている人の姿に切り替わります。このフェーズは10秒間続きます。その後、信号機は数字 9, 8, ..., 0 を表示し、奇数はオレンジ色、偶数は緑色で着色されます。カウントダウンが0に達すると、信号機はデフォルト状態に戻ります。

そのような歩行者用信号機を実装する世界プログラムを設計しなさい。キーボードのスペースキーを押すと、信号機はデフォルト状態から切り替わります。それ以外のすべての遷移はクロックティックに対する反応でなければなりません。以下の画像を使用しても構いませんし、

> [image: pedestrian_traffic_light_red.png] [image: pedestrian_traffic_light_green.png]

あるいは `2htdp/image` ティーチパックを使って独自の棒人間を作成しても構いません。

**Exercise 109.** 一ることもできます。

**Exercise 109.** 一連の `KeyEvent` におけるパターンを認識するワールドプログラムを設計しなさい。初期状態では、プログラムは 100 × 100 の白い長方形を表示します。プログラムが最初の意図された文字を検出すると、同じサイズの黄色の長方形を表示します。最後の文字を検出した後、長方形の色は緑色に変わります。何か「不正な（bad）」キーイベントが発生した場合、プログラムは赤色の長方形を表示します。

```
+--------------------------------------------------------------------------------+
| Figure 36: Two ways of writing a data definition for FSMs                      |
|                                                                                |
| +--------------------------------------+--+----------------------------------- |
| | conventional | | defined abbreviations |                                     |
| +--------------------------------------+--+----------------------------------- |
| |; ExpectsToSee.v1 is one of: ⏎; – … | |; ExpectsToSee.v2 is one of: ⏎; –  |
| … |                                                                            |
| |;ExpectsToSee.v1 is one of: | | |                                            |
| |;–"start, expect an 'a'" | | |                                               |
| |;–"expect 'b', 'c', or 'd'" | | |                                            |
| |;–"finished" | | |                                                           |
| |;–"error, illegal key" | | |                                                 |
| |;ExpectsToSee.v2 is one of: | | |                                            |
| |;–AA | | |                                                                   |
| |;–BB | | |                                                                   |
| |;–DD | | |                                                                   |
| |;–ER | | |                                                                   |
| | (define AA "start,...") | | |                                               |
| | (define BB "expect...") | | |                                               |
| | (define DD "finished") | | |                                                 |
| | (define ER "error,...") | | |                                               |
| +--------------------------------------+--+----------------------------------- |
+--------------------------------------------------------------------------------+
```

プログラムが探索する特定のシーケンスは `"a"` で始まり、その後に `"b"` と `"c"` の任意の長さの混合が続き、`"d"` で終わります。明らかに、`"acbd"` は受け入れ可能な文字列のひとつの例です。その他の2つの例は `"ad"` と `"abcbbbcd"` です。もちろん、`"da"`、`"aa"`、`"d"` などはマッチしません。

```
+------------------------------------------------+
| Figure 37: A finite state machine as a diagram |
|                                                |
| [image: pict_88.png]                           |
| [image: pict_88.png]                           |
+------------------------------------------------+
```

ヒント あなたの解法は有限状態機械（FSM: finite state machine）を実装します。これは、ワールドプログラムの背後にあるひとつの設計原則として「有限状態の世界（Finite State Worlds）」で導入された概念です。その名前が示すように、FSM プログラムは有限の個数の状態のうちの1つをとります。最初の状態は初期状態（initial state）と呼ばれます。それぞれのキーイベントにより、マシンは現在の状態を再検討します。同じ状態に遷移することもあれば、別の状態に遷移することもあります。プログラムが適切なキーイベントのシーケンスを認識すると、最終状態（final state）へと遷移します。

シーケンス認識問題では、状態は通常、マシンが次に現れることを期待している文字を表します。データ定義については figure 36 を参照してください。不正な入力が検出されたことを示す最後の状態を確認してください。Figure 37 は、これらの状態とその関係を図形式で捉える方法を示しています。各ノードは4つの有限状態のうちの1つに対応し、各矢印はどの `KeyEvent` によってプログラムがある状態から別の状態へ遷移するかを指定しています。

> **Note:** 右側のデータ定義は、演習問題 61 で導入された命名手法を使用しています。

歴史 1950年代、私たちがコンピュータ科学者と呼ぶであろうスティーヴン・C・クリーネ（Stephen C. Kleene）は、テキストパターンを認識する問題のための記法として正規表現（regular expressions）を発明しました。上記の問題に対して、クリーネなら次のように記述するでしょう。

> `a (b|c)* d`

これは、`d` に遭遇するまで `a` の後に任意回数の `b` または `c` が続くことを意味します。


### 6.3 入力エラー (Input Errors)

本章の中心的なポイントの1つは、述語（predicates）の役割に関するものです。述語は、データの混合（mixes of data）を処理する関数を設計しなければならない場合に不可欠となります。このような混合は、問題文で多様な種類の情報が言及されている場合に自然と現れますが、自分が作成した関数やプログラムを他人に渡す際にも現れます。結局のところ、あなた自身は自分のデータ定義や関数のシグネチャを理解し、尊重しています。しかし、友人や同僚がどのように振る舞うかは分かりませんし、特にBSLやプログラミングの知識がない人があなたのプログラムをどのように使うかは全く予想できません。したがって、本節では不適切な入力からプログラムを保護するための一つの方法を紹介します。

> **Note:** 私たちが常に自分自身の関数のシグネチャを尊重していると期待するのは、一種の自己欺瞞です。誤った種類のデータに対して関数を呼び出してしまうことは、誰にでも起こり得ます。多くの言語はBSLのようにプログラマ自身がシグネチャをチェックすることを期待していますが、他の言語では多少の複雑さと引き換えにそれを自動的に行います。

この点を単純なプログラム、すなわち円盤の面積を計算する関数を使って実証してみましょう。

```racket
; Number -> Number
; computes the area of a disk with radius r
(define (area-of-disk r)
  (* 3.14 (* r r)))
```

私たちの友人は、幾何学の宿題のためにこの関数を使いたいと思うかもしれません。あいにく、友人がこの関数を使う際、誤って数値ではなく文字列に適用してしまう可能性があります。それが起こると、関数は不可解なエラーメッセージとともにプログラムの実行を停止します。

```racket
> (area-of-disk "my-disk")
*:expects a number, given "my-disk"
```

述語を使用すると、このような分かりにくいエラーメッセージを防ぎ、自分で選択した分かりやすいエラーを発生させることができます。

```
+-------------------------------------+
| Figure 38: The universe of BSL data |
|                                     |
|; Any BSL value is one of:          |
|; – Number                          |
|; – Boolean                         |
|; – String                          |
|; – Image                           |
|; – (make-posn Any Any)             |
|;...                               |
|; – (make-tank Any Any)             |
|;...                               |
+-------------------------------------+
```

具体的には、関数を友人に渡したい場合、関数のチェック機能付きバージョン（checked versions）を定義できます。友人はBSLをあまり知らないかもしれないため、彼らがこのチェック機能付き関数を任意のBSL値（数値、文字列、画像、Posn など）に適用することを想定しなければなりません。BSLでどの構造体型が定義されるかをあらかじめ予測することはできませんが、すべてのBSL値のコレクションに対するデータ定義の大まかな形状は分かっています。Figure 38 は、このデータ定義の形状を示しています。「データの宇宙（The Universe of Data）」で議論したように、あらゆる構造体型の定義が新しいインスタンスを追加するため、Any に対するデータ定義はオープンエンド（拡張可能）です。これらのインスタンスには再は再び Any の値を保持する可能性があるため、これは Any のデータ定義が自分自身を参照しなければならないことを意味します。これは最初は恐ろしく思える考え方です。

この箇条書き（itemization）に基づくと、チェック付き関数のテンプレートはおおむね次のような形状になります。

```racket
; Any ->???
(define (checked-f v)
  (cond
    [(number? v)...]
    [(boolean? v)...]
    [(string? v)...]
    [(image? v)...]
    [(posn? v) (...(posn-x v)... (posn-y v)...)]
...
; which selectors are needed in the next clause?
    [(tank? v)...]
...))
```

もちろん、誰もこの定義のすべての節（clause）を列挙することはできません。幸いなことに、その必要はありません。私たちが知っているのは、元の関数が定義されている値のクラスに含まれるすべての値に対して、チェック付きバージョンが同じ結果を生成しなければならないということ、そしてそれ以外のすべての値に対してはエラーを発行しなければならないということです。

具体的には、サンプル関数 checked-area-of-disk は任意の BSL 値を受け取り、入力が数値である場合は area-of-disk を使って円盤の面積を計算します。そうでない場合はエラーメッセージとともに停止しなければなりません。BSL では、これを実現するために関数 error を使用します。error 関数は文字列を受け取り、プログラムを停止させます。

```racket
(error "area-of-disk: number expected")
```

したがって、checked-area-of-disk のおおまかな定義は次のようになります。

```racket
(define MESSAGE "area-of-disk: number expected")

(define (checked-area-of-disk v)
  (cond
    [(number? v) (area-of-disk v)]
    [(boolean? v) (error MESSAGE)]
    [(string? v) (error MESSAGE)]
    [(image? v) (error MESSAGE)]
    [(posn? v) (error MESSAGE)]
...
    [(tank? v) (error MESSAGE)]
...))
```

else を使用することで、この定義を自然な形で完成させることができます。

```racket
; Any -> Number
; computes the area of a disk with radius v,
; if v is a number
(define (checked-area-of-disk v)
  (cond
    [(number? v) (area-of-disk v)]
    [else (error "area-of-disk: number expected")]))
```

意図通りの結果が得られることを確認するために、実験してみましょう。

```racket
> (checked-area-of-disk "my-disk")
area-of-disk:number expected
```

自作のプログラムを他人に使ってもらうために配布する場合、チェック付き関数を書くことは重要です。しかし、正しく動作するプログラムを設計することの方がはるかに重要です。本書は適切なプログラム設計のための設計プロセスに焦点を当てており、注意を散漫にさせることなくこれを行うため、私たちは常にデータ定義とシグネチャを厳守することに同意します。少なくとも、ほぼ常にそうすることとし、ごくまれに関数やプログラムのチェック付きバージョンを設計するようお願いする場合があります。

**Exercise 110.** area-of-disk のチェック付きバージョンは、関数の引数が単なる任意の数値ではなく正の数であることを強制することもできます。checked-area-of-disk をこのように変更してください。

**Exercise 111.** 以下の定義を見てください。

```racket
(define-struct vec [x y])
; A vec is
;   (make-vec PositiveNumber PositiveNumber)
; interpretation represents a velocity vector
```

基本操作 make-vec のチェック付きバージョンとして理解される関数 checked-make-vec を作成してください。この関数は、make-vec への引数が正の数であることを保証します。言い換えれば、checked-make-vec は非形式的なデータ定義を強制します。

**述語 (Predicates)**

どのようにして独自の述語を設計できるのか疑問に思うかもしれません。結局のところ、チェック付き関数は実際には次のような一般的な形状をしているように見えます。

```racket
; Any ->...
; checks that a is a proper input for function g
(define (checked-g a)
  (cond
    [(XYZ? a) (g a)]
    [else (error "g: bad input")]))
```

ここで、g 自体は次のように定義されています。

```racket
; XYZ ->...
(define (g some-x)...)
```

XYZ というラベルのついたデータ定義が存在し、a が XYZ の要素である場合には (XYZ? a) が #true を生成し、そうでない場合には #false を生成すると仮定します。

Number を受け取る area-of-disk については、number? が明らかに適切な述語です。対照的に、前出の missile-render のようないくつかの関数については、MissileOrNot が組み込みのデータコレクションではなく作り出されたものであるため、明らかに独自の述語を定義する必要があります。それでは、MissileOrNot のための述語を設計してみましょう。

述語のシグネチャを思い出してみましょう。

```racket
; Any -> Boolean
; is a an element of the MissileOrNot collection
(define (missile-or-not? a) #false)
```

述語の目的文（purpose statements）として疑問文を使用するのは良い習慣です。なぜなら、述語を適用することは値について質問することに似ているからです。名前の末尾にある疑問符「?」もこの考え方を強固にします。そのような関数の名前を発音する際に、末尾に「huh」を付け足す人もいます。

例を作成するのも簡単です。

```racket
(check-expect (missile-or-not? #false) #true)
(check-expect (missile-or-not? (make-posn 9 2)) #true)
(check-expect (missile-or-not? "yellow") #false)
```

最初の2つの例は、MissileOrNot のすべての要素が #false または何らかの Posn のいずれかであることを思い出させます。3番目のテストは、文字列がこのコレクションの要素ではないことを示しています。さらに3つのテストを示します。

```racket
(check-expect (missile-or-not? #true) #false)
(check-expect (missile-or-not? 10) #false)
(check-expect (missile-or-not? empty-image) #false)
```

期待される答えの理由を説明してください！

述語は可能なすべての BSL 値を受け取るため、そのテンプレートは checked-f のテンプレートとまったく同じです。手を止めてください！読み進める前に、テンプレートを見つけてもう一度確認してください。

チェック付き関数と同様に、述語はすべての可能な cond 行を必要とするわけではありません。#true を生成する可能性がある行だけが必要です。

```racket
(define (missile-or-not? v)
  (cond
    [(boolean? v)...]
    [(posn? v) (... (posn-x v)... (posn-y v)...)]
    [else #false]))
```

他のすべてのケースは、#false を生成する else 行を介してまとめられます。

テンプレートが与えられれば、missile-or-not? の定義は、各ケースについて熟考するだけの簡単な問題です。

```racket
(define (missile-or-not? v)
  (cond
    [(boolean? v) (boolean=? #false v)]
    [(posn? v) #true]
    [else #false]))
```

#false のみが正当な MissileOrNot であり、#true は違います。私たちはこのアイデアを (boolean=? #false v) で表現しますが、(false? v) でも構いません。

```racket
(define (missile-or-not? v)
  (cond
    [(false? v) #true]
    [(posn? v) #true]
    [else #false]))
```

当然ながら、Posn のすべての要素は MissileOrNot のメンバーでもあり、これが2行目の #true の理由を説明しています。

**Exercise 112.** or 式を使用して、この述語を再定式化してください。

**Exercise 113.** 前節のデータ定義である SIGS、Coordinate（exercise 105）、および VAnimal に対する述語を設計してください。

締めくくりとして、ワールドプログラムで使用したくなるかもしれない2つの重要な述語として key-event? と mouse-event? を挙げておきます。これらは期待される性質を検証しますが、何が計算されるのかを確実に理解するために、それらのドキュメントを確認しておく必要があります。


### 6.4 ワールドのチェック (Checking the World)

ワールドプログラムでは、多くのことがうまく行かなくなる可能性があります。関数は常に適切な種類のデータに適用されると信頼することに同意したばかりですが、ワールドプログラムでは一度にあまりにも多くのことを同時にこなすため、自分自身にそこまでの信頼を寄せることはできないかもしれません。クロックティック、マウスクリック、キー入力、そして描画を処理するワールドプログラムを設計するとき、それらの相互作用のどれかを誤ってしまうのはあまりにも簡単なことです。もちろん、誤りが生じたからといって、BSLがその間違いを即座に認識するわけではありません。例えば、私たちの関数の1つが、ワールド状態のためのデータ表現の要素とは言えない結果を生成してしまうかもしれません。それと同時に、`big-bang` はこのデータを受け取り、次のイベントが発生するまでそれを保持し続けます。プログラムが失敗するのは、後続のイベントハンドラがこの不適切なデータを受け取った時だけかもしれません。しかし、事態はさらに悪化することもあります。というのも、2番目、3番目、あるいは4番目のイベント処理ステップでさえ、実際には不適切な状態値をなんとか処理できてしまい、処理のずっと後になってからすべてが破綻する可能性があるからです。

この種の問題に対処するため、`big-bang` には、ワールド状態に対する述語を受け取るオプションの `check-with` 節が用意されています。例えば、すべてのワールド状態を `Number` で表現することを選択した場合、この事実を次のように簡単に表現できます：

```racket
(define (main s0)
  (big-bang s0... [check-with number?]...))
```

いずれかのイベント処理関数が数値以外のものを生成した瞬間、ワールドは適切なエラーメッセージを出して停止します。

`check-with` 節は、データ定義が `number?` のような組み込み述語を持つ単なるデータのクラスではなく、次のような区間定義のように繊細なものである場合、さらに役立ちます：

```racket
; A UnitWorld is a number
;   between 0 (inclusive) and 1 (exclusive).
```

```racket
; Any -> Boolean
; is x between 0 (inclusive) and 1 (exclusive)

(check-expect (between-0-and-1? "a") #false)
(check-expect (between-0-and-1? 1.2) #false)
(check-expect (between-0-and-1? 0.2) #true)
(check-expect (between-0-and-1? 0.0) #true)
(check-expect (between-0-and-1? 1.0) #false)

(define (between-0-and-1? x)
  (and (number? x) (<= 0 x) (< x 1)))
```

その場合、この区間のための述語を定式化したくなります：

```racket
; Any -> Boolean
; is x between 0 (inclusive) and 1 (exclusive)

(check-expect (between-0-andxpect (between-0-and-1? 0.0) #true)
(check-expect (between-0-and-1? 1.0) #false)

(define (between-0-and-1? x)
  (and (number? x) (<= 0 x) (< x 1)))
```

この述語を使えば、ワールドプログラムにおけるあらゆる単一の遷移を監視できるようになります：

```racket
(define (main s0)
  (big-bang s0
...
            [check-with between-0-and-1?]
...))
```

ワールドを生成するハンドラのいずれかが区間外の数値を生成したり、さらに悪いことに非数値の値を生成したりした場合、プログラムはその間違いを即座に発見し、修正する機会を与えてくれます。

Exercise 114. 演習113の述語を使用して、インベーダーゲームのワールドプログラム、バーチャルペットのプログラム（演習106）、およびエディタプログラム（グラフィカルエディタ）をチェックしなさい。

### 6.5 等価述語 (Equality Predicates)

等価述語とは、同じデータコレクションの2つの要素を比較する関数です。3つの文字列 "red"、"green"、"yellow" のコレクションである TrafficLight の定義を思いだしてください。light=? 関数を定義する1つの方法を以下に示します：

```racket
; TrafficLight TrafficLight -> Boolean
; are the two (states of) traffic lights equal

(check-expect (light=? "red" "red") #true)
(check-expect (light=? "red" "green") #false)
(check-expect (light=? "green" "green") #true)
(check-expect (light=? "yellow" "yellow") #true)

(define (light=? a-value another-value)
  (string=? a-value another-value))
```

*RUN* をクリックすると、すべてのテストが成功しますが、残念ながら他の対話によって意図との不一致が明らかになります：

```racket
> (light=? "salad" "greens")
#false
> (light=? "beans" 10)
string=?:expects a string, given 10
```

これらの対話を、他の組み込み等価述語と比較してみてください：

```racket
> (boolean=? "#true" 10)
boolean=?:expects a boolean as 1st argument, given "#true"
```

ご自身で (string=? 10 #true) や (= 20 "help") を試してみてください。それらはすべて、誤った種類の引数に適用されたというエラーを発生させます。

チェック機能を備えた改訂版の light=? は、両方の引数が TrafficLight に属していることを強制します。属していない場合、組み込みの等価述語が発生させるようなエラーを送出します。簡潔にするため、TrafficLight に対する述語を light? と呼ぶことにします：

> **注:** 文字の大文字と小文字は区別されます。"red" は "Red" や "RED" とは異なります。

```racket
; Any -> Boolean
; is the given value an element of TrafficLight
(define (light? x)
  (cond
    [(string? x) (or (string=? "red" x)
                     (string=? "green" x)
                     (string=? "yellow" x))]
    [else #false]))
```

これで、最初の分析に従って light=? の改訂を締めくくることができます。まず、関数は2つの入力が TrafficLight の要素であるかどうかを判定し、そうでない場合は error を使用して間違いを通知します：

```racket
(define MESSAGE
  "traffic light expected, given some other value")

; Any Any -> Boolean
; are the two values elements of TrafficLight and,
; if so, are they equal

(check-expect (light=? "red" "red") #true)
(check-expect (light=? "red" "green") #false)
(check-expect (light=? "green" "green") #true)
(check-expect (light=? "yellow" "yellow") #true)

(define (light=? a-value another-value)
  (if (and (light? a-value) (light? another-value))
      (string=? a-value another-value)
      (error MESSAGE)))
```

Exercise 115. 2つの引数のうちどちらが TrafficLight の要素でないかをエラーメッセージで明示するように、light=? を改訂しなさい。

作成するプログラムで light=? を使う可能性は低いかもしれませんが、前サブセクションの最後で簡単に触れた2つの等価述語である key=? と mouse=? は使用すべきです。当然ながら、key=? は2つの KeyEvent を比較するための操作であり、同様に mouse=? は2つの MouseEvt を比較します。どちらのイベントの種類も文字列として表現されますが、すべての文字列がキーイベントやマウスイベントを表すわけではないことを理解しておくことが重要です。

今後は、キーイベントハンドラでは key=? を、マウスイベントハンドラでは mouse=? を使用することを推奨します。キーイベントハンドラで key=? を使用することで、その関数が任意の文字列ではなく、確実にキーイベントを表す文字列同士を比較するようになります。例えば、誤って関数が "hello\n world" に適用されると即座に、key=? はエラーを発生させ、何か異常があることを知らせてくれます。

## 7 まとめ (Summary)

本書のこの第I部では、シンプルでありながら重要な教訓をいくつも学んだ。以下にまとめる：

1. 優れたプログラマーはプログラムを**設計する**。下手なプログラマーは、プログラムが動いているように見えるまでいじくり回す。
2. 設計レシピには2つの側面がある。1つは設計の**プロセス**、すなわち実行すべき一連の手順に関わる。もう1つは、選んだデータの**表現**が設計プロセスにどのように影響するかを説明する。
3. 適切に設計されたプログラムはすべて、多くの定数定義、構造体型定義、データ定義、および関数定義で構成される。バッチプログラムでは、1つの関数が「main」関数となり、通常その計算を行うために他のいくつかの関数を組み合わせる。対話型プログラムでは、`big-bang` 関数が main 関数の役割を果たす。これはプログラムの初期状�
�、画像を生成する出力関数、および高��
3つのイベントハンドラ（クロックティック用、マウスクリック用、キーイベント用にそれぞれ1つ）を指定する。どちらの種類のプログラムでも、関数定義は「トップダウン」で提示される。つまり main 関数から始まり、main 関数��
で言及されている関数が続き、以下同様に続く。
4. すべてのプログラミング言語と同様に、Beginning Student Language には語彙と文法がある。プログラマーは、�
�力が与えられたときにプログラムがどのように計算を実行するかを予測できるよう、言語��
の各文の意味を判断できなければならない。続く Intermezzo では、この考え方を詳細に説明する。
5. BSL を含むプログラミング言語には、プログラマーが毎回車輪の再発明をしなくて済むよう、豊富なティーチパックが付属している。プログラマーは、ティーチパックが提供する関数、特にそのシグネチャと目的記述に�
�れ親しむべきである。そうすることで作業がシンプルになる。
6. プログラマーは、選んだプログラミング言語が提供する「ツール」を熟知しなければならない。これらのツールは、`cond` や `max` のように言語自体の一部であるか、ティーチパックから「インポート」されたもののいずれかである。この精神に則り、以下の用語を確実に理解しているか確認せよ：構造体型定義（structure type definition）、関数定義（function definition）、定数定義（constant definition）、構造体インスタンス（structure instance）、データ定義（data definition）、`big-bang`、およびイベントハンドラ関数（event-handling function）。
