## 8 リスト (Lists)

おそらくこれまでに、自己参照による定義（self-referential definitions）に出会ったことはないでしょう。英語の先生は間違いなくこうした定義を避けますし、多くの数学の授業でも、このような定義に関しては曖昧な説明にとどまります。プログラマーが曖昧さを受け入れるわけにはいきません。プログラマーの仕事には精密さが求められるからです。一般的に、定義は自身への参照を複数含むこともありますが、本章では、リストに関する定義を切り出しとして、参照が1つだけで足りる有用な例を紹介します。

リストの導入は、私たちが取り扱うことのできるアプリケーションの種類をいっそう刺激的なものにします。本章では、例を通じてあなたの直感を丁寧に養うとともに、自己参照型のデータ定義を扱う関数を体系的に作成する方法を説明する次章でのデザインレシピの改訂へと橋渡しを行います。

### 8.1 リストの作成 (Creating Lists)

私たちは誰しも、日常的にリストを作成しています。食料品の買い物に行く前には、購入したい品物のリストを書き出します。毎朝ToDoリストを書く人もいます。12月になると、多くの子供たちがクリスマスに欲しいもののリストを準備します。パーティーを計画する際には、招待客のリストを作成します。このように情報をリストの形で整理することは、私たちの生活において至る所で見られる日常的な行為です。

情報がリストの形態をとる以上、そうしたリストをBSLデータとしてどのように表現するかを明確に学ぶ必要があります。実際、リストは非常に重要であるため、BSLには直交座標上の点（posn）に対するサポートと同様に、リストの作成や操作のための組込みサポートが備わっています。点とは対照的に、リストのデータ定義を記述することは常にあなた自身に委ねられます。しかし、まずは第一歩から始めましょう。リストの作成からスタートします。

リストを生成するとき、私たちは常に空のリスト（empty list）から出発します。BSLでは、空のリストを次のように表現します。

```racket
'()
```

これは「エンプティ（empty）」と発音され、「空のリスト（empty list）」の略です。#true や 5 と同様に、'() も単なる定数です。リストに何かを追加するとき、私たちは別のリストを構築します。BSLでは、cons 操作がこの目的を果たします。例えば、

```racket
(cons "Mercury" '())
```

は、'() リストと文字列 "Mercury" からリストを構築します。Figure 44 は、構造体で用いたのと同じ図的表現でこのリストを示しています。cons のボックスには、first と rest という2つのフィールドがあります。この特定の例では、first フィールドに "Mercury" が含まれ、rest フィールドに '() が含まれています。

```
+--------------------------------------------------------------------+
| Figure 44: Building a list                                         |
|                                                                    |
| [image: pict_89.png]                                               |
| [image: pict_90.png]                                               |
| [image: pict_91.png]                                               |
|                                                                    |
| +--------------------------------------+--+----------------------+ |
| | list                                 |  | diagram              | |
| +--------------------------------------+--+----------------------+ |
| | (cons "Mercury" ⏎ '())               |  | [image: pict_89.png] | |
| | (cons "Mercury"                      |  |                      | |
| |   '())                               |  |                      | |
| | (cons "Venus" ⏎ (cons "Mercury" ⏎ '… |  | [image: pict_90.png] | |
| | (cons "Venus"                        |  |                      | |
| |   (cons "Mercury"                    |  |                      | |
| |     '()))                            |  |                      | |
| | (cons "Earth" ⏎ (cons "Venus" ⏎ (co… |  | [image: pict_91.png] | |
| | (cons "Earth"                        |  |                      | |
| |   (cons "Venus"                      |  |                      | |
| |     (cons "Mercury"                  |  |                      | |
| |       '())))                         |  |                      | |
| +--------------------------------------+--+----------------------+ |
+--------------------------------------------------------------------+
```

1つの要素を含むリストが得られたら、再び cons を使用することで、2つの要素を含むリストを構築できます。その1例がこちらです：

```racket
(cons "Venus" (cons "Mercury" '()))
```

そして、もう1つの例がこちらです：

```racket
(cons "Earth" (cons "Mercury" '()))
```

Figure 44 の中央の行は、2つの要素を含むリストのイメージを示しています。これも2つのフィールドを持つボックスですが、今回の rest フィールドにはボックスが含まれています。実際、そこには同じ図の上の行のボックスが含まれています。

最後に、3つの要素を持つリストを構築します：

```racket
(cons "Earth" (cons "Venus" (cons "Mercury" '())))
```

Figure 44 の最後の行は、3つの要素を持つリストを示しています。その rest フィールドには、さらにボックスを含むボックスが入っています。したがって、リストを作成するにつれて、私たちはボックスの中にボックス、さらにその中にボックスを入れていくことになります。一見すると奇妙に思えるかもしれませんが、これは誕生日に時々もらうような、中国の入れ子式の贈り物用ボックスや入れ子のコップのセットと全く同じです。唯一の違いは、BSLプログラムはどんな職人が物理的な箱を入れ子にするよりもはるかに深くリストを入れ子にできるという点です。

```
+-----------------------------------------------------------------------------+
| Figure 45: Drawing a list                                                   |
|                                                                             |
| [image: pict_92.png]                                                        |
| [image: pict_93.png]                                                        |
|                                                                             |
| +--------------------------------------------------+----------------------+ |
| | (cons "Earth" ⏎ (cons "Venus" ⏎ (cons "Mercury"… | [image: pict_92.png] | |
| +--------------------------------------------------+----------------------+ |
| | (cons "Earth"                                    |                      | |
| |   (cons "Venus"                                  |                      | |
| |     (cons "Mercury"                              |                      | |
| |       '())))                                     |                      | |
| |                                                  | [image: pict_93.png] | |
| +--------------------------------------------------+----------------------+ |
+-----------------------------------------------------------------------------+
```

腕の良い芸術家であっても、深く入れ子になった構造を描くのには苦労するため、コンピュータ科学者は代わりに箱と矢印による図（box-and-arrow diagrams）を用います。Figure 45 は、Figure 44 の最後の行をどのように再配置するかを示しています。各 cons 構造は別々のボックスになります。rest フィールドが複雑すぎてボックスの内部に描けない場合は、代わりに黒丸（bullet）を描き、そこからそれが含んでいるボックスへと矢印付きの線を引きます。ボックスの配置方法によって、2種類の図が得られます。Figure 45 の上の行に表示されている最初の図は、ボックスが作成された順序で並べています。下の行に表示されている2番目の図は、ボックスが cons によって結合された順序で並べています。したがって、2番目の図を見れば、リストがどれほど長くても、そのリストに first を適用したときに何が生成されるかがすぐにわかります。そのため、プログラマーは2番目の配置を好みます。

**Exercise 129.** 以下を表現するBSLリストを作成しなさい：

1. 天体のリスト（例えば、少なくとも太陽系のすべての惑星）
2. 食事の品目のリスト（例えば、ステーキ、フライドポテト、豆、パン、水、ブリー・チーズ、アイスクリーム）
3. 色のリスト

figure 44 や figure 45 にあるような、これらのリストのボックス表現のスケッチを描きなさい。あなたはどちらのスケッチが好みですか。

数値のリストを作成することもできます。以下は 10 個の一桁の数字を含むリストです：

```racket
(cons 0
  (cons 1
    (cons 2
      (cons 3
        (cons 4
          (cons 5
            (cons 6
              (cons 7
                (cons 8
                  (cons 9 '()))))))))))
```

このリストを構築するには、10 回のリスト構築と 1 つの '() が必要です。例えば、任意の 3 つの数値のリストの場合、

```racket
(cons pi
  (cons e
    (cons -22.3 '())))
```

3 回の cons が必要となります。

一般に、リストは 1 種類の値だけを含む必要はなく、任意の値を含めることができます：

```racket
(cons "Robbie Round"
  (cons 3
    (cons #true
      '())))
```

このリストの最初の要素は文字列、2 番目の要素は数値、最後の要素は Boolean です。このリストを、従業員の名前、勤続年数、会社経由の医療保険に加入しているかどうかという 3 つのデータからなる人事記録の表現とみなすこともできます。あるいは、ゲームのバーチャルプレイヤーを表していると考えることもできるでしょう。データ定義がなければ、そのデータが何を表しているのかを知ることはできません。

> **Note:** とはいえ、このリストが固定された数のデータ項目からなる記録を表現するものである場合は、代わりに構造体型（structure type）を使用してください。

以下は cons を含む最初のデータ定義です：

```racket
; A 3LON is a list of three numbers:
;   (cons Number (cons Number (cons Number '())))
; interpretation a point in 3-dimensional space
```

もちろん、このデータ定義は、構造体のインスタンスに対するコンストラクタを他の定義で使うのと同様に cons を使用しており、ある意味で cons は特別なコンストラクタにすぎません。このデータ定義が示すことができていないのは、任意の長さのリスト、つまり要素が 0 個、1 個、2 個、10 個、あるいは 1,438,901 個含まれるリストをどのように形成するかということです。

そこでお試しに、もう一度試してみましょう：

```racket
; A List-of-names is one of:
; – '()
; – (cons String List-of-names)
; interpretation a list of invitees, by last name
```

深呼吸して、もう一度読んでみてください。このデータ定義は、あなたがこれまでに巡り合った定義の中で最も異質なものの1つです。自分自身を参照する定義など、以前に見たことがないでしょう。これが意味をなしているのかさえ即座にはわかりません。なにしろ、英語の先生に「『テーブルとはテーブルである』というのが『テーブル』という単語の定義です」と言ったとしたら、「ナンセンス！」と返ってくるのが関の山だからです。自己参照の定義は、単語の意味を説明してくれないからです。

しかし、コンピュータサイエンスやプログラミングにおいて、自己参照型の定義（self-referential definitions）は中心的な役割を果たしており、注意深く扱えば、そのような定義は実際に意味をなします。ここで「意味をなす」とは、データ定義を本来の目的のために使用できること、すなわち、定義されているクラスに属するデータの例を生成したり、与えられたデータが定義されたデータクラスに属しているかどうかをチェックしたりできることを意味します。この観点から見ると、List-of-names の定義は完全に意味をなしています。最低限、箇条書きの最初の条項を使用して、1つの例として '() を生成することができます。List-of-names の要素として '() が与えられれば、2つ目の例を作るのは簡単です：

```racket
(cons "Findler" '())
```

ここでは、箇条書きの 2 番目の条項に従い、String と List-of-names から得られた唯一のリストを使用して、データを生成しています。同じルールを使って、この種のリストをさらに多く生成できます：

```racket
(cons "Flatt" '())
(cons "Felleisen" '())
(cons "Krishnamurthi" '())
```

そして、これらのリストはいずれも 1 つの名前（String として表現）を含んでいますが、データ定義の 2 行目を使用して、より多くの名前を含むリストを作成することも実際に可能です：

```racket
(cons "Felleisen" (cons "Findler" '()))
```

このデータは、"Felleisen" が String であり、(cons"Findler"'()) が確認済みの List-of-names であるため、List-of-names に属します。

**Exercise 130.** 5 つの String を含む List-of-names の要素を作成しなさい。figure 44 にあるものと同様の、そのリストのボックス表現をスケッチしなさい。

なぜ

```racket
(cons "1" (cons "2" '()))
```

が List-of-names の要素であり、なぜ (cons2'()) はそうでないのかを説明しなさい。

**Exercise 131.** Boolean 値のリストを表現するためのデータ定義を提供しなさい。そのクラスには、任意の長さのすべての Booleans のリストが含まれます。
れます。

### 8.2 '()とは何か、consとは何か (What Is '(), What Is cons)

少し立ちし立ってまっ と c'() につ に詳して詳てみ見てょうしょ述の前述り、おは単 は定数る定数です。5 や "this is a string" のような定数と比較する数名関数名や変数のように見えます。しかし、#true や #false と比較してみれば、それが単に空リストを表す BSL の表現にすぎないことが容易に理解できるでしょう。

評価規則の観点から見ると、'() は新しい種類の原子値（atomic vaなど、他のあらゆる種類とは区別される新しい種類の原子値（atomic value）です。また、Posn のような複合値（compound value）でもありません。実に '() は非常にユニークであり、それ自体だけで単独のクラスに属しています。そのため、この値のクラスには、'() だけを認識し、それ以外は一切認識しない述語が用意されています。

```racket
; Any -> Boolean
; is the given value '()
(define (empty? x)...)
```

すべての述語と同様に、empty? は BSL の値の全体集合からの任意の値に対して適用できます。これが #true を出力するのは、'() に適用されたときに限られます。

```racket
> (empty? '())
#true
> (empty? 5)
#false
> (empty? "hello world")
#false
> (empty? (cons 1 '()))
#false
> (empty? (make-posn 0 0))
#false
```

次に cons について見ていきます。これまで見てきたすべてのことから、cons は構造体型の定義によって導入されるコンストラクタとまったく同様のコンストラクタであることが示唆されます。より正確には、cons は2つのフィールドを持つ構造体のコンストラクタであるように見えます。1つ目のフィールドはあらゆる種類の値のためのものであり、2つ目のフィールドはリスト状のあらゆる値のためのものです。以下の定義は、この考え方を BSL に変換したものです。

```racket
(define-struct pair [left right])
; A ConsPair is a structure:
;   (make-pair Any Any).

; Any Any -> ConsPair
(define (our-cons a-value a-list)
  (make-pair a-value a-list))
```

唯一の問題は、our-cons が2番目の引数としてありとあらゆる BSL の値を受け入れられるのに対し、cons は受け入れないという点です。これは以下の実験によって確認できます。

```racket
> (cons 1 2)
cons:second argument must be a list, but received 1 and 2
```

言い方を変えれば、cons は実質的に「要素の列挙と構造体 (Itemizations and Structures)」で議論された種類のチェック付き関数（checked function）であり、このことから次のような洗練形が示唆されます。

```racket
; A ConsOrEmpty is one of:
; – '()
; – (make-pair Any ConsOrEmpty)
; interpretation ConsOrEmpty is the class of all lists

; Any Any -> ConsOrEmpty
(define (our-cons a-value a-list)
  (cond
    [(empty? a-list) (make-pair a-value a-list)]
    [(pair? a-list) (make-pair a-value a-list)]
    [else (error "cons: second argument...")]))
```

もし cons がチェック付きコンストラクタ関数であるなら、結果として得られる構造体からどのようにして各要素を取り出せばよいのか疑問に思うかもしれません。結局のところ、「構造体の追加 (Adding Structure)」で述べられているように、構造体を用いたプログラミングにはセレクタが必要です。cons 構造体には2つのフィールドがあるため、2つのセレクタ first と rest が存在します。これらも pair 構造体を用いて簡単に定義できます。

```racket
; ConsOrEmpty -> Any
; extracts the left part of the given pair
(define (our-first a-list)
  (if (empty? a-list)
      (error 'our-first "...")
      (pair-left a-list)))
```

Stop! our-rest を定義しなさい。

プログラムが pair の構造体型定義にアクセスできる場合、右側のフィールドに '() や別の pair を含まない pair を作成することは容易です。そのような不良なインスタンスが意図的であれ偶発的であれ作成されると、それらは関数やプログラムを奇妙な方法で破壊しがちです。したがって BSL は、これらの問題を回避するために cons の実際の構造体型定義を隠蔽しています。「局所定義 (Local Definitions)」では、プログラムがそのような定義を隠す方法の1つを示していますが、現時点ではこの機能は必要ありません。

```
+------------------------------------------------------+
| Figure 46: List primitives                           |
|                                                      |
| +--------+--+--------------------------------------+ |
| | '()    |  | a special value, mostly to represen… | |
| +--------+--+--------------------------------------+ |
| | empty? |  | a predicate to recognize '() and no… | |
| | cons   |  | a checked constructor to create two… | |
| | first  |  | the selector to extract the last it… | |
| | rest   |  | the selector to extract the extended… | |
| | cons?  |  | a predicate to recognize instances … | |
| +--------+--+--------------------------------------+ |
+------------------------------------------------------+
```

```
+------------------------------------------------------+
| Figure 46: List primitives                           |
|                                                      |
| +--------+--+--------------------------------------+ |
| | '()    |  | a special value, mostly to represen… | |
| +--------+--+--------------------------------------+ |
| | empty? |  | a predicate to recognize '() and no… | |
| | cons   |  | a checked constructor to create two… | |
| | first  |  | the selector to extract the last it… | |
| | rest   |  | the selector to extract the extende… | |
| | cons?  |  | a predicate to recognize instances … | |
| +--------+--+--------------------------------------+ |
+------------------------------------------------------+
```

Figure 46 はこの節をまとめたものです。重要な洞察は、'() がユニークな値であり、cons はリスト値を生成するチェック付きコンストラクタであるということです。さらに、first、rest、cons? は通常の述語やセレクタに対する単なる異なる名称にすぎません。したがって、この章が教えているのは、データの新しい作成方法ではなく、データ定義の新しい定式化方法なのです。

### 8.3 リストを用いたプログラミング (Programming with Lists)

あるリストを使って友人を管理しているとしましょう。そして、そのリストが非常に大きくなったため、ある名前がリストにあるかどうかを判定するプログラムが必要になったとします。このアイディアを具体的にするために、例題として定式化してみましょう：

> **注記:** ここでは「友人（friend）」という言葉を、現実世界ではなくソーシャルネットワークの意味で使っています。

> 例題 あなたは新しい携帯電話の連絡先リストの作成に取り組んでいます。電話の持ち主は様々な機会にこのリストを更新し、参照します。今のととりあえず、あなたはこの連絡先リストを受け取り、そこに "Flatt" という名前が含まれているかどうかを判定する関数を設計するタスクを割り当てられました。

この例題に対する解法が得られたら、それを一般化して、リスト上の任意の名前を見つける関数にします。

前節の List-of-names のデータ定義は、関数が検索する名前のリストを表現するのに適しています。次にヘッダー部分に取り組みます。

```racket
; List-of-names -> Boolean
; determines whether "Flatt" is on a-list-of-names
(define (contains-flatt? a-list-of-names)
  #false)
```

`a-list-of-names` は関数が受け取る名前のリストの名前として優れていますが、長くて口にしづらいため、`alon` に短縮します。

一般的な設計レシピに従い、次に関数の目的を説明する例をいくつか作成します。まず、最も単純な入力である `'()` に対する出力を決定します。このリストには文字列が一切含まれていないため、"Flatt" が含まれていないことは確実です。

```racket
(check-expect (contains-flatt? '()) #false)
```

次に、要素が 1 つだけのリストを考えます。以下に 2 つの例を示します。

```racket
(check-expect (contains-flatt? (cons "Find" '()))
              #false)
(check-expect (contains-flatt? (cons "Flatt" '()))
              #true)
```

最初のケースでは、リスト上の唯一の要素が "Flatt" ではないため、答えは `#false` になります。2 番目のケースでは、唯一の要素が "Flatt" であるため、答えは `#true` になります。最後に、より一般的な例を挙げます。

```racket
(check-expect
  (contains-flatt?
    (cons "A" (cons "Flatt" (cons "C" '()))))
  #true)
```

この場合も、リストに "Flatt" が含まれているため、答えは `#true` でなければなりません。手を止めて考えてみましょう！ 答えが `#false` にならなければならない一般的な例を作成してください。

ひと息ついて、プログラムを実行してください。ヘッダーは関数の「ダミー」定義であり、いくつかの例を作成し、それらはテストに変換されました。そして素晴らしいことに、そのうちのいくつかは実際に成功します。間違った理由で成功しているのですが、成功することには変わりありません。ここまでの内容が理解できたら、読み進めてください。

第 4 のステップは、データ定義に一致する関数テンプレートを設計することです。文字列のリストに対するデータ定義には 2 つの節があるため、関数の本体は 2 つの節を持つ `cond` 式でなければなりません。2 つの条件によって、関数が 2 種類のリストのどちらを受け取ったかが決定されます。

```racket
(define (contains-flatt? alon)
  (cond
    [(empty? alon)...]
    [(cons? alon)...]))
```

`(cons? alon)` の代わりに、2 番目の節で `else` を使用することもできます。

`cond` 式の各節を順に調べることで、テンプレートにもう 1 つのヒントを追加できます。具体的には、対応する入力クラスが複合体で構成されている場合、各節にセレクタ式をアノテーションとして注記することが設計レシピで提案されていたことを思い出してください。今回のケースでは、`'()` には複合体がないため、構成要素はありません。そうでない場合、リストは文字列と別の文字列のリストから構築されるため、テンプレートに `(first alon)` と `(rest alon)` を追加することで、この事実を思い出せるようにします。

```racket
(define (contains-flatt? alon)
  (cond
    [(empty? alon)...]
    [(cons? alon)
     (... (first alon)... (rest alon)...)]))
```

いよいよ本題のプログラミング作業、つまり設計レシピの第 5 ステップに移行します。これはテンプレートから始まり、各 `cond` 節を個別に処理します。`(empty? alon)` が真の場合、入力は空リストであり、この場合、関数は結果 `#false` を生成しなければなりません。2 番目のケースでは、`(cons? alon)` が真になります。テンプレート内の注記は、最初の文字列が存在し、その後に残りのリストが続くことを思い出させてくれます。そこで、このカテゴリに該当する例を考えてみましょう。

```racket
(cons "A"
  (cons...
... '()))
```

関数は人間と同じように、最初の要素を "Flatt" と比較しなければなりません。この例では、最初の要素は "A" であり "Flatt" ではないため、比較結果は `#false` になります。代わりに別の例、例えば以下のようなものを考えていたとしましょう。

```racket
(cons "Flatt"
  (cons...
... '()))
```

関数は入力の最初の要素が "Flatt" であると判定し、したがって `#true` を返します。このことは、`cond` 式の 2 行目にリスト上の最初の名前と "Flatt" を比較する式が含まれるべきであることを意味しています。

```racket
(define (contains-flatt? alon)
  (cond
    [(empty? alon) #false]
    [(cons? alon)
     (... (string=? (first alon) "Flatt")
... (rest alon)...)]))
```

さらに、比較結果が `#true` を生成した場合、関数は `#true` を出力しなければなりません。比較結果が `#false` の場合、残るのは別の文字列のリスト、すなわち `(rest alon)` です。明らかに、この場合関数は最終的な答えを知ることができません。なぜなら、答えは「`...`」が何を表しているかに依存するからです。言い換えれば、最初の要素が "Flatt" でない場合、リストの残りに "Flatt" が含まれているかどうかをチェックする何らかの方法が必要になります。

幸いなことに、私達には `contains-flatt?` があり、それが目的にぴったり合致しています。その目的記述によれば、この関数はリストに "Flatt" が含まれているかどうかを判定します。この記述は、`(contains-flatt? l)` が文字列のリスト `l` に "Flatt" が含まれているかどうかを教えてくれることを意味します。そして同様に、`(contains-flatt? (rest alon))` は "Flatt" が `(rest alon)` の要素であるかどうかを判定します。これこそが、まさに私たちが知る必要のあることです。

手短に言えば、最後の行は `(contains-flatt? (rest alon))` となるべきです。

```racket
; List-of-names -> Boolean
(define (contains-flatt? alon)
  (cond
    [(empty? alon) #false]
    [(cons? alon)
     (... (string=? (first alon) "Flatt")...
... (contains-flatt? (rest alon))...)]))
```

コツは、2 つの式の値を適切な方法で組み合わせることです。前述のように、最初の式が `#true` を生成する場合、リストの残りを検索する必要はありません。しかし、それが `#false` であっても、2 番目の式が依然として `#true` を生成する可能性があり、それは名前 "Flatt" がリストの残りに存在することを意味します。これらすべては、最後の行の最初の式または 2 番目の式のいずれかが `#true` を生成する場合、`(contains-flatt? alon)` の結果が `#true` になることを示唆しています。

```
+--------------------------------------------------------------+
| Figure 47: Searching a list                                  |
|                                                              |
|; List-of-names -> Boolean                                   |
|; determines whether "Flatt" occurs on alon                  |
| (check-expect                                                |
|   (contains-flatt? (cons "X" (cons "Y"  (cons "Z" '()))))    |
|   #false)                                                    |
| (check-expect                                                |
|   (contains-flatt? (cons "A" (cons "Flatt" (cons "C" '())))) |
|   #true)                                                     |
| (define (contains-flatt? alon)                               |
|   (cond                                                      |
|     [(empty? alon) #false]                                   |
|     [(cons? alon)                                            |
|      (or (string=? (first alon) "Flatt")                     |
|          (contains-flatt? (rest alon)))]))                   |
+--------------------------------------------------------------+
```

図 47（Figure 47）は完全な定義を示しています。全体として、本書の第 1 章にある定義とそれほど違いはないように見えます。シグネチャ、目的記述、2 つの例、そして定義で構成されています。この関数定義がこれまでに見たものと唯一異なる点は、自己参照（self-reference）、つまり `define` の本体内で `contains-flatt?` を参照している点です。しかしよく考えると、データ定義も自己参照的であるため、ある意味では関数内の自己参照もそれほど驚くべきことではありません。

**Exercise 132.** この例で `contains-flatt?` を実行するために DrRacket を使用してください:

```racket
(cons "Fagan"
  (cons "Findler"
    (cons "Fisler"
      (cons "Flanagan"
        (cons "Flatt"
          (cons "Felleisen"
            (cons "Friedman" '())))))))
```

どのような答えを予想しますか？

**Exercise 133.** 以下は `contains-flatt?` の 2 番目の `cond` 節を定式化するもう 1 つの方法です:

```racket
... (cond
      [(string=? (first alon) "Flatt") #true]
      [else (contains-flatt? (rest alon))])...
```

なぜこの式が図 47（figure 47）のバージョンの `or` 式と同じ答えを生成するのか説明してください。あなたにとってどちらのバージョンがよりわかりやすいですか？ 理由を説明してください。

**Exercise 134.** 与えられた文字列が、与えられた文字列のリストに含まれているかどうかを判定する `contains?` 関数を開発してください。

注: BSL には実際には `member?` が付属しています。これは 2 つの値を受け取り、最初の値が 2 番目の値（リスト）に含まれているかどうかをチェックする関数です:

```racket
> (member? "Flatt" (cons "b" (cons "Flatt" '())))
#true
```

`contains?` 関数を定義するために `member?` を使用しないでください。

```
+---------------------------------------------------------------+
| Figure 48: Computing with lists, step 1                       |
|                                                               |
| (contains-flatt? (cons "Flatt" (cons "C" '())))               |
| ==                                                            |
| (cond                                                         |
|   [(empty? (cons "Flatt" (cons "C" '()))) #false]             |
|   [(cons? (cons "Flatt" (cons "C" '())))                      |
|    (or                                                        |
|     (string=? (first (cons "Flatt" (cons "C" '()))) "Flatt")  |
|     (contains-flatt? (rest (cons "Flatt" (cons "C" '())))))]) |
+---------------------------------------------------------------+
```

### 8.4 リストによる計算 (Computing with Lists)

私たちはまだ BSL を使用しているため、代数のルール（合間1（intermezzo 1）を参照）に従えば、DrRacket を使わなくても次のような式の値を求める方法が分かります。

```racket
(contains-flatt? (cons "Flatt" (cons "C" '())))
```

プログラマはこの種の計算がどのように動作するかを直感的に理解している必要があるため、この簡単な例についてステップごとに追っていきます。

図 48 は最初のステップを示しており、関数適用の値を決定するために通常の代入規則を使用しています。代数の先生が言うように関数が段階的に定義されているため、結果は条件式になります。

```
+-----------------------------------------------------------------+
| Figure 49: Computing with lists, step 2                         |
|                                                                 |
|...                                                             |
| ==                                                              |
| (cond                                                           |
|  [#false #false]                                                |
|  [(cons? (cons "Flatt" (cons "C" '())))                         |
|   (or (string=? (first (cons "Flatt" (cons "C" '()))) "Flatt")  |
|       (contains-flatt? (rest (cons "Flatt" (cons "C" '())))))]) |
| ==                                                              |
| (cond                                                           |
|  [(cons? (cons "Flatt" (cons "C" '())))                         |
|   (or (string=? (first (cons "Flatt" (cons "C" '()))) "Flatt")  |
|       (contains-flatt? (rest (cons "Flatt" (cons "C" '())))))]) |
| ==                                                              |
| (cond                                                           |
|  [#true                                                         |
|   (or (string=? (first (cons "Flatt" (cons "C" '()))) "Flatt")  |
|       (contains-flatt? (rest (cons "Flatt" (cons "C" '())))))]) |
| ==                                                              |
| (or (string=? (first (cons "Flatt" (cons "C" '()))) "Flatt")    |
|     (contains-flatt? (rest (cons "Flatt" (cons "C" '())))))     |
+-----------------------------------------------------------------+
```

計算は図 49 で続きを行います。`cond` 式の正しい節を見つけるために、条件の値を1つずつ決定していく必要があります。`cons` で作られたリストは空ではないため、最初の条件の結果は `#false` となり、したがって最初の `cond` 節を取り除きます。最終的に、`cons` で作られたリストに対する `cons?` が成り立つため、2番目の節の条件は `#true` に評価されます。

```
+-------------------------------------------------------------+
| Figure 50: Computing with lists, step 3                     |
|                                                             |
|...                                                         |
| ==                                                          |
| (or (string=? "Flatt" "Flatt")                              |
|     (contains-flatt? (rest (cons "Flatt" (cons "C" '()))))) |
| == (or #true (contains-flatt?...))                         |
| == #true                                                    |
+-------------------------------------------------------------+
```

ここからは、最終的な結果を得るまであとわずか3ステップの算術計算です。図 50 はその3つのステップを示しています。最初のステップは、`first` の法則に従って `(first (cons "Flatt" ...))` を `"Flatt"` に評価します。2番目のステップでは、`"Flatt"` が文字列であり `"Flatt"` と等しいことを確認します。3番目のステップでは、`X` が何であれ `(or #true X)` は `#true` になるとしています。

**Exercise 135.** DrRacket のステッパーを使って、次に対する計算を確認しなさい。

```racket
(contains-flatt? (cons "Flatt" (cons "C" '())))
```

また、ステッパーを使って次の値を決定しなさい。

```racket
(contains-flatt?
  (cons "A" (cons "Flatt" (cons "C" '()))))
```

"Flatt" を "B" に置き換えると何が起こりますか。

**Exercise 136.** DrRacket のステッパーで以下を検証しなさい。

```racket
(our-first (our-cons "a" '())) == "a"
(our-rest (our-cons "a" '())) == '()
```

これらの関数の定義については What Is '(), What Is cons を参照してください。

```
+-------------------------------------------------------------------------+
| Figure 51: Arrows for self-references in data definitions and templates |
|                                                                         |
| [image: pict_94.png]                                                    |
| [image: pict_95.png]                                                    |
|                                                                         |
| +----------------------+--+----------------------+                      |
| | [image: pict_94.png] |  | [image: pict_95.png] |                      |
| +----------------------+--+----------------------+                      |
| +----------------------+--+----------------------+                      |
+-------------------------------------------------------------------------+
```

