## 9 自己参照データ定義を用いた設計 (Designing with Self-Referential Data Definitions)

一見すると、自己参照データ定義（self-referential data definitions）は混合データ（mixed data）の定義よりも遥かに複雑に思えるかもしれません。しかし、`contains-flatt?` の例が示すように、設計レシピの6つのステップは引き続き機能します。それにもかかわらず、本節では設計レシピを一般化し、自己参照データ定義に対してさらに効果的に機能するようにします。新しい部分は、自己参照データ定義がいつ必要であるかを発見するプロセス、テンプレートを導出するプロセス、そして関数本体を定義するプロセスに関わっています：

1. 問題文が任意サイズの情報（information of arbitrary size）に関するものである場合、それを表現するために自己参照データ定義が必要となります。現時点では、そのようなクラスとして `List-of-names` の1つしか見ていません。figure 51 の左側は、同じ方法で `List-of-strings` を定義する方法を示しています。アトミックデータの他のリストも同様に機能します。自己参照データ定義が有効であるためには、2つの条件を満たす必要があります。第一に、少なくとも2つの節（clauses）を含んでいなければなりません。第二に、節のうち少なくとも1つは、定義しようとしているデータのクラスを参照（逆参照）していてはなりません。データ定義内の参照から定義対象の用語へと戻る矢印を用いて、自己参照を明示的に特定することは優れた実践です。そのような注釈の例については figure 51 を参照してください。自己参照データ定義の妥当性は、データ例を作成することによって確認しなければなりません。データ定義を参照しない節から始め、次に節自体が定義を参照する箇所で最初の例を使用しながら、もう一方の節へと進みます。figure 51 のデータ定義の場合、次のような3つのリストが得られます：

`'()` 第1節による
`(cons "a" '())` 第2節および直前の例による
`(cons "b" (cons "a" '()))` 第2節および直前の例による

データ定義から例を生成することが不可能な場合、その定義は無効です。例を生成できても、段階的に大きな例を生成する方法が見当たらない場合、その定義は解釈（interpretation）に合致していない可能性があります。
  > **注:** 数値も任意に大きく思えるかもしれません。不正確な数値（inexact numbers）においては、これは錯覚です。正確な整数（precise integers）においては、実際にその通りです。したがって、整数を扱うことも本章の一部となります。

2. ヘッダー部分（シグネチャ、目的の文、ダミー定義）に関しては何も変わりません。目的の文（purpose statement）を作成するときは、関数がどのように処理を進めるか（特に与えられたデータのインスタンスをどのように巡回するか）ではなく、関数が何を計算するかに焦点を当てます。この設計レシピを具体化するための例を以下に示します：

```racket
;List-of-strings ->Number;counts how many stringsalos contains(define (how-many alos) 0)
```

目的の文は、関数が与えられた入力に含まれる文字列を単純に数えるだけであることを明確に述べています。この考えを BSL 関数としてどのように定式化できるかについて、事前に思い悩む必要はありません。

3. 機能的な例（functional examples）に関しては、データ定義の自己参照節を複数回使用する入力を必ず試してください。これは、後で関数定義全体をカバーするテストを定式化するための最良の方法です。今回の進行中の例では、目的の文がデータ例からほぼ自然に機能的な例を生成します：

| 与えられた入力 (given) | 期待される出力 (wanted) |
|---|---|
| `'()` | `0` |
| `(cons "a" '())` | `1` |
| `(cons "b" (cons "a" '()))` | `2` |

1行目は空のリスト（empty list）に関するものであり、空のリストには何も含まれていないことがわかっています。2行目は1つの文字列からなるリストであるため、1 が期待される答えです。最後の行は2つの文字列からなるリストに関するものです。

4. 本質的に、自己参照データ定義は混合データのデータ定義と似ています。したがって、テンプレートの開発は「項挙と構造体（Itemizations and Structures）」のレシピに従って進めることができます。具体的には、データ定義に含まれる節の数と同じ数の `cond` 節を持つ `cond` 式を定式化し、各識別条件（recognizing condition）をデータ定義の対応する節に一致させ、複合値（compound values）を処理するすべての `cond` 行に適切なセレクタ式を書き出します。

| 質問 (Question) | 回答 (Answer) |
|---|---|
| データ定義はデータの異なるサブクラスを区別していますか？ | テンプレートには、データ定義が区別するサブクラスの数だけ `cond` 節が必要です。 |
| サブクラスは互いにどのように異なりますか？ | その違いを利用して、節ごとに条件を定式化します。 |
| 構造化された値を扱う節はありますか？ | もしそうなら、適切なセレクタ式をその節に追加します。 |
| データ定義は自己参照を使用していますか？ | データ定義の自己参照を表現するために、テンプレートに対して「自然再帰（natural recursions）」を定式化します。 |
| データ定義が他のデータ定義を参照している場合、その相互参照はどこにありますか？ | その他のデータ定義に合わせてテンプレートを特化させます。このテンプレートを参照してください。「再・項挙と構造体を用いた設計 (Designing with Itemizations, Again)」の設計レシピのステップ 4 および 5 を参照してください。 |

figure 52: データ定義をテンプレートへと変換する方法 (How to translate a data definition into a template)

figure 52 はこの考え方を一問一答形式で表しています。左列には引数のデータ定義に関する質問が記載され、右列にはその回答がテンプレートの構築において何を意味するかが説明されています。

最後の行を無視して、`List-of-strings` を受け取る任意の関数に最初の3つの質問を適用すると、次のような形に至ります：

```racket
(define (fun-for-los alos) (cond [(empty? alos)...] [else (... (first alos)... (rest alos)...)]))
```

ただし、テンプレートの目的はデータ定義を関数のレイアウトとして表現することであるのを思い出してください。つまり、テンプレートは入力のデータ定義が英語と BSL の混在として表現している内容をコードとして表現します。したがって、データ定義のすべての重要な部分はテンプレート内に対応するものを持たなければならず、このガイドラインはデータ定義が自己参照的である場合（定義内部から定義対象の用語への矢印が含まれている場合）にも当てはまるべきです。特に、データ定義が $i$ 番目の節およびそこで言及されている構造体の $k$ 番目のフィールドで自己参照的である場合、テンプレートは $i$ 番目の `cond` 節および $k$ 番目のフィールドに対するセレクタ式において自己参照的であるべきです。そのようなセレクタ式ごとに、関数のパラメータへと戻る矢印を追加します。最終的に、テンプレートはデータ定義にあるのと同じ数の矢印を持たなければなりません。

figure 51 は、`List-of-strings` を受け取る関数のテンプレートをデータ定義と並べて表示することでこの考え方を図示しています。両者とも、2番目の節（それぞれ `rest` フィールドおよびセレクタ）から発生し、それぞれの定義の先頭を指し示す1本の矢印を含んでいます。

BSL およびほとんどのプログラミング言語はテキスト指向であるため、矢印の代替手段、すなわち適切なセレクタ式に対する関数の自己適用（self-application）を使用する必要があります：

```racket
(define (fun-for-los alos) (cond [(empty? alos)...] [else (... (first alos)...... (fun-for-los (rest alos))...)]))
```

関数自体の呼び出しを再帰（recursion）と呼び、本書の最初の4つのパートでは自然再帰（natural recursion）と呼びます。

5. 関数本体については、基底ケース（base cases）として知られる、再帰的な関数呼び出しを含まない `cond` 行から始めます。対応する回答は通常、定式化が容易であるか、すでに例として与えられています。

次に、自己参照ケースを扱います。まず、テンプレート行の各式が何を計算するかを自分自身に思い出させることから始めます。自然再帰については、関数が目的の文で指定された通りにすでに機能していると仮定します。この最後のステップは「信仰の跳躍（leap of faith）」ですが、これから見るように、常に機能します。

残りは、さまざまな値を組み合わせる問題です。

| 質問 (Question) | 回答 (Answer) |
|---|---|
| 非再帰的な `cond` 節に対する回答は何ですか？ | 例を見れば、ここでどの値が必要かがわかるはずです。そうでない場合は、適切な例とテストを定式化してください。 |
| 再帰節のセレクタ式は何を計算しますか？ | データ定義はこれらの式がどの種類のデータを抽出するかを示し、データ定義の解釈はそのデータが何を表現しているかを示します。 |
| 自然再帰は何を計算しますか？ | 関数の目的の文を使用して、再帰の値が何を意味するかを判断します（その答えをどのように計算するかではありません）。目的の文から答えが得られない場合は、目的の文を改善してください。 |
| 関数は希望する答えを得るためにこれらの値をどのように組み合わせることができますか？ | 値を組み合わせる BSL の関数を見つけます。あるいは、それが機能しない場合は、補助関数を要請（make a wish）します。多くの関数にとって、この最後のステップは簡単です。目的、例、およびテンプレートが組み合わさって、利用可能な値を適切な結果へと組み合わせる関数または式を示してくれます。既存の用語を少し乱用して、この関数または式をコンビネータ（combinator）と呼びます。 |

figure 53: テンプレートを関数定義へと変換する方法 (How to turn a template into a function definition)

| 質問 (Question) | 回答 (Answer) |
|---|---|
| したがって、ここで行き詰まった場合は… | …第3ステップの例を表に整理します。1列目に与えられた入力を、最後の列に希望する出力を配置します。中間列にはセレクタ式と自然再帰の値を入力します。コンビネータを示唆するパターンが現れるまで例を追加します。 |
| テンプレートが他のテンプレートを参照している場合、補助関数は何を計算しますか？ | もう一方の関数の目的の文と例を参照して何が計算されるかを判断し、その補助関数の設計が終わっていなくてもその結果を使用してよいと仮定します。 |

figure 54: テンプレートを関数へと変換するテーブル法 (Turning a template into a function, the table method)

figure 53 は、このステップに関する最初の4つの質問と回答を定式化しています。このゲームを使って `how-many` の定義を完成させましょう。`fun-for-los` テンプレートの名前を `how-many` に変更すると、次のようになります：

```racket
;List-of-strings ->Number;determines how many strings are onalos(define (how-many alos) (cond [(empty? alos)...] [else (... (first alos)...... (how-many (rest alos))...)]))
```

機能的な例がすでに示唆しているように、基底ケースに対する回答は `0` です。第2節の2つの式は、最初の項目と `(rest alos)` に含まれる文字列の数を計算します。`alos` 全体に何個の文字列があるかを計算するには、関数は後者の式の値に `1` を加えるだけで済みます：

```racket
(define (how-many alos) (cond [(empty? alos) 0] [else (+ (how-many (rest alos)) 1)]))
```

値を希望する答えに組み合わせる正しい方法を見つけることが、常にそれほど簡単であるとは限りません。初心者プログラマーはよくこのステップで行き詰まります。figure 54 が示唆するように、機能的な例を、テンプレート内の式の値も明記した表に整理することをお勧めします。figure 55 は、`how-many` の例におけるこの表がどのようになるかを示しています。一番左の列にはサンプル入力が一覧表示され、一番右の列にはそれらの入力に対して希望する答えが含まれています。中間の3つの列は、テンプレート式の値を示しています：`(first alos)`、`(rest alos)`、および自然再帰である `(how-many (rest alos))` です。この表をじっくり見つめると、結果の列が常に自然再帰の列の値よりも1大きいことに気づくでしょう。したがって、

```racket
(+ (how-many (rest alos)) 1)
```

が希望する結果を計算する式であると推測できるかもしれません。DrRacket はこの種の推測の検証が速いため、それを組み込んで *RUN* をクリックします。テストに変換された例が合格した場合は、式を熟考して、それがすべてのリストで正しく機能することを確認してください。合格しない場合は、別の考えが浮かぶまで表に例の行を追加してください。

表はまた、テンプレート内のいくつかのセレクタ式が実際の定義においては無関係である可能性もあることを指摘しています。ここで `(first alos)` は最終的な答えを計算するのに必要ありません。これはテンプレートからの両方の式を使用する `contains-flatt?` とは極めて対照的です。

本書の残りを進めるにあたり、多くのケースにおいて組み合わせのステップは BSL の基本操作（例えば `+`、`and`、または `cons`）で表現できることを心に留めておいてください。ただし、場合によっては「願い事をする（make a wish）」、つまり補助関数を設計しなければならないこともあります。さらに別のケースでは、入れ子になった条件式が必要になる場合もあります。

  > **注:** 好奇心旺盛な読者のために付言すると、任意に大きなデータに対する設計レシピは、数学におけるいわゆる「帰納法による証明（proofs by induction）」に対応し、「信仰の跳躍（leap of faith）」はそのような証明の帰納ステップ（inductive step）における帰納法の仮定（induction hypothesis）の使用を表しています。論理学は帰納法定理（Induction Theorem）によってこの証明手法の妥当性を証明しています。
  > **注:** Felix Klock がコンビネータを推測するためのこのテーブルベースのアプローチを提案しました。

6. 最後に、すべての例をテストに変換し、これらのテストが合格し、それらを実行することで関数のすべての部分がカバーされていることを確認してください。以下は、テストに変換された `how-many` の例です：

```racket
(check-expect (how-many '()) 0)(check-expect (how-many (cons "a" '())) 1)(check-expect (how-many (cons "b" (cons "a" '()))) 2)
```

例をテストとして直接定式化するのが最善であり、BSL ではこれが可能であることを覚えておいてください。そうすることは、前のステップのテーブルベースの推測アプローチに頼る必要がある場合にも役立ちます。

```
+--------------------------------------------------------------------------------+
| Figure 55: Tabulating arguments, intermediate values, and results              |
|                                                                                |
| +--------------------------------------+--+----------------+--+--------------- |
| | alos | | (first ⏎ alos) | | (rest ⏎ alos) | | (how-many ⏎ (rest alos)) | |   |
| (how-many ⏎ alos) |                                                            |
| +--------------------------------------+--+----------------+--+--------------- |
| | (first | | | | | | | | |                                                     |
| | alos) | | | | | | | | |                                                      |
| | (rest | | | | | | | | |                                                      |
| | alos) | | | | | | | | |                                                      |
| | (how-many | | | | | | | | |                                                  |
| | (rest alos)) | | | | | | | | |                                               |
| | (how-many | | | | | | | | |                                                  |
| | alos) | | | | | | | | |                                                      |
| | (cons "a" ⏎ '()) | | "a" | | '() | | 0 | | 1 |                               |
| | (cons "a" | | | | | | | | |                                                  |
| | '()) | | | | | | | | |                                                       |
| | (cons "b" ⏎ (cons "a" ⏎ '())) | | "b" | | (cons "a" ⏎ '()) | | 1 | | 2 |     |
| | (cons "b" | | | | | | | | |                                                  |
| | (cons "a" | | | | | | | | |                                                  |
| | '())) | | | | | | | | |                                                      |
| | (cons "a" | | | | | | | | |                                                  |
| | '()) | | | | | | | | |                                                       |
| | (cons "x" ⏎ (cons "b" ⏎ (cons "a" ⏎… | | "x" | | (cons "b" ⏎ (cons "a" ⏎     |
| '())) | | 2 | | 3 |                                                            |
| | (cons "x" | | | | | | | | |                                                  |
| | (cons "b" | | | | | | | | |                                                  |
| | (cons "a" | | | | | | | | |                                                  |
| | '()))) | | | | | | | | |                                                     |
| | (cons "b" | | | | | | | | |                                                  |
| | (cons "a" | | | | | | | | |                                                  |
| | '())) | | | | | | | | |                                                      |
| +--------------------------------------+--+----------------+--+--------------- |
+--------------------------------------------------------------------------------+
```

figure 56 は、本節の設計レシピを表形式でまとめています。第1列は設計レシピのステップ名を提示し、第2列は各ステップの期待される結果（outcome）を提示します。第3列では、そこに到達するためのアクティビティ（activity）について説明しています。この図は、本章で使用する自己参照リスト定義の種類に合わせて調整されています。いつものように、練習はプロセスの習得に役立ちますので、レシピをいくつかの種類の例に適用することを求める以下の演習（exercises）に取り組むことを強くお勧めします。

> **注:** figure 56 をインデックスカードの片面にコピーし、裏面にこの設計レシピに対するお気に入りの質問と回答のバージョンを書き留めておくとよいでしょう。そして、今後の参考のためにそれを持ち歩いてください。

```
+--------------------------------------------------------------------------------+
| Figure 56: Designing a function for self-referential data                      |
|                                                                                |
| +------------------+--+--------------------------------------+--+------------- |
| | steps | | outcome | | activity |                                             |
| +------------------+--+--------------------------------------+--+------------- |
| | problem analysis | | data definition | | Develop a data representation for   |
| t… |                                                                           |
| | header | | signature; purpose; dummy definition | | Write down a signature   |
| using define… |                                                                |
| | examples | | examples and tests | | Work through several examples, at l… |   |
| | template | | function template | | Translate the data definition into … |    |
| | definition | | full-fledged definition | | Find a function that combines the |
| v… |                                                                           |
| | test | | validated tests | | Turn them into check-expect tests a… |          |
| +------------------+--+--------------------------------------+--+------------- |
+--------------------------------------------------------------------------------+
```

### 9.1 指の運動：リスト (Finger Exercises: Lists)

**Exercise 137.** `contains-flatt?` のテンプレートと `how-many` のテンプレートを比較しなさい。関数名を除けば、これらは同じです。この類似性について説明しなさい。

**Exercise 138. 以下は金額の系列を表現するためのデータ定義です：**

```racket
; A List-of-amounts is one of:
; – '()
; – (cons PositiveNumber List-of-amounts)
```

データ定義を確実に理解するために、いくつかの例を作成しなさい。また、自己参照を表す矢印を追加しなさい。

`List-of-amounts` を受け取り、それらの金額の合計を計算する `sum` 関数を設計しなさい。DrRacket のステッパーを使用して、`List-of-amounts` の短いリスト `l` に対して `(suml)` がどのように動作するかを確認しなさい。

**Exercise 139. 次に、このデータ定義を見てみましょう：**

```racket
; A List-of-numbers is one of:
; – '()
; – (cons Number List-of-numbers)
```

このデータクラスの要素のいくつかは演習 138 の `sum` に対する適切な入力となりますが、そうでないものもあります。

`List-of-numbers` を受け取り、すべての数値が正の数であるかどうかを判定する関数 `pos?` を設計しなさい。言い換えれば、`(pos?l)` が `#true` を産出する場合、`l` は `List-of-amounts` の要素です。DrRacket のステッパーを使用して、`(cons5'())` や `(cons-1'())` に対して `pos?` がどのように動作するかを理解しなさい。

また、`checked-sum` も設計しなさい。この関数は `List-of-numbers` を受け取ります。入力が `List-of-amounts` にも属している場合はそれらの合計を産出し、そうでなければエラーをシグナルします。ヒント: `check-error` を使用したことを思い出してください。

`sum` は `List-of-numbers` の要素に対して何を計算しますか？

**Exercise 140.** ブーリアン値のリストを受け取り、それらすべてが `#true` であるかどうかを判定する関数 `all-true` を設計しなさい。言い換えれば、リスト上に1つでも `#false` があれば、この関数は `#false` を産出します。

次に、ブーリアン値のリストを受け取り、リスト上の少なくとも1つの項目が `#true` であるかどうかを判定する関数 `one-true` を設計しなさい。

コーディングにはテーブルに基づくアプローチを採用しなさい。これはベースケースの検討に役立つかもしれません。DrRacket のステッパーを使用して、これらの関数がリスト `(cons#true'())`、`(cons#false'())`、および `(cons#true(cons#false'()))` をどのように処理するかを確認しなさい。

**Exercise 141. 文字列のリストを受け取り、それらをすべて1つの長い文字列に連結する関数 `cat` の設計を求められた場合、最終的に次のような部分的な定義に至ることが保証されています：**

```racket
; List-of-string -> String
; concatenates all strings in l into one long string

(check-expect (cat '()) "")
(check-expect (cat (cons "a" (cons "b" '()))) "ab")
(check-expect
  (cat (cons "ab" (cons "cd" (cons "ef" '()))))
  "abcdef")

(define (cat l)
  (cond
    [(empty? l) ""]
    [else (... (first l)... (cat (rest l))...)]))
```

```
+--------------------------------------------------------------------------------+
| Figure 57: A table for cat                                                     |
|                                                                                |
| +--------------------------------------+--+-----------+--+----------+--+------ |
| | l | | (first l) | | (rest l) | | (cat (rest l)) | | (cat l) |                |
| +--------------------------------------+--+-----------+--+----------+--+------ |
| | (cons "a" ⏎ (cons "b" ⏎ '())) | |??? | |??? | |??? | | "ab" |             |
| | (cons "a" | | | | | | | | |                                                  |
| | (cons "b" | | | | | | | | |                                                  |
| | '())) | | | | | | | | |                                                      |
| | (cons ⏎ "ab" ⏎ (cons "cd" ⏎ (cons "… | |??? | |??? | |??? | | "abcdef" |  |
| | (cons | | | | | | | | |                                                      |
| | "ab" | | | | | | | | |                                                       |
| | (cons "cd" | | | | | | | | |                                                 |
| | (cons "ef" | | | | | | | | |                                                 |
| | '()))) | | | | | | | | |                                                     |
| +--------------------------------------+--+-----------+--+----------+--+------ |
+--------------------------------------------------------------------------------+
```

figure 57 の表を埋めなさい。部分式によって計算された値から望ましい結果を作成できる関数を推測しなさい。

DrRacket のステッパーを使用して `(cat(cons"a"'()))` を評価しなさい。

**Exercise 142.** 画像のリスト `loi` と正の数 `n` を受け取る `ill-sized?` 関数を設計しなさい。この関数は、`loi` の中で \(n \times n\) の正方形ではない最初の画像を産出します。そのような画像が見つからない場合は `#false` を産出します。

ヒント シグネチャの結果部分には次を使用しなさい

```racket
; ImageOrFalse is one of:
; – Image
; – #false
```
is one of:
; – Image
; – #false
```

### 9.2 空でないリスト (Non-empty Lists)

これで、`cons` を使用し、リストのデータ定義を作成するのに十分な知識が得られました。前節の終わりの演習問題を（いくつかでも）解いたのであれば、さまざまな種類の数値のリスト、ブール値のリスト、画像のリストなどを扱うことができるようになっています。本節では、リストとは何か、そしてそれをどのように処理するかについて探求を続けます。

まず、一見単純に思える「気温のリストの平均値を計算する」という問題から始めましょう。単純化するために、以下のデータ定義を提供します。

```racket
; A List-of-temperatures is one of:
; – '()
; – (cons CTemperature List-of-temperatures)

(define ABSOLUTE0 -272)
; A CTemperature is a Number greater than ABSOLUTE0.
```

```racket
; A List-of-temperatures is one of:
; – '()
; – (cons CTemperature List-of-temperatures)

(define ABSOLUTE0 -272)
; A CTemperature is a Number greater than ABSOLUTE0.
```

ここでの目的上、気温は普通の数値として考えてかまいませんが、2番目のデータ定義は、現実にはすべての数値が気温というわけではないということを思い出させます。このことを念頭に置いておいてください。

ヘッダー部分は極めてシンプルです。

```racket
; List-of-temperatures -> Number
; computes the average temperature
(define (average alot) 0)
```

この問題の例を作成するのも簡単ですので、テストを1つだけ作成します。

```racket
(check-expect
  (average (cons 1 (cons 2 (cons 3 '())))) 2)
```

期待される結果は、当然、気温の合計を気温の個数で割ったものになります。

少し考えれば、`average` のテンプレートはこれまで見てきたものと類似しているはずだとわかるでしょう。

```racket
(define (average alot)
  (cond
    [(empty? alot)...]
    [(cons? alot)
     (... (first alot)...
... (average (rest alot))...)]))
```

2つの `cond` 節はデータ定義の2つの節を反映しています。質問部分は空リストと空でないリストを区別し、データ定義における自己参照のために自然な再帰が必要となります。

しかしながら、このテンプレートを関数定義へと完成させるのは非常に困難です。最初の `cond` 節には空の気温コレクションの平均を表す数値が必要ですが、そのような数値は存在しません。同様に、2番目の節は気温と残りの気温の平均を組み合わせて新しい平均を求める関数を要求します。可能ではありますが、このような方法で平均を計算するのは極めて不自然です。

気温の平均を計算するとき、私たちはそれらの合計を個数で割ります。単純な小さい例を定式化した際にもそう述べました。この文は、`average` が合計（summing）、カウント（counting）、除算（dividing）の3つのタスクからなる関数であることを示唆しています。「固定サイズのデータ (Fixed-Size Data)」のガイドラインでは、タスクごとに関数を1つ書くよう規定されています。それに従えば、`average` の設計は明快になります。

```racket
; List-of-temperatures -> Number
; computes the average temperature
(define (average alot)
  (/ (sum alot) (how-many alot)))

; List-of-temperatures -> Number
; adds up the temperatures on the given list
(define (sum alot) 0)

; List-of-temperatures -> Number
; counts the temperatures on the given list
(define (how-many alot) 0)
```

もちろん、最後の2つの関数定義は希望（wish）であり、完全な定義を設計する必要があります。上記で出てきた `how-many` は `List-of-strings` にも `List-of-temperatures` にもそのまま機能するため（なぜでしょうか？）、そして `sum` の設計もおなじみの手順に従うだけなので、これを行うのは簡単です。

```racket
; List-of-temperatures -> Number
; adds up the temperatures on the given list
(define (sum alot)
  (cond
    [(empty? alot) 0]
    [else (+ (first alot) (sum (rest alot)))]))
```

手を止めてください！ `average` の例を使って `sum` の例を作成し、テストが正しく実行されることを確認してください。その後、`average` のテストを実行してください。

いまこの `average` の定義を読むと、学校で誰もが学ぶ平均の計算方法と直接対応しているため、明らかに正しいことがわかります。とはいえ、プログラムは自分たちのためだけでなく、他の人のためにも実行されます。特に、他の人がシグネチャを読み、その関数を使って、有益な回答を期待できるようにすべきです。しかし、私たちの `average` の定義は、空の気温リストに対しては機能しません。

**Exercise 143.** 空リストに適用されたとき、`average` が DrRacket でどのように動作するかを確認しなさい。次に、`'()` に適用されたときに役立つエラーメッセージを出力する関数 `checked-average` を設計しなさい。

> **Note:** 数学の言葉を借りれば、演習問題 143 は `average` が `'()` に対してエラーを発生させるため、部分関数（partial function）であることを示している、と言えます。

代替の解決策は、`average` が空リストに対して動作しないことをシグネチャを通じて将来の読者に伝えることです。そのためには、`'()` を除外するようなリストのデータ表現が必要となります。たとえば次のようなものです。

```racket
; An NEList-of-temperatures is one of:
; –???
; – (cons CTemperature NEList-of-temperatures)
```

問題は、`'()` リストが除外されつつも、他のすべての気温リストが構築可能なままになるように、「???」を何に置き換えるべきかということです。1つのヒントとして、空リストが最も短いリストである一方で、1つの気温からなるリストがその次に短いリストであるということが挙げられます。ここから、最初の節は1つの気温からなるすべての可能なリストを記述すべきだということが示唆されます。

```racket
; An NEList-of-temperatures is one of:
; – (cons CTemperature '())
; – (cons CTemperature NEList-of-temperatures)
; interpretation non-empty lists of Celsius temperatures
```

この定義はこれまでのリスト定義とは異なりますが、重要な要素を共有しています。すなわち、自己参照と、自己参照を使用しない節です。設計レシピを厳密に順守するには、`NEList-of-temperatures` の例をいくつか作成して、定義が理にかなっていることを確認する必要があります。いつものように、基本節から始める必要があります。つまり、例は次のようになるはずです。

```racket
(cons c '())
```

ここで `c` は `CTemperature` を表します（例: `(consABSOLUTE0'())`）。また、`List-of-temperatures` のすべての空でない要素が、この新しいデータクラスの要素でもあることは明らかです。`(cons2(cons3'()))` が適合するのであれば `(cons1(cons2(cons3'())))` も適合しますし、先ほど確認したように `(cons3'())` が `NEList-of-temperatures` の要素であるため、`(cons2(cons3'()))` は `NEList-of-temperatures` に属します。`NEList-of-temperatures` のサイズに制限がないことを自分で確認してください。

それでは、`average` が空でないリスト専用であることを誰もが知ることができるよう、`average` を設計し直す問題に戻りましょう。`NEList-of-temperatures` の定義により、シグネチャで意図した内容を表現する手段が得られました。

> **Note:** この代替の展開は、この場合、`average` の定義域を狭めることで全域関数（total function）を作成できることを説明しています。

```racket
; NEList-of-temperatures -> Number
; computes the average temperature

(check-expect (average (cons 1 (cons 2 (cons 3 '()))))
              2)

(define (average ne-l)
  (/ (sum ne-l)
     (how-many ne-l)))
```

当然ながら、残りの部分（目的文、例とテスト、関数定義）は同じままです。なにしろ、平均を計算するという考え方自体が空でない数値のコレクションを前提としており、それこそが私たちの議論のすべてのポイントだったからです。

**Exercise 144.** `sum` や `how-many` は、`List-of-temperatures` からの入力を想定して設計されているにもかかわらず、`NEList-of-temperatures` に対しても動作するでしょうか。動かないと思う場合は反例を示しなさい。動作すると思う場合はその理由を説明しなさい。

それにもかかわらず、この定義は `sum` や `how-many` が `NEList-of-temperatures` のインスタンスを受け取るようになったため、これらをどのように設計するかという新たな問いを提起します。以下は設計レシピの最初の3つのステップから導かれる明確な結果です。

```racket
; NEList-of-temperatures -> Number
; computes the sum of the given temperatures
(check-expect
  (sum (cons 1 (cons 2 (cons 3 '())))) 6)
(define (sum ne-l) 0)
```

この例は `average` の例を適応させたものです。ダミー定義は数値を生成しますが、与えられたテストに対しては誤った数値となります。

第4ステップは、`NEList-of-temperatures` に対する `sum` の設計において最も興味深い部分です。これまでのすべての設計例では、データ定義が適切な形状を持っていたため、空リストと空でない（つまり `cons` された）リストを区別するテンプレートが必要でした。しかし、`NEList-of-temperatures` ではそうではありません。ここでは両方の節が `cons` されたリストを追加します。ただし、2つの節はそれらのリストの `rest` フィールドにおいて異なります。特に、最初の節は `rest` フィールドに常に `'()` を使用し、2番目の節は代わりに `cons` を使用します。したがって、1番目のデータと2番目のデータを区別するための適切な条件は、`rest` フィールドを抽出してから `empty?` を使用することになります。

```racket
; NEList-of-temperatures -> Number
(define (sum ne-l)
  (cond
    [(empty? (rest ne-l))...]
    [else...]))
```

ここで `else` は `(cons?(restne-l))` を置き換えています。

次に、両方の節を点検し、一方または両方の節が `ne-l` を構造体であるかのように扱っているかどうかを判断する必要があります。`ne-l` に対して無条件に `rest` が使用されていることが示す通り、もちろんこれは当てはまります。言い換えれば、2つの節に適切なセレクター式を追加します。

```racket
(define (sum ne-l)
  (cond
    [(empty? (rest ne-l)) (... (first ne-l)...)]
    [else (... (first ne-l)... (rest ne-l)...)]))
```

読み進める前に、最初の節にセレクター式 `(restne-l)` が含まれていない理由を説明してください。

テンプレート設計の最後の問いは、データ定義における自己参照に関するものです。ご存知のように、`NEList-of-temperatures` には1つの自己参照が含まれているため、`sum` のテンプレートには1つの再帰的呼び出しが必要となります。

```racket
(define (sum ne-l)
  (cond
    [(empty? (rest ne-l)) (... (first ne-l)...)]
    [else
     (... (first ne-l)... (sum (rest ne-l))...)]))
```

具体的には、データ定義の類似するポイントで自己参照されているため、2番目の節で `sum` は `(restne-l)` に対して呼び出されます。

第5の設計ステップとして、私たちがすでにどれだけのものを手にしているかを理解しましょう。最初の `cond` 節は、再帰的関数呼び出しを伴う2番目の節よりも大幅に単純に見えるため、そちらから始めるべきです。この特定のケースでは、条件は `sum` がちょうど1つの気温 `(firstne-l)` を持つリストに適用されることを意味しています。明らかに、この1つの気温が、与えられたリストのすべての気温の合計となります。

```racket
(define (sum ne-l)
  (cond
    [(empty? (rest ne-l)) (first ne-l)]
    [else
     (... (first ne-l)... (sum (rest ne-l))...)]))
```

2番目の節は、リストが1つの気温と少なくとももう1つの要素から構成されていることを表しています。`(firstne-l)` が先頭の位置を抽出し、`(restne-l)` が残りの部分を抽出します。さらに、テンプレートは `(sum(restne-l))` の結果を使用することを示唆しています。しかし、`sum` はまさにあなたが定義している最中の関数であり、それが `(restne-l)` をどのように利用するかをあらかじめ知る由もありません。知っているのは、目的文が述べていること、すなわち `sum` が与えられたリスト（ここでは `(restne-l)`）のすべての気温を足し合わせるということだけです。この文が正しいとすれば、`(sum(restne-l))` は `ne-l` の1つの数値を除くすべての数値を足し合わせることになります。合計を得るには、関数は最初の気温を加算するだけでよいのです。

```racket
(define (sum ne-l)
  (cond
    [(empty? (rest ne-l)) (first ne-l)]
    [else (+ (first ne-l) (sum (rest ne-l)))]))
```

ここでこの関数のテストを実行すると、「信頼の飛躍（leap of faith）」が正当化されることがわかります。実際、本書の範囲を超える理由により、この飛躍は常に正当化されます。それこそが、それが設計レシピの固有の構成要素となっている理由です。

**Exercise 145.** `NEList-of-temperatures` を受け取り、気温が降順にソートされている（つまり、2番目が1番目より小さく、3番目が2番目より小さく、以下同様である）場合に `#true` を返し、それ以外の場合に `#false` を返す述語 `sorted>?` を設計しなさい。

ヒント: この問題は、コンビネータを推測するための表に基づいた方法がうまく機能するもう1つの問題です。figure 58 にいくつかの例の表（一部）を示します。表の残りを埋めなさい。そして、その構成要素から結果を計算する式を作成しなさい。

```
+--------------------------------------------------------------------------------+
| Figure 58: A table for sorted>?                                                |
|                                                                                |
| +--------------------------------------+--+-----------+--+-------------------- |
| | l | | (first l) | | (rest l) | | (sorted>? ⏎ (rest l)) | | (sorted>? ⏎ l) |  |
| +--------------------------------------+--+-----------+--+-------------------- |
| | (sorted>? | | | | | | | | |                                                  |
| | (rest l)) | | | | | | | | |                                                  |
| | (sorted>? | | | | | | | | |                                                  |
| | l) | | | | | | | | |                                                         |
| | (cons 1 ⏎ (cons 2 ⏎ '())) | | 1 | |??? | | #true | | #false |               |
| | (cons 1 | | | | | | | | |                                                    |
| | (cons 2 | | | | | | | | |                                                    |
| | '())) | | | | | | | | |                                                      |
| | (cons 3 ⏎ (cons 2 ⏎ '())) | | 3 | | (cons 2 '()) | |??? | | #true |         |
| | (cons 3 | | | | | | | | |                                                    |
| | (cons 2 | | | | | | | | |                                                    |
| | '())) | | | | | | | | |                                                      |
| | (cons 0 ⏎ (cons 3 ⏎ (cons 2 ⏎ '()))) | | 0 | | (cons 3 ⏎ (cons 2 ⏎ '())) | | |
|??? | |??? |                                                                  |
| | (cons 0 | | | | | | | | |                                                    |
| | (cons 3 | | | | | | | | |                                                    |
| | (cons 2 | | | | | | | | |                                                    |
| | '()))) | | | | | | | | |                                                     |
| | (cons 3 | | | | | | | | |                                                    |
| | (cons 2 | | | | | | | | |                                                    |
| | '())) | | | | | | | | |                                                      |
| +--------------------------------------+--+-----------+--+-------------------- |
+--------------------------------------------------------------------------------+
```

**Exercise 146.** `NEList-of-temperatures` に対する `how-many` を設計しなさい。これを行うことで `average` が完成するので、`average` がすべてのテストに合格することも確認しなさい。

**Exercise 147.** ブール値の空でないリストの表現である `NEList-of-Booleans` のデータ定義を作成しなさい。次に、演習問題 140 の関数 `all-true` および `one-true` を再設計しなさい。

**Exercise 148.** 本節の関数定義（`sum`、`how-many`、`all-true`、`one-true`）を、前節までの対応する関数定義と比較しなさい。空でないリストの定義と比較して、空リストを受け入れるデータ定義を扱う方がよいでしょうか。なぜでしょうか、あるいはなぜそうではないのでしょうか。

### 9.3 自然数 (Natural Numbers)

BSL プログラミング言語は、リストを消費する多くの関数だけでなく、リストを生成する関数もいくつか提供しています。その中にあるのが `make-list` で、これは数値 `n` と他の何らかの値 `v` を受け取り、`v` を `n` 個含むリストを生成します。以下にいくつか例を示します。

```racket
> (make-list 2 "hello")
(cons "hello" (cons "hello" '()))
> (make-list 3 #true)
(cons #true (cons #true (cons #true '())))
> (make-list 0 17)
'()
```

要するに、この関数はアトミックな（不可分の）データを受け取るにもかかわらず、任意に大きなデータを生成します。疑問に思うべきは、これがなぜ可能なのかということです。

私たちの答えは、`make-list` の入力は単なる数値ではなく、特別な種類の数値であるということです。幼稚園でこれらの数値は「物を数える数（counting numbers）」と呼ばれていました。つまり、これらの数値はオブジェクトを数えるために使用されます。コンピュータサイエンスでは、これらの数値は自然数（natural numbers）と呼ばれます。通常の数値とは異なり、自然数にはデータ定義が存在します。

```racket
; An N is one of:
; – 0
; – (add1 N)
; interpretation represents the counting numbers
```

最初の条項は 0 が自然数であることを示しています。これは数えるべきオブジェクトが存在しないことを表すために用いられます。2 番目の条項は、もし `n` が自然数であれば `n+1` も自然数であることを示しています。なぜなら `add1` は与えられた任意の数値に 1 を加える関数だからです。この 2 番目の条項を `(+ n 1)` と書くこともできますが、`add1` を使用することで、この加算が特別なものであると示すことを意図しています。

この `add1` の使用における特別な点は、通常の関数というよりも構造体型定義のコンストラクタ（構造子）のように振る舞うことです。その理由から、BSL には `add1` に対応する「セレクタ（選択子）」である関数 `sub1` も用意されています。0 に等しくない任意の自然数 `m` が与えられたとき、`sub1` を使用して `m` の構築に使用された数値を調べることができます。言い換えれば、`add1` は `cons` のようなものであり、`sub1` は `first` や `rest` のようなものです。

この時点で、0 を 0 でない自然数と区別する述語（predicate）が何であるか疑問に思うかもしれません。リストの場合と同様に 2 つあります。与えられた数値が 0 であるかを判定する `zero?` と、数値が 0 より大きいかを判定する `positive?` です。

これで、`make-list` のような自然数を扱う関数を自分自身で設計する準備が整いました。データ定義はすでに利用可能なので、ヘッダー情報を追加しましょう。

```racket
; N String -> List-of-strings
; creates a list of n copies of s

(check-expect (copier 0 "hello") '())
(check-expect (copier 2 "hello")
              (cons "hello" (cons "hello" '())))

(define (copier n s)
  '())
```

テンプレートを作成することが次のステップです。テンプレートに関する問いは、`copier` の本体が 2 つの条項を持つ `cond` 式であることを示唆しています。1 つは 0 のための条項、もう 1 つは正の数のための条項です。さらに、0 はアトミックとみなされ、正の数は構造化された値とみなされるため、テンプレートの 2 番目の条項にはセレクタ式が必要です。最後に非常に重要なこととして、`N` のデータ定義は 2 番目の条項において自己参照的（再帰的）です。したがって、テンプレートは 2 番目の条項で対応するセレクタ式に対する再帰呼び出しを必要とします。

```racket
(define (copier n s)
  (cond
    [(zero? n)...]
    [(positive? n) (... (copier (sub1 n) s)...)]))
```

```
+----------------------------------------------------+
| Figure 59: Creating a list of copies               |
|                                                    |
|; N String -> List-of-strings                      |
|; creates a list of n copies of s                  |
|                                                    |
| (check-expect (copier 0 "hello") '())              |
| (check-expect (copier 2 "hello")                   |
|               (cons "hello" (cons "hello" '())))   |
|                                                    |
| (define (copier n s)                               |
|   (cond                                            |
|     [(zero? n) '()]                                |
|     [(positive? n) (cons s (copier (sub1 n) s))])) |
+----------------------------------------------------+
```

Figure 59 には、テンプレートから得られた `copier` 関数の完全な定義が含まれています。このステップを慎重に再構築してみましょう。いつものように、再帰呼び出しを持たない `cond` 条項から始めます。ここでは条件が（重要な）入力が 0 であることを伝えており、それは関数が 0 個の要素、つまり要素を持たないリストを生成しなければならないことを意味します。もちろん、2 番目の例を検討したことで、このケースはすでに明確になっています。次に、もう一方の `cond` 条項に移り、その式が何を計算するかを思い出してみましょう。

1. `(sub1 n)` は `n` の構築に使用された自然数を取り出します。`n` は 0 より大きいことがわかっています。
2. `(copier (sub1 n) s)` は `copier` の目的記述に従って、`(sub1 n)` 個の文字列 `s` のリストを生成します。

しかし、関数には `n` が与えられているため、`n` 個の文字列 `s` を持つリストを生成しなければなりません。文字列が 1 つだけ不足しているリストが与えられた場合、関数が単に `(copier (sub1 n) s)` の結果に 1 つの `s` を `cons` すればよいことは容易にわかります。そしてそれこそが、2 番目の条項が正確に指定していることです。

この時点で、テストを実行して、この関数が少なくとも作成した 2 つの例で正しく動作することを確認する必要があります。さらに、いくつかの追加の入力に対して関数を使用してみるのもよいでしょう。

**Exercise 149.** 自然数と Boolean や画像（image）に対して `copier` を適用したとき、`copier` は適切に動作しますか？それとも別の関数を設計する必要がありますか？答えについては「抽象化（Abstraction）」を参照してください。

`copier` の代替定義では `else` を使用するかもしれません。

```racket
(define (copier.v2 n s)
  (cond
    [(zero? n) '()]
    [else (cons s (copier.v2 (sub1 n) s))]))
```

`copier` と `copier.v2` を `0.1` と `"x"` に適用したとき、それらはどのように振る舞いますか？説明しなさい。説明を確認するために DrRacket のステッパー（stepper）を使用しなさい。

**Exercise 150.** 関数 `add-to-pi` を設計しなさい。この関数は自然数 `n` を受け取り、基本操作の `+` を使用せずにそれを `pi` に加算します。以下が出発点です。

```racket
; N -> Number
; computes (+ n pi) without using +

(check-within (add-to-pi 3) (+ 3 pi) 0.001)

(define (add-to-pi n)
  pi)
```

完全な定義ができたら、関数を `add` に一般化しなさい。この関数は、`+` を使用せずに自然数 `n` を任意の実数 `x` に加算します。スケルトン（骨組み）が `check-within` を使用しているのはなぜですか？

**Exercise 151.** 関数 `multiply` を設計しなさい。この関数は自然数 `n` を受け取り、`*` を使用せずに数値 `x` と掛け合わせます。

DrRacket のステッパーを使用して、好きな `x` に対する `(multiply 3 x)` を評価しなさい。`multiply` は小学校で学んだこととどのように関連していますか？

**Exercise 152.** 2 つの関数 `col` と `row` を設計しなさい。

関数 `col` は自然数 `n` と画像 `img` を受け取ります。`img` の `n` 個のコピーを縦方向（垂直）に並べた列（column）を生成します。

関数 `row` は自然数 `n` と画像 `img` を受け取ります。`img` の `n` 個のコピーを横方向（水平）に並べた行（row）を生成します。

```
+----------------------------------------------------------------+
| Figure 60: Random attacks                                      |
|                                                                |
| [image: pict_96.png]                                           |
| [image: pict_97.png]                                           |
| [image: pict_98.png]                                           |
| [image: pict_96.png] [image: pict_97.png] [image: pict_98.png] |
+----------------------------------------------------------------+
```

**Exercise 153.** この演習問題の目的は、1968年スタイルのヨーロッパの学生運動（暴動）の結果を視覚化することです。大まかなアイディアは次のとおりです。学生の小さなグループが集まって絵の具の入った風船を作り、講義室に入り、出席者に向かってランダムに風船を投げます。あなたのプログラムは、風船が講義室の座席をどのように着色するかを表示します。

演習問題 152 の 2 つの関数を使用して、それぞれサイズが 10×10 の正方形で構成される `COLUMNS` × `ROWS` の長方形を作成しなさい。それを同じサイズの `empty-scene` に配置します。この画像があなたの講義室となります。

`add-balloons` を設計しなさい。この関数は、座標が講義室の寸法内に収まる `Posn` のリストを受け取ります。`Posn` のリストで指定された位置に赤いドットが追加された講義室の画像を生成します。

Figure 60 は、10 列の `COLUMNS` と 20 行の `ROWS` に対する、ある `Posn` のリストを与えたときの私たちの解の出力を示しています。一番左は何も汚れていない講義室、2 番目は 2 つの風船が当たった後、最後は 10 回の命中による極めて起こりそうにない分布です。10 番目の風船はどこにありますか？
が当たった後、そして最後は10回の命中という非常にありそうにない分布です。10個目はどこにあるでしょうか？

### 9.4 マトリョーシカ人形 (Russian Dolls)

Wikipedia では、マトリョーシカ人形（Russian doll）を「大きさが徐々に小さくなる人形のセットで、一つがもう一つの内側に配置されているもの」と定義し、次の画像で説明しています。

> [image: russian-doll-from-wikipedia.png]

この画像では、見る人が人形をすべて確認できるように、人形が分解されています。

> **Note:** この問題                                                        り、あるいはばかばかしくさえ思えるかもしれません。なぜマトリョーシカを表現したいのか、あるいはそのような表現で何をするのかが明確ではないからです。とりあえず今は、付き合ってみてください。

さて、このようなマトリョーシカを BSL のデータで表現する問題を考えてみましょう。少し想像力を働かせれば、職人が任意の数の人形からなるマトリョーシカの入れ子を作成できることは簡単に理解できます。結局のところ、与えられたマトリョーシカの周りに、いつでも別の層（レイヤー）を巻きつけることができるからです。一方で、その一番奥深くには、中身が何もない固形の人形が存在することも分かっています。

マトリョーシカの各層について、私たちはさまざまなことに関心を持つことができます。入れ子の深さに関連してはいますがそのサイズ、色、表面に描かれている絵、などです。ここではそのうちの1つ、すなわち人形の色だけを選び、それを文字列（string）で表現することにします。それをふまえると、マトリョーシカの各層は2つのプロパティ（属性）を持つことがわかります。その色と、内側にある人形です。2つのプロパティを持つ情報を表現するために、私たちは常に構造体型（structure type）を定義します。

```racket
(define-struct layer [color doll])
```

そして、次のようにデータ定義を追加します。

```racket
; An RD (short for Russian doll) is one of:
; – String
; – (make-layer String RD)
```

当然のことながら、このデータ定義の第1条項（clause）は一番内側の人形、正確に言えばその色を表しています。第2条項は、与えられたマトリョーシカの周りに層を追加するためのものです。これを `layer` のインスタンスで表現しますが、これには明らかに人形の色と、もう1つのフィールド、すなわちこの人形のすぐ内側に入れ子になっている人形が含まれています。

次の人形を見てみましょう。

> [image: pict_99.png]

これは3つの人形から構成されています。赤い人形が一番内側にあり、緑色の人形が中央に位置し、黄色い人形が現在の最も外側のラッパー（包み）です。この人形を RD の要素として表現するには、どちらの端から始めても構いません。ここでは内側から外側へと進めていきます。赤い人形は RD として簡単に表現できます。中には何も入っておらず、赤色であるため、文字列 `"red"` で十分です。2番目の層には、次のように記述します。

```racket
(make-layer "green" "red")
```

これは、緑色の（空洞の）人形が赤い人形を含んでいることを示しています。最後に、一番外側を得るには、この最後の人形の周りにさらにもう1つの層を巻きつけるだけです。

```racket
(make-layer "yellow" (make-layer "green" "red"))
```

このプロセスによって、任意の色付きマトリョーシカのセットからデータ表現へと変換する方法がよく理解できるはずです。ただし、プログラマは逆の操作、つまりデータから具体的な情報へと遡ることもできなければならないことを心に留めておいてください。この精神に基づき、次の RD の要素に対応するマトリョーシカの概略図を描いてみましょう。

```racket
(make-layer "pink" (make-layer "black" "white"))
```

これを BSL で実際に試してみるのもよいでしょう。

これでデータ定義が手に入り、実際の人形をどのように表現するか、また RD の要素をどのように人形として解釈するかを理解できたので、RD を受け取る関数を設計する準備が整いました。具体的には、マトリョーシカのセットに何個の人形が含まれているかを数える関数を設計してみましょう。この文は優れた目的文（purpose statement）であり、シグネチャ（signature）も決定します。

```racket
; RD -> Number
; how many dolls are part of an-rd
```

データの例については、`(make-layer "yellow" (make-layer "green" "red"))` から始めましょう。上の画像は、赤い人形、緑色の人形、黄色い人形の3つの人形があるため、期待される答えが 3 であることを教えてくれます。この例を1つ作業するだけでも、入力が次の人形の表現である場合には

> [image: pict_100.png]

答えが 1 になることが分かります。

ステップ4ではテンプレートの開発が求められます。このステップの標準的な質問を使用すると、次のテンプレートが作成されます。

```racket
; RD -> Number
; how many dolls are a part of an-rd
(define (depth an-rd)
  (cond
    [(string? an-rd)...]
    [(layer? an-rd)
     (... (layer-color an-rd)...
... (depth (layer-doll an-rd))...)]))
```

`cond` 条項の数は、RD の定義における条項の数によって決まります。それぞれの条項は対象とするデータがどのような種類のものであるかを具体的に明記しており、これにより `string?` 述語と `layer?` 述語を使用することがわかります。文字列は複合データ（compound data）ではありませんが、`layer` のインスタンスは2つの値を含んでいます。関数がこれらの値を必要とする場合、セレクタ式 `(layer-color an-rd)` および `(layer-doll an-rd)` を使用します。最後に、データ定義の第2条項には、`layer` 構造体の `doll` フィールドから定義自体への自己参照が含まれています。したがって、2番目のセレクタ式には再帰的な関数呼び出しが必要です。

例とテンプレートを見れば、関数の定義はほぼ自ずと決まります。非再帰的な `cond` 条項の場合、答えが明らかになるのは 1 です。再帰的な条項の場合、テンプレートの式は次のような結果を計算します。

- `(layer-color an-rd)` は、現在の層の色を表す文字列を抽出します。
- `(layer-doll an-rd)` は、現在の層に含まれている人形を抽出します。
- `(depth (layer-doll an-rd))` は、`depth` の目的文にしたがって、`(layer-doll an-rd)` に何個の人形が含まれているかを決定します。

この最後の数値は求めたい答えにほぼ近いですが、完全ではありません。なぜなら、`an-rd` と `(layer-doll an-rd)` の差は1つの層、つまり1個追加の人形を意味するからです。言い換えると、実際の答えを得るには、関数は再帰的な結果に 1 を加えなければなりません。

```racket
; RD -> Number
; how many dolls are a part of an-rd
(define (depth an-rd)
  (cond
    [(string? an-rd) 1]
    [else (+ (depth (layer-doll an-rd)) 1)]))
```

関数の定義において、第2条項で `(layer-color an-rd)` が使用されていないことに注意してください。ここでも改めて分かるように、テンプレートはデータ定義に関して私たちが知っているすべてのことの整理スキーム（構成スキーマ）ですが、実際の定義においてそれらの要素すべてが必要になるとは限りません。

最後に、例をテストへと変換しましょう。

```racket
(check-expect (depth "red") 1)
(check-expect
  (depth
   (make-layer "yellow" (make-layer "green" "red")))
  3)
```

これらを DrRacket で実行すると、それらの評価が `depth` の定義のすべての部分に触れていることが確認できます。

**Exercise 154.** 関数 `colors` を設計しなさい。この関数はマトリョーシカを受け取り、すべての色をカンマとスペースで区切った1つの文字列を生成します。したがって、私たちの例では次を出力する必要があります。

```racket
"yellow, green, red"
```

**Exercise 155.** 関数 `inner` を設計しなさい。この関数は RD を受け取り、一番内側の人形の（色）を生成します。お気に入りの `rd` に対して `(inner rd)` を評価するために、DrRacket のステッパーを使用しなさい。

### 9.5 リストとワールド (Lists and World)

一般に、リストや自己参照的なデータ定義を使用すると、有限のデータを使用する場合よりもはるかに興味深いワールドプログラムを設計して実行できるようになります。『要素列挙と構造体 (Itemizations and Structures)』で扱ったスペースインベーダープログラムを拡張し、プレイヤーが戦車から好きなだけ弾を発射できるようにしたバージョンを作成できる場面を想像してみてください。まずは、この問題の単純化されたバージョンから始めてみましょう。

> **Note:** しばらくワールドプログラムを設計していない場合は、『ワールドプログラムの設計 (Designing World Programs)』を読み返してください。

> 例題: 弾の発射をシミュレートするワールドプログラムを設計しなさい。「プレイヤー」がスペースキーを押すたびに、プログラムはキャンバスの最下部に弾を1つ追加します。これらの弾は、1クロックティックあたり1ピクセルの割合で垂直に上昇します。

ワールドプログラムの設計は、情報を「定数」と「絶えず変化するワールドの状態の要素」に分離することから始まります。前者については物理的定数および画像定数を導入し、後者についてはワールド状態のデータ表現を構築する必要があります。この例題は具体的な詳細について比較的曖昧ですが、縦線に沿って弾が描画される長方形の背景を前提としていることは明らかです。明らかに弾の位置はクロックティックごとに変化しますが、背景のサイズと弾のラインの x 座標は一定のままです。

```racket
(define HEIGHT 80); distances in terms of pixels
(define WIDTH 100)
(define XSHOTS (/ WIDTH 2))

; graphical constants
(define BACKGROUND (empty-scene WIDTH HEIGHT))
(define SHOT (triangle 3 "solid" "red"))
```

問題文のどこにもこれらの特定の選択を要求するものはありませんが、それらを簡単に変更できる（単一の定義を編集するだけで変更できることを意味します）限り、目標は達成されたと言えます。

変化する「ワールド」の側面に関して、問題文は2つのことに言及しています。第1に、スペースキーを押すと弾が追加されること。第2に、すべての弾が1クロックティックあたり1ピクセルずつまっすぐ上に移動することです。プレイヤーが何発の弾を「発射」するか予測できないため、それらを表現するためにリストを使用します。

```racket
; A List-of-shots is one of:
; – '()
; – (cons Shot List-of-shots)
; interpretation the collection of shots fired
```

残る唯一の疑問は、個々の弾をどのように表現するかです。すべての弾が同じ x 座標を持ち、その座標は終始一定であることはすでにわかっています。さらに、すべての弾の見た目は同じです。したがって、それらが互いに異なる唯一の属性は y 座標です。それゆえ、各弾を数値として表現すれば十分です。

```racket
; A Shot is a Number.
; interpretation represents the shot's y-coordinate
```

すべての弾がキャンバスの最下部から発射され、その後上に移動すること、つまり y 座標が継続的に減少することを知っているため、弾の表現を HEIGHT 未満の数値の区間に制限することもできます。

このワールドを表現するために、次のようなデータ定義を使用することもできます。

```racket
; A ShotWorld is List-of-numbers.
; interpretation each number on such a list
;   represents the y-coordinate of a shot
```

結局のところ、上記の2つの定義はどちらも数値のリストを表しています。私たちはすでに数値のリストの定義を持っており、ShotWorld という名前は、このデータのクラスが何に関するものであるかを皆に伝えています。

定数を定義し、ワールドの状態に関するデータ表現を構築したら、主要なタスクはどのイベントハンドラを採用するかを選択し、そのシグネチャを与えられた問題に適合させることです。実行中の例ではクロックティックとスペースキーに言及しており、これらはすべて3つの関数からなるウィッシュリストに変換されます。

- ワールド状態を画像に変換する関数：
;ShotWorld ->Image;adds the image of a shot for each y onw;at (MID,y) to the background image(define (to-image w) BACKGROUND)問題が視覚的な描画を要求しているため。
- ティックイベントを処理するための関数：
;ShotWorld ->ShotWorld;moves each shot onw up by one pixel(define (tock w) w)
- そして、キーイベントを処理するための関数：
;ShotWorldKeyEvent ->ShotWorld;adds a shot to the world;if the player presses the space bar(define (keyh w ke) w)

最初のウィッシュリストに加えて、実際にワールドをセットアップしハンドラを組み込む main 関数も定義する必要があることを忘れないでください。figure 61 には、設計されたものではなく標準的なスキーマの修正として定義されたこの関数の1つが含まれています。

to-image の設計から始めましょう。シグネチャ、目的ステートメント、およびヘッダーが揃っているので、次は例が必要です。データ定義には2つの節があるため、少なくとも2つの例が存在するはずです。'() と、例えば (cons 9 '()) のような cons されたリストです。'() に対する期待される結果は明らかに BACKGROUND です。しかし、y 座標が存在する場合、関数は弾の画像を MID と指定された座標に配置しなければなりません。

```racket
(check-expect (to-image (cons 9 '()))
              (place-image SHOT XSHOTS 9 BACKGROUND))
```

読み進める前に、2つの Shot のリストに to-image を適用する例を手作業で解いてみてください。そうすることで、関数がどのように動作するかを理解するのに役立ちます。

第4ステップは、データ定義をテンプレートに変換することです。

```racket
; ShotWorld -> Image
(define (to-image w)
  (cond
    [(empty? w)...]
    [else
     (... (first w)... (to-image (rest w))...)]))
```

リストのデータ定義に対するテンプレートはすでにおなじみのものであるため、詳細な説明は不要でしょう。疑問がある場合は、figure 52 の質問を読み返し、自分自身でテンプレートを設計してみてください。

ここから関数を定義するのは簡単です。重要なのは、例とテンプレートを組み合わせ、figure 53 の質問に答えることです。それらに従い、まず空の弾リストというベースケースから始めると、例から期待される答えが BACKGROUND であることがわかります。次に、2番目の cond のテンプレート式が何を計算するかを定式化します。

- (first w) はリストから最初の座標を取り出します。
- (rest w) は残りの座標です。
- (to-image (rest w)) は、to-image の目的ステートメントに従って、(rest w) にある各弾を背景画像に追加します。

言い換えると、(to-image (rest w)) はリストの残りの部分を画像として描画するため、作業のほとんどを実行します。不足しているのは最初の弾、(first w) です。ここで、これら2つの式に目的ステートメントを適用すると、2番目の cond 節に必要な式が得られます。

```racket
(place-image SHOT XSHOTS (first w)
             (to-image (rest w)))
```

追加されるアイコンは弾の標準画像です。2つの座標は目的ステートメントで明確に記述されています。そして place-image への引数の最後は、リストの残りの部分から構築された画像です。

figure 61 には、to-image の完全な関数定義、およびプログラムの残りの部分が表示されています。tock の設計は to-image の設計とまったく同様であるため、ご自身で取り組んでみてください。ただし、keyh ハンドラのシグネチャは1つの興味深い疑問を提起します。このハンドラは、軽微ではないデータ定義を持つ2つの入力を消費すると規定されています。一方では ShotWorld が自己参照的なデータ定義であり、他方では KeyEvents の定義が大きな列挙型です。今のところは、2つの引数のうちどちらがテンプレートの開発を主導すべきかを「推測」してもらいます。後ほど、このようなケースについて深く学習します。

```
+------------------------------------------------------+
| Figure 61: A list-based world program                |
|                                                      |
|; ShotWorld -> ShotWorld                             |
| (define (main w0)                                    |
|   (big-bang w0                                       |
|     [on-tick tock]                                   |
|     [on-key keyh]                                    |
|     [to-draw to-image]))                             |
|                                                      |
|; ShotWorld -> ShotWorld                             |
|; moves each shot up by one pixel                    |
| (define (tock w)                                     |
|   (cond                                              |
|     [(empty? w) '()]                                 |
|     [else (cons (sub1 (first w)) (tock (rest w)))])) |
|                                                      |
|; ShotWorld KeyEvent -> ShotWorld                    |
|; adds a shot to the world if the space bar is hit   |
| (define (keyh w ke)                                  |
|   (if (key=? ke " ") (cons HEIGHT w) w))             |
|                                                      |
|; ShotWorld -> Image                                 |
|; adds each shot y on w at (XSHOTS,y} to BACKGROUND  |
| (define (to-image w)                                 |
|   (cond                                              |
|     [(empty? w) BACKGROUND]                          |
|     [else (place-image SHOT XSHOTS (first w)         |
|                        (to-image (rest w)))]))       |
+------------------------------------------------------+
```

ワールドプログラムに関して言えば、keyh のようなキーハンドラは、それが消費するキーイベントに関係しています。したがって、それを主引数とみなし、そのデータ定義を使用してテンプレートを導出します。具体的には、『列挙型 (Enumerations)』で示した KeyEvent のデータ定義に従うと、この関数には次のような多数の節を持つ cond 式が必要であることが決定付けられます。

```racket
(define (keyh w ke)
  (cond
    [(key=? ke "left")...]
    [(key=? ke "right")...]
...
    [(key=? ke " ")...]
...
    [(key=? ke "a")...]
...
    [(key=? ke "z")...]))
```

もちろん、すべての可能な BSL 値を消費する関数と同様に、キーハンドラが通常すべての可能なケースを検査する必要はありません。現在の問題では、キーハンドラがスペースキーにのみ反応し、他のすべてのキーは無視されることが具体的にわかっています。したがって、" " の節を除き、すべての cond 節を else 節に折りたたむのが自然です。

**Exercise 156.** figure 61 のプログラムにテストを追加し、それらのテストに合格することを確認しなさい。main が何を行うかを説明しなさい。その後、main を介してプログラムを実行しなさい。

**Exercise 157.** 定数に関する任意の決定が容易に変更可能かどうかを実験して判断しなさい。例えば、単一の定数定義を変更するだけで、意図した結果が得られるかどうかを判断しなさい。

- キャンバスの高さを 220 ピクセルに変更する。
- キャンバスの幅を 30 ピクセルに変更する。
- 弾のラインの x 位置を「中央よりやや左側のどこか」に変更する。
- 背景を緑色の長方形に変更する。
- 弾の描画を赤色の細長い長方形に変更する。

また、他のものを何も変更せずに弾のサイズを2倍にすることや、その色を黒に変更することが可能かどうかも確認しなさい。

**Exercise 158.** main を実行し、スペースキーを押して（弾を発射し）、十分な時間待つと、弾はキャンバスから消えます。しかし、ワールドのキャンバスを終了すると、その結果には依然としてこの見えない弾が含まれているワールドが得られます。

1クロックティックあたり弾を1ピクセル移動させるだけでなく、座標がキャンバスの上外に出た弾を除外する代替の tock 関数を設計しなさい。ヒント: 再帰的な cond 節のための補助関数の設計を検討するとよいでしょう。

**Exercise 159.** exercise 153 の解法をワールドプログラムに変換しなさい。riot と名付けられたその main 関数は、学生が投げたい風船の数を消費します。その視覚化は、1秒間に1個の割合で風船が次々と落下する様子を示します。この関数は風船が当たった Posn のリストを生成します。

ヒント (1) 可能なデータ表現の1つを以下に示します：

```racket
(define-struct pair [balloon# lob])
; A Pair is a structure (make-pair N List-of-posns)
; A List-of-posns is one of:
; – '()
; – (cons Posn List-of-posns)
; interpretation (make-pair n lob) means n balloons
; must yet be thrown and added to lob
```

(2) big-bang 式は実際には単なる式です。それを別の式の中にネストすることは正当です。

(3) random が乱数を生成することを思い出しなさい。
す。それを別の式の中にネスト（入れ子）にすることは正当です。

(3) `random` は乱数を生成することを思い出してください。

### 9.6 リストと集合についての注意 (A Note on Lists and Sets)

本書は、集合を BSL 値のコレクションとする直感的な理解に依存しています。「データの宇宙（The Universe of Data）」では特に、データ定義は BSL 値の集合に名前を導入するものであると述べています。本書が集合に関して一貫して投げかける問いが1つあり、それは「ある要素が与えられたある集合に含まれるかどうか」です。たとえば、4 は Number に含まれますが、"four" は含まれません。また、本書では、ある値が特定の名前付き集合の要素（member）であるかどうかをチェックするためにデータ定義を使用する方法や、データ定義の一部を使用して集合のサンプル要素を生成する方法も示していますが、これら2つの手続きはデータ定義に関するものであり、集合そのものに関するものではありません。

同時に、リストも値のコん。

それと同時に、リストもまた値の集まりを表現します。したがって、リストと集合の違いは何なのか、あるいはこれは不要な区別なのではないか疑問に思っているかもしれません。もしそうなら、この節はあなたのためのものです。

現時点での集合とリストの主な違いは、前者がコードの設計におけるステップを議論するために使用する概念であるのに対し、後者は私たちが選択したプログラミング言語であるBSLにおける多数のデータ形式の1つである点です。これら2つのアイデアは、私たちの議論においてかなり異なるレベルに位置しています。しかし、データ定義がBSL内部の実際の情報のデータ表現を導入すること、そして集合が情報の集まりであることを考えると、集合がBSL内部でデータとしてどのように表現されるのかという疑問が湧くかもしれません。

> **注:** 多くの本格的なプログラミング言語は、リストと集合の両方のデータ表現を直接サポートしています。

BSLにおいてリストは特別な地位を占めていますが、集合にはそれがありません。しかし同時に、集合はどこかリストに似ています。主な違いは、プログラムが通常どちらかの形式のデータに対して使用する関数の種類にあります。BSLはリストに対していくつかの基本的な定数や関数——たとえば `empty`、`empty?`、`cons`、`cons?`、`first`、`rest`——を提供しており、また自分で定義できる関数もいくつか存在します——たとえば `member?`、`length`、`remove`、`reverse` などです。以下は、自分で定義できるもののBSLには標準で付属していない関数の例です。

```racket
; List-of-string String -> N
; determines how often s occurs in los
(define (count los s)
  0)
```

手を止めましょう！（Stop!）この関数の設計を完成させてください。

素直で、もしかすると単純すぎる方法で進め、集合とは基本的にリストであると仮定してみましょう。そして、さらに単純化するために、この節では数値のリストに注目することにします。ここで、数値が集合の一部であるか否かだけが重要であると受け入れるならば、集合を表現するためにリストを2つの異なる方法で使用できることがほぼ直ちに明らかになります。

```
+--------------------------------------------------------------------------------+
| Figure 62: Two data representations for sets                                   |
|                                                                                |
| +--------------------------------------+--+----------------------------------- |
| |; A Son.L is one of: ⏎; – empty ⏎ … | |; A Son.R is one of: ⏎; – empty ⏎  |
| … |                                                                            |
| +--------------------------------------+--+----------------------------------- |
| |;ASon.L is one of: | | |                                                     |
| |;–empty | | |                                                                |
| |; – (cons Number Son.L) | | |                                                |
| |; | | |                                                                      |
| |;Son is used when it | | |                                                   |
| |;applies toSon.L andSon.R | | |                                              |
| |;ASon.R is one of: | | |                                                     |
| |;–empty | | |                                                                |
| |; – (cons Number Son.R) | | |                                                |
| |; | | |                                                                      |
| |;Constraint Ifs is aSon.R, | | |                                             |
| |;no number occurs twice ins | | |                                            |
| +--------------------------------------+--+----------------------------------- |
+--------------------------------------------------------------------------------+
```

図62（Figure 62）は2つのデータ定義を示しています。どちらも基本的には、集合が数値のリストとして表現されることを述べています。違いは、右側の定義には、リスト上に同じ数値が複数回現れてはならないという制約が付いていることです。結局のところ、私たちが集合に関して問いかける最も重要な問いは、ある数値がその集合に含まれているかどうかであり、それが集合の中に1回、2回、あるいは3回入っているかは関係ありません。

どちらの定義を選択したとしても、すでに2つの重要な概念を定義することができます。

```racket
; Son
(define es '())

; Number Son -> Boolean
; is x in s
(define (in? x s)
  (member? x s))
```

最初のものは空集合（empty set）であり、どちらの場合も空リスト（empty list）によって表現されます。2つ目は要素判定（membership test）です。

より大きな集合を構築する1つの方法は、`cons` と上記の定義を使用することです。たとえば、1、2、および3を含む集合の表現を構築したいとしましょう。そのような表現の1つが以下です。

```racket
(cons 1 (cons 2 (cons 3 '())))
```

そして、これは両方のデータ表現で機能します。しかし、

```racket
(cons 2 (cons 1 (cons 3 '())))
```

は本当に同じ集合の表現ではないのでしょうか？ あるいは、

```racket
(cons 1 (cons 2 (cons 1 (cons 3 '()))))
```

はどうでしょうか？ 答えは、主な関心が数値が集合に含まれているかどうかである限り、肯定的でなければなりません。それでも、`cons` の順序は関係あり得ない一方で、右側のデータ定義における制約により、最後のリストは1が2回含まれているため `Son.R` から除外されます。

```
+--------------------------------------------------------------------------------+
| Figure 63: Functions for the two data representations of sets                  |
|                                                                                |
| +--------------------------------------+--+----------------------------------- |
| |; Number Son.L -> Son.L ⏎; removes… | |; Number Son.R -> Son.R ⏎;         |
| removes… |                                                                     |
| +--------------------------------------+--+----------------------------------- |
| |;NumberSon.L ->Son.L | | |                                                   |
| |;removesx froms | | |                                                        |
| | (define s1.L | | |                                                           |
| | (cons 1 (cons 1 '()))) | | |                                                 |
| | (check-expect | | |                                                          |
| | (set-.L 1 s1.L) es) | | |                                                    |
| | (define (set-.L x s) | | |                                                   |
| | (remove-all x s)) | | |                                                      |
| |;NumberSon.R ->Son.R | | |                                                   |
| |;removesx froms | | |                                                        |
| | (define s1.R | | |                                                           |
| | (cons 1 '())) | | |                                                          |
| | (check-expect | | |                                                          |
| | (set-.R 1 s1.R) es) | | |                                                    |
| | (define (set-.R x s) | | |                                                   |
| | (remove x s)) | | |                                                          |
| +--------------------------------------+--+----------------------------------- |
+--------------------------------------------------------------------------------+
```

2つのデータ定義の違いは、関数を設計する際に現れます。たとえば、集合から数値を削除する関数が必要だとしましょう。両方の表現に適用できるウィッシュリストのエントリは以下のとおりです。

```racket
; Number Son -> Son
; subtracts x from s
(define (set- x s)
  s)
```

目的のステートメント（purpose statement）で「引く（subtract）」という言葉が使われているのは、論理学者や数学者が集合を扱う際にこの言葉を使用するからです。

図63（Figure 63）はその結果を示しています。2つの列は次の2つの点で異なります。

1. 左側のテストは1を2回含むリストを使用していますが、右側のテストは同じ集合を単一の `cons` で表現しています。
2. これらの違いにより、左側の `set-` は `remove-all` を使用しなければなりませんが、右側の `set-` は `remove` だけで済みます。

手を止めましょう！（Stop!）コードをDrRacketの定義領域にコピーし、テストが合格することを確認してください。その後、読み進めながらコードで実験してみてください。

図63（Figure 63）の好ましくない点は、テストで期待される結果としてプレーンなリストである `es` を使用していることです。この問題は一見すると些細なことに思えるかもしれません。しかし、次の例を考えてみてください。

```racket
(set- 1 set123)
```

ここで `set123` は、1、2、および3を含む集合を以下の2つの方法のいずれかで表現しています。

```racket
(define set123-version1
  (cons 1 (cons 2 (cons 3 '()))))

(define set123-version2
  (cons 1 (cons 3 (cons 2 '()))))
```

どちらの表現を選択したとしても、`(set- 1 set123)` はこれら2つのリストのいずれかに評価されます。

```racket
(define set23-version1
  (cons 2 (cons 3 '())))

(define set23-version2
  (cons 3 (cons 2 '())))
```

しかし、`set-` がそれら2つのどちらを生成するかを予測することはできません。

2つの選択肢というシンプルなケースでは、次のように `check-member-of` テスト機能を使用することが可能です。

```racket
(check-member-of (set-.v1 1 set123.v1)
                 set23-version1
                 set23-version2)
```

期待される集合が3つの要素を含む場合、左側のデータ定義が許可する重複を伴う表現を除いたとしても、6通りのバリエーションが存在します。

この問題を解決するには、2つのアイデアを組み合わせる必要があります。第1に、`set-` は本質的に、指定された要素が結果の中に現れないことを保証するためのものであるということを思い出してください。これは、例をテストへと変換する私たちのやり方では伝わらないアイデアです。第2に、BSLの `check-satisfied` テスト機能を使用すれば、まさにこのアイデアを正確に記述することができます。

「合間1：初等学生言語（Intermezzo 1: Beginning Student Language）」では `check-satisfied` について簡単に触れていますが、かいつまんで言えば、この機能はある式が特定の性質（property）を満たしているかどうかを判定します。性質とは、値から `Boolean` への関数です。今回の具体的なケースでは、1が特定の集合のメンバではないことを表明したいと思います。

```racket
; Son -> Boolean
; #true if 1 is not a member of s;  #false otherwise
(define (not-member-1? s)
  (not (in? 1 s)))
```

`not-member-1?` を使用すると、テストケースを次のように定式化できます。

```racket
(check-satisfied (set- 1 set123) not-member-1?)
```

そして、このバリエーションは関数が何を達成することになっているのかを明確に示しています。さらに都合が良いことに、この定式化は入力または出力の集合がどのように表現されているかにまったく依存しません。

要約すると、リストと集合はどちらも値の集まりに関するものであるという点で関連していますが、大きく異なる点もあります。

> | 性質 (property) | リスト (lists) | 集合 (sets) |
> | --- | --- | --- |
> | メンバ関係 (membership) | 多数の中の1つ (one among many) | 極めて重要 (critical) |
> | 順序 (ordering) | 極めて重要 (critical) | 無関係 (irrelevant) |
> | 出現回数 (# of occurrences) | 意味を持つ (sensible) | 無関係 (irrelevant) |
> | サイズ (size) | 有限だが任意 (finite but arbitrary) | 有限または無限 (finite or infinite) |

この表の最後の行は、新しいアイデアを提示していますが、同時に明白なものでもあります。本書で言及されている集合の多くは無限に大きく、たとえば `Number`、`String`、そして `List-of-strings` などがあります。対照的に、リストは任意に多数の項目を含むことができますが、常に有限です。

要約すると、この節では集合とリストの本質的な違いと、有限集合を有限リストで表現する2つの異なる方法について説明しました。BSLは無限集合を表現できるほど表現力が高くありません。演習問題299（exercise 299）では、無限集合にも対処できる、まったく異なる集合の表現方法を導入します。ただし、実際のプログラミング言語がどのように集合を表現するかという問題は、本書の範囲を超えています。

**Exercise 160.** 左側および右側のデータ定義に対して、それぞれ与えられた集合 `s` に数値 `x` を追加することで集合を作成する関数 `set+.L` および `set+.R` を設計しなさい。

